import { Flashcard } from '../../types';

/**
 * LPI LPIC-2 Exam 202 - Topic 209: File Sharing
 * Part 2: Cards 2951 - 3000
 * - Objective 209.1: SAMBA Server Configuration & Clients - Part 2 (Cards 2951-2960) [10 Cards] (Weight 5)
 * - Objective 209.2: NFS Server Configuration (Cards 2961-3000) [40 Cards] (Weight 3)
 */

export const topic209Part2: Flashcard[] = [
  // =========================================================================
  // OBJECTIVE 209.1: SAMBA Server Configuration & Clients (Cards 2951-2960)
  // =========================================================================
  {
    id: 2951,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.1',
    topicNumber: 209,
    category: 'SAMBA Server Configuration',
    command: 'smbclient -L',
    question: 'Which command queries a remote Samba/SMB server to list all published shares, workgroups, and NetBIOS services?',
    answer: '`smbclient -L <server_ip_or_name> -U <username>` (or `smbclient -L <server> -N` for anonymous listing).',
    definition: 'Enumerates and lists all available shares, printers, and IPC services exposed by an SMB server.',
    example: 'smbclient -L 192.168.1.10 -U alice',
    exampleExplanation: 'Authenticates as `alice` and outputs a formatted table showing share names, types (Disk, Printer, IPC), and server comments.',
    examTip: 'Exam favorite: `-L` stands for List. If credentials are not provided, `-N` suppresses the password prompt for anonymous querying.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2952,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.1',
    topicNumber: 209,
    category: 'SAMBA Server Configuration',
    command: 'smbclient //server/share',
    question: 'How do you open an interactive, FTP-like command-line session to navigate and download files from an SMB share on Linux?',
    answer: '`smbclient //server/share -U <username>` (or `smbclient \\\\server\\share -U <username>`).',
    definition: 'An interactive CLI client providing FTP-like commands (`ls`, `cd`, `get`, `put`, `mkdir`) to interact with an SMB share.',
    example: 'smbclient //192.168.1.10/docs -U bob\nPassword: ****\nsmb: \\> ls\nsmb: \\> get report.pdf',
    exampleExplanation: 'Connects to share `docs` on `192.168.1.10`, lists directory contents, and downloads `report.pdf` to the local working directory.',
    examTip: 'Forward slashes (`//server/share`) do not require shell escaping, whereas backslashes (`\\\\server\\share`) require quoting or double-escaping.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2953,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.1',
    topicNumber: 209,
    category: 'SAMBA Server Configuration',
    command: 'smbclient interactive commands',
    question: 'Inside an interactive `smbclient` session, which commands change local directory, toggle prompt confirmation, and download files matching a wildcard?',
    answer: '`lcd` changes local directory; `prompt` toggles prompt confirmation off/on; `mget <wildcard>` downloads multiple files matching the mask.',
    definition: 'Navigation and transfer commands modeled after the standard UNIX `ftp` client.',
    example: 'smb: \\> lcd /tmp\nsmb: \\> prompt\nsmb: \\> mget *.log',
    exampleExplanation: 'Switches local working directory to `/tmp`, turns off interactive confirmation prompts, and downloads all `.log` files in batch.',
    examTip: 'To upload multiple files in batch, use `mput <wildcard>` with `prompt` turned off.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2954,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.1',
    topicNumber: 209,
    category: 'SAMBA Server Configuration',
    command: 'smbclient -c',
    question: 'How can you run a batch file transfer non-interactively using `smbclient` inside a shell script?',
    answer: 'Use the `-c` flag with semicolon-separated commands: `smbclient //server/share -U user%password -c "command1; command2"`.',
    definition: 'Executes a command string directly without launching an interactive shell session, exiting immediately upon completion.',
    example: 'smbclient //backup-srv/archive -U backup%SecretPass123 -c "lcd /var/backups; put daily.tar.gz"',
    exampleExplanation: 'Uploads `daily.tar.gz` to the `archive` share non-interactively in an automated cron backup script.',
    examTip: 'You can embed the password using `user%password` syntax with `-U` for script automation.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2955,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.1',
    topicNumber: 209,
    category: 'SAMBA Server Configuration',
    command: 'smbclient -N',
    question: 'What does the `-N` flag signify when running `smbclient`?',
    answer: 'Suppresses the password prompt, attempting a null/anonymous connection without asking the user for a password.',
    definition: 'Instructs the client to attempt connection without supplying credentials.',
    example: 'smbclient -L 192.168.1.5 -N',
    exampleExplanation: 'Attempts to list shares on `192.168.1.5` anonymously without waiting for password input.',
    examTip: 'Essential when scripting automated share discovery or testing public/guest share access.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2956,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.1',
    topicNumber: 209,
    category: 'SAMBA Server Configuration',
    command: 'mount -t cifs',
    question: 'Which command mounts an SMB share `//192.168.1.10/projects` to `/mnt/cifs` as user `alice` with domain `CORP`?',
    answer: '`mount -t cifs //192.168.1.10/projects /mnt/cifs -o username=alice,password=secret,domain=CORP` (or `mount.cifs`).',
    definition: 'Mounts a remote SMB/CIFS filesystem into the local Linux virtual filesystem (VFS) tree.',
    example: 'mount -t cifs //192.168.1.10/projects /mnt/cifs -o username=alice,password=SecretPass123',
    exampleExplanation: 'Attaches the remote SMB share at `/mnt/cifs`, allowing standard Linux tools (`cp`, `cat`, `nano`) to access files directly.',
    examTip: 'The `mount -t cifs` command is provided by the `cifs-utils` package. Without it, the kernel CIFS mount helper cannot execute.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2957,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.1',
    topicNumber: 209,
    category: 'SAMBA Server Configuration',
    command: 'CIFS credentials file',
    question: 'How do you prevent passwords from being exposed in command history or `/etc/fstab` when mounting CIFS shares?',
    answer: 'Use the `credentials=/path/to/file` mount option with a dedicated credentials file formatted with `username=...`, `password=...`, and `domain=...`, secured with permissions `0600`.',
    definition: 'A protected text file containing authentication credentials referenced by `mount.cifs`.',
    example: 'cat /etc/samba/cifs.creds\nusername=backupuser\npassword=TopSecretPassword\ndomain=COMPANY\nchmod 600 /etc/samba/cifs.creds',
    exampleExplanation: 'Secures credentials so only root can read them, eliminating plain-text passwords from `ps` process lists and world-readable `/etc/fstab`.',
    examTip: 'LPIC-2 exam favorite: Permissions on the credentials file MUST be `0600` (read/write root only) for security compliance.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2958,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.1',
    topicNumber: 209,
    category: 'SAMBA Server Configuration',
    command: '/etc/fstab CIFS entry',
    question: 'Which mount options in `/etc/fstab` ensure that an SMB share is mounted after network initialization and that local user `developer` (UID 1000) owns the files?',
    answer: '`_netdev,credentials=/etc/samba/creds,uid=1000,gid=1000,file_mode=0664,dir_mode=0775`',
    definition: 'Persistent configuration for network-attached SMB filesystems in `/etc/fstab`.',
    example: '//server/data /mnt/data cifs credentials=/etc/samba/creds,_netdev,uid=1000,gid=1000 0 0',
    exampleExplanation: '`_netdev` delays mounting until network interfaces are up, avoiding boot-time hangs. `uid=1000` overrides remote file ownership locally.',
    examTip: 'Crucial: Without `_netdev`, systemd will attempt to mount the CIFS share before networking is up, resulting in boot failures or timeouts.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2959,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.1',
    topicNumber: 209,
    category: 'SAMBA Server Configuration',
    command: 'smbstatus',
    question: 'Which diagnostic tool shows active Samba connections, connected client IP addresses, open file handles, and locked files?',
    answer: '`smbstatus`',
    definition: 'A reporting tool that reads Samba\'s shared memory lock database and outputs current connections, sessions, and active file locks.',
    example: 'smbstatus\nsmbstatus -b # Brief (PIDs and machines)\nsmbstatus -L # Locks only\nsmbstatus -S # Shares only',
    exampleExplanation: 'Dumps currently connected machines, which shares they are accessing, their process IDs, and active byte-range locks on files.',
    examTip: 'To troubleshoot why a file cannot be deleted or renamed due to an SMB lock, run `smbstatus -L`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2960,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.1',
    topicNumber: 209,
    category: 'SAMBA Server Configuration',
    command: 'nmblookup and os level',
    question: 'Which utility queries NetBIOS names across a subnet, and what directive in `smb.conf` ensures Samba wins browse master elections?',
    answer: '`nmblookup`. Setting `os level = 33` (or higher, e.g. 65 or 255) along with `preferred master = yes` and `local master = yes` ensures Samba wins elections.',
    definition: 'NetBIOS query tool and election prioritization parameters in `smb.conf`.',
    example: 'nmblookup -A 192.168.1.10\n# smb.conf:\n[global]\n  os level = 65\n  preferred master = yes',
    exampleExplanation: 'Queries the NetBIOS name table of a remote IP, while `os level = 65` outranks all Windows NT/2000/XP/Server machines in master browser elections.',
    examTip: 'Windows NT Server defaults to `os level = 32`. Setting `os level = 33` ensures Samba beats NT Server; `os level = 255` ensures it wins unconditionally.',
    difficulty: 'Advanced',
    status: 'unseen',
  },

  // =========================================================================
  // OBJECTIVE 209.2: NFS Server Configuration (Cards 2961-3000) [40 Cards]
  // =========================================================================
  {
    id: 2961,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'NFSv3 vs NFSv4 Architecture',
    question: 'What are the fundamental architectural differences between NFSv3 and NFSv4 regarding ports, daemons, and statefulness?',
    answer: 'NFSv3 is stateless and requires multiple auxiliary RPC daemons (`rpcbind` on 111, `mountd`, `statd`, `lockd` on random ports). NFSv4 is stateful, operates strictly over a single well-known port (TCP 2049), requires no `rpcbind`, and integrates mounting and locking into the main daemon.',
    definition: 'The architectural evolution from multi-port stateless RPC (v3) to a unified, firewall-friendly, stateful protocol with lease-based locks (v4).',
    example: 'ss -tulpn | grep 2049 # NFS daemon listening on TCP 2049',
    exampleExplanation: 'Verifies that NFSv4 only needs TCP port 2049 through firewalls, whereas NFSv3 requires opening ports for portmapper, mountd, and statd.',
    examTip: 'High-frequency exam topic: NFSv4 does NOT require `rpcbind` and uses only TCP port 2049.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2962,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: '/etc/exports Syntax Rule',
    question: 'What is the syntax for an entry in `/etc/exports`, and why is placing a space between the client specification and the opening parenthesis `(` a critical mistake?',
    answer: 'Syntax: `/path/to/share client(options)`. Placing a space (e.g. `/data 192.168.1.0/24 (rw)`) causes NFS to treat the client as matching with default options (`ro`), and applies `(rw)` to the wildcard host `*`, inadvertently giving read-write access to the entire world!',
    definition: 'The strict whitespace rule in `/etc/exports` governing host-to-option association.',
    example: '# CORRECT:\n/srv/data 192.168.1.0/24(rw,sync,no_subtree_check)\n# WRONG (DANGEROUS):\n/srv/data 192.168.1.0/24 (rw,sync,no_subtree_check)',
    exampleExplanation: 'The space separates the client from the options block, causing `(rw)` to apply globally to all clients.',
    examTip: 'Exam favorite "gotcha": NEVER put a space between the client name/subnet and the options parenthesis `(`. This is tested on almost every LPIC-2 exam.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2963,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: '/etc/exports.d/*.exports',
    question: 'What is the purpose of the `/etc/exports.d/` directory in modern NFS server implementations?',
    answer: 'Allows drop-in modular export configuration files ending in `.exports`. `exportfs` parses all `.exports` files in this directory alongside `/etc/exports`.',
    definition: 'A configuration drop-in directory enabling automated configuration management (Ansible, Puppet) without modifying the monolithic `/etc/exports`.',
    example: 'cat /etc/exports.d/projects.exports\n/srv/projects 10.0.0.0/8(rw,sync)',
    exampleExplanation: 'Modularizes export rules by service, department, or automation recipe.',
    examTip: 'Only files ending with the `.exports` extension are parsed by `exportfs` inside `/etc/exports.d/`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2964,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'Client Subnet Formats in /etc/exports',
    question: 'How can client network addresses be specified in `/etc/exports` using CIDR and netmask notation?',
    answer: 'Using CIDR: `192.168.1.0/24(rw)` or traditional netmask: `192.168.1.0/255.255.255.0(rw)`.',
    definition: 'IP subnet definitions identifying which group of network hosts is allowed to mount the exported directory.',
    example: '/data/shared 10.10.0.0/16(rw,sync)\n/data/ops 172.16.0.0/255.255.0.0(rw,sync)',
    exampleExplanation: 'Exports `/data/shared` to the entire 10.10.0.0/16 network and `/data/ops` to the 172.16.0.0/16 network.',
    examTip: 'Both CIDR prefix length (`/24`) and dotted-decimal netmask (`/255.255.255.0`) are valid in `/etc/exports`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2965,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'Hostname Wildcards and Netgroups',
    question: 'How do you export a share to all hosts in the `example.com` domain and to a NIS/LDAP netgroup named `sysadmins`?',
    answer: 'Wildcard: `*.example.com(ro)`. Netgroup: `@sysadmins(rw)`.',
    definition: 'Domain wildcard and netgroup client matching syntax in `/etc/exports`.',
    example: '/srv/software *.lab.example.com(ro,sync)\n/srv/admin @sysadmins(rw,sync)',
    exampleExplanation: 'Matches any hostname ending in `.lab.example.com` for read-only access, and grants read-write access to the `@sysadmins` netgroup.',
    examTip: 'The `@` symbol denotes a netgroup (configured via NIS or LDAP). Single asterisk `*` exports to all hosts.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2966,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'ro vs rw export options',
    question: 'What is the default permission mode in `/etc/exports` if neither `ro` nor `rw` is specified, and how do you enable write permissions?',
    answer: 'The default is `ro` (read-only). To allow client write operations, you must explicitly specify `rw`.',
    definition: 'Export access mode flags controlling write permissions to the underlying filesystem.',
    example: '/srv/public 192.168.1.0/24(ro,sync) # Read-only\n/srv/upload 192.168.1.0/24(rw,sync) # Read-write',
    exampleExplanation: 'Clients mounting `/srv/public` can only read files; clients mounting `/srv/upload` can create and modify files (provided filesystem permissions permit).',
    examTip: 'Even if `rw` is configured in `/etc/exports`, write operations will still fail if the underlying Linux directory permissions or mount options do not allow writing.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2967,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'sync vs async export options',
    question: 'What is the difference between `sync` and `async` in an NFS export, and what is the default in modern Linux NFS servers?',
    answer: '`sync` (default) ensures changes are committed to persistent disk storage before replying to the client, guaranteeing data integrity. `async` replies to the client as soon as data is received in RAM buffers, improving speed but risking data corruption if the server crashes.',
    definition: 'Write-acknowledgment behavior controlling storage persistence and protocol performance.',
    example: '/data/db 192.168.1.0/24(rw,sync) # Safe\n/data/scratch 192.168.1.0/24(rw,async) # Fast, unsafe on crash',
    exampleExplanation: 'Database storage requires `sync` to guarantee transactional durability. Temporary scratch space can use `async` for maximum throughput.',
    examTip: 'Modern NFS server implementations default to `sync`. If you do not specify `sync` or `async`, `exportfs` will emit a warning.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2968,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'wdelay vs no_wdelay',
    question: 'What does the `wdelay` option do in `/etc/exports`, and when must `no_wdelay` be used?',
    answer: '`wdelay` (default) delays writing to disk if NFS suspects another write request is related, coalescing writes into a single disk transaction. `no_wdelay` forces immediate disk writes for each request and requires `sync` to be active.',
    definition: 'Write-delay optimization option for batching adjacent disk write operations.',
    example: '/srv/fastwrite 10.0.0.0/24(rw,sync,no_wdelay)',
    exampleExplanation: 'Disables write coalescing, useful if the server handles small, non-sequential writes where delaying does not improve throughput.',
    examTip: '`no_wdelay` has no effect if `async` is set, because `async` does not wait for disk commits anyway.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2969,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'root_squash',
    question: 'What is the security effect of the default `root_squash` export option?',
    answer: 'Maps requests from client UID 0 / GID 0 (root) to the unprivileged anonymous user (`nobody` / `anonuid`, typically UID 65534) on the server, preventing client root users from having administrative control over the exported filesystem.',
    definition: 'A fundamental NFS security mechanism preventing remote client superusers from executing commands as root on the server.',
    example: '/srv/share 192.168.1.0/24(rw,sync,root_squash)',
    exampleExplanation: 'If `root` on a client tries to `rm -rf /` on the mount, the server treats the request as user `nobody` and denies permission.',
    examTip: '`root_squash` is the default setting. It is the most heavily tested security feature in LPIC-2 Topic 209.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2970,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'no_root_squash',
    question: 'What does `no_root_squash` do, and in what legitimate scenarios is it required despite the security risk?',
    answer: 'Allows client UID 0 (root) requests to be executed with full root privileges (UID 0) on the server. Required for network netboot/diskless clients, remote system backups, and cluster shared roots.',
    definition: 'Disables root identity squashing, giving remote client root accounts full superuser power on the exported filesystem.',
    example: '/srv/diskless/root client1.cluster(rw,sync,no_root_squash)',
    exampleExplanation: 'Enables a diskless client booting over network to create device nodes (`mknod`) and modify system configuration files as root.',
    examTip: 'Security risk: Never enable `no_root_squash` on untrusted networks or general user shares.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2971,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'all_squash',
    question: 'What is the effect of setting `all_squash` on an exported NFS directory?',
    answer: 'Maps ALL incoming client requests (regardless of client UID/GID) to the anonymous user (`nobody` / `anonuid`), stripping all individual identity.',
    definition: 'Forces all client users to operate as the anonymous account on the server filesystem.',
    example: '/srv/dropbox *(rw,sync,all_squash,anonuid=1005,anongid=1005)',
    exampleExplanation: 'Ensures that every file uploaded by any client user is owned by UID 1005 / GID 1005 on the server.',
    examTip: '`all_squash` is ideal for public dropboxes, guest access shares, or web upload staging areas.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2972,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'anonuid and anongid',
    question: 'Which export options specify the numeric UID and GID assigned to squashed requests in an NFS share?',
    answer: '`anonuid=<UID>` and `anongid=<GID>`',
    definition: 'Overrides the default anonymous identity (`nobody` / 65534) to a specific numeric user and group ID on the server.',
    example: '/srv/shared *(rw,sync,all_squash,anonuid=1500,anongid=1500)',
    exampleExplanation: 'Forces all squashed client operations to execute under UID 1500 and GID 1500 on the server.',
    examTip: 'Values must be numeric UIDs/GIDs (e.g. `anonuid=1000`), not string usernames.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2973,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'subtree_check vs no_subtree_check',
    question: 'Why is `no_subtree_check` strongly recommended when an entire filesystem partition is exported via NFS?',
    answer: 'Subtree checking verifies whether a requested file is in an exported sub-directory of a larger volume. Disabling it with `no_subtree_check` eliminates overhead and avoids file access errors when an open file is renamed or moved across directories on the client.',
    definition: 'Controls whether the NFS server verifies that an accessed file handle resides within the exported subtree.',
    example: '/srv/data 192.168.1.0/24(rw,sync,no_subtree_check)',
    exampleExplanation: 'Improves transfer reliability and performance when `/srv/data` represents a whole mounted partition.',
    examTip: 'Always include `no_subtree_check` when exporting whole filesystems to prevent "Stale File Handle" errors when renaming active files.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2974,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'fsid=0 / fsid=root',
    question: 'What does `fsid=0` (or `fsid=root`) designate in NFSv4 server configurations?',
    answer: 'Designates the root directory of the exported NFSv4 pseudo-filesystem. All other exported directories in NFSv4 are presented to clients relative to this pseudo-root.',
    definition: 'The root filesystem identifier establishing the virtual root tree for NFSv4 client mounts.',
    example: '/srv/nfs4 *(rw,sync,fsid=0,no_subtree_check)\n/srv/nfs4/data *(rw,sync,no_subtree_check)',
    exampleExplanation: 'Defines `/srv/nfs4` as the virtual root. Clients mounting `server:/` see `/data` directly.',
    examTip: 'In NFSv4, a client can mount `server:/` instead of `server:/srv/nfs4` because `/srv/nfs4` has `fsid=0`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2975,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'crossmnt',
    question: 'What is the function of the `crossmnt` option in `/etc/exports`?',
    answer: 'Allows clients to transparently traverse from an exported parent filesystem into mounted child filesystems without requiring separate mount commands on the client.',
    definition: 'Enables automatic traversal across filesystem boundaries in exported directory trees.',
    example: '/srv/nfs4 *(rw,sync,fsid=0,crossmnt)\n/srv/nfs4/storage *(rw,sync)',
    exampleExplanation: 'Allows clients mounting the root `/` to seamlessly access files inside `/storage`, even though `/storage` is a separate mounted disk partition.',
    examTip: 'Particularly useful in NFSv4 pseudo-filesystem hierarchies created with bind mounts.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2976,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'exportfs -a',
    question: 'What does the `exportfs -a` command do on an NFS server?',
    answer: 'Exports or unexports all directories listed in `/etc/exports` and `/etc/exports.d/*.exports`.',
    definition: 'Applies export rules for all entries defined across configuration files.',
    example: 'exportfs -a',
    exampleExplanation: 'Adds all configured export definitions to the kernel export table.',
    examTip: 'To re-export everything after modifying configuration files, use `exportfs -ra` (or `exportfs -r`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2977,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'exportfs -r / exportfs -ra',
    question: 'Which command re-reads `/etc/exports` and synchronizes the kernel export table dynamically without restarting the NFS service?',
    answer: '`exportfs -r` (or `exportfs -ra`).',
    definition: 'Re-evaluates `/etc/exports` and removes entries that are no longer present, updating active exports without client disconnection.',
    example: 'exportfs -ra',
    exampleExplanation: 'Applies edits made to `/etc/exports` with zero downtime for connected clients.',
    examTip: 'LPIC-2 golden rule: Never restart `nfs-server` just to apply new shares; run `exportfs -ra` instead.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2978,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'exportfs -v',
    question: 'What information does `exportfs -v` display when executed on an NFS server?',
    answer: 'Displays a verbose list of all currently active exports, including all explicit and internal default flags (such as `sync`, `wdelay`, `root_squash`).',
    definition: 'Verbose export status display listing the full parameter matrix for every active export.',
    example: 'exportfs -v',
    exampleExplanation: 'Outputs lines like `/srv/data 192.168.1.0/24(rw,wdelay,root_squash,no_subtree_check,sec=sys,rw,secure,root_squash,no_all_squash)`.',
    examTip: '`-v` is essential for verifying what implied default options the kernel assigned to an export.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2979,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'exportfs -u',
    question: 'How do you immediately unexport a specific active share without removing it from `/etc/exports`?',
    answer: '`exportfs -u <client>:<path>` (or `exportfs -u <path>`).',
    definition: 'Temporarily unexports an active share, preventing clients from accessing it.',
    example: 'exportfs -u 192.168.1.0/24:/srv/data',
    exampleExplanation: 'Revokes export access for the 192.168.1.0/24 subnet to `/srv/data` in the kernel export table.',
    examTip: 'To unexport everything at once: `exportfs -ua`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2980,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'exportfs -o (dynamic export)',
    question: 'Which command exports `/mnt/fast` with options `rw,no_root_squash` to `node5` directly from the command line without editing `/etc/exports`?',
    answer: '`exportfs -o rw,no_root_squash node5:/mnt/fast`',
    definition: 'Dynamically exports a filesystem with arbitrary options on the fly from the terminal.',
    example: 'exportfs -o rw,sync,no_root_squash 10.0.0.5:/var/scratch',
    exampleExplanation: 'Immediately exports `/var/scratch` to `10.0.0.5` without touching configuration files.',
    examTip: 'Dynamic exports created via `exportfs -o` do not persist across reboot or `exportfs -ra`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2981,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: '/var/lib/nfs/etab and /proc/fs/nfsd/exports',
    question: 'Where does the Linux kernel maintain the active runtime table of exported NFS filesystems?',
    answer: '`/var/lib/nfs/etab` (user-space state maintained by `exportfs`) and `/proc/fs/nfs/exports` (or `/proc/fs/nfsd/exports`, kernel-level active export table).',
    definition: 'The internal state tables reflecting currently established and exported NFS filesystems.',
    example: 'cat /var/lib/nfs/etab\ncat /proc/fs/nfsd/exports',
    exampleExplanation: 'Inspects active export definitions directly from the kernel filesystem interface.',
    examTip: '`etab` stands for Export TABle. If `/etc/exports` changes, `exportfs` updates `etab` and informs the kernel.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2982,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'showmount -e',
    question: 'Which command queries a remote NFS server from a client to list all directories currently exported by that server?',
    answer: '`showmount -e <server_ip_or_hostname>`',
    definition: 'Queries the remote `mountd` daemon to discover all export paths and their allowed client lists.',
    example: 'showmount -e 192.168.1.10\nExport list for 192.168.1.10:\n/srv/data 192.168.1.0/24\n/srv/backup *.corp.local',
    exampleExplanation: 'Verifies network connectivity and confirms that the server is publishing the expected shares to the client.',
    examTip: '`-e` stands for Exports. If `showmount -e` fails with RPC errors, check portmapper (`rpcbind`) or firewall on port 111/TCP.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2983,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'showmount -a',
    question: 'What information does `showmount -a` display when executed on or against an NFS server?',
    answer: 'Lists all client IP addresses (or hostnames) and the specific exported directories they currently have mounted (reads `/var/lib/nfs/rmtab`).',
    definition: 'Queries the remote mount table maintained by the `mountd` service.',
    example: 'showmount -a\n192.168.1.50:/srv/data\n192.168.1.55:/srv/backup',
    exampleExplanation: 'Shows active NFS clients and their mounted directories.',
    examTip: 'Note: `showmount -a` relies on NFSv3 `mountd` notifications; pure NFSv4 clients do not register in `rmtab`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2984,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'showmount -d',
    question: 'What does the `showmount -d` flag output?',
    answer: 'Lists only the exported directories that are currently mounted by at least one client (without showing client IPs).',
    definition: 'Filters `showmount` output to show only active directory mount points.',
    example: 'showmount -d 192.168.1.10\nDirectories on 192.168.1.10:\n/srv/data\n/srv/backup',
    exampleExplanation: 'Summarizes which export paths are actively in use by network clients.',
    examTip: 'Mnemonic: `-e` = all exports; `-a` = all clients and mounts; `-d` = directories only.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2985,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'rpcinfo -p',
    question: 'Which command queries a server\'s portmapper (`rpcbind`) to display a list of all registered RPC program numbers, versions, protocols, and port numbers?',
    answer: '`rpcinfo -p <server_ip_or_hostname>`',
    definition: 'The standard diagnostic command for querying the RPC portmapper database on a host.',
    example: 'rpcinfo -p 192.168.1.10\n   program vers proto   port  service\n    100000    4   tcp    111  portmapper\n    100003    3   tcp   2049  nfs\n    100005    3   tcp  40048  mountd',
    exampleExplanation: 'Lists every RPC daemon registered with `rpcbind`, showing whether `nfs`, `mountd`, and `statd` are running and their port allocations.',
    examTip: 'RPC program number 100003 is `nfs`, 100005 is `mountd`, 100000 is `portmapper`, 100021 is `nlockmgr`, 100024 is `status` (`statd`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2986,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'rpcinfo -u and rpcinfo -t',
    question: 'How do you use `rpcinfo` to test RPC connectivity specifically over UDP or TCP to the `mountd` service on a server?',
    answer: '`rpcinfo -u <server> mountd` (tests UDP) or `rpcinfo -t <server> mountd` (tests TCP).',
    definition: 'Performs direct RPC ping tests over specified transport protocols.',
    example: 'rpcinfo -t 192.168.1.10 mountd\nprogram 100005 version 1 ready and waiting\nprogram 100005 version 2 ready and waiting\nprogram 100005 version 3 ready and waiting',
    exampleExplanation: 'Confirms that TCP RPC calls to `mountd` succeed and lists the supported protocol versions.',
    examTip: 'If `rpcinfo -t` succeeds but `rpcinfo -u` times out, a firewall may be blocking UDP port traffic.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2987,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'rpcbind / portmap',
    question: 'What is the role of `rpcbind` (formerly `portmap`) on an NFSv3 server, and which port does it use?',
    answer: 'Maps RPC program numbers (like NFS, mountd, statd) to dynamically assigned TCP/UDP port numbers. Listens on TCP and UDP port 111.',
    definition: 'The centralized RPC service directory daemon that translates RPC program IDs into socket port numbers.',
    example: 'systemctl status rpcbind\nss -tulpn | grep 111',
    exampleExplanation: 'Verifies that `rpcbind` is running on port 111 to broker RPC connections for clients.',
    examTip: 'If `rpcbind` is not running, NFSv3 clients cannot locate the port numbers for `mountd` or `statd` and mounts will fail.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2988,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'rpc.mountd',
    question: 'What is the responsibility of the `rpc.mountd` daemon in an NFS installation?',
    answer: 'Processes NFS mount requests from clients, verifies `/etc/exports` permissions, and passes file handles back to authorized clients.',
    definition: 'The NFS mount daemon that handles initial filesystem mounting requests.',
    example: 'systemctl status nfs-mountd # Debian/Ubuntu\nps aux | grep mountd',
    exampleExplanation: 'Ensures `rpc.mountd` is running to validate client mount attempts against `/etc/exports`.',
    examTip: '`rpc.mountd` can be configured to bind to a static port (in `/etc/nfs.conf`) to allow firewall traversal.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2989,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'rpc.statd and lockd',
    question: 'What functions do `rpc.statd` (NSM) and `lockd` (NLM) provide for NFSv3 clients and servers?',
    answer: '`lockd` (Network Lock Manager) implements byte-range file locking; `rpc.statd` (Network Status Monitor) tracks server/client reboots to recover and restore active locks.',
    definition: 'Auxiliary NFSv3 daemons providing distributed file locking and lock state recovery.',
    example: 'systemctl status rpc-statd',
    exampleExplanation: 'Verifies the lock recovery status monitor is operational on both client and server.',
    examTip: 'NFSv4 eliminated both `statd` and `lockd` by integrating lease-based locks directly into the core NFSv4 protocol.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2990,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'rpc.idmapd and /etc/idmapd.conf',
    question: 'What is the role of `rpc.idmapd` in NFSv4, and what causes files on an NFSv4 mount to show ownership as `nobody:nobody`?',
    answer: 'Translates numeric UIDs/GIDs into `user@domain` strings across the wire. If the domain configured in `/etc/idmapd.conf` (`Domain = ...`) does not match between client and server, mapping fails and ownership falls back to `nobody:nobody`.',
    definition: 'The NFSv4 ID mapping daemon responsible for string-based user and group identity translation.',
    example: 'cat /etc/idmapd.conf\n[General]\nDomain = corp.example.com',
    exampleExplanation: 'Sets the shared ID mapping domain so `alice@corp.example.com` is mapped to the local `alice` account on both systems.',
    examTip: 'Classic exam scenario: Files showing `nobody:nobody` on NFSv4 indicates a mismatch in `/etc/idmapd.conf` `Domain =` settings.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2991,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'nfsstat -s',
    question: 'What does the command `nfsstat -s` display?',
    answer: 'Displays NFS server-side RPC statistics, breaking down call counts by NFS version (v2, v3, v4) and operation type (read, write, lookup, getattr).',
    definition: 'Server-side performance and operation call counter reporting tool.',
    example: 'nfsstat -s',
    exampleExplanation: 'Shows the number of NFSv3 vs NFSv4 operations processed by the server, packet counts, and authentication failures.',
    examTip: '`-s` = Server stats; `-c` = Client stats; `-m` = Mount options; `-r` = RPC communication stats.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2992,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'nfsstat -c',
    question: 'Which command displays client-side NFS operation counters, RPC retries, and timeout statistics?',
    answer: '`nfsstat -c`',
    definition: 'Outputs client NFS call statistics, retransmissions, timeouts, and cache operations.',
    example: 'nfsstat -c',
    exampleExplanation: 'Inspects whether the client is experiencing excessive retransmissions (`retrans`) or timeouts due to server lag.',
    examTip: 'A high `retrans` count in `nfsstat -c` indicates network packet loss or an overloaded NFS server.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2993,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'nfsstat -m',
    question: 'What does `nfsstat -m` output when executed on an NFS client?',
    answer: 'Lists all currently mounted NFS filesystems and the full set of effective runtime mount options (including server version, rsize, wsize, and timeout flags).',
    definition: 'Reports detailed mount flags and performance metrics for active NFS mounts.',
    example: 'nfsstat -m',
    exampleExplanation: 'Outputs mount details such as `vers=4.2,rsize=1048576,wsize=1048576,hard,proto=tcp,timeo=600,retrans=2`.',
    examTip: 'Faster and more detailed than parsing `/proc/mounts` when verifying negotiated NFS transfer parameters.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2994,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'mount -t nfs',
    question: 'What is the syntax to mount an exported directory `/srv/data` from server `192.168.1.10` to local directory `/mnt/nfs`?',
    answer: '`mount -t nfs 192.168.1.10:/srv/data /mnt/nfs`',
    definition: 'Mounts a remote NFS filesystem into the local client directory hierarchy.',
    example: 'mount -t nfs 192.168.1.10:/srv/data /mnt/nfs\ndf -h /mnt/nfs',
    exampleExplanation: 'Mounts the share and verifies the filesystem capacity and type using `df -h`.',
    examTip: 'Requires `nfs-common` (Debian/Ubuntu) or `nfs-utils` (RHEL/CentOS) installed on the client.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2995,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'mount -t nfs -o vers=4',
    question: 'How do you force an NFS client to mount using NFS version 4, and how does the mount path differ from NFSv3?',
    answer: 'Use `mount -t nfs -o vers=4 server:/share /mnt` (or `mount -t nfs4 server:/share /mnt`). In NFSv4, the path is relative to the `fsid=0` pseudo-root (e.g. `server:/` instead of `server:/srv/nfs4`).',
    definition: 'Specifies protocol version 4 and mounts against the pseudo-filesystem root.',
    example: 'mount -t nfs -o vers=4 192.168.1.10:/ /mnt/nfsroot',
    exampleExplanation: 'Mounts the top of the exported NFSv4 virtual tree.',
    examTip: 'You can use `vers=4`, `vers=4.1`, or `vers=4.2` to select specific minor protocol revisions.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2996,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'hard vs soft mount options',
    question: 'What is the crucial difference between `hard` and `soft` mount options on an NFS client, and why is `hard` strongly recommended for data integrity?',
    answer: '`hard` (default) causes client processes to wait and retry indefinitely if the NFS server becomes unreachable, preventing data loss. `soft` causes the client to report an I/O error after a set number of retries (`retrans`), which can cause silent file corruption in write-heavy applications.',
    definition: 'Client error-handling behavior during server outages or network interruptions.',
    example: 'mount -t nfs -o hard,intr 10.0.0.1:/data /mnt/data',
    exampleExplanation: 'Uses `hard` mounting with `intr` to ensure file operations pause safely until the server recovers, while allowing users to interrupt hung processes.',
    examTip: 'Always choose `hard` mounts for writable filesystems to prevent silent data corruption.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2997,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'intr mount option',
    question: 'What does the `intr` mount option do when paired with a `hard` NFS mount?',
    answer: 'Allows a process that is hanging in an uninterruptible sleep state while waiting on a `hard` mount to be terminated by a user interrupt signal (such as `Ctrl+C` / SIGINT or SIGQUIT).',
    definition: 'Enables signal interruption for processes waiting on unreachable NFS servers.',
    example: 'mount -t nfs -o hard,intr server:/srv/share /mnt/share',
    exampleExplanation: 'If the server goes offline, a script running `ls /mnt/share` can be cleanly aborted with `Ctrl+C` rather than hanging permanently.',
    examTip: 'Note: In modern Linux kernels (NFSv4), `intr` is enabled by default and mostly deprecated, but still tested on LPIC-2 exams.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2998,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'timeo and retrans',
    question: 'What do the `timeo` and `retrans` mount options configure on an NFS client?',
    answer: '`timeo` specifies the initial timeout in tenths of a second (e.g. `timeo=600` is 60 seconds) before an unacknowledged RPC request is resent. `retrans` specifies the number of retry attempts before reporting an error (for `soft`) or logging a major timeout (for `hard`).',
    definition: 'RPC timeout and retry frequency tuning parameters.',
    example: 'mount -t nfs -o timeo=14,retrans=3 server:/share /mnt',
    exampleExplanation: 'Sets a 1.4-second timeout with 3 retry attempts.',
    examTip: 'Remember the unit: `timeo` is measured in tenths of a second (0.1s), NOT milliseconds or seconds.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2999,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: 'rsize and wsize',
    question: 'What do the `rsize` and `wsize` options control when mounting an NFS share?',
    answer: 'Specifies the maximum chunk size (in bytes) that the NFS client uses for read (`rsize`) and write (`wsize`) requests transferred across the network.',
    definition: 'I/O transfer buffer block size options used to optimize network throughput.',
    example: 'mount -t nfs -o rsize=1048576,wsize=1048576 server:/srv/media /mnt/media',
    exampleExplanation: 'Configures 1 MB (1,048,576 bytes) transfer chunks for high-performance Gigabit/10GbE network throughput.',
    examTip: 'Modern Linux NFS negotiates up to 1 MB buffer sizes automatically over TCP. Manual tuning was common in 100Mbps/UDP days.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3000,
    deck: 'Topic 209: File Sharing',
    objectiveId: '209.2',
    topicNumber: 209,
    category: 'NFS Server Configuration',
    command: '/etc/fstab NFS entry & _netdev',
    question: 'What is the purpose of the `_netdev` mount option in an `/etc/fstab` NFS entry?',
    answer: 'Informs systemd and init scripts that the filesystem resides on a network device, ensuring the system delays mounting until network interfaces are fully up and unmounts it before the network is brought down during shutdown.',
    definition: 'A systemd mount flag signaling network dependency to prevent boot-time hangs.',
    example: 'nfs.corp.local:/srv/data /mnt/data nfs defaults,_netdev,vers=4.2 0 0',
    exampleExplanation: 'Guarantees that `/mnt/data` is only mounted after network connectivity is established, preventing boot hangs.',
    examTip: 'High-yield exam question: Any network-based filesystem (NFS, CIFS, iSCSI) in `/etc/fstab` should include the `_netdev` option.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
];
