import { Flashcard } from '../../types';

/**
 * LPI LPIC-2 Exam 202 - Topic 208: Web Services
 * Part 1: Cards 2801 - 2850
 * - Objective 208.1: Basic Apache Configuration (Cards 2801-2830) [30 Cards] (Weight 4)
 * - Objective 208.2: Apache Configuration for HTTPS - Part 1 (Cards 2831-2850) [20 Cards] (Weight 3)
 */

export const topic208Part1: Flashcard[] = [
  // =========================================================================
  // OBJECTIVE 208.1: Basic Apache Configuration (Cards 2801-2830) [30 Cards]
  // =========================================================================
  {
    id: 2801,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'apache2ctl configtest / apachectl -t',
    question: 'Which command checks the syntax of the Apache web server configuration files without restarting or reloading the daemon?',
    answer: '`apache2ctl configtest` (Debian/Ubuntu) or `apachectl -t` / `httpd -t` (RHEL/CentOS).',
    definition: 'Validates all included configuration files and virtual hosts. Returns "Syntax OK" if there are no errors, or specifies the exact file and line number of any syntax violation.',
    example: 'apache2ctl configtest',
    exampleExplanation: 'Executes a configuration sanity check. Exits with status code 0 on success, preventing downtime caused by reloading broken configs.',
    examTip: 'Exam favorite: Always execute `apache2ctl configtest` or `apachectl -t` prior to issuing a `systemctl reload apache2` or `httpd`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2802,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'apache2ctl -S / httpd -S',
    question: 'Which Apache command-line flag parses and displays a structured dump of all virtual hosts, their listening ports, and the configuration files where they are defined?',
    answer: '`apache2ctl -S` or `apachectl -S` (or `httpd -S`).',
    definition: 'Displays how Apache parsed all virtual host definitions, including IP/port combinations, default virtual hosts, ServerNames, and the exact configuration file paths and line numbers.',
    example: 'apache2ctl -S',
    exampleExplanation: 'Outputs name-based and IP-based vhost tables, showing which vhost acts as the fallback default for unmatched requests.',
    examTip: 'Essential diagnostic tool on LPIC-2: `-S` is the fastest way to detect conflicting ServerNames or overlapping default VirtualHosts.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2803,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'apache2ctl -M / httpd -M',
    question: 'Which Apache control flag outputs a complete list of all currently loaded static and shared modules (DSO)?',
    answer: '`apache2ctl -M` (or `httpd -M`).',
    definition: 'Dumps all compiled-in static modules and dynamically loaded shared modules (`mod_*`) enabled in the current Apache runtime.',
    example: 'apache2ctl -M | grep ssl',
    exampleExplanation: 'Checks whether `ssl_module (shared)` is loaded in the active configuration.',
    examTip: 'Remember: On Debian/Ubuntu, `a2enmod` creates symlinks from `mods-available/` to `mods-enabled/`, but `apache2ctl -M` verifies what is actually loaded.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2804,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'a2ensite / a2dissite',
    question: 'In Debian and Ubuntu Apache installations, which command-line utilities enable and disable virtual host configuration files?',
    answer: '`a2ensite <site-name>` to enable, and `a2dissite <site-name>` to disable.',
    definition: '`a2ensite` creates a symbolic link in `/etc/apache2/sites-enabled/` pointing to the target file in `/etc/apache2/sites-available/`. `a2dissite` removes the symlink.',
    example: 'a2ensite 001-example.com.conf && systemctl reload apache2',
    exampleExplanation: 'Creates symlink `/etc/apache2/sites-enabled/001-example.com.conf` and reloads Apache to activate the site.',
    examTip: 'The `.conf` extension must match the file in `/etc/apache2/sites-available/`. RHEL/CentOS does not use these helper scripts; it uses `conf.d/*.conf` directly.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2805,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'a2enmod / a2dismod',
    question: 'Which commands enable and disable Apache modules on Debian-based Linux distributions?',
    answer: '`a2enmod <module-name>` to enable, and `a2dismod <module-name>` to disable.',
    definition: 'Creates or removes symbolic links in `/etc/apache2/mods-enabled/` targeting `.load` and `.conf` files located in `/etc/apache2/mods-available/`.',
    example: 'a2enmod rewrite ssl headers && systemctl restart apache2',
    exampleExplanation: 'Enables URL rewriting (`mod_rewrite`), SSL/TLS encryption (`mod_ssl`), and custom HTTP header manipulation (`mod_headers`).',
    examTip: 'Do not include the `mod_` prefix when invoking `a2enmod`. Use `a2enmod rewrite`, not `a2enmod mod_rewrite`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2806,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'DocumentRoot',
    question: 'Which Apache directive specifies the local filesystem directory path from which web pages are served for a VirtualHost?',
    answer: '`DocumentRoot <directory-path>`',
    definition: 'Sets the root directory of the web document tree that is mapped directly to the root URL path (`/`) requested by HTTP clients.',
    example: '<VirtualHost *:80>\n  ServerName example.com\n  DocumentRoot /var/www/example.com/public_html\n</VirtualHost>',
    exampleExplanation: 'A request for `http://example.com/logo.png` serves the file from `/var/www/example.com/public_html/logo.png`.',
    examTip: 'DocumentRoot should NOT have a trailing slash (e.g., `/var/www/html`, not `/var/www/html/`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2807,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'ServerName / ServerAlias',
    question: 'In a name-based `<VirtualHost>` configuration, what is the difference between `ServerName` and `ServerAlias`?',
    answer: '`ServerName` defines the primary FQDN of the vhost, while `ServerAlias` defines alternate hostnames or wildcard patterns matching the same vhost.',
    definition: 'Apache checks the HTTP `Host:` header against `ServerName` first, then against all `ServerAlias` directives to match incoming requests to the correct VirtualHost.',
    example: '<VirtualHost *:80>\n  ServerName example.com\n  ServerAlias www.example.com *.example.com\n</VirtualHost>',
    exampleExplanation: 'Requests with `Host: example.com`, `Host: www.example.com`, or any subdomain like `blog.example.com` are served by this single vhost block.',
    examTip: 'You can define only one `ServerName` per `<VirtualHost>`, but multiple `ServerAlias` directives (or space-separated names).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2808,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'DirectoryIndex',
    question: 'Which Apache directive specifies the default resource filename to serve when a client requests a directory path ending with a slash?',
    answer: '`DirectoryIndex <file1> [file2 ...]`',
    definition: 'Specifies an ordered list of resources that Apache looks for when a directory URL is requested (e.g., `http://example.com/docs/`).',
    example: 'DirectoryIndex index.html index.php default.htm',
    exampleExplanation: 'Apache checks for `index.html` first; if not found, it checks for `index.php`, and then `default.htm`.',
    examTip: 'If none of the specified files exist in the target directory, Apache either generates a file index listing (if `Options Indexes` is active) or returns a 403 Forbidden.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2809,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'Options -Indexes',
    question: 'Which configuration directive prevents Apache from displaying a directory listing of files when no `DirectoryIndex` file is present?',
    answer: '`Options -Indexes`',
    definition: 'Disables the automated generation of an HTML directory listing table by `mod_autoindex`. With `-Indexes`, Apache serves HTTP 403 Forbidden instead.',
    example: '<Directory /var/www/html/downloads>\n  Options -Indexes +FollowSymLinks\n</Directory>',
    exampleExplanation: 'Disables directory file browsing in `/downloads`, while keeping symbolic link resolution enabled.',
    examTip: 'Security best practice tested on LPIC-2: Prefixing options with `+` or `-` merges them with parent `<Directory>` blocks rather than overriding the entire list.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2810,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'Options FollowSymLinks / SymLinksIfOwnerMatch',
    question: 'What is the security difference between `Options FollowSymLinks` and `Options SymLinksIfOwnerMatch` in Apache?',
    answer: '`FollowSymLinks` follows all symbolic links, whereas `SymLinksIfOwnerMatch` follows links only if the target file has the same UID owner as the symlink itself.',
    definition: '`SymLinksIfOwnerMatch` prevents shared hosting users from creating symbolic links pointing to sensitive system files owned by other users or `root` (such as `/etc/shadow`).',
    example: '<Directory /var/www/html>\n  Options -Indexes +SymLinksIfOwnerMatch\n</Directory>',
    exampleExplanation: 'Restricts symlink traversal to files owned by the same user who owns the symlink.',
    examTip: 'Note that `SymLinksIfOwnerMatch` incurs extra filesystem stat calls and slight performance overhead compared to `FollowSymLinks`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2811,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'AllowOverride None / All',
    question: 'What does `AllowOverride None` specify inside an Apache `<Directory>` configuration block?',
    answer: 'It instructs Apache to completely ignore all `.htaccess` files located in that directory and its subdirectories.',
    definition: 'Controls which directive categories inside `.htaccess` can override server configuration. `None` disables `.htaccess` inspection, boosting performance and security.',
    example: '<Directory /var/www/html>\n  AllowOverride None\n  Require all granted\n</Directory>',
    exampleExplanation: 'Apache skips searching for `.htaccess` on every path segment, eliminating redundant disk I/O and preventing users from altering server rules.',
    examTip: 'Exam alert: If you set `AllowOverride None`, no rules written in `.htaccess` (such as mod_rewrite or AuthType) will ever take effect.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2812,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'AllowOverride AuthConfig FileInfo',
    question: 'If you want to permit `.htaccess` files to define user authentication and URL rewrite rules, but NOT directory options or arbitrary overrides, which `AllowOverride` values should you specify?',
    answer: '`AllowOverride AuthConfig FileInfo`',
    definition: '`AuthConfig` allows authentication directives (`AuthType`, `AuthName`, `Require`). `FileInfo` allows document-type and rewrite directives (`RewriteRule`, `ErrorDocument`, `AddType`).',
    example: '<Directory /var/www/html/app>\n  AllowOverride AuthConfig FileInfo\n</Directory>',
    exampleExplanation: 'Permits authentication and rewrite rules in `.htaccess`, while blocking dangerous overrides like `Options` or `Limit`.',
    examTip: 'AllowOverride options include: `None`, `All`, `AuthConfig`, `FileInfo`, `Indexes`, `Limit`, `Options`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2813,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'Require all granted / Require all denied',
    question: 'In Apache 2.4 (mod_authz_core), which directive pair replaced Apache 2.2\'s legacy `Order allow,deny / Allow from all` syntax?',
    answer: '`Require all granted` and `Require all denied`.',
    definition: 'Apache 2.4 modernized authorization directives. Access to directories is denied by default at the root `/` level and explicitly permitted via `Require all granted`.',
    example: '<Directory /var/www/html>\n  Require all granted\n</Directory>',
    exampleExplanation: 'Grants access to any client requesting files in `/var/www/html`.',
    examTip: 'Very common LPIC-2 exam question: Distinguishing legacy 2.2 (`Order`, `Allow from`, `Deny from`) from Apache 2.4 `Require` syntax.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2814,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'Require ip / Require host',
    question: 'How do you restrict access to a specific directory in Apache 2.4 so that only clients from the subnet `192.168.1.0/24` are allowed?',
    answer: '`Require ip 192.168.1.0/24` inside the `<Directory>` or `<Location>` container.',
    definition: 'Restricts authorization based on client IPv4/IPv6 address or CIDR network block using `mod_authz_host`.',
    example: '<Directory /var/www/admin>\n  Require ip 192.168.1.0/24 10.0.0.5\n</Directory>',
    exampleExplanation: 'Only clients originating from `192.168.1.0/24` or the single host `10.0.0.5` can access the `/var/www/admin` directory.',
    examTip: 'You can specify CIDR notation (`192.168.1.0/24`), netmask notation (`192.168.1.0/255.255.255.0`), or partial IP (`192.168.1`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2815,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: '<RequireAll> / <RequireAny>',
    question: 'Which Apache 2.4 authorization grouping containers allow logical AND and logical OR combinations of `Require` directives?',
    answer: '`<RequireAll>` (logical AND) and `<RequireAny>` (logical OR).',
    definition: '`<RequireAll>` requires all nested authorization conditions to succeed. `<RequireAny>` grants access if at least one nested authorization check passes.',
    example: '<Directory /var/www/secure>\n  <RequireAll>\n    Require ip 10.0.0.0/8\n    Require valid-user\n  </RequireAll>\n</Directory>',
    exampleExplanation: 'The client must both connect from the internal `10.0.0.0/8` network AND present valid authentication credentials.',
    examTip: 'A bare list of `Require` directives inside a `<Directory>` defaults to `<RequireAny>` behavior.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2816,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'htpasswd -c',
    question: 'Which command creates a new Apache HTTP Basic Authentication user database file and adds an initial user `alice`?',
    answer: '`htpasswd -c /etc/apache2/.htpasswd alice`',
    definition: '`-c` creates a new password file (or overwrites an existing one). It prompts interactively for `alice`\'s password and writes a salted cryptographic hash.',
    example: 'htpasswd -c /etc/apache2/.htpasswd alice',
    exampleExplanation: 'Creates `/etc/apache2/.htpasswd` and stores the username and hashed password.',
    examTip: 'Warning on exam: NEVER use `-c` when adding subsequent users, as it will overwrite and erase all previously created users in the file!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2817,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'htpasswd -B -b / -D',
    question: 'Which `htpasswd` flags enforce bcrypt hashing, accept passwords non-interactively on the command line, and delete an existing user?',
    answer: '`-B` enforces bcrypt hashing; `-b` takes password from command line (`htpasswd -b file user pass`); `-D` deletes the user.',
    definition: '`-B` utilizes modern Blowfish/bcrypt encryption instead of older MD5 or crypt. `-b` allows scripted non-interactive execution. `-D` removes user entry.',
    example: 'htpasswd -B /etc/apache2/.htpasswd bob\nhtpasswd -D /etc/apache2/.htpasswd charlie',
    exampleExplanation: 'Adds or updates `bob` using bcrypt; removes user `charlie` from the password file.',
    examTip: 'Be sure to protect `.htpasswd` files with `chmod 640` and keep them outside public `DocumentRoot` directories.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2818,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'AuthType Basic / AuthUserFile',
    question: 'Which directives configure HTTP Basic Authentication inside an Apache `<Directory>` block?',
    answer: '`AuthType Basic`, `AuthName "<Realm>"`, `AuthUserFile <path>`, and `Require valid-user` (or `Require user <names>`).',
    definition: 'Enforces HTTP 401 Unauthorized password challenge using standard RFC 7617 Basic Access Authentication.',
    example: '<Directory /var/www/html/private>\n  AuthType Basic\n  AuthName "Restricted Intranet"\n  AuthUserFile /etc/apache2/.htpasswd\n  Require valid-user\n</Directory>',
    exampleExplanation: 'Browsers prompt for username/password and submit them base64-encoded in the HTTP `Authorization` header.',
    examTip: '`Require valid-user` permits any user listed in the `AuthUserFile`, while `Require user alice bob` restricts access only to `alice` and `bob`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2819,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'AuthGroupFile',
    question: 'Which Apache directive specifies the file mapping user groups to individual user accounts for group-based authorization?',
    answer: '`AuthGroupFile <path>` used with `Require group <group-name>`.',
    definition: 'Points to a text file where each line defines a group name followed by a colon and space-separated usernames (e.g., `developers: alice bob`).',
    example: '<Directory /var/www/html/dev>\n  AuthType Basic\n  AuthName "Dev Access"\n  AuthUserFile /etc/apache2/.htpasswd\n  AuthGroupFile /etc/apache2/.htgroups\n  Require group developers\n</Directory>',
    exampleExplanation: 'Only users who authenticate successfully AND belong to the `developers` group in `/etc/apache2/.htgroups` are permitted.',
    examTip: 'Exam question: Know both `AuthUserFile` (contains usernames + hashed passwords) and `AuthGroupFile` (contains groups + plain usernames).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2820,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'CustomLog / LogFormat',
    question: 'Which Apache directives define the formatting layout and the file destination for client access requests?',
    answer: '`LogFormat "<format-string>" <nickname>` defines layout, and `CustomLog <destination> <nickname>` records requests.',
    definition: '`mod_log_config` directives. Common predefined formats are `common` (CLF) and `combined` (includes Referer and User-Agent headers).',
    example: 'LogFormat "%h %l %u %t \\"%r\\" %>s %b \\"%{Referer}i\\" \\"%{User-Agent}i\\"" combined\nCustomLog /var/log/apache2/access.log combined',
    exampleExplanation: 'Logs every HTTP request using the standardized NCSA combined log format.',
    examTip: 'Key format tokens: `%h` = client IP/host, `%u` = authenticated remote user, `%t` = time, `%r` = request line, `%>s` = final HTTP status, `%b` = bytes sent.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2821,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'ErrorLog / LogLevel',
    question: 'Which Apache directives define the destination and severity threshold for server diagnostics, errors, and warnings?',
    answer: '`ErrorLog <destination>` and `LogLevel <level>` (e.g., `warn`, `error`, `info`, `debug`).',
    definition: '`ErrorLog` specifies the file path (or pipe) where runtime errors are written. `LogLevel` filters message verbosity from `emerg` (most severe) down to `trace8` / `debug`.',
    example: 'ErrorLog /var/log/apache2/error.log\nLogLevel warn',
    exampleExplanation: 'Writes warnings, errors, critical events, and alerts to `/var/log/apache2/error.log`.',
    examTip: 'Standard production `LogLevel` is `warn`. Temporarily set `LogLevel debug` or `LogLevel rewrite:trace3` to troubleshoot complex mod_rewrite rules.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2822,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'Piped Logs (rotatelogs)',
    question: 'How can Apache pass access or error log data directly into an external rotation utility without restarting or reloading the web server?',
    answer: 'By prefixing the `CustomLog` or `ErrorLog` destination with a pipe character (`|`), e.g., `|/usr/bin/rotatelogs`.',
    definition: 'Spawns a background process that reads log lines from standard input and automatically rotates log files based on time interval (e.g., 86400s) or file size.',
    example: 'CustomLog "|/usr/bin/rotatelogs -l /var/log/apache2/access.%Y-%m-%d.log 86400" combined',
    exampleExplanation: 'Pipes logs directly to `rotatelogs`, creating a new log file every 24 hours (86400 seconds) using local time (`-l`).',
    examTip: 'LPIC-2 tests piped logs: The syntax `CustomLog "|command args" format` avoids log file lockups and eliminates log rotation reload glitches.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2823,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'KeepAlive / KeepAliveTimeout',
    question: 'What is the purpose of the `KeepAlive` directive in Apache, and what does `KeepAliveTimeout` control?',
    answer: '`KeepAlive On` enables persistent HTTP connections, and `KeepAliveTimeout` specifies how many seconds Apache waits for a new request on an idle connection before closing it.',
    definition: 'Allows multiple HTTP requests (HTML, CSS, JS, images) to be transferred over a single TCP connection, reducing latency and connection handshake overhead.',
    example: 'KeepAlive On\nMaxKeepAliveRequests 100\nKeepAliveTimeout 5',
    exampleExplanation: 'Keeps connection open for up to 100 requests, closing it if idle for more than 5 seconds.',
    examTip: 'On high-traffic servers with worker or prefork MPM, setting `KeepAliveTimeout` too high (e.g. 15+ seconds) wastes worker threads on idle clients.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2824,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'MPM (Multi-Processing Modules)',
    question: 'What are the three main Apache Multi-Processing Modules (MPMs), and which one is the modern event-driven default?',
    answer: '`mpm_prefork`, `mpm_worker`, and `mpm_event`. `mpm_event` is the modern default.',
    definition: '`mpm_prefork` uses non-threaded processes (safe for non-thread-safe libraries). `mpm_worker` uses hybrid multi-process multi-threaded architecture. `mpm_event` handles KeepAlive connections via dedicated listening threads.',
    example: 'a2dismod mpm_prefork && a2enmod mpm_event && systemctl restart apache2',
    exampleExplanation: 'Switches Apache from prefork to event MPM for higher concurrency and lower memory usage.',
    examTip: 'Remember: In modern setups, `mpm_event` is paired with PHP-FPM (`mod_proxy_fcgi`) instead of embedding `mod_php` inside `mpm_prefork`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2825,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'MaxRequestWorkers / ThreadsPerChild',
    question: 'In Apache threaded MPMs (`worker` and `event`), which directives define the number of worker threads per child process and the maximum concurrent requests served?',
    answer: '`ThreadsPerChild` defines threads per process, and `MaxRequestWorkers` sets the maximum total simultaneous requests.',
    definition: '`MaxRequestWorkers` (formerly `MaxClients`) caps concurrent active connections. The maximum number of child processes is `MaxRequestWorkers / ThreadsPerChild`.',
    example: '<IfModule mpm_event_module>\n  StartServers 3\n  MinSpareThreads 75\n  MaxSpareThreads 250\n  ThreadsPerChild 25\n  MaxRequestWorkers 400\n</IfModule>',
    exampleExplanation: 'Configures up to 16 child processes (400 / 25), each managing 25 worker threads.',
    examTip: 'If `MaxRequestWorkers` is reached, additional incoming connections are queued in the OS TCP backlog (`ListenBacklog`).',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2826,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'Alias',
    question: 'Which Apache directive from `mod_alias` maps a client-requested URL prefix to a filesystem directory outside the defined `DocumentRoot`?',
    answer: '`Alias <URL-path> <filesystem-path>`',
    definition: 'Allows documents stored anywhere on the local filesystem to be accessed via the web server without moving them into `DocumentRoot`.',
    example: 'Alias /icons/ /usr/share/apache2/icons/\n<Directory /usr/share/apache2/icons>\n  Require all granted\n</Directory>',
    exampleExplanation: 'Requests to `http://example.com/icons/alert.png` fetch the file from `/usr/share/apache2/icons/alert.png`.',
    examTip: 'Crucial syntax detail: If `<URL-path>` includes a trailing slash (`/icons/`), the `<filesystem-path>` MUST also include a trailing slash (`/icons/`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2827,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'Redirect / RedirectMatch',
    question: 'How do you configure an HTTP 301 Permanent Redirect in Apache using `mod_alias`?',
    answer: '`Redirect 301 <old-path> <new-url>` (or `Redirect permanent <old-path> <new-url>`).',
    definition: 'Informs client browsers and search engine crawlers that the requested resource has permanently moved to a new destination URL.',
    example: 'Redirect 301 /store https://store.example.com/shop',
    exampleExplanation: 'Any request starting with `/store` returns an HTTP 301 status with the `Location: https://store.example.com/shop` header.',
    examTip: 'Default redirect status code is 302 (Temporary). Always specify `301` or `permanent` if the move is intended to be permanent.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2828,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'RewriteEngine On / RewriteRule',
    question: 'Which directives enable the Apache URL rewriting engine and rewrite all requests from `/user/([0-9]+)` to `/profile.php?id=$1`?',
    answer: '`RewriteEngine On` followed by `RewriteRule ^/user/([0-9]+)$ /profile.php?id=$1 [L]`.',
    definition: '`mod_rewrite` uses regular expressions to inspect and transform incoming URL paths internally or issue external redirects.',
    example: '<IfModule mod_rewrite.c>\n  RewriteEngine On\n  RewriteRule ^/products/([A-Za-z0-9-]+)/?$ /item.php?slug=$1 [L,QSA]\n</IfModule>',
    exampleExplanation: 'Rewrites `/products/laptop` internally to `/item.php?slug=laptop` while preserving query string parameters (`QSA`).',
    examTip: 'Flags to know: `[L]` = Last rule (stop processing further rules); `[R=301]` = external redirect; `[QSA]` = Query String Append.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2829,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'RewriteCond',
    question: 'Which `mod_rewrite` directive defines a test condition that must evaluate to true before the following `RewriteRule` is executed?',
    answer: '`RewriteCond <test-string> <condition-pattern> [flags]`',
    definition: 'Evaluates server variables (such as `%{HTTP_HOST}`, `%{REQUEST_FILENAME}`, `%{HTTPS}`) against regex or file test operators (`-f`, `-d`).',
    example: 'RewriteEngine On\nRewriteCond %{REQUEST_FILENAME} !-f\nRewriteCond %{REQUEST_FILENAME} !-d\nRewriteRule ^(.*)$ /index.php [L]',
    exampleExplanation: 'If the requested URI does not match an existing file (`!-f`) or directory (`!-d`), route all traffic to `/index.php` (standard front-controller pattern).',
    examTip: 'By default, multiple consecutive `RewriteCond` lines operate as a logical AND. Use the `[OR]` flag for logical OR.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2830,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.1',
    topicNumber: 208,
    category: 'Basic Apache Configuration',
    command: 'Listen',
    question: 'In `/etc/apache2/ports.conf` or `httpd.conf`, which directive instructs Apache which network interfaces and TCP ports to bind to?',
    answer: '`Listen [IP-address:]<port-number>`',
    definition: 'Tells Apache to open listening sockets. If no IP address is specified (e.g., `Listen 80`), Apache binds to port 80 on all IPv4 and IPv6 interfaces.',
    example: 'Listen 80\nListen 192.168.1.100:8080\nListen 443',
    exampleExplanation: 'Listens on all interfaces on ports 80 and 443, and binds port 8080 exclusively on the `192.168.1.100` interface.',
    examTip: 'Every port referenced in `<VirtualHost *:<port>>` must have a corresponding `Listen` directive, otherwise Apache will not receive connections on it.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },

  // =========================================================================
  // OBJECTIVE 208.2: Apache Configuration for HTTPS (Cards 2831-2850) [Part 1: 20 Cards]
  // =========================================================================
  {
    id: 2831,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLEngine on',
    question: 'Which directive activates the SSL/TLS encryption protocol engine within an Apache `<VirtualHost *:443>` block?',
    answer: '`SSLEngine on`',
    definition: 'Provided by `mod_ssl`. Enables TLS handshake negotiation, public key cryptography, and secure HTTPS protocol handling on that virtual host.',
    example: '<VirtualHost *:443>\n  ServerName secure.example.com\n  DocumentRoot /var/www/secure\n  SSLEngine on\n  SSLCertificateFile /etc/ssl/certs/example.crt\n  SSLCertificateKeyFile /etc/ssl/private/example.key\n</VirtualHost>',
    exampleExplanation: 'Activates TLS encryption for connections established to `secure.example.com` on TCP port 443.',
    examTip: 'Forgetting `SSLEngine on` is a classic mistake: Apache will accept plain unencrypted HTTP on port 443, causing TLS handshake errors in browsers.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2832,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLCertificateFile',
    question: 'Which Apache directive points to the server\'s public X.509 certificate file (and optionally intermediate CA certificates)?',
    answer: '`SSLCertificateFile <path-to-pem-file>`',
    definition: 'Specifies the path to the PEM-encoded public certificate file presented to clients during the TLS handshake to authenticate the server.',
    example: 'SSLCertificateFile /etc/ssl/certs/www.example.com.crt',
    exampleExplanation: 'Points Apache to the domain\'s public certificate signed by a Certificate Authority.',
    examTip: 'In Apache 2.4.8 and newer, `SSLCertificateFile` can contain the full certificate chain (server certificate followed by intermediate CA certificates), replacing `SSLCertificateChainFile`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2833,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLCertificateKeyFile',
    question: 'Which Apache directive specifies the path to the server\'s secret private key file corresponding to the public certificate?',
    answer: '`SSLCertificateKeyFile <path-to-private-key>`',
    definition: 'Points to the PEM-encoded private RSA/ECDSA key file used to decrypt pre-master secrets or generate digital signatures during TLS negotiation.',
    example: 'SSLCertificateKeyFile /etc/ssl/private/www.example.com.key',
    exampleExplanation: 'Loads the server private key from the restricted `/etc/ssl/private/` directory.',
    examTip: 'Filesystem security: The private key file must have restricted permissions (`chmod 600` or `400`, owned by `root`) so unprivileged users cannot read it.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2834,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLCACertificateFile',
    question: 'Which Apache directive specifies a file containing concatenated PEM certificates of Certificate Authorities trusted for client certificate authentication?',
    answer: '`SSLCACertificateFile <path-to-ca-bundle>`',
    definition: 'Sets the CA bundle against which client-presented certificates are validated when mutual TLS (mTLS) / client authentication is enabled.',
    example: 'SSLCACertificateFile /etc/ssl/certs/corporate-internal-ca.pem\nSSLVerifyClient require\nSSLVerifyDepth 2',
    exampleExplanation: 'Tells Apache to authenticate client certificates signed by the designated internal Certificate Authority.',
    examTip: 'Do not confuse `SSLCACertificateFile` (for validating client certificates) with intermediate certificates supplied to browsers.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2835,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'Server Name Indication (SNI)',
    question: 'What TLS extension enables Apache to serve multiple independent HTTPS VirtualHosts with different domain certificates using a single IP address?',
    answer: 'Server Name Indication (SNI).',
    definition: 'An extension to the TLS protocol where the client includes the requested hostname in the initial unencrypted TLS `ClientHello` handshake, allowing the server to select the matching certificate.',
    example: '<VirtualHost *:443>\n  ServerName site-a.com\n  SSLCertificateFile /etc/ssl/certs/site-a.crt\n</VirtualHost>\n<VirtualHost *:443>\n  ServerName site-b.com\n  SSLCertificateFile /etc/ssl/certs/site-b.crt\n</VirtualHost>',
    exampleExplanation: 'Both virtual hosts share port 443 on the same IP; SNI tells Apache whether to send `site-a.crt` or `site-b.crt`.',
    examTip: 'LPIC-2 core concept: Prior to SNI, each HTTPS virtual host required its own unique dedicated IP address because the HTTP `Host:` header was encrypted inside TLS.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2836,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLProtocol',
    question: 'Which Apache directive configures permitted TLS protocol versions, and how do you explicitly disable insecure legacy protocols (SSLv3, TLS 1.0, TLS 1.1)?',
    answer: '`SSLProtocol all -SSLv3 -TLSv1 -TLSv1.1` (or `SSLProtocol TLSv1.2 TLSv1.3`).',
    definition: 'Defines the cryptographic transport protocol versions supported for client handshakes, mitigating attacks like POODLE and BEAST.',
    example: 'SSLProtocol all -SSLv3 -TLSv1 -TLSv1.1',
    exampleExplanation: 'Enforces TLS 1.2 and TLS 1.3 only, disabling outdated, vulnerable protocols.',
    examTip: 'Minus sign (`-`) excludes a protocol version; plus sign (`+`) includes it. SSLv2 and SSLv3 must always be excluded.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2837,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLCipherSuite',
    question: 'Which directive configures the prioritized list of cryptographic cipher algorithms permitted during the TLS handshake in Apache?',
    answer: '`SSLCipherSuite <cipher-spec>`',
    definition: 'Controls the algorithms used for key exchange (ECDHE, DHE), bulk encryption (AES-GCM, CHACHA20), and message authentication (SHA256, SHA384).',
    example: 'SSLCipherSuite HIGH:!aNULL:!MD5:!3DES:!CAMELLIA:!RC4\nSSLHonorCipherOrder on',
    exampleExplanation: 'Requires high-strength ciphers and explicitly bans anonymous authentication (`!aNULL`), MD5, 3DES, and RC4.',
    examTip: '`SSLHonorCipherOrder on` forces the server\'s cipher preference order over the client\'s preference, preventing downgrade attacks.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2838,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'HSTS (Strict-Transport-Security)',
    question: 'How do you configure Apache to send the HTTP Strict Transport Security (HSTS) response header to enforce HTTPS-only connections in client browsers?',
    answer: '`Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"` using `mod_headers`.',
    definition: 'Informs browsers that the domain must only ever be accessed over HTTPS for the next `max-age` seconds (e.g. 1 year), preventing SSL-stripping man-in-the-middle attacks.',
    example: '<VirtualHost *:443>\n  ...\n  Header always set Strict-Transport-Security "max-age=63072000; includeSubDomains; preload"\n</VirtualHost>',
    exampleExplanation: 'Forces browsers to automatically rewrite any `http://` URL to `https://` before sending requests over the wire.',
    examTip: 'Requires `a2enmod headers`. The `always` keyword ensures the header is included on error responses (like 404 or 500) as well as 200 OK.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2839,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'HTTP to HTTPS Redirect',
    question: 'What is the recommended Apache `<VirtualHost *:80>` configuration to automatically redirect all unencrypted HTTP traffic to HTTPS?',
    answer: '`<VirtualHost *:80>\n  ServerName example.com\n  ServerAlias www.example.com\n  Redirect permanent / https://example.com/\n</VirtualHost>`',
    definition: 'Issues an HTTP 301 Permanent Redirect response that instructs clients to resubmit their request over secure port 443.',
    example: '<VirtualHost *:80>\n  ServerName example.com\n  Redirect permanent / https://example.com/\n</VirtualHost>',
    exampleExplanation: 'A request for `http://example.com/about.html` is redirected to `https://example.com/about.html`.',
    examTip: 'Alternatively, `RewriteEngine On` with `RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [R=301,L]` can be used, but `Redirect permanent` is simpler and faster.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2840,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'openssl genrsa / genpkey',
    question: 'Which `openssl` command generates a new 2048-bit RSA private key and saves it to `/etc/ssl/private/server.key`?',
    answer: '`openssl genrsa -out /etc/ssl/private/server.key 2048` (or `openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out /etc/ssl/private/server.key`).',
    definition: 'Generates mathematically paired private key primes used as the foundation for SSL/TLS certificates.',
    example: 'openssl genrsa -out /etc/ssl/private/server.key 2048 && chmod 600 /etc/ssl/private/server.key',
    exampleExplanation: 'Generates a 2048-bit RSA key and sets read-write permissions exclusively for the root user.',
    examTip: 'To protect the key with a passphrase, add `-aes256` or `-des3`. To generate an unencrypted key, omit encryption flags.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2841,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'openssl req -new',
    question: 'Which command generates a Certificate Signing Request (CSR) from an existing private key `server.key`?',
    answer: '`openssl req -new -key server.key -out server.csr`',
    definition: 'Creates a PKCS#10 CSR file containing the public key and organization identity attributes (Country, State, Locality, Organization, Common Name / SAN) to be sent to a CA for signing.',
    example: 'openssl req -new -key /etc/ssl/private/server.key -out /tmp/server.csr',
    exampleExplanation: 'Prompts interactively for distinguished name (DN) fields, including the critical FQDN in Common Name (CN).',
    examTip: 'Exam alert: The `Common Name (CN)` or Subject Alternative Name (SAN) must match the exact domain or wildcard (e.g. `*.example.com`) requested by clients.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2842,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'openssl req -x509 (Self-Signed)',
    question: 'Which single `openssl` command generates both a new 2048-bit RSA private key and a self-signed X.509 certificate valid for 365 days without prompting for a passphrase?',
    answer: '`openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout server.key -out server.crt`',
    definition: 'Combines key generation and self-signing. `-nodes` ("no DES") prevents passphrase encryption so Apache can boot unattended. `-x509` outputs a self-signed certificate instead of a CSR.',
    example: 'openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout /etc/ssl/private/self.key -out /etc/ssl/certs/self.crt',
    exampleExplanation: 'Creates a ready-to-use self-signed key and certificate pair for testing or internal staging.',
    examTip: 'A frequent LPIC-2 practical test command: Remember `-x509` produces a certificate, while `-new` (without `-x509`) produces a CSR.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2843,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'openssl x509 -text -noout',
    question: 'Which command displays the human-readable text details of an X.509 certificate file, including Issuer, Validity dates, Subject, and SANs?',
    answer: '`openssl x509 -in cert.crt -text -noout`',
    definition: 'Parses the binary ASN.1/DER or base64 PEM structure of a certificate and outputs human-readable fields. `-noout` suppresses dumping the raw PEM block.',
    example: 'openssl x509 -in /etc/ssl/certs/server.crt -text -noout | grep -A 2 "Validity"',
    exampleExplanation: 'Inspects certificate expiration dates (`Not Before` and `Not After`).',
    examTip: 'To check only the expiration date directly, use `openssl x509 -in cert.crt -enddate -noout`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2844,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'openssl req -text -noout',
    question: 'Which command inspects and prints the contents of a Certificate Signing Request (CSR) file?',
    answer: '`openssl req -in request.csr -text -noout`',
    definition: 'Verifies the Subject DN details, public key algorithm, and SAN extensions inside a CSR before submitting it to a Certificate Authority.',
    example: 'openssl req -in /tmp/server.csr -text -noout -verify',
    exampleExplanation: 'Prints all CSR information and verifies the cryptographic signature of the requester.',
    examTip: 'Remember: Use `openssl x509` for certificates (`.crt`/`.pem`) and `openssl req` for signing requests (`.csr`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2845,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'openssl s_client -connect',
    question: 'Which command initiates an interactive SSL/TLS handshake with a remote HTTPS server to test the certificate chain, TLS protocol, and cipher negotiation?',
    answer: '`openssl s_client -connect <hostname>:443 -servername <hostname>`',
    definition: 'Acts as a generic SSL/TLS diagnostic client. Displays certificate chains, verification errors, negotiated protocols, and cipher suites.',
    example: 'openssl s_client -connect www.example.com:443 -servername www.example.com',
    exampleExplanation: '`-servername` passes the SNI hostname, ensuring the web server returns the correct virtual host certificate.',
    examTip: 'Exam essential: Always pass `-servername` when troubleshooting SNI-enabled servers, or you may receive the default vhost\'s certificate instead.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2846,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'Key / Certificate Modulus Verification',
    question: 'How do you verify with OpenSSL that a private key file matches a public certificate file?',
    answer: 'By comparing their MD5 checksums of the RSA modulus: `openssl x509 -noout -modulus -in cert.crt | openssl md5` and `openssl rsa -noout -modulus -in key.key | openssl md5`.',
    definition: 'If the MD5/SHA256 hashes of the mathematical modulus are identical, the private key and public certificate are a matching pair.',
    example: 'diff <(openssl x509 -noout -modulus -in srv.crt | md5sum) <(openssl rsa -noout -modulus -in srv.key | md5sum)',
    exampleExplanation: 'If output is empty (diff finds no difference), the key and certificate belong together.',
    examTip: 'Classic troubleshooting scenario: Apache fails to start with "SSL Library Error: error:0B080074... key values mismatch" when the key and cert don\'t match.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2847,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'Removing Key Passphrase (openssl rsa)',
    question: 'If an Apache private key is encrypted with a passphrase, preventing unattended server boots, how can you remove the passphrase using OpenSSL?',
    answer: '`openssl rsa -in encrypted.key -out decrypted.key`',
    definition: 'Prompts once for the decryption passphrase and writes out an unencrypted RSA private key.',
    example: 'openssl rsa -in /etc/ssl/private/server.key -out /etc/ssl/private/server-nopass.key',
    exampleExplanation: 'Removes the passphrase requirement, allowing Apache to read the key without manual operator interaction during system reboots.',
    examTip: 'Alternative Apache directive: `SSLPassPhraseDialog exec:/path/to/script.sh` can supply passphrases automatically.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2848,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'Certbot (Let\'s Encrypt)',
    question: 'Which tool automatically requests, validates, and deploys free DV certificates from Let\'s Encrypt using the ACME protocol for Apache?',
    answer: '`certbot` (with `--apache` plugin).',
    definition: 'The official Electronic Frontier Foundation (EFF) ACME client. It modifies Apache configs automatically or obtains standalone certificates via HTTP-01 / DNS-01 challenges.',
    example: 'certbot --apache -d example.com -d www.example.com',
    exampleExplanation: 'Proves domain ownership via HTTP-01 challenge, downloads certificates to `/etc/letsencrypt/live/example.com/`, and updates Apache vhost configuration.',
    examTip: 'Automated renewal is tested via `certbot renew --dry-run`, typically managed by a systemd timer (`certbot.timer`) or cron job.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2849,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'certbot certonly --webroot',
    question: 'How do you instruct `certbot` to obtain certificates without modifying existing Apache configuration files, using an existing webroot directory for validation?',
    answer: '`certbot certonly --webroot -w /var/www/html -d example.com`',
    definition: '`certonly` requests and saves certificates without touching Apache configuration files. `--webroot` places challenge tokens under `<webroot>/.well-known/acme-challenge/`.',
    example: 'certbot certonly --webroot -w /var/www/example.com/public_html -d example.com -d www.example.com',
    exampleExplanation: 'Places challenge files in the webroot directory for the CA to verify over standard HTTP.',
    examTip: 'Great for production environments where administrators want full manual control over their Apache configuration files.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2850,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLSessionCache',
    question: 'Which Apache directive configures a shared memory cache for storing TLS session parameters to accelerate handshake performance for returning clients?',
    answer: '`SSLSessionCache <type>:<location>` (e.g., `shmcb:/run/apache2/ssl_scache(512000)`).',
    definition: 'Enables TLS session resumption (session IDs / session tickets). Avoids full expensive asymmetric RSA/ECDHE renegotiations on subsequent connections.',
    example: 'SSLSessionCache shmcb:/run/apache2/ssl_scache(512000)\nSSLSessionCacheTimeout 300',
    exampleExplanation: 'Allocates a 512KB cyclic buffer in shared memory (`shmcb`) with a 300-second session validity timeout.',
    examTip: '`shmcb` (shared memory cyclic buffer) is the highest performance and recommended storage type for `SSLSessionCache`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
];
