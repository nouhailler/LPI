import { Flashcard } from '../../types';

/**
 * LPI LPIC-2 Exam 202 - Topic 208: Web Services
 * Part 2: Cards 2851 - 2900
 * - Objective 208.2: Apache Configuration for HTTPS - Part 2 (Cards 2851-2855) [5 Cards]
 * - Objective 208.3: Implementing Squid as a Caching Proxy (Cards 2856-2875) [20 Cards] (Weight 2)
 * - Objective 208.4: Implementing Nginx as a Web Server and Reverse Proxy (Cards 2876-2900) [25 Cards] (Weight 3)
 */

export const topic208Part2: Flashcard[] = [
  // =========================================================================
  // OBJECTIVE 208.2: Apache Configuration for HTTPS - Part 2 (Cards 2851-2855) [5 Cards]
  // =========================================================================
  {
    id: 2851,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLUseStapling (OCSP Stapling)',
    question: 'Which Apache directives enable OCSP Stapling to improve TLS handshake speed and preserve client privacy during certificate revocation checks?',
    answer: '`SSLUseStapling on` and `SSLStaplingCache "shmcb:/run/apache2/ocsp(128000)"`.',
    definition: 'Directs Apache to periodically query the Certificate Authority\'s OCSP server, cache the digitally signed revocation response, and "staple" it into the TLS handshake for clients.',
    example: 'SSLUseStapling on\nSSLStaplingCache "shmcb:/run/apache2/ocsp(128000)"\nSSLStaplingResponseMaxAge 900',
    exampleExplanation: 'Avoids having every visiting browser independently contact external CA servers, eliminating latency and privacy leaks.',
    examTip: 'Must be configured in the global server context (outside `<VirtualHost>`), while `SSLUseStapling on` can be toggled per vhost.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2852,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLVerifyClient (Mutual TLS / mTLS)',
    question: 'Which Apache directive requires connecting clients to present a valid, trusted SSL/TLS certificate to access a secure resource?',
    answer: '`SSLVerifyClient require` (along with `SSLVerifyDepth <number>`).',
    definition: 'Enforces client certificate authentication (two-way TLS / mTLS). If the client does not provide a certificate signed by a trusted CA in `SSLCACertificateFile`, access is rejected.',
    example: '<Location /secure-api>\n  SSLVerifyClient require\n  SSLVerifyDepth 1\n</Location>',
    exampleExplanation: 'Protects the `/secure-api` endpoint so that only clients with a verified certificate issued by the server\'s trusted CA can connect.',
    examTip: 'Values for `SSLVerifyClient`: `none` (default), `optional`, `require`, `optional_no_ca`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2853,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLOptions +StdEnvVars',
    question: 'Which `SSLOptions` flag exports TLS environment variables (such as `SSL_CLIENT_S_DN`, `SSL_CIPHER`, `SSL_PROTOCOL`) to CGI, PHP, and proxy scripts?',
    answer: '`SSLOptions +StdEnvVars`',
    definition: 'Passes cryptographic metadata from the TLS session to backend applications via standard CGI environment variables.',
    example: '<Directory /var/www/html/cgi-bin>\n  SSLOptions +StdEnvVars +ExportCertData\n</Directory>',
    exampleExplanation: 'Allows backend scripts to inspect the client certificate and cipher suite used for the active connection.',
    examTip: 'Because extracting and injecting these environment variables incurs per-request overhead, it is typically disabled globally and enabled only where needed.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2854,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLCARevocationFile / SSLCARevocationPath',
    question: 'Which directives specify the Certificate Revocation List (CRL) file or hashed directory used by Apache to verify that client certificates have not been revoked?',
    answer: '`SSLCARevocationFile <file>` or `SSLCARevocationPath <directory>` (with `SSLCARevocationCheck chain`).',
    definition: 'Tells `mod_ssl` where to find PEM-formatted CRLs published by the issuing CA to reject compromised client certificates.',
    example: 'SSLCARevocationFile /etc/ssl/certs/ca-crl.pem\nSSLCARevocationCheck chain',
    exampleExplanation: 'Checks incoming client certificates against the revocation list before granting access.',
    examTip: 'If using `SSLCARevocationPath`, files in the directory must be named with their OpenSSL hash (generated via `c_rehash`).',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2855,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.2',
    topicNumber: 208,
    category: 'Apache Configuration for HTTPS',
    command: 'SSLCompression off',
    question: 'Why should the directive `SSLCompression off` always be set in modern Apache SSL configurations?',
    answer: 'To protect against the CRIME (Compression Ratio Info-leak Made Easy) attack.',
    definition: 'TLS-level data compression can leak sensitive information (such as session cookies) when an attacker can inject plaintext into requests and observe compressed cipher lengths.',
    example: 'SSLCompression off',
    exampleExplanation: 'Disables TLS compression in OpenSSL, closing the vector for CRIME side-channel attacks.',
    examTip: 'Disabled by default in recent OpenSSL and Apache releases, but tested frequently in LPIC-2 security sections.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },

  // =========================================================================
  // OBJECTIVE 208.3: Implementing Squid as a Caching Proxy (Cards 2856-2875) [20 Cards]
  // =========================================================================
  {
    id: 2856,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'http_port',
    question: 'Which directive in `squid.conf` specifies the TCP port on which Squid listens for incoming client proxy requests, and what is the default port number?',
    answer: '`http_port 3128` (default TCP port is 3128).',
    definition: 'Defines the network interface socket and port where Squid accepts HTTP forward proxy traffic from clients.',
    example: 'http_port 3128\nhttp_port 192.168.1.1:8080',
    exampleExplanation: 'Listens on all interfaces on default port 3128, and additionally on `192.168.1.1` on port 8080.',
    examTip: 'LPIC-2 core fact: Port 3128 is Squid\'s standard assigned IANA port. Be prepared to identify this port in firewall and configuration questions.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2857,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'squid -z',
    question: 'Which command must be executed on a freshly installed or reconfigured Squid proxy to initialize its swap disk cache directories?',
    answer: '`squid -z` (or `squid -z -N` to run in foreground).',
    definition: 'Scans the paths specified in `cache_dir` directives and creates the required level-1 and level-2 subdirectories on disk before Squid can store cached objects.',
    example: 'squid -z',
    exampleExplanation: 'Creates swap directory structures under `/var/spool/squid` (e.g. `00/`, `01/`, ...).',
    examTip: 'If you start Squid before running `squid -z`, it will fail to start with errors about missing swap directories.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2858,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'squid -k reconfigure',
    question: 'Which command tells the running Squid daemon to re-read and apply its configuration file without terminating active client connections?',
    answer: '`squid -k reconfigure` (or sending `SIGHUP` to the Squid master process).',
    definition: '`-k` manages running Squid processes. `reconfigure` re-reads `/etc/squid/squid.conf`, applies ACL modifications, and re-opens log files.',
    example: 'squid -k reconfigure',
    exampleExplanation: 'Safely updates running Squid proxy rules with zero downtime.',
    examTip: 'Other `-k` actions to know: `squid -k parse` (test syntax), `squid -k shutdown` (graceful stop), `squid -k interrupt` (immediate kill), `squid -k rotate` (rotate logs).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2859,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'squid -k parse',
    question: 'Which Squid command checks `/etc/squid/squid.conf` for syntax errors without altering the running service or initializing cache?',
    answer: '`squid -k parse`',
    definition: 'Parses and validates all directives and ACL definitions in `squid.conf`. If syntax errors exist, it outputs line numbers and specific warnings.',
    example: 'squid -k parse',
    exampleExplanation: 'Verifies the configuration file before reloading or restarting Squid.',
    examTip: 'Equivalent to `apachectl configtest` or `nginx -t` for Squid administration.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2860,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'cache_dir',
    question: 'In the directive `cache_dir ufs /var/spool/squid 10000 16 256`, what do the parameters `10000`, `16`, and `256` represent?',
    answer: '`10000` is disk storage size in Megabytes (10 GB); `16` is the number of first-level subdirectories; `256` is the number of second-level subdirectories.',
    definition: 'Defines the on-disk storage engine (`ufs`, `aufs`, `diskd`), base directory, maximum storage allocation, and hierarchical directory structure to prevent filesystem inode lookups from slowing down.',
    example: 'cache_dir ufs /var/spool/squid 10000 16 256',
    exampleExplanation: 'Allocates up to 10GB of disk space in `/var/spool/squid`, dividing it across 16 first-level folders each containing 256 second-level folders.',
    examTip: 'High probability exam question: Know the exact order of parameters: `cache_dir <Type> <Directory> <Size-in-MB> <L1-dirs> <L2-dirs>`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2861,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'cache_mem',
    question: 'Which directive sets the maximum amount of RAM Squid uses to store hot cache objects, transit objects, and negative-cached responses?',
    answer: '`cache_mem <size>` (e.g., `cache_mem 256 MB`).',
    definition: 'Specifies memory dedicated to the in-RAM object cache. Note that total Squid RAM usage will exceed this value due to index structures and connection buffers.',
    example: 'cache_mem 512 MB\nmaximum_object_size_in_memory 64 KB',
    exampleExplanation: 'Allocates 512MB of RAM for caching objects up to 64KB in size.',
    examTip: 'Do not confuse `cache_mem` with the total process memory of Squid. As a rule of thumb, each 1GB of disk cache requires roughly 10-14MB of additional RAM just for indexing.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2862,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'maximum_object_size / minimum_object_size',
    question: 'Which directives set the size thresholds for objects that Squid is allowed to store in its disk cache?',
    answer: '`maximum_object_size <size>` and `minimum_object_size <size>`.',
    definition: 'Objects larger than `maximum_object_size` are transferred directly to the client without being written to disk cache.',
    example: 'maximum_object_size 100 MB\nminimum_object_size 0 KB',
    exampleExplanation: 'Caches any file up to 100MB in size.',
    examTip: 'The default `maximum_object_size` is typically 4 MB. Increase this if you want Squid to cache large ISOs or software update packages.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2863,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'acl <name> src',
    question: 'How do you define a Squid Access Control List (ACL) named `local_lan` that matches client IP addresses originating from `192.168.10.0/24`?',
    answer: '`acl local_lan src 192.168.10.0/24`',
    definition: '`src` matches client IP addresses or network subnets connecting to Squid.',
    example: 'acl local_lan src 192.168.10.0/24 10.0.0.0/8\nhttp_access allow local_lan',
    exampleExplanation: 'Matches clients coming from either `192.168.10.0/24` or `10.0.0.0/8`.',
    examTip: 'Defining an `acl` alone does nothing; it MUST be paired with an `http_access allow` or `http_access deny` directive to take effect.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2864,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'acl <name> dstdomain',
    question: 'How do you define a Squid ACL matching requests to `example.com` and all of its subdomains (e.g., `mail.example.com`)?',
    answer: '`acl company_domain dstdomain .example.com` (note the leading dot).',
    definition: '`dstdomain` matches the requested destination server hostname. A leading dot (`.`) matches the domain and any subdomains recursively.',
    example: 'acl blocked_sites dstdomain .badsite.com .gambling.org\nhttp_access deny blocked_sites',
    exampleExplanation: 'Blocks access to `badsite.com`, `www.badsite.com`, `api.badsite.com`, etc.',
    examTip: 'Exam favorite: A leading dot (`.example.com`) matches `example.com` and all subdomains. Without the dot (`example.com`), it matches only the exact domain.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2865,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'acl <name> url_regex',
    question: 'Which ACL type matches URLs using regular expressions (case-sensitive or case-insensitive)?',
    answer: '`acl <name> url_regex [-i] <regex>` (`-i` makes matching case-insensitive).',
    definition: 'Tests the entire requested URL string against regular expressions. CPU-intensive compared to `dstdomain`.',
    example: 'acl media_files url_regex -i \\.(mp3|mp4|avi|mkv)$ \nhttp_access deny media_files',
    exampleExplanation: 'Denies downloading media files matching the given file extensions regardless of case.',
    examTip: 'Use `url_regex -i` for case-insensitivity. For plain domain matching, prefer `dstdomain` as it is much faster.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2866,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'acl <name> time',
    question: 'Which ACL type restricts proxy access based on days of the week and hours of the day?',
    answer: '`acl <name> time [days] [hh:mm-hh:mm]`',
    definition: 'Restricts access to specified schedule windows. Day letters: `S` (Sunday), `M` (Monday), `T` (Tuesday), `W` (Wednesday), `H` (Thursday), `F` (Friday), `A` (Saturday).',
    example: 'acl work_hours time M T W H F 09:00-17:00\nhttp_access allow local_lan work_hours\nhttp_access deny local_lan',
    exampleExplanation: 'Allows `local_lan` clients proxy access only Monday through Friday from 09:00 to 17:00.',
    examTip: 'Notice the day codes: `H` represents tHursday (since T is Tuesday), and `A` represents sAturday (since S is Sunday).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2867,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'http_access Evaluation Order',
    question: 'How does Squid evaluate multiple `http_access` rules, and what is the standard recommended final rule?',
    answer: 'Squid evaluates rules sequentially from top to bottom; the first matching rule terminates evaluation. The final rule should always be `http_access deny all`.',
    definition: 'First-match-wins model. If no rule matches, the default action is the inverse of the last rule checked, making an explicit `http_access deny all` at the bottom essential.',
    example: 'http_access deny bad_sites\nhttp_access allow localnet\nhttp_access deny all',
    exampleExplanation: 'Denies bad sites first, then allows local clients to reach anything else, and finally denies all other requests.',
    examTip: 'Placing `http_access allow localnet` before `http_access deny bad_sites` would erroneously allow local users to visit bad sites!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2868,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'Safe_ports / SSL_ports (CONNECT)',
    question: 'Why does default `squid.conf` include `acl CONNECT method CONNECT` and `http_access deny CONNECT !SSL_ports`?',
    answer: 'To prevent clients from using the HTTP `CONNECT` tunneling method to turn Squid into an open proxy for arbitrary non-HTTPS ports (e.g. SMTP on port 25).',
    definition: '`CONNECT` creates a raw TCP tunnel through the proxy (required for HTTPS). Restricting it to `SSL_ports` (port 443) prevents abuse like spam relaying.',
    example: 'acl SSL_ports port 443\nacl Safe_ports port 80 21 443\nacl CONNECT method CONNECT\nhttp_access deny !Safe_ports\nhttp_access deny CONNECT !SSL_ports',
    exampleExplanation: 'Enforces that clients can only access standard safe ports, and `CONNECT` tunnels can only target port 443.',
    examTip: 'A frequent LPIC-2 exam security question: The `CONNECT` method must be strictly locked down to SSL ports.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2869,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'auth_param basic program',
    question: 'Which directive configures Squid to use an external authentication helper (such as `basic_ncsa_auth`) with an Apache-style password file?',
    answer: '`auth_param basic program <path-to-helper> <path-to-htpasswd>`',
    definition: 'Delegates user authentication to an external helper executable. `basic_ncsa_auth` checks usernames and passwords against an `htpasswd` file.',
    example: 'auth_param basic program /usr/lib/squid/basic_ncsa_auth /etc/squid/passwords\nacl auth_users proxy_auth REQUIRED\nhttp_access allow auth_users',
    exampleExplanation: 'Prompts clients for HTTP Basic authentication credentials verified against `/etc/squid/passwords`.',
    examTip: '`proxy_auth REQUIRED` creates an ACL that requires successful authentication before access is allowed.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2870,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'squidclient',
    question: 'Which bundled command-line diagnostic tool queries a running Squid proxy cache for operational statistics and cached object status?',
    answer: '`squidclient [options] <URL>` (e.g., `squidclient mgr:info`).',
    definition: 'Acts as an HTTP client that communicates with Squid\'s internal Cache Manager interface to report hit ratios, memory usage, and storage stats.',
    example: 'squidclient mgr:info',
    exampleExplanation: 'Dumps general cache information, uptime, byte hit rates, and request hit ratios from Squid.',
    examTip: 'To check if an object is cached: `squidclient http://example.com/logo.png`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2871,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: '/var/log/squid/access.log',
    question: 'What does the primary Squid log file `/var/log/squid/access.log` record, and what does a `TCP_HIT` entry indicate?',
    answer: 'It records all client HTTP requests; `TCP_HIT` indicates the requested object was served directly from Squid\'s cache without contacting the upstream server.',
    definition: 'Each line logs timestamp, response duration, client IP, cache result code (e.g., `TCP_HIT`, `TCP_MISS`, `TCP_MEM_HIT`, `TCP_DENIED`), HTTP status, bytes transferred, and URL.',
    example: '1693800000.123  15 192.168.1.50 TCP_HIT/200 4521 GET http://example.com/file.zip - HIER_NONE/- application/zip',
    exampleExplanation: 'The client request was successfully satisfied entirely from local cache in 15ms.',
    examTip: 'Key codes: `TCP_HIT` = served from disk; `TCP_MEM_HIT` = served from RAM; `TCP_MISS` = fetched from upstream; `TCP_DENIED` = blocked by ACL.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2872,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: '/var/log/squid/cache.log',
    question: 'Which Squid log file records daemon startup messages, operational warnings, fatal errors, and storage rebuild notifications?',
    answer: '`/var/log/squid/cache.log`',
    definition: 'The diagnostic log file for Squid daemon events. Used to diagnose configuration errors, disk corruption, crashes, or helper process failures.',
    example: 'tail -f /var/log/squid/cache.log',
    exampleExplanation: 'Monitors Squid operational status and swap rebuild progress in real time.',
    examTip: 'If Squid exits immediately on startup, always inspect `/var/log/squid/cache.log` to find the exact fatal error message.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2873,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'visible_hostname',
    question: 'Which directive in `squid.conf` defines the hostname shown in Squid error pages and reported in the `Via` HTTP response header?',
    answer: '`visible_hostname <FQDN>`',
    definition: 'Sets the public identity of the proxy server. If Squid cannot determine its FQDN via DNS or `/etc/hosts`, it will fail to start unless `visible_hostname` is explicitly set.',
    example: 'visible_hostname proxy.example.com',
    exampleExplanation: 'Prevents "Could not determine fully qualified hostname" startup errors.',
    examTip: 'Frequently tested troubleshooting question: When Squid refuses to boot in testing VMs without valid reverse DNS, add `visible_hostname`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2874,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'refresh_pattern',
    question: 'Which directive in `squid.conf` controls how long cached objects without explicit expiration headers remain fresh before Squid checks the origin server?',
    answer: '`refresh_pattern [-i] <regex> <min-minutes> <percent>% <max-minutes> [options]`',
    definition: 'Configures cache freshness algorithms based on object age, minimum lifetime, and maximum time before revalidation.',
    example: 'refresh_pattern -i \\.(jpg|png|gif)$ 1440 50% 10080\nrefresh_pattern . 0 20% 4320',
    exampleExplanation: 'Images remain fresh for at least 1440 minutes (24h) up to a max of 10080 minutes (7 days).',
    examTip: 'The final `refresh_pattern . 0 20% 4320` is the catch-all rule for all other unclassified web objects.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2875,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.3',
    topicNumber: 208,
    category: 'Implementing Squid as a Caching Proxy',
    command: 'Reverse Proxy (Accelerator Mode)',
    question: 'How do you configure `http_port` to run Squid as an HTTP accelerator / reverse proxy in front of an internal web server at `192.168.1.50:80`?',
    answer: '`http_port 80 accel defaultsite=example.com` and `cache_peer 192.168.1.50 parent 80 0 no-query originserver`.',
    definition: 'Runs Squid in accelerator mode, intercepting incoming public requests on port 80 and caching static content on behalf of the backend origin server.',
    example: 'http_port 80 accel defaultsite=www.example.com\ncache_peer 192.168.1.50 parent 80 0 no-query originserver name=webbackend',
    exampleExplanation: 'Squid caches public requests in front of the private origin server `192.168.1.50`.',
    examTip: 'Forward proxy = client-side caching; Reverse proxy / accelerator = server-side offloading and acceleration.',
    difficulty: 'Advanced',
    status: 'unseen',
  },

  // =========================================================================
  // OBJECTIVE 208.4: Implementing Nginx as a Web Server and Reverse Proxy (Cards 2876-2900) [25 Cards]
  // =========================================================================
  {
    id: 2876,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'nginx -t',
    question: 'Which command tests the syntax of all Nginx configuration files without interrupting or reloading the web server?',
    answer: '`nginx -t` (or `nginx -t -c /path/to/nginx.conf`).',
    definition: 'Parses the master `nginx.conf` and all included configuration files. Reports whether syntax is OK and tests that referenced files (like certificates and sockets) are accessible.',
    example: 'nginx -t',
    exampleExplanation: 'Returns "nginx: configuration file /etc/nginx/nginx.conf test is successful" on valid configuration.',
    examTip: 'Never reload or restart Nginx without testing first: `nginx -t && systemctl reload nginx`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2877,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'nginx -s reload',
    question: 'What command sends a signal to the Nginx master process to perform a zero-downtime graceful configuration reload?',
    answer: '`nginx -s reload` (or `systemctl reload nginx`).',
    definition: 'The master process re-reads configuration files, starts new worker processes with the updated config, and gracefully terminates old workers after they finish in-flight requests.',
    example: 'nginx -s reload',
    exampleExplanation: 'Applies configuration updates instantly without dropping client TCP connections.',
    examTip: 'Other signals: `nginx -s stop` (fast shutdown), `nginx -s quit` (graceful shutdown), `nginx -s reopen` (reopen log files).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2878,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'nginx -V',
    question: 'Which Nginx command flag displays the version along with all compiled-in build options, paths, and third-party modules?',
    answer: '`nginx -V` (uppercase `V`).',
    definition: 'Outputs compiler arguments, `--prefix`, configuration paths, and bundled dynamic modules. Lowercase `nginx -v` only displays the version string.',
    example: 'nginx -V 2>&1 | grep -o with-http_ssl_module',
    exampleExplanation: 'Verifies whether `mod_ssl` support (`--with-http_ssl_module`) was compiled into the Nginx binary.',
    examTip: 'Remember the difference: `-v` is version only; `-V` is version AND full compilation configuration arguments.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2879,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'worker_processes / worker_connections',
    question: 'Which Nginx directives determine concurrency capacity, setting the number of worker processes and maximum simultaneous connections per worker?',
    answer: '`worker_processes` (in the `main` context) and `worker_connections` (inside the `events` block).',
    definition: '`worker_processes auto;` spawns one worker process per available CPU core. `worker_connections 1024;` allows each worker to handle up to 1024 concurrent connections.',
    example: 'worker_processes auto;\nevents {\n  worker_connections 2048;\n}',
    exampleExplanation: 'On a 4-core machine, allows up to 8,192 simultaneous connections (4 * 2048).',
    examTip: 'Theoretical maximum client capacity is `worker_processes * worker_connections` (divided by 2 for reverse proxy mode, since each client requires a client and backend connection).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2880,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'server block / server_name',
    question: 'How do you define a virtual host in Nginx listening on port 80 for `example.com` and `www.example.com`?',
    answer: '`server { listen 80; server_name example.com www.example.com; ... }`',
    definition: 'The `server` block is Nginx\'s equivalent of Apache\'s `<VirtualHost>`. `server_name` tests the HTTP `Host:` header against exact names, wildcards (`*.example.com`), or regular expressions.',
    example: 'server {\n  listen 80;\n  server_name example.com www.example.com;\n  root /var/www/example.com/html;\n  index index.html;\n}',
    exampleExplanation: 'Directs requests with `Host: example.com` or `www.example.com` to `/var/www/example.com/html`.',
    examTip: 'To set a default fallback server in Nginx: `listen 80 default_server;`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2881,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'root vs alias',
    question: 'What is the critical difference between the `root` and `alias` directives inside an Nginx `location` block?',
    answer: '`root` appends the full URI path to the specified directory, whereas `alias` replaces the matching location prefix with the alias path.',
    definition: 'For `location /images/ { root /var/data; }`, request `/images/pic.jpg` maps to `/var/data/images/pic.jpg`. For `location /images/ { alias /var/data/; }`, request `/images/pic.jpg` maps to `/var/data/pic.jpg`.',
    example: 'location /static/ {\n  alias /var/www/assets/;\n}',
    exampleExplanation: 'A request for `/static/logo.png` is read directly from `/var/www/assets/logo.png`.',
    examTip: 'A legendary Nginx exam trap: When using `alias`, remember that if the location has a trailing slash, the alias path MUST also have a trailing slash!',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2882,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'location Modifiers Priority',
    question: 'In Nginx, what is the order of priority for matching incoming request URIs against `location` blocks with different modifiers?',
    answer: '1. Exact match (`=`); 2. Preferential prefix match (`^~`); 3. Regex matches (`~` case-sensitive, `~*` case-insensitive); 4. Standard prefix match (`/path`).',
    definition: 'Nginx searches for the most specific prefix match first, but if an exact match (`=`) or preferential match (`^~`) matches, it halts immediately without checking regular expressions.',
    example: 'location = /login { ... }       # 1. Exact match\nlocation ^~ /images/ { ... }    # 2. Preferential prefix\nlocation ~* \\.(jpg|png)$ { ... } # 3. Case-insensitive regex\nlocation / { ... }              # 4. Fallback prefix',
    exampleExplanation: 'The exact order of precedence determines which block handles the request.',
    examTip: 'High-frequency exam topic: Remember that regex locations (`~` and `~*`) override general prefix matches unless `^~` is used!',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2883,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'try_files',
    question: 'What does the directive `try_files $uri $uri/ /index.php?$args;` accomplish in an Nginx configuration?',
    answer: 'It checks if a file matches `$uri`; if not, checks if a directory matches `$uri/`; if neither exists, it rewrites the request internally to `/index.php?$args`.',
    definition: 'The standard routing directive for front-controller applications (WordPress, Laravel, Single-Page Apps). It avoids 404s for virtual application routes.',
    example: 'location / {\n  try_files $uri $uri/ /index.php?$args;\n}',
    exampleExplanation: 'Serves static files directly from disk if they exist, passing all dynamic routes to the PHP router.',
    examTip: 'The final parameter is the fallback destination, which can be an internal URI, a named location (e.g. `@fallback`), or an HTTP status code (e.g. `=404`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2884,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'proxy_pass',
    question: 'Which Nginx directive passes incoming client HTTP requests to a backend application server or reverse proxy upstream pool?',
    answer: '`proxy_pass <protocol>://<backend-address>;`',
    definition: 'Provided by `ngx_http_proxy_module`. Forwards requests to a backend HTTP server over TCP or UNIX domain socket.',
    example: 'location /api/ {\n  proxy_pass http://127.0.0.1:8000;\n}',
    exampleExplanation: 'Forwards all requests starting with `/api/` to the application server listening on local port 8000.',
    examTip: 'Notice the URI trailing slash behavior: If `proxy_pass http://127.0.0.1:8000/` has a trailing slash, Nginx strips the `/api/` prefix. Without a slash, it preserves `/api/`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2885,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'proxy_set_header',
    question: 'Which Nginx directives ensure that the backend application receives the original client IP address and requested hostname when behind a reverse proxy?',
    answer: '`proxy_set_header Host $host;` and `proxy_set_header X-Real-IP $remote_addr;` (and `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`).',
    definition: 'Rewrites or appends HTTP request headers before forwarding the packet to the upstream backend server.',
    example: 'location / {\n  proxy_pass http://backend_pool;\n  proxy_set_header Host $host;\n  proxy_set_header X-Real-IP $remote_addr;\n  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n  proxy_set_header X-Forwarded-Proto $scheme;\n}',
    exampleExplanation: 'Preserves the original client IP and SSL protocol scheme (`http` or `https`) for the backend app.',
    examTip: 'Without `proxy_set_header Host $host;`, the backend receives the `Host` header set to the upstream name or IP instead of the client\'s requested domain.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2886,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'upstream block',
    question: 'How do you define a named backend server pool for load balancing in Nginx, and where is the `upstream` block placed?',
    answer: 'Inside the `http` context: `upstream <name> { server <ip1>:<port>; server <ip2>:<port>; }`.',
    definition: 'Defines a group of servers that can be referenced by `proxy_pass http://<name>;`. Defaults to round-robin load distribution.',
    example: 'upstream web_backends {\n  server 10.0.0.1:8080 weight=3;\n  server 10.0.0.2:8080;\n  server 10.0.0.3:8080 backup;\n}\nserver {\n  location / {\n    proxy_pass http://web_backends;\n  }\n}',
    exampleExplanation: 'Distributes traffic with 75% going to server 1, 25% to server 2, and uses server 3 only as a backup when others fail.',
    examTip: 'Parameters: `weight=<n>` (ratio), `backup` (standby), `down` (temporarily disabled), `max_fails` and `fail_timeout` (health check criteria).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2887,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'ip_hash / least_conn',
    question: 'Which directives inside an Nginx `upstream` block configure session persistence (sticky sessions) based on client IP, and routing to the server with fewest active connections?',
    answer: '`ip_hash;` for client IP-based session persistence, and `least_conn;` for least connections balancing.',
    definition: '`ip_hash` hashes client IPv4/IPv6 addresses to guarantee requests from the same client always reach the same backend server unless it goes down.',
    example: 'upstream cluster {\n  ip_hash;\n  server 192.168.1.10:80;\n  server 192.168.1.11:80;\n}',
    exampleExplanation: 'Directs the same client IP to the same backend node across requests.',
    examTip: 'Note: If one of the servers in an `ip_hash` pool is taken offline for maintenance, mark it with the `down` parameter to preserve existing hash mappings.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2888,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'fastcgi_pass',
    question: 'Which directives route dynamic PHP requests from Nginx to a local PHP-FPM service listening on a UNIX domain socket?',
    answer: '`fastcgi_pass unix:/run/php/php-fpm.sock;` combined with `include fastcgi_params;` and `fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;`.',
    definition: '`ngx_http_fastcgi_module` communicates with FastCGI daemons (like PHP-FPM) to execute server-side application scripts.',
    example: 'location ~ \\.php$ {\n  include snippets/fastcgi-php.conf;\n  fastcgi_pass unix:/run/php/php8.1-fpm.sock;\n}',
    exampleExplanation: 'Sends `.php` script execution requests to the PHP 8.1 FPM UNIX socket.',
    examTip: 'Can pass via UNIX socket (`unix:/path/socket.sock`) or TCP socket (`fastcgi_pass 127.0.0.1:9000;`). UNIX sockets have lower latency and overhead on the same host.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2889,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'SSL in Nginx (ssl_certificate)',
    question: 'How do you configure an Nginx `server` block to listen on port 443 with SSL/TLS enabled, using specific certificate and private key files?',
    answer: '`listen 443 ssl;` with `ssl_certificate <path-to-bundle>;` and `ssl_certificate_key <path-to-key>;`.',
    definition: 'Enables TLS on the socket. `ssl_certificate` must contain the server certificate followed by intermediate CA certificates in a single PEM bundle.',
    example: 'server {\n  listen 443 ssl http2;\n  server_name secure.example.com;\n  ssl_certificate /etc/ssl/certs/fullchain.pem;\n  ssl_certificate_key /etc/ssl/private/privkey.pem;\n  ssl_protocols TLSv1.2 TLSv1.3;\n}',
    exampleExplanation: 'Listens on port 443 with TLS and HTTP/2 support enabled.',
    examTip: 'Unlike Apache (which historically had separate `SSLCACertificateFile`), Nginx requires the server certificate and intermediate certificates concatenated together in `ssl_certificate`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2890,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'HTTP to HTTPS Redirect in Nginx',
    question: 'What is the most efficient and recommended Nginx configuration to permanently redirect all plain HTTP requests on port 80 to HTTPS?',
    answer: '`server { listen 80; server_name example.com; return 301 https://$host$request_uri; }`',
    definition: 'Uses the built-in `return 301` directive, which evaluates faster and consumes fewer resources than regular expression rewrites.',
    example: 'server {\n  listen 80;\n  server_name example.com www.example.com;\n  return 301 https://$host$request_uri;\n}',
    exampleExplanation: 'Returns an HTTP 301 status with the exact matching secure URI.',
    examTip: 'Best practice: Avoid `rewrite ^ https://$server_name$request_uri? permanent;` when `return 301` is cleaner and natively optimized in Nginx.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2891,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'access_log / error_log in Nginx',
    question: 'Which directives define the logging paths and formats for client access and server errors in Nginx?',
    answer: '`access_log <path> [format];` and `error_log <path> [level];`.',
    definition: '`access_log` writes client requests (can be toggled `off` for static assets). `error_log` records server notices and failures with levels like `warn`, `error`, `crit`, `debug`.',
    example: 'access_log /var/log/nginx/example.com.access.log combined;\nerror_log /var/log/nginx/example.com.error.log warn;',
    exampleExplanation: 'Configures custom access and error logging for a specific virtual host.',
    examTip: 'To completely disable access logging for performance (e.g. for high-volume static images): `access_log off;`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2892,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'log_format',
    question: 'Which directive defines a custom log output format in Nginx, and in which configuration context must it be declared?',
    answer: '`log_format <name> \'<format_string>\';` declared inside the `http` context.',
    definition: 'Constructs custom access log lines using Nginx variables (e.g., `$remote_addr`, `$time_local`, `$request`, `$status`, `$body_bytes_sent`, `$http_referer`, `$http_user_agent`).',
    example: 'http {\n  log_format custom \'$remote_addr - $remote_user [$time_local] "$request" \' \n                    \'$status $body_bytes_sent "$http_referer" \' \n                    \'"$http_user_agent" "$upstream_response_time"\';\n  access_log /var/log/nginx/access.log custom;\n}',
    exampleExplanation: 'Creates a custom format named `custom` that includes backend upstream response time.',
    examTip: '`log_format` CANNOT be placed inside a `server` block; it must be defined at the `http` block level.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2893,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'error_page',
    question: 'How do you configure custom error pages for HTTP 404 and 50x server errors in Nginx?',
    answer: '`error_page 404 /404.html;` and `error_page 500 502 503 504 /50x.html;` with a matching `location = /50x.html { root /usr/share/nginx/html; }`.',
    definition: 'Specifies custom static HTML templates to display when backend services or static lookups return HTTP error status codes.',
    example: 'error_page 500 502 503 504 /custom_50x.html;\nlocation = /custom_50x.html {\n  root /var/www/error_pages;\n  internal;\n}',
    exampleExplanation: 'Serves a custom branded error page for 50x errors. `internal` prevents clients from directly requesting the error page URL.',
    examTip: 'The `internal` directive inside a `location` ensures the resource can only be reached via internal Nginx redirects and error handlers.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2894,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'allow / deny (Access Control in Nginx)',
    question: 'How do you restrict access to an Nginx `location` so that only the IP `192.168.1.50` and subnet `10.0.0.0/8` are permitted, while all other IPs are rejected with HTTP 403 Forbidden?',
    answer: '`allow 192.168.1.50; allow 10.0.0.0/8; deny all;`',
    definition: '`ngx_http_access_module` evaluates access rules in order from top to bottom. The first matching rule takes precedence.',
    example: 'location /admin/ {\n  allow 192.168.1.50;\n  allow 10.0.0.0/8;\n  deny all;\n}',
    exampleExplanation: 'Permits the designated host and subnet, blocking anyone else.',
    examTip: 'Evaluation stops at the first match. Always put `deny all;` at the bottom of the list.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2895,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'auth_basic / auth_basic_user_file',
    question: 'Which directives enable HTTP Basic Authentication in Nginx using a standard `htpasswd` password file?',
    answer: '`auth_basic "<Realm-Name>";` and `auth_basic_user_file <path-to-htpasswd>;`.',
    definition: '`ngx_http_auth_basic_module` challenges clients with HTTP 401 Unauthorized until valid credentials matching the `htpasswd` file are provided.',
    example: 'location /metrics {\n  auth_basic "Monitoring Access";\n  auth_basic_user_file /etc/nginx/.htpasswd;\n}',
    exampleExplanation: 'Protects the `/metrics` endpoint with password authentication.',
    examTip: 'Nginx does not ship with its own `htpasswd` generator tool. Install `apache2-utils` (Debian) or `httpd-tools` (RHEL) to generate `.htpasswd` files.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2896,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'gzip on / gzip_types',
    question: 'Which directives enable on-the-fly HTTP response compression in Nginx to reduce bandwidth usage for text, HTML, and JSON data?',
    answer: '`gzip on;` and `gzip_types text/plain text/css application/json application/javascript text/xml;`.',
    definition: 'Compresses outgoing responses using gzip compression if the client sends an `Accept-Encoding: gzip` request header.',
    example: 'gzip on;\ngzip_min_length 1000;\ngzip_comp_level 5;\ngzip_types text/plain text/css application/json application/javascript text/xml;',
    exampleExplanation: 'Compresses matching MIME types for responses exceeding 1000 bytes.',
    examTip: 'Note: `text/html` is always compressed by default when `gzip on;` is set and does not need to be specified in `gzip_types`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2897,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'proxy_cache_path / proxy_cache',
    question: 'Which directives configure Nginx to cache reverse proxy backend responses on local disk storage?',
    answer: '`proxy_cache_path <path> keys_zone=<name>:<size>;` in the `http` context, and `proxy_cache <name>;` inside `location`.',
    definition: 'Activates response caching for reverse proxy workloads. Stores cached responses on disk while indexing cache keys in a shared memory zone.',
    example: 'http {\n  proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=my_cache:10m max_size=1g inactive=60m;\n  server {\n    location / {\n      proxy_cache my_cache;\n      proxy_cache_valid 200 302 10m;\n      proxy_pass http://backend;\n    }\n  }\n}',
    exampleExplanation: 'Creates a 10MB memory zone indexing up to 1GB of disk cache, caching 200/302 responses for 10 minutes.',
    examTip: 'Add `add_header X-Cache-Status $upstream_cache_status;` to return `HIT`, `MISS`, `BYPASS`, or `EXPIRED` status headers to clients for easy debugging.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2898,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'ssl_session_cache / ssl_session_timeout',
    question: 'How do you optimize TLS handshake performance in Nginx by configuring a shared SSL session cache?',
    answer: '`ssl_session_cache shared:SSL:10m;` and `ssl_session_timeout 10m;`.',
    definition: 'Allocates a shared memory zone (`10m` = approx 40,000 sessions) across all worker processes so returning clients can resume TLS sessions without a full handshake.',
    example: 'ssl_session_cache shared:SSL:10m;\nssl_session_timeout 1d;\nssl_session_tickets off;',
    exampleExplanation: 'Reuses negotiated TLS session parameters for up to 1 day, significantly cutting CPU load on SSL handshakes.',
    examTip: 'Always use `shared:SSL:<size>` over `builtin` because the built-in cache is private to each worker and causes memory fragmentation.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2899,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'rewrite in Nginx',
    question: 'What is the syntax for the `rewrite` directive in Nginx, and what do the flags `last` and `break` do?',
    answer: '`rewrite <regex> <replacement> [flag];`. `last` stops processing current rewrite directives and restarts location search with the new URI; `break` stops rewrite processing without leaving the current location.',
    definition: 'Manipulates request URIs using PCRE regular expressions. Other flags include `redirect` (HTTP 302) and `permanent` (HTTP 301).',
    example: 'rewrite ^/items/([0-9]+)$ /item.php?id=$1 last;',
    exampleExplanation: 'Rewrites `/items/42` to `/item.php?id=42` and searches for a location matching `/item.php`.',
    examTip: 'Key difference: Use `last` when rewriting to a URI that needs to be handled by another location block (like PHP); use `break` when rewriting inside a location that serves files directly.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2900,
    deck: 'Topic 208: Web Services',
    objectiveId: '208.4',
    topicNumber: 208,
    category: 'Implementing Nginx as a Web Server and Reverse Proxy',
    command: 'Nginx vs Apache Architecture',
    question: 'From an architectural perspective, why does Nginx achieve significantly higher concurrency with lower memory consumption than traditional Apache with `mpm_prefork`?',
    answer: 'Nginx uses an asynchronous, non-blocking, event-driven loop where single worker processes handle thousands of concurrent connections, whereas Apache `prefork` allocates a dedicated process per connection.',
    definition: 'Process/thread-per-connection architectures suffer from memory exhaustion and high CPU context-switching overhead under high loads (the C10K problem). Nginx uses multiplexed I/O (`epoll` on Linux) to serve connections within unified event loops.',
    example: 'ps aux | grep nginx\n# Master process (root) + worker processes (nginx user)',
    exampleExplanation: 'A small number of workers (typically equal to CPU cores) handles all client traffic without spawning thousands of heavy child processes.',
    examTip: 'A foundational concept tested across LPIC-2: Apache provides rich `.htaccess` flexibility and dynamic modules, while Nginx provides superior raw concurrency and reverse proxy performance.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
];
