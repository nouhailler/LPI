import { Flashcard } from '../../types';

/**
 * Topic 364: Single Node High Availability (LPIC-3 High Availability and Storage Clusters Exam 306-300) - Part 2 (Cards 4951-5000)
 * 
 * Objectives Covered:
 * - 364.3: Advanced LVM - Part 2: LVM Cache, LVM RAID & Snapshots (Cards 4951-4965) [15 Cards] (Weight 3)
 * - 364.4: Network High Availability (Cards 4966-5000) [35 Cards] (Weight 5)
 */
export const topic364Part2: Flashcard[] = [
  // ------------------------------------------------------------------------------------------------
  // Objective 364.3: Advanced LVM - Part 2: LVM Cache, LVM RAID & Snapshots (Cards 4951-4965)
  // ------------------------------------------------------------------------------------------------
  {
    id: 4951,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'LVM Caching Architecture (dm-cache / lvmcache)',
    definition: 'Accelerates high-capacity, slower mechanical hard drives (HDDs) by attaching a fast solid-state drive (SSD or NVMe) as a transparent caching tier using the Linux kernel `dm-cache` target.',
    example: 'lvs -a -o +devices,cache_mode\n# Shows origin LV, cache pool, and hit percentage',
    exampleExplanation: 'Inspects active logical volumes with cache attributes, showing the association between fast SSD storage and backing HDD storage.',
    question: 'What is the purpose of LVM caching (`dm-cache`), and how does it improve single-node storage performance?',
    answer: 'It uses small, fast SSDs/NVMes as a transparent caching layer for larger, slower HDDs to dramatically accelerate random I/O and hot-data access.',
    category: 'LVM Caching',
    examTip: 'LVM cache operates at the block level beneath the filesystem, making it completely transparent to ext4, XFS, and database applications.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4952,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'LVM Cache Modes: writethrough vs writeback',
    definition: '`writethrough` mode writes data simultaneously to both the SSD cache and slow origin HDD (safer; SSD failure loses no data); `writeback` acknowledges writes once committed to SSD cache and flushes later (faster write performance; requires mirrored SSDs to avoid data loss).',
    example: 'lvcreate -L 20G -n fast_cache_pool vg_storage /dev/nvme0n1\nlvconvert --type cache --cachepool fast_cache_pool --cachemode writeback vg_storage/slow_lv',
    exampleExplanation: 'Attaches cache pool in `writeback` mode, buffering write I/O in high-speed NVMe before flushing to slow storage.',
    question: 'What is the critical reliability difference between LVM cache modes `writethrough` and `writeback`?',
    answer: '`writethrough` writes synchronously to both SSD and HDD (safe against cache SSD failure), whereas `writeback` buffers writes in SSD first, requiring SSD redundancy.',
    category: 'LVM Caching',
    examTip: 'If using `writeback` mode in production, always ensure the cache pool is backed by mirrored (RAID 1) SSDs to prevent data loss if a single SSD dies.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4953,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Creating an LVM Cache Pool (--type cache-pool)',
    definition: 'Creates a dedicated cache pool logical volume comprising data blocks and metadata blocks residing on fast physical volumes (SSDs).',
    example: 'lvcreate --type cache-pool -L 50G -n cpool vg_data /dev/nvme0n1',
    exampleExplanation: 'Allocates a 50GB cache pool named `cpool` on NVMe storage `/dev/nvme0n1` inside volume group `vg_data`.',
    question: 'Which `lvcreate` command creates a 50GB cache pool named `cpool` on fast disk `/dev/nvme0n1`?',
    answer: '`lvcreate --type cache-pool -L 50G -n cpool vg_data /dev/nvme0n1`.',
    category: 'LVM Caching',
    examTip: 'LVM automatically splits the allocation into `cpool_cdata` (cache data) and `cpool_cmeta` (cache metadata) blocks.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4954,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Attaching Cache Pool to Origin Volume (lvconvert --type cache)',
    definition: 'Converts an existing standard logical volume into a cached logical volume by binding an available cache pool to it online.',
    example: 'lvconvert --type cache --cachepool vg_data/cpool vg_data/lv_database',
    exampleExplanation: 'Binds cache pool `cpool` to origin logical volume `lv_database`, immediately enabling read/write acceleration.',
    question: 'How do you attach an existing cache pool `cpool` to origin logical volume `lv_database` in Volume Group `vg_data`?',
    answer: '`lvconvert --type cache --cachepool vg_data/cpool vg_data/lv_database`.',
    category: 'LVM Caching',
    examTip: 'The conversion can be performed while the origin logical volume is mounted and servicing active filesystem transactions.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4955,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Inspecting Cache Utilization and Hit Rates',
    definition: 'Queries LVM reporting tools to evaluate cache pool occupancy, metadata utilization percentage, cache hit ratios, and the volume of dirty blocks waiting to be written to backing disks.',
    example: 'lvs -a -o lv_name,cache_mode,cache_percent,metadata_percent,cache_dirty_blocks vg_data',
    exampleExplanation: 'Reports the percentage of cache filled, metadata consumption, and count of unwritten dirty blocks in writeback mode.',
    question: 'Which `lvs` command arguments display the cache hit percentage and dirty block count for cached LVM volumes?',
    answer: '`lvs -a -o +cache_percent,cache_dirty_blocks` (or specifying cache columns).',
    category: 'LVM Caching',
    examTip: 'A consistently high `cache_dirty_blocks` count in `writeback` mode indicates that backing HDDs are struggling to absorb the flushed write throughput.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4956,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Detaching and Splitting a Cache Pool (--uncache / --splitcache)',
    definition: 'Removes caching from an origin volume. `--uncache` flushes all dirty blocks to the origin disk and deletes the cache pool LV; `--splitcache` flushes dirty blocks but preserves the cache pool as a standalone LV for reuse.',
    example: 'lvconvert --uncache vg_data/lv_database\n# or to keep the cache pool LV:\nlvconvert --splitcache vg_data/lv_database',
    exampleExplanation: 'Flushes cached blocks from SSD to backing HDD and removes the caching association cleanly without data corruption.',
    question: 'What is the operational difference between `lvconvert --uncache` and `lvconvert --splitcache`?',
    answer: '`--uncache` flushes dirty blocks and deletes the cache pool; `--splitcache` flushes dirty blocks and detaches the cache pool while preserving it for reuse.',
    category: 'LVM Caching',
    examTip: 'Never physically remove the SSD while in `writeback` mode without running `--uncache` or `--splitcache` first; doing so causes data corruption.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4957,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'LVM RAID Architectures (dm-raid vs mdadm)',
    definition: 'LVM2 includes built-in RAID targets (`dm-raid`) that integrate RAID 0, 1, 4, 5, 6, and 10 directly into logical volumes without needing a prior `mdadm` array layer, allowing per-LV redundancy choices.',
    example: 'lvcreate --type raid1 -m 1 -L 50G -n lv_mirrored vg_storage\nlvcreate --type raid5 -i 3 -L 150G -n lv_raid5 vg_storage',
    exampleExplanation: 'Creates a 2-way mirrored LV and a 3-stripe RAID 5 LV natively inside LVM using `dm-raid` kernel modules.',
    question: 'How does LVM RAID (`--type raidX`) differ from traditional LVM on top of `mdadm`?',
    answer: 'LVM RAID builds parity and mirroring natively inside logical volumes using `dm-raid`, allowing different RAID levels on different LVs in the same VG.',
    category: 'LVM RAID',
    examTip: 'With LVM RAID, volume group physical extents can be combined into RAID 1 for critical databases and RAID 0 for temporary caches in the same volume group.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4958,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Creating LVM RAID 1 Mirrored Volumes (--type raid1)',
    definition: 'Creates a redundant logical volume where all data blocks are mirrored across distinct Physical Volumes (PVs). The `-m 1` flag creates 1 mirror copy (2 identical copies total).',
    example: 'lvcreate --type raid1 -m 1 -L 100G -n lv_data vg_storage /dev/sdb1 /dev/sdc1',
    exampleExplanation: 'Allocates a 100GB mirrored logical volume specifically across physical volumes `/dev/sdb1` and `/dev/sdc1`.',
    question: 'Which command creates a 100GB 2-way mirrored LVM RAID 1 volume named `lv_data` across PVs `/dev/sdb1` and `/dev/sdc1`?',
    answer: '`lvcreate --type raid1 -m 1 -L 100G -n lv_data vg_storage /dev/sdb1 /dev/sdc1`.',
    category: 'LVM RAID',
    examTip: 'Unlike legacy LVM mirrors (`--type mirror`), modern `--type raid1` provides superior synchronization handling, write-intent logging, and failure recovery.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4959,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Creating LVM RAID 5 and RAID 6 Volumes (--type raid5 / raid6)',
    definition: 'Creates logical volumes with distributed parity stripes. `-i <stripes>` defines the number of data stripes (excluding parity). RAID 5 requires stripes + 1 PVs; RAID 6 requires stripes + 2 PVs.',
    example: 'lvcreate --type raid5 -i 3 -L 300G -n lv_archive vg_storage\nlvcreate --type raid6 -i 4 -L 400G -n lv_secure vg_storage',
    exampleExplanation: 'Creates a RAID 5 LV striped across 3 data disks + 1 parity disk (4 PVs total), and a RAID 6 LV with 4 data stripes + 2 parity stripes (6 PVs total).',
    question: 'In `lvcreate --type raid5 -i 3 ...`, how many total Physical Volumes (PVs) are required to allocate the logical volume?',
    answer: '4 PVs (3 data stripe devices + 1 parity device).',
    category: 'LVM RAID',
    examTip: 'In LVM RAID syntax, `-i` specifies data stripes only; total required disks is always `-i` + 1 for RAID 5, and `-i` + 2 for RAID 6.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4960,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Replacing a Failed Physical Volume in LVM RAID (lvconvert --repair)',
    definition: 'Initiates automated repair and rebuild of a degraded LVM RAID volume by replacing failed physical legs with available free space in the Volume Group.',
    example: 'vgreduce --removemissing --force vg_storage\nlvconvert --repair vg_storage/lv_raid5',
    exampleExplanation: 'Purges missing PV definitions and commands LVM to reconstruct missing RAID 5 parity/data legs using remaining available PVs.',
    question: 'Which command rebuilds and repairs a degraded LVM RAID volume `lv_raid5` after a disk failure?',
    answer: '`lvconvert --repair vg_storage/lv_raid5`.',
    category: 'LVM RAID',
    examTip: 'Ensure a replacement physical volume has already been initialized (`pvcreate`) and added to the volume group (`vgextend`) before initiating repair.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4961,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Converting Linear Volume to Mirrored RAID (Online Redundancy Upgrade)',
    definition: 'Converts an existing non-redundant linear logical volume into a fault-tolerant RAID 1 mirrored volume online without unmounting the filesystem.',
    example: 'lvconvert --type raid1 -m 1 vg_storage/lv_linear /dev/sdd1',
    exampleExplanation: 'Adds physical volume `/dev/sdd1` as a mirror leg to `lv_linear`, initiating background synchronization to create a redundant RAID 1 LV.',
    question: 'How can an administrator convert a single-disk linear LV `lv_linear` into a 2-way RAID 1 mirror online using `/dev/sdd1`?',
    answer: '`lvconvert --type raid1 -m 1 vg_storage/lv_linear /dev/sdd1`.',
    category: 'LVM RAID',
    examTip: 'To revert a mirrored volume back to a linear volume, use `lvconvert -m 0 vg_storage/lv_mirrored <pv_to_remove>`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4962,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Traditional LVM Snapshots (Copy-On-Write COW Limitations)',
    definition: 'Traditional LVM snapshots allocate a fixed COW table. When original data blocks are modified, the old block is copied to the snapshot volume. If the snapshot volume reaches 100% capacity, it becomes invalidated (dropped) and unusable.',
    example: 'lvcreate -L 20G -s -n snap_db /dev/vg_data/lv_db\nlvs\n# Check Snap% column to ensure snapshot volume does not overflow',
    exampleExplanation: 'Creates a 20GB COW snapshot named `snap_db`. If changed data exceeds 20GB, the snapshot is permanently dropped.',
    question: 'What happens to a traditional LVM Copy-On-Write (COW) snapshot if the volume of changed blocks exceeds its allocated snapshot size?',
    answer: 'The snapshot overflows, becomes invalid (INACTIVE / drop flag set in `lvs`), and can no longer be mounted or restored.',
    category: 'LVM Snapshots',
    examTip: 'Monitor the `Data%` / `Snap%` column of `lvs`. Configure `snapshot_autoextend_threshold` in `/etc/lvm/lvm.conf` to auto-expand COW snapshots.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4963,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Thin Snapshots (Zero-Allocation Instant Branching)',
    definition: 'Snapshots created from thin volumes share the underlying thin pool. They consume zero extra physical storage upon creation, support recursive snapshots (snapshots of snapshots), and never overflow independently.',
    example: 'lvcreate -s --name snap_web_v1 vg_data/thin_web\nlvcreate -s --name snap_web_v2 vg_data/snap_web_v1',
    exampleExplanation: 'Creates instantaneous thin snapshots and recursive snapshots that only consume shared thin pool space as diverged blocks are written.',
    question: 'Why are thin snapshots superior to traditional COW snapshots for virtual machine images and container root filesystems?',
    answer: 'They require no initial fixed size, share thin pool space dynamically, support recursive branching, and do not crash from fixed-size overflow.',
    category: 'LVM Snapshots',
    examTip: 'Thin snapshots can also be created read-only using `lvcreate -s --name snap_ro -p r vg_data/thin_vol` to guarantee forensic immutability.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4964,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Snapshot Rollback and Merging (lvconvert --merge)',
    definition: 'Reverts the origin logical volume back to the exact state captured in the snapshot, overwriting all subsequent modifications made since the snapshot was taken.',
    example: 'umount /mnt/app_data\nlvconvert --merge vg_data/snap_app_backup\nmount /mnt/app_data',
    exampleExplanation: 'Unmounts the filesystem, rolls the origin volume back to the snapshot state, and automatically removes the merged snapshot volume.',
    question: 'Which command rolls an origin logical volume back to its previous state using snapshot `snap_app_backup`?',
    answer: '`lvconvert --merge vg_data/snap_app_backup`.',
    category: 'LVM Snapshots',
    examTip: 'If the origin LV is currently in use or mounted (e.g. root filesystem), the merge is automatically scheduled to execute upon the next system reboot.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4965,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'LVM Configuration Profiles (/etc/lvm/profile/)',
    definition: 'Specialized profile files used to customize LVM daemon behavior for specific volume groups or thin pools (such as per-pool auto-extend thresholds, metadata allocation sizes, and error policies).',
    example: '# /etc/lvm/profile/thin_profile.profile\nthin_pool_autoextend_threshold = 70\nthin_pool_autoextend_percent = 15\n# Attach to pool:\nlvchange --metadataprofile thin_profile vg_data/tp_storage',
    exampleExplanation: 'Applies a custom auto-extend policy specifically to thin pool `tp_storage` without affecting other volume groups.',
    question: 'Where are custom LVM profile definitions stored to apply distinct configuration policies to specific thin pools or VGs?',
    answer: '`/etc/lvm/profile/` (applied using `lvchange --metadataprofile <profile_name>`).',
    category: 'Advanced LVM',
    examTip: 'LVM provides built-in profiles including `command.profile` and `thin-performance.profile` located in `/etc/lvm/profile/`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },

  // -------------------------------------------------------------------------
  // Objective 364.4: Network High Availability (Cards 4966-5000) [35 Cards]
  // -------------------------------------------------------------------------
  {
    id: 4966,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Network Link Aggregation and Fault Tolerance Goals',
    definition: 'Combines two or more physical network interface controllers (NICs) into a single logical network link to deliver redundancy (transparent failover upon cable/port death) and increased throughput (bandwidth aggregation).',
    example: 'ip link show bond0\ncat /proc/net/bonding/bond0',
    exampleExplanation: 'Inspects status of the aggregated interface, verifying active slave interfaces and link redundancy health.',
    question: 'What are the two primary objectives of implementing network interface bonding or teaming on a Linux server?',
    answer: 'Fault tolerance (redundancy upon hardware/link failure) and bandwidth aggregation (load balancing across multiple links).',
    category: 'Network High Availability',
    examTip: 'High availability clusters require link aggregation for cluster interconnects (Corosync/Pacemaker) to prevent split-brain from single cable disconnects.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4967,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Linux Bonding Kernel Module and Status (/proc/net/bonding/)',
    definition: 'The `bonding` kernel driver aggregates physical slave network adapters under a virtual master device (`bond0`). Real-time link health, active slave, MII status, and failure counts are exposed in `/proc/net/bonding/<bond_name>`.',
    example: 'cat /proc/net/bonding/bond0',
    exampleExplanation: 'Displays the active bonding mode, transmit hash policy, current primary interface, and slave link states.',
    question: 'Which virtual procfs directory contains real-time status and operational details for all active Linux bonding interfaces?',
    answer: '`/proc/net/bonding/` (e.g. `/proc/net/bonding/bond0`).',
    category: 'Network High Availability',
    examTip: 'Always check `/proc/net/bonding/bondX` when diagnosing network failover issues; it shows slave link failure counts and LACP actor/partner state.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4968,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Bonding Mode 0: balance-rr (Round-Robin)',
    definition: 'Transmits packets sequentially starting from the first available slave interface through the last in round-robin fashion. Provides load balancing and fault tolerance, but can cause out-of-order packet delivery requiring static switch trunking.',
    example: 'modprobe bonding mode=0 miimon=100\n# or mode=balance-rr',
    exampleExplanation: 'Initializes the bonding module in round-robin mode with 100ms link monitoring.',
    question: 'Which bonding mode distributes packets across all slave interfaces in sequential order but frequently causes out-of-order TCP packet arrival?',
    answer: 'Mode 0 (`balance-rr`).',
    category: 'Network High Availability',
    examTip: 'Mode 0 requires the physical switch to have static etherchannel/trunking configured; otherwise MAC address flapping will disrupt communication.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4969,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Bonding Mode 1: active-backup (Active-Backup Failover)',
    definition: 'Only one slave interface is active at any time. A standby slave only becomes active if and when the primary active interface loses physical carrier link. Requires NO special switch configuration.',
    example: 'nmcli con add type bond con-name bond0 ifname bond0 bond.options "mode=active-backup,miimon=100,primary=eth0"',
    exampleExplanation: 'Creates an active-backup bond with `eth0` designated as preferred primary and 100ms MII link polling.',
    question: 'Which Linux bonding mode provides complete fault tolerance with only one active adapter at a time and requires NO switch-side configuration?',
    answer: 'Mode 1 (`active-backup`).',
    category: 'Network High Availability',
    examTip: 'Mode 1 is the safest and most widely deployed HA bonding mode because it connects to two completely separate, independent unstacked top-of-rack switches.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4970,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Bonding Mode 2: balance-xor (Hash-Based Distribution)',
    definition: 'Transmits packets based on a hash of source MAC, destination MAC, and IP addresses. All packets for a specific destination travel over the same slave interface, preventing out-of-order delivery. Requires switch trunking.',
    example: 'ip link add name bond0 type bond mode balance-xor',
    exampleExplanation: 'Creates a `balance-xor` bond providing deterministic path selection based on hash calculation.',
    question: 'Which bonding mode uses a hash calculation of MAC/IP addresses to select the transmitting interface while maintaining static switch compatibility?',
    answer: 'Mode 2 (`balance-xor`).',
    category: 'Network High Availability',
    examTip: 'Mode 2 provides deterministic link selection; traffic to a single remote server always uses the same physical adapter.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4971,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Bonding Mode 3: broadcast (Redundant Transmission)',
    definition: 'Transmits identical copies of every outgoing packet across ALL slave interfaces simultaneously. Provides extreme fault tolerance for mission-critical UDP or multicast telemetry, but consumes multi-fold network bandwidth.',
    example: 'nmcli con add type bond con-name bond0 ifname bond0 bond.options "mode=broadcast,miimon=100"',
    exampleExplanation: 'Configures broadcast mode where each outgoing frame is sent on all active slave links.',
    question: 'Which bonding mode sends every outgoing network packet across all slave network interfaces concurrently?',
    answer: 'Mode 3 (`broadcast`).',
    category: 'Network High Availability',
    examTip: 'Mode 3 is rarely used for standard TCP traffic due to duplicate packet processing overhead, but is valuable in financial trading or radar/sensor data feeds.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4972,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Bonding Mode 4: 802.3ad (Dynamic LACP Link Aggregation)',
    definition: 'IEEE 802.3ad Dynamic Link Aggregation Control Protocol (LACP). Automatically negotiates aggregated link groups with the upstream switch. Provides dynamic bandwidth aggregation, automatic slave removal on link failure, and hash-based load sharing.',
    example: 'nmcli con add type bond con-name bond0 ifname bond0 bond.options "mode=802.3ad,miimon=100,xmit_hash_policy=layer2+3,lacp_rate=fast"',
    exampleExplanation: 'Configures dynamic 802.3ad LACP bonding with Layer 2+3 hashing and 1-second fast LACP packet exchange rate.',
    question: 'Which Linux bonding mode implements the IEEE 802.3ad standard and requires the physical switch to support and configure dynamic LACP?',
    answer: 'Mode 4 (`802.3ad` or LACP).',
    category: 'Network High Availability',
    examTip: 'The upstream switch ports MUST be configured in an active LACP port-channel (`channel-group 1 mode active` on Cisco switches).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4973,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Bonding Mode 5: balance-tlb (Transmit Load Balancing)',
    definition: 'Adaptive Transmit Load Balancing: Outgoing traffic is distributed according to current slave interface load calculations based on ARP negotiation. Incoming traffic is received only on the currently active slave. Requires NO switch support.',
    example: 'nmcli con add type bond con-name bond0 ifname bond0 bond.options "mode=balance-tlb,miimon=100"',
    exampleExplanation: 'Configures transmit load balancing without requiring switch-side port channel configuration.',
    question: 'Which bonding mode balances outgoing traffic based on current adapter load without requiring any special switch configuration?',
    answer: 'Mode 5 (`balance-tlb`).',
    category: 'Network High Availability',
    examTip: 'Because Mode 5 does not balance incoming traffic, it is ideal for outgoing data broadcasters or backup servers pushing data outward.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4974,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Bonding Mode 6: balance-alb (Adaptive Load Balancing)',
    definition: 'Adaptive Load Balancing: Includes transmit load balancing (TLB) plus receive load balancing (RLB) for IPv4 traffic. RLB intercepts ARP replies and rewrites MAC addresses so distinct clients communicate with different slave NICs. Requires NO switch support.',
    example: 'nmcli con add type bond con-name bond0 ifname bond0 bond.options "mode=balance-alb,miimon=100"',
    exampleExplanation: 'Configures full bi-directional transmit and receive load balancing without upstream switch aggregation.',
    question: 'Which bonding mode delivers both transmit AND receive load balancing across multiple adapters without requiring switch-side LACP or trunking?',
    answer: 'Mode 6 (`balance-alb`).',
    category: 'Network High Availability',
    examTip: 'Mode 6 modifies outgoing ARP replies dynamically. It does not balance receive traffic for non-IP traffic or clients crossing a Layer 3 router.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4975,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'MII Link Monitoring (miimon, updelay, downdelay)',
    definition: 'Media Independent Interface (MII) monitoring inspects whether physical carrier link is present on each slave interface. `miimon` sets check frequency in milliseconds; `updelay` and `downdelay` debounce link flapping.',
    example: 'bond.options "mode=active-backup,miimon=100,updelay=200,downdelay=200"',
    exampleExplanation: 'Checks link state every 100ms; waits 200ms before declaring an interface up or down to avoid flapping storms.',
    question: 'What bonding option specifies the link inspection polling frequency in milliseconds using the network driver\'s carrier state?',
    answer: '`miimon` (e.g. `miimon=100`).',
    category: 'Network High Availability',
    examTip: '`updelay` and `downdelay` MUST be configured as multiples of `miimon`. If `miimon=100`, valid delays are 0, 100, 200, 300ms, etc.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4976,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'ARP Link Monitoring (arp_interval & arp_ip_target)',
    definition: 'An alternative to MII monitoring that validates end-to-end network reachability by transmitting periodic ARP queries to a gateway or target IP address. If no ARP replies return, the link is declared dead.',
    example: 'bond.options "mode=active-backup,arp_interval=1000,arp_ip_target=192.168.1.1,arp_validate=all"',
    exampleExplanation: 'Sends ARP requests to 192.168.1.1 every 1000ms, validating that slave links actually pass traffic to the upstream gateway.',
    question: 'When is ARP link monitoring (`arp_interval` + `arp_ip_target`) preferred over MII link monitoring (`miimon`)?',
    answer: 'When a network switch port maintains link carrier status even if upstream switch uplinks or gateways have failed.',
    category: 'Network High Availability',
    examTip: 'You cannot use `miimon` and `arp_interval` simultaneously; they are mutually exclusive link monitoring methods in the bonding driver.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4977,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Primary Slave Preference (primary & primary_reselect)',
    definition: 'In `active-backup` mode, designates a preferred physical interface (e.g. a 10GbE fiber NIC over a 1GbE copper NIC). When the primary adapter recovers from an outage, `primary_reselect` controls whether it immediately preempts the standby.',
    example: 'bond.options "mode=active-backup,primary=eth0,primary_reselect=always"',
    exampleExplanation: 'Forces `eth0` to be the active interface whenever its physical link is online, preempting secondary interfaces.',
    question: 'Which bonding option designates a specific slave interface as the preferred active link whenever it is operational?',
    answer: '`primary=<interface_name>` (e.g. `primary=eth0`).',
    category: 'Network High Availability',
    examTip: '`primary_reselect` policies include `always` (preempts immediately), `better` (preempts if higher speed/duplex), and `failure` (only reselects if current slave fails).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4978,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'LACP Transmit Hash Policy (xmit_hash_policy)',
    definition: 'Defines the packet header fields used in the mathematical hash algorithm to distribute outbound packets across slave links in Mode 2 and Mode 4 (802.3ad).',
    example: 'bond.options "mode=802.3ad,xmit_hash_policy=layer3+4"',
    exampleExplanation: 'Hashes IP addresses and TCP/UDP port numbers (`layer3+4`) to distribute traffic evenly across links, even between two single hosts.',
    question: 'Which `xmit_hash_policy` hashes both IP addresses and TCP/UDP ports to balance multiple network connections between the same two server endpoints?',
    answer: '`layer3+4`.',
    category: 'Network High Availability',
    examTip: 'Default policy is `layer2` (MAC addresses only). `layer3+4` provides vastly superior load distribution for web servers, databases, and multi-threaded applications.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4979,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'LACP Packet Transmission Rate (lacp_rate)',
    definition: 'Specifies the frequency at which LACP control packets (LACPDU) are requested from the upstream switch partner: `slow` (every 30 seconds, default) or `fast` (every 1 second).',
    example: 'bond.options "mode=802.3ad,lacp_rate=fast"',
    exampleExplanation: 'Requests 1-second LACPDU transmission for sub-second detection of switch port or physical link failure.',
    question: 'Which `lacp_rate` setting accelerates LACP partner link failure detection from 30 seconds down to approximately 1 second?',
    answer: '`lacp_rate=fast` (or `lacp_rate=1`).',
    category: 'Network High Availability',
    examTip: 'In enterprise high-availability environments, always configure `lacp_rate=fast` to minimize packet drops during switch reboots or fiber cuts.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4980,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Sysfs Bonding Control (/sys/class/net/bonding_masters)',
    definition: 'The sysfs control interface used to dynamically create, destroy, enslave, and release network bonding interfaces without reloading the kernel module.',
    example: 'echo +bond0 > /sys/class/net/bonding_masters\necho +eth0 > /sys/class/net/bond0/bonding/slaves\necho -eth0 > /sys/class/net/bond0/bonding/slaves\necho -bond0 > /sys/class/net/bonding_masters',
    exampleExplanation: 'Creates virtual master `bond0`, enslaves physical interface `eth0`, releases `eth0`, and deletes `bond0`.',
    question: 'Which sysfs file is written to dynamically add (`+bond0`) or remove (`-bond0`) bonding master interfaces in the kernel?',
    answer: '`/sys/class/net/bonding_masters`.',
    category: 'Network High Availability',
    examTip: 'To enslave an interface via sysfs, the slave interface MUST be brought administratively down (`ip link set eth0 down`) before writing to `slaves`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4981,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Network Teaming Architecture (teamd vs Linux Bonding)',
    definition: 'Network Teaming is a modular, userspace-driven alternative to the legacy kernel bonding driver. The `teamd` daemon handles logic, link monitoring, and protocol exchange in userspace, while the kernel simply executes fast data-path switching.',
    example: 'systemctl status teamd@team0\nteamdctl team0 state',
    exampleExplanation: 'Checks status of the `teamd` daemon process controlling team interface `team0` via D-Bus and Netlink.',
    question: 'What architectural advantage does Network Teaming (`teamd`) provide over the traditional in-kernel bonding driver?',
    answer: 'It moves complex management logic, protocol state machines (LACP), and link monitoring to a modular userspace daemon (`teamd`).',
    category: 'Network Teaming',
    examTip: 'Teaming utilizes JSON-formatted configuration files and allows zero-downtime updates to runner algorithms and port properties via D-Bus.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4982,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'teamd Runners: roundrobin, activebackup, loadbalance, lacp',
    definition: 'In Network Teaming, "runners" implement the operational link aggregation modes: `roundrobin`, `activebackup`, `loadbalance` (BPF hash-based), `lacp` (802.3ad), and `random`.',
    example: 'teamd -g -f /etc/teamd/team0.conf -d\n# In team0.conf: {"runner": {"name": "activebackup"}}',
    exampleExplanation: 'Launches `teamd` in debug daemon mode reading a JSON configuration selecting the `activebackup` runner.',
    question: 'What is the Network Teaming equivalent term for what the Linux bonding driver calls "modes"?',
    answer: 'Runners (e.g. `activebackup`, `loadbalance`, `lacp`, `roundrobin`).',
    category: 'Network Teaming',
    examTip: 'The `loadbalance` runner uses Berkeley Packet Filters (BPF) to compute dynamic transmission hashes, providing fine-grained flow distribution.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4983,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'teamd JSON Configuration Format (/etc/teamd/*.conf)',
    definition: 'Network Teaming configurations are structured in standard JSON format, defining the team device name, runner specification, link watcher settings, and member port attributes.',
    example: '{\n  "device": "team0",\n  "runner": {"name": "lacp", "active": true, "fast_rate": true},\n  "link_watch": {"name": "ethtool"},\n  "ports": {"eth1": {}, "eth2": {}}\n}',
    exampleExplanation: 'A complete team JSON specification deploying an 802.3ad LACP team with fast-rate LACPDU exchange and ethtool link detection.',
    question: 'In what file format are native configuration files for the `teamd` daemon written?',
    answer: 'JSON (JavaScript Object Notation).',
    category: 'Network Teaming',
    examTip: 'Link watchers in team JSON configs can be set to `"ethtool"` (equivalent to `miimon`), `"arp_ping"` (equivalent to ARP monitoring), or `"nsna_ping"` for IPv6.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4984,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'teamdctl Querying and State Inspection',
    definition: 'The primary command-line tool used to interact with the running `teamd` process via D-Bus, querying current state, active ports, link watchers, and runner variables.',
    example: 'teamdctl team0 state\nteamdctl team0 state view -v',
    exampleExplanation: 'Displays comprehensive operational state of team `team0`, showing the currently active port and link watcher status.',
    question: 'Which command displays the real-time operational status, active runner, and member port states of a team interface `team0`?',
    answer: '`teamdctl team0 state`.',
    category: 'Network Teaming',
    examTip: '`teamdctl team0 config dump` outputs the active runtime JSON configuration of the team interface.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4985,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'teamdctl Dynamic Port Management (port add / remove / config)',
    definition: 'Adds, removes, or modifies port priorities and JSON configuration parameters on a live team interface without restarting the daemon.',
    example: 'teamdctl team0 port add eth1\nteamdctl team0 port config update eth1 \'{"prio": 100}\'\nteamdctl team0 port remove eth2',
    exampleExplanation: 'Dynamically attaches `eth1`, raises its priority to 100 so it becomes preferred primary, and detaches `eth2`.',
    question: 'How do you dynamically change the priority of port `eth1` in team `team0` to 100 using `teamdctl`?',
    answer: '`teamdctl team0 port config update eth1 \'{"prio": 100}\'`.',
    category: 'Network Teaming',
    examTip: 'Higher port priority numbers indicate preferred active ports in the `activebackup` runner.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4986,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'teamnl Low-Level Kernel Netlink Query Tool',
    definition: 'A low-level diagnostic tool that interacts directly with the team kernel driver via Generic Netlink, bypassing the userspace `teamd` daemon to query ports and options.',
    example: 'teamnl team0 ports\nteamnl team0 options',
    exampleExplanation: 'Queries the kernel directly to list all enslaved ports and active kernel options for interface `team0`.',
    question: 'Which utility queries the Linux kernel\'s team device driver directly via Netlink to inspect registered team ports and driver options?',
    answer: '`teamnl` (e.g. `teamnl team0 ports`).',
    category: 'Network Teaming',
    examTip: '`teamnl` is valuable when troubleshooting communication disconnects between the userspace `teamd` daemon and the kernel driver.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4987,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Creating a Bond with NetworkManager (nmcli)',
    definition: 'Creates a persistent master bonding connection profile using NetworkManager, specifying IP configuration and bonding options.',
    example: 'nmcli con add type bond con-name bond0 ifname bond0 bond.options "mode=active-backup,miimon=100" ip4 192.168.10.50/24 gw4 192.168.10.1',
    exampleExplanation: 'Creates a persistent master bond named `bond0` in active-backup mode with static IPv4 addressing.',
    question: 'Which `nmcli` command creates an active-backup bond master named `bond0` with 100ms MII monitoring?',
    answer: '`nmcli con add type bond con-name bond0 ifname bond0 bond.options "mode=active-backup,miimon=100"`.',
    category: 'Network Configuration',
    examTip: 'NetworkManager stores connection files in `/etc/NetworkManager/system-connections/` (or legacy `/etc/sysconfig/network-scripts/`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4988,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Adding Slave Interfaces to a Bond with nmcli',
    definition: 'Enslaves physical Ethernet network adapters into an existing NetworkManager master bond profile.',
    example: 'nmcli con add type ethernet slave-type bond con-name bond0-port1 ifname eth1 master bond0\nnmcli con add type ethernet slave-type bond con-name bond0-port2 ifname eth2 master bond0\nnmcli con up bond0-port1\nnmcli con up bond0-port2',
    exampleExplanation: 'Attaches physical ports `eth1` and `eth2` as slaves to master `bond0` and activates both slave connections.',
    question: 'How do you attach physical interface `eth1` as a slave to master bond `bond0` using `nmcli`?',
    answer: '`nmcli con add type ethernet slave-type bond con-name bond0-p1 ifname eth1 master bond0`.',
    category: 'Network Configuration',
    examTip: 'Do NOT assign IP addresses to slave interfaces; all IP addresses, gateways, and DNS servers must be configured strictly on the master `bond0` interface.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4989,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Creating a Team with NetworkManager (nmcli)',
    definition: 'Creates a persistent team master connection profile using NetworkManager, specifying the runner algorithm and team parameters.',
    example: 'nmcli con add type team con-name team0 ifname team0 team.runner activebackup team.link-watchers "name=ethtool" ip4 10.0.0.10/24',
    exampleExplanation: 'Creates a master team `team0` with the `activebackup` runner, ethtool link watcher, and static IP.',
    question: 'Which `nmcli` command creates a team interface `team0` using the `activebackup` runner?',
    answer: '`nmcli con add type team con-name team0 ifname team0 team.runner activebackup`.',
    category: 'Network Configuration',
    examTip: 'You can pass raw JSON configurations to `team.config` in `nmcli`: `team.config \'{"runner": {"name": "lacp"}}\'`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4990,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Adding Ports to a Team with nmcli',
    definition: 'Attaches physical Ethernet ports as subordinate members to an existing NetworkManager team master connection.',
    example: 'nmcli con add type ethernet slave-type team con-name team0-port1 ifname eth1 master team0\nnmcli con add type ethernet slave-type team con-name team0-port2 ifname eth2 master team0',
    exampleExplanation: 'Attaches physical interfaces `eth1` and `eth2` as ports to master `team0`.',
    question: 'How do you enslave physical interface `eth1` to team `team0` using `nmcli`?',
    answer: '`nmcli con add type ethernet slave-type team con-name team0-p1 ifname eth1 master team0`.',
    category: 'Network Configuration',
    examTip: 'To set port priorities in NetworkManager, use `nmcli con mod team0-port1 team-port.config \'{"prio": 100}\'`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4991,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'MAC Address Failover Handling (fail_over_mac)',
    definition: 'Controls whether all slave interfaces in an active-backup bond share the same MAC address (`none`, default), or change the master\'s MAC address to match the active slave (`active`), or only change MAC upon failover (`follow`).',
    example: 'bond.options "mode=active-backup,fail_over_mac=active,miimon=100"',
    exampleExplanation: 'Sets `fail_over_mac=active`, ensuring the bond MAC address always matches the hardware MAC address of the active adapter.',
    question: 'Which bonding option is required when upstream switches or virtualized hypervisors block multiple interfaces from sharing the same MAC address?',
    answer: '`fail_over_mac=active` (or `fail_over_mac=1`).',
    category: 'Network High Availability',
    examTip: 'In VMware ESXi or cloud hypervisors with MAC spoofing protections enabled, setting `fail_over_mac=active` is mandatory to avoid dropped packets upon failover.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4992,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: '802.1Q VLAN Trunking over Bonded Interfaces (bond0.VLAN)',
    definition: 'Creates IEEE 802.1Q tagged virtual LAN sub-interfaces on top of a redundant bond master device, isolating traffic across multiple VLANs while maintaining full link fault tolerance.',
    example: 'nmcli con add type vlan con-name bond0.100 ifname bond0.100 dev bond0 id 100 ip4 10.0.100.5/24\nnmcli con add type vlan con-name bond0.200 ifname bond0.200 dev bond0 id 200 ip4 10.0.200.5/24',
    exampleExplanation: 'Configures VLAN 100 and VLAN 200 sub-interfaces on top of master `bond0`, routing tagged frames over the redundant bonded link.',
    question: 'How do you create an 802.1Q tagged VLAN interface with ID 100 on top of existing bond `bond0` using `nmcli`?',
    answer: '`nmcli con add type vlan con-name bond0.100 ifname bond0.100 dev bond0 id 100 ip4 <ip/mask>`.',
    category: 'VLAN Trunking',
    examTip: 'Traffic tagged on `bond0.100` inherits all fault-tolerance and load-balancing benefits of the underlying `bond0` device.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4993,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: '802.1Q VLAN Trunking over Team Interfaces (team0.VLAN)',
    definition: 'Creates tagged 802.1Q VLAN sub-interfaces on top of a Network Teaming master device (`team0`).',
    example: 'nmcli con add type vlan con-name team0.150 ifname team0.150 dev team0 id 150 ip4 172.16.150.10/24',
    exampleExplanation: 'Creates VLAN 150 on master device `team0`, combining Network Teaming with multi-VLAN network trunking.',
    question: 'What interface name standard is typically used when configuring a VLAN sub-interface for VLAN ID 150 on top of `team0`?',
    answer: '`team0.150`.',
    category: 'VLAN Trunking',
    examTip: 'Ensure the upstream switch trunk ports permit the required VLAN IDs in their allowed trunk list (`switchport trunk allowed vlan add 100,150,200`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4994,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'MTU and Jumbo Frames on Aggregated Interfaces',
    definition: 'Maximum Transmission Unit (MTU) size must be identical across all slave interfaces, the master bond/team, and all VLAN sub-interfaces. Jumbo frames (MTU 9000) reduce CPU interrupts for high-throughput SAN storage.',
    example: 'ip link set eth1 mtu 9000\nip link set eth2 mtu 9000\nip link set bond0 mtu 9000\nip link set bond0.100 mtu 9000',
    exampleExplanation: 'Configures consistent MTU 9000 across all physical slave interfaces, the master bond, and the VLAN sub-interface.',
    question: 'What happens if a slave network interface has a lower MTU (e.g. 1500) than the master bond interface (e.g. 9000)?',
    answer: 'The slave cannot be added to the bond, or large packets will be dropped / fragmented, disrupting storage traffic.',
    category: 'Network Optimization',
    examTip: 'Always configure the MTU on the master interface; NetworkManager automatically propagates MTU adjustments down to participating slave interfaces.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4995,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Switch Redundancy and Multi-Chassis Link Aggregation (MLAG / vPC)',
    definition: 'Standard LACP (Mode 4) requires all links to terminate on a single logical switch. To connect an LACP bond to two physical switches for complete hardware redundancy, the switches must support Multi-Chassis Link Aggregation (MLAG / vPC / VSS).',
    example: '# On Cisco Switch:\ninterface Port-channel10\n vpc 10\n switchport mode trunk',
    exampleExplanation: 'Configures a Virtual Port Channel (vPC) allowing a single LACP bond on the server to terminate across two distinct physical switches.',
    question: 'Why can standard 802.3ad LACP (Mode 4) NOT be connected to two separate standalone switches without MLAG/vPC technology?',
    answer: 'LACP requires all member links to communicate with a single LACP partner; independent switches cannot coordinate LACP state without MLAG/stacking.',
    category: 'Network High Availability',
    examTip: 'If connecting a server to two independent unstacked switches without MLAG, use Mode 1 (`active-backup`) or Mode 6 (`balance-alb`), NEVER Mode 4.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4996,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Bonding Module Loading and Options (/etc/modprobe.d/bonding.conf)',
    definition: 'Configures kernel module parameters for the `bonding` driver, specifying global default bonding options and alias mappings for systems not using NetworkManager.',
    example: '# /etc/modprobe.d/bonding.conf\nalias bond0 bonding\noptions bonding mode=1 miimon=100',
    exampleExplanation: 'Defines module alias for `bond0` and sets default parameters to active-backup with 100ms MII checking.',
    question: 'Which configuration file is used to set default kernel module parameters and aliases for the Linux `bonding` driver?',
    answer: '`/etc/modprobe.d/bonding.conf`.',
    category: 'Network Configuration',
    examTip: 'In modern systemd and NetworkManager environments, configuring options directly in connection profiles is preferred over global `modprobe.d` directives.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4997,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Diagnosing Link Flapping and Packet Drops (ethtool & dmesg)',
    definition: 'Uses `dmesg` to inspect kernel link state transition timestamps and `ethtool -S` to inspect hardware PHY error counters, CRC alignment errors, and dropped frames on member adapters.',
    example: 'dmesg -T | grep -E "(bond0|eth1|eth2)"\nethtool -S eth1 | grep -E "(drop|error|crc)"',
    exampleExplanation: 'Reviews timestamped link up/down transitions and queries hardware NIC error counters to locate bad cables or failing SFP transceivers.',
    question: 'Which tool queries hardware-level NIC error counters (CRC errors, frame alignment errors, dropped packets) on physical slave interface `eth1`?',
    answer: '`ethtool -S eth1` (or `ethtool --statistics eth1`).',
    category: 'Network Troubleshooting',
    examTip: 'A rising count of `rx_crc_errors` on a slave port indicates a damaged network patch cable or dirty optical fiber connector.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4998,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Testing Network Failover with Zero Downtime',
    definition: 'Methodology for verifying link failover resilience by physically pulling a cable or administratively disabling the active slave interface while running a continuous ICMP ping or TCP stream.',
    example: 'ping -i 0.2 192.168.1.1\n# In another terminal:\nip link set eth1 down\ncat /proc/net/bonding/bond0',
    exampleExplanation: 'Simulates sudden link loss on active slave `eth1` and verifies that `bond0` seamlessly shifts traffic to `eth2` with zero dropped packets.',
    question: 'How can an administrator test active-backup bond failover without physically touching server rack cabling?',
    answer: 'By bringing down the active slave interface administratively (`ip link set <active_slave> down`) while observing network continuity.',
    category: 'Network Troubleshooting',
    examTip: 'With `miimon=100`, failover typically completes within 100-200ms, resulting in at most 1 dropped packet in a rapid ping stream.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4999,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'ethtool Link Speed and Duplex Verification',
    definition: 'Ensures all slave interfaces aggregated into a bond or team share identical operational speeds (e.g. 10000Mb/s) and full duplex. Aggregating mismatched speeds degrades LACP and creates severe performance bottlenecks.',
    example: 'ethtool eth1 | grep -E "(Speed|Duplex|Link detected)"\nethtool eth2 | grep -E "(Speed|Duplex|Link detected)"',
    exampleExplanation: 'Verifies that both physical interfaces have successfully negotiated identical speeds and duplex states.',
    question: 'Why is it critical that all slave interfaces participating in an 802.3ad LACP bond have identical link speeds and duplex settings?',
    answer: 'LACP aggregates links into an operational bundle only if all ports share identical speed and duplex; mismatched ports cannot join the aggregator.',
    category: 'Network Optimization',
    examTip: 'If one port negotiates at 1Gbps and another at 10Gbps, LACP will only activate one port and place the slower port in standby.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 5000,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.4',
    command: 'Predictable Network Interface Names and Systemd Links',
    definition: 'Systemd predictable interface names (e.g. `enp3s0f0`, `ens192`) prevent race conditions during boot where kernel NIC enumeration order swaps `eth0` and `eth1`, which could attach the wrong physical interfaces to bonding groups.',
    example: 'cat /etc/systemd/network/99-default.link\n# Override via kernel cmdline: net.ifnames=1 biosdevname=0',
    exampleExplanation: 'Inspects systemd link naming policies based on firmware, PCI topology, and MAC addresses to guarantee deterministic device naming across reboots.',
    question: 'What systemd feature ensures that physical network interfaces retain permanent, predictable names across reboots to prevent bonding misconfigurations?',
    answer: 'Predictable Network Interface Names (governed by systemd `.link` files and `udev`).',
    category: 'Network Configuration',
    examTip: 'Card 5000 milestone! Predictable naming guarantees that cluster heartbeat cables, SAN storage NICs, and client LAN adapters never swap identities upon reboot.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
];
