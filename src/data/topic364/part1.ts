import { Flashcard } from '../../types';

/**
 * Topic 364: Single Node High Availability (LPIC-3 High Availability and Storage Clusters Exam 306-300) - Part 1 (Cards 4901-4950)
 * 
 * Objectives Covered:
 * - 364.1: Hardware and Resource High Availability (Cards 4901-4920) [20 Cards] (Weight 2)
 * - 364.2: Advanced RAID (Cards 4921-4940) [20 Cards] (Weight 2)
 * - 364.3: Advanced LVM - Part 1: Thin Provisioning & Thin Pools (Cards 4941-4950) [10 Cards] (Weight 3)
 */
export const topic364Part1: Flashcard[] = [
  // -------------------------------------------------------------------------
  // Objective 364.1: Hardware and Resource High Availability (Cards 4901-4920)
  // -------------------------------------------------------------------------
  {
    id: 4901,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'lm_sensors Hardware Sensor Detection (sensors-detect)',
    definition: 'An interactive configuration utility that probes the motherboard I2C/SMBus buses and Super I/O chips to identify hardware monitoring chips and generate appropriate kernel module loading directives.',
    example: 'sensors-detect --auto\nsystemctl restart systemd-modules-load\nsensors',
    exampleExplanation: 'Automatically scans hardware sensors, confirms default safe probe choices, loads necessary kernel drivers (such as coretemp or nct6775), and queries current sensor values.',
    question: 'Which command probes system buses and Super I/O controllers to identify hardware sensors and configure required kernel monitoring modules?',
    answer: '`sensors-detect` (or `sensors-detect --auto` for non-interactive scan).',
    category: 'Hardware Monitoring',
    examTip: 'The detected kernel drivers are typically appended to `/etc/modules` or `/etc/modules-load.d/` so hardware sensor drivers load automatically at boot time.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4902,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'sensors Hardware Telemetry Query',
    definition: 'Displays real-time hardware telemetry values including CPU core temperatures, fan speeds (RPM), motherboard voltage rails (+12V, +5V, +3.3V, Vcore), and critical overheat alarm thresholds reported by kernel sensor drivers.',
    example: 'sensors -u\nsensors -j',
    exampleExplanation: '`sensors -u` displays raw unformatted metric values; `sensors -j` produces JSON output suitable for automated cluster monitoring scripts.',
    question: 'How can an administrator display current CPU temperatures, fan RPMs, and power supply voltages from the command line in Linux?',
    answer: 'By running the `sensors` command from the `lm_sensors` package.',
    category: 'Hardware Monitoring',
    examTip: 'Alarm trip thresholds (min, max, crit) can be customized inside `/etc/sensors.d/*.conf` or `/etc/sensors3.conf`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4903,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'smartctl Disk Health Inspection (-H / -a)',
    definition: 'Controls the Self-Monitoring, Analysis and Reporting Technology (SMART) system built into ATA/SATA, SAS, and NVMe storage drives to assess drive health, error rates, and failure warnings.',
    example: 'smartctl -H /dev/sda\nsmartctl -a /dev/nvme0n1',
    exampleExplanation: 'Checks whether the drive passes the SMART overall health assessment (`-H`) or displays all SMART attributes and counters (`-a`).',
    question: 'Which `smartctl` option performs a rapid evaluation of a drive\'s overall operational health status without printing full attribute tables?',
    answer: '`smartctl -H <device>` (or `--health`).',
    category: 'Storage Reliability',
    examTip: 'A result of `PASSED` indicates normal drive operation; `FAILING_NOW` or `FAILED!` demands immediate drive replacement before unrecoverable I/O failures occur.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4904,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'smartctl Diagnostic Self-Tests (-t short / -t long)',
    definition: 'Initiates background internal diagnostic self-tests executed directly by the drive firmware to detect bad sectors, head degradation, and internal mechanical or electrical faults without affecting running workloads.',
    example: 'smartctl -t short /dev/sdb\nsmartctl -t long /dev/sdb\nsmartctl -l selftest /dev/sdb',
    exampleExplanation: 'Runs an initial 2-minute short test or comprehensive multi-hour long test, and subsequently reviews the drive\'s internal self-test execution log.',
    question: 'Which command initiates an extended, comprehensive offline self-test on disk `/dev/sdb` and prints test completion estimates?',
    answer: '`smartctl -t long /dev/sdb` (followed by `smartctl -l selftest /dev/sdb` to view results).',
    category: 'Storage Reliability',
    examTip: 'Background SMART tests do not block filesystem I/O, but self-test duration increases if the drive is concurrently servicing heavy application workloads.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4905,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'smartd Daemon and Automated Alerting (/etc/smartd.conf)',
    definition: 'The SMART monitoring daemon (`smartd`) continuously polls all storage drives at periodic intervals, analyzes SMART attribute changes (e.g. reallocated sectors), and dispatches automated email alerts or scripts upon impending drive failure.',
    example: '# In /etc/smartd.conf:\n/dev/sda -a -o on -S on -s (S/../.././02|L/../../6/03) -m admin@example.com -M test',
    exampleExplanation: 'Monitors `/dev/sda`, enables automatic attribute autosave, schedules short tests daily at 2 AM and long tests Saturdays at 3 AM, and emails alerts to admin.',
    question: 'In `/etc/smartd.conf`, what directive schedules automated short tests daily at 02:00 and long tests every Sunday at 04:00?',
    answer: '`-s (S/../.././02|L/../../7/04)` (or day of week corresponding to Sunday).',
    category: 'Storage Reliability',
    examTip: 'The `-M test` parameter causes `smartd` to send a test email notification immediately upon daemon startup to verify alerting pipeline functionality.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4906,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'Linux Kernel Watchdog Device (/dev/watchdog)',
    definition: 'A special character device interface exposed by the kernel that links userspace health daemons to a hardware or software timer. If the device is not periodically written to (kicked/reset) before the timer reaches zero, the system is forcefully rebooted.',
    example: 'ls -l /dev/watchdog*\nwdctl /dev/watchdog',
    exampleExplanation: 'Verifies the presence of the watchdog character device and inspects its configured timeout, identity, and active capabilities.',
    question: 'What kernel device node serves as the interface between the watchdog userspace daemon and the hardware timer controller?',
    answer: '`/dev/watchdog` (and `/dev/watchdog0`).',
    category: 'Watchdog & Recovery',
    examTip: 'Opening `/dev/watchdog` arms the timer. If the controlling process crashes or stops writing keepalive bytes, hardware forces an immediate system reset.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4907,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'watchdog Daemon Configuration (/etc/watchdog.conf)',
    definition: 'The userspace `watchdog` daemon monitors essential system vitals (load average, memory exhaustion, network reachability, process responsiveness, disk space, file timestamps) and reboots the host if metrics exceed tolerable safety limits.',
    example: '# /etc/watchdog.conf\nwatchdog-device = /dev/watchdog\nmax-load-1 = 24\nmin-memory = 50M\nping = 192.168.1.1\ninterval = 5\nwatchdog-timeout = 60',
    exampleExplanation: 'Configures the watchdog daemon to trigger a hardware reboot if 1-minute load exceeds 24, free RAM drops below 50MB, or gateway 192.168.1.1 is unreachable.',
    question: 'In `/etc/watchdog.conf`, which parameter instructs the watchdog daemon to force a reboot if available system memory falls below a critical threshold?',
    answer: '`min-memory = <size>` (e.g. `min-memory = 50M` or in memory pages).',
    category: 'Watchdog & Recovery',
    examTip: 'The "Magic Close" feature: if the daemon writes the character \'V\' to `/dev/watchdog` before closing the file descriptor, the watchdog disarms cleanly without triggering a reboot.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4908,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'wdctl Watchdog Status Inspection Utility',
    definition: 'A utility from the `util-linux` package used to inspect the status, driver identity, active timeout, pretimeout interval, and time left before expiration on hardware and software watchdog devices.',
    example: 'wdctl\nwdctl --settimeout 30 /dev/watchdog',
    exampleExplanation: 'Displays active watchdog card details, driver flags, and adjusts the hardware timeout duration to 30 seconds.',
    question: 'Which modern command-line tool displays the properties, identity, and remaining time before expiration of the active Linux watchdog device?',
    answer: '`wdctl` (Watchdog Control).',
    category: 'Watchdog & Recovery',
    examTip: '`wdctl` also shows supported hardware flags such as `KEEPALIVEPING`, `SETTIMEOUT`, `MAGICCLOSE`, and `POWEROVERHEAT`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4909,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'softdog Software Watchdog Module',
    definition: 'A kernel module (`softdog`) providing a software-emulated watchdog timer using the kernel timer subsystem, useful when physical enterprise server hardware lacks a dedicated hardware timer chip or IPMI watchdog.',
    example: 'modprobe softdog soft_margin=60\nsystemctl start watchdog',
    exampleExplanation: 'Loads the software watchdog kernel driver with a 60-second expiration margin and starts the watchdog service.',
    question: 'Which Linux kernel driver provides software-based watchdog functionality when no physical hardware timer ASIC is present on the server?',
    answer: '`softdog` (loaded via `modprobe softdog`).',
    category: 'Watchdog & Recovery',
    examTip: 'Because `softdog` relies on the kernel timer interrupt, a complete kernel panic or hard lockup with interrupts disabled might prevent `softdog` from executing a reset.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4910,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'IPMI and Baseboard Management Controller (BMC)',
    definition: 'Intelligent Platform Management Interface (IPMI) is an industry-standard hardware management specification that operates out-of-band via an autonomous microcontroller (the BMC) independent of the main CPU, OS, and BIOS state.',
    example: 'modprobe ipmi_devintf\nmodprobe ipmi_si\nipmitool -I open mc info',
    exampleExplanation: 'Loads kernel IPMI drivers and queries local BMC firmware version, manufacturer ID, and supported IPMI protocol specification.',
    question: 'What embedded hardware subsystem runs independently of the host CPU and operating system to provide out-of-band server telemetry and power control?',
    answer: 'The BMC (Baseboard Management Controller), using the IPMI specification.',
    category: 'Out-of-Band Management',
    examTip: 'Enterprise vendor implementations include Dell iDRAC, HPE iLO, Lenovo XClarity, and Cisco CIMC, all of which support standard IPMI 2.0 protocols.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4911,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'ipmitool Chassis Power Control',
    definition: 'Commands used to query the power state or forcefully manipulate physical host power (on, off, reset, power-cycle, soft graceful shutdown) via out-of-band BMC control.',
    example: 'ipmitool -I lanplus -H 10.0.0.50 -U admin -P secret chassis power status\nipmitool -I lanplus -H 10.0.0.50 -U admin -P secret chassis power reset',
    exampleExplanation: 'Connects remotely over IPMI 2.0 (`lanplus`), checks server power state, and issues a hardware reset to unfreeze an unresponsive server.',
    question: 'Which `ipmitool` sub-command forcefully resets a host server over the IPMI LAN channel without waiting for OS cooperation?',
    answer: '`ipmitool chassis power reset` (or `chassis power cycle`).',
    category: 'Out-of-Band Management',
    examTip: 'In Pacemaker High Availability clusters, IPMI chassis power control is the foundation of `fence_ipmilan` STONITH agents.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4912,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'ipmitool System Event Log (SEL)',
    definition: 'The non-volatile log maintained by the BMC recording critical hardware occurrences, such as thermal trips, voltage excursions, power supply unit (PSU) redundancy loss, ECC memory errors, and chassis intrusion events.',
    example: 'ipmitool sel list\nipmitool sel elist\nipmitool sel clear',
    exampleExplanation: '`sel list` prints standard event logs; `sel elist` prints extended descriptions decoded with Sensor Data Record (SDR) records; `sel clear` flushes the log.',
    question: 'Which `ipmitool` command displays timestamped hardware fault occurrences (such as memory ECC faults or fan failures) recorded directly by the BMC?',
    answer: '`ipmitool sel list` (or `ipmitool sel elist`).',
    category: 'Out-of-Band Management',
    examTip: 'When an amber server chassis LED lights up, inspecting `ipmitool sel elist` is the fastest method to pinpoint the exact failing hardware component.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4913,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'ipmitool Sensor Data Repository (SDR)',
    definition: 'Queries the BMC\'s Sensor Data Repository to read hardware sensors, sensor reading bounds, analog voltages, fan tachometers, and ambient chassis temperatures.',
    example: 'ipmitool sdr list\nipmitool sensor get "CPU1 Temp"',
    exampleExplanation: 'Lists all physical hardware sensors managed by the BMC alongside current values and status flags (OK, nc, cr).',
    question: 'Which command retrieves the full inventory of physical sensors, fan RPMs, and power supply states directly from the BMC Sensor Data Repository?',
    answer: '`ipmitool sdr list` (or `ipmitool sensor list`).',
    category: 'Out-of-Band Management',
    examTip: 'Unlike `lm_sensors`, `ipmitool sdr` functions even when the host operating system is powered off, as long as standby auxiliary power is supplied to the BMC.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4914,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'ipmitool Serial-over-LAN (SOL)',
    definition: 'Redirects the host server\'s text serial console output over the IPMI out-of-band LAN connection, enabling remote BIOS configuration, bootloader manipulation, and recovery access without physical KVM switches.',
    example: 'ipmitool -I lanplus -H 10.0.0.50 -U admin -P secret sol activate\n# Escape sequence: ~. (tilde then period)',
    exampleExplanation: 'Establishes a live Serial-over-LAN session to access the server console remotely over the network.',
    question: 'What IPMI capability enables remote command-line access to BIOS and Linux kernel console output over the BMC network interface without a graphical monitor?',
    answer: 'Serial-over-LAN (SOL), invoked via `ipmitool sol activate`.',
    category: 'Out-of-Band Management',
    examTip: 'To exit an active `ipmitool sol` session back to your local shell, press `~.` (tilde followed by period).',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4915,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'Network UPS Tools (NUT) Architecture: upsd & upsmon',
    definition: 'A modular client/server UPS management framework. Hardware drivers communicate with physical UPS units; the `upsd` data server collects status; and one or more `upsmon` client daemons monitor power state and trigger shutdowns.',
    example: 'systemctl status nut-server\nsystemctl status nut-client\nupsc smartups@localhost',
    exampleExplanation: 'Checks status of the NUT server (`upsd`), the client monitor (`upsmon`), and queries the metrics of the UPS named `smartups`.',
    question: 'In the Network UPS Tools (NUT) architecture, what is the role of `upsmon` compared to `upsd`?',
    answer: '`upsd` acts as the server communicating with hardware drivers; `upsmon` is the client that monitors `upsd` and executes system shutdown when batteries are exhausted.',
    category: 'Power Reliability',
    examTip: 'A single central `upsd` daemon can protect an entire rack of servers running lightweight `upsmon` secondary (slave) clients over the local network.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4916,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'NUT Hardware Configuration (/etc/nut/ups.conf)',
    definition: 'The configuration file defining each physically connected UPS device, specifying its driver (e.g. `usbhid-ups`, `snmp-ups`, `apcsmart`), communication port, serial number, and battery thresholds.',
    example: '# /etc/nut/ups.conf\n[rack_ups]\n  driver = usbhid-ups\n  port = auto\n  desc = "APC Smart-UPS 1500"\n  pollinterval = 2',
    exampleExplanation: 'Defines a local UPS named `rack_ups` using the standard USB HID driver on auto-detected USB ports.',
    question: 'Which NUT configuration file defines the hardware driver and communication port for locally attached UPS devices?',
    answer: '`/etc/nut/ups.conf`.',
    category: 'Power Reliability',
    examTip: 'After editing `/etc/nut/ups.conf`, use `upsdrvctl start` to verify that the specified driver successfully establishes communication with the physical UPS hardware.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4917,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'NUT Forced Shutdown Sequence (FSD)',
    definition: 'The Emergency Forced ShutDown command issued by `upsmon` (master mode) during a critical power failure (battery depleted) that alerts secondary clients, waits for OS shutdowns, and commands the UPS inverter to power down.',
    example: 'upsmon -c fsd',
    exampleExplanation: 'Forces an immediate simulated power-loss shutdown sequence to test automated server shutdown and UPS power-off scripts.',
    question: 'Which `upsmon` command signals an immediate Forced ShutDown (FSD) event across all monitored cluster nodes to test emergency power-loss handling?',
    answer: '`upsmon -c fsd`.',
    category: 'Power Reliability',
    examTip: 'FSD sets the `flag` that informs the system startup/shutdown scripts to power-cycle the UPS outlets after servers halt so they automatically restart when AC utility power returns.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4918,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'apcupsd Daemon and Status Query (apcaccess)',
    definition: 'An alternative dedicated daemon for APC brand UPS systems. It monitors battery capacity and utility line quality via USB, serial, or SNMP, executing graceful shutdown scripts when battery reserves drop below configured thresholds.',
    example: 'apcaccess status\n# Key metrics:\n# STATUS   : ONLINE\n# BCHARGE  : 100.0 Percent\n# TIMELEFT : 45.2 Minutes',
    exampleExplanation: 'Displays comprehensive operational status, battery charge percentage, remaining runtime, internal temperature, and AC voltage from `apcupsd`.',
    question: 'Which command queries the running `apcupsd` daemon to display battery charge, load percentage, and estimated runtime remaining?',
    answer: '`apcaccess` (or `apcaccess status`).',
    category: 'Power Reliability',
    examTip: 'Thresholds in `/etc/apcupsd/apcupsd.conf` include `BATTERYLEVEL` (e.g. 15%), `MINUTES` (e.g. 5 minutes remaining), and `TIMEOUT` (maximum seconds on battery).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4919,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'ECC Memory Error Detection (EDAC / edac-util)',
    definition: 'Error Detection and Correction (EDAC) kernel drivers monitor memory controller registers to detect and count single-bit Correctable Errors (CE) and multi-bit Uncorrectable Errors (UE) on server RAM DIMMs.',
    example: 'edac-util -v\nedac-util --report=ce',
    exampleExplanation: 'Prints verbose details on detected memory errors, identifying the exact memory controller, channel, and DIMM slot experiencing bit-flips.',
    question: 'Which command-line utility reports ECC memory bit-flip counts, distinguishing between Correctable Errors (CE) and Uncorrectable Errors (UE)?',
    answer: '`edac-util` (part of the EDAC tools suite).',
    category: 'Hardware Reliability',
    examTip: 'A rising rate of Correctable Errors (CE) on a specific DIMM slot indicates imminent hardware failure; replace the DIMM before an Uncorrectable Error (UE) triggers a kernel panic.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4920,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.1',
    command: 'rasdaemon Reliability, Availability, and Serviceability Logging',
    definition: 'The modern Linux kernel RAS (Reliability, Availability, and Serviceability) logging daemon that captures memory controller ECC events, PCIe AER (Advanced Error Reporting), and CPU MCEs (Machine Check Exceptions) into a SQLite database.',
    example: 'systemctl enable --now rasdaemon\nras-mc-ctl --error-count\nras-mc-ctl --summary',
    exampleExplanation: 'Enables real-time hardware error accounting and summarizes recorded memory errors, PCIe link errors, and CPU cache parity events.',
    question: 'What modern Linux service replaces legacy EDAC and mcelog tools to record hardware errors (PCIe AER, memory ECC, and CPU MCEs) via kernel tracepoints?',
    answer: '`rasdaemon` (managed using `ras-mc-ctl`).',
    category: 'Hardware Reliability',
    examTip: '`rasdaemon` stores events in `/var/lib/rasdaemon/ras-mc_event.db`, allowing historical analysis of hardware degradation trends across the node.',
    difficulty: 'Advanced',
    status: 'unseen',
  },

  // -------------------------------------------------------------
  // Objective 364.2: Advanced RAID (Cards 4921-4940) [20 Cards]
  // -------------------------------------------------------------
  {
    id: 4921,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'mdadm Software RAID Levels and Minimum Disks',
    definition: 'Linux kernel Multi-Device (MD) software RAID provides block storage redundancy: RAID 0 (striping, min 2), RAID 1 (mirroring, min 2), RAID 5 (distributed parity, min 3), RAID 6 (dual parity, min 4), and RAID 10 (mirrored stripes, min 4).',
    example: 'mdadm --create /dev/md0 --level=6 --raid-devices=4 /dev/sdb1 /dev/sdc1 /dev/sdd1 /dev/sde1',
    exampleExplanation: 'Creates a 4-disk RAID 6 array capable of surviving the simultaneous failure of any two member disks without data loss.',
    question: 'What is the minimum number of physical block devices required to create a functional RAID 6 array in Linux?',
    answer: '4 disks (providing usable capacity of N - 2 disks with dual distributed parity).',
    category: 'Advanced RAID',
    examTip: 'RAID 5 can survive 1 drive loss; RAID 6 can survive 2 concurrent drive failures, making it standard for high-capacity SATA/SAS drives where rebuild times are long.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4922,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Monitoring RAID Status via /proc/mdstat',
    definition: 'The virtual procfs file exposing real-time kernel MD driver state, active personalities, array sync progress percentage, rebuild speed, active drive layout, and failed drive flags.',
    example: 'cat /proc/mdstat\nwatch -n 1 cat /proc/mdstat',
    exampleExplanation: 'Monitors real-time RAID synchronization progress, current rebuild transfer rate (KB/sec), and estimated time to completion (finish=XXmin).',
    question: 'Which virtual file provides immediate kernel-level status, synchronization percentages, and drive health for all active software RAID arrays?',
    answer: '`/proc/mdstat`.',
    category: 'Advanced RAID',
    examTip: 'Drive status indicators in `/proc/mdstat`: `[UU_U]` indicates disk 3 is missing or failed in a 4-disk array; `(F)` denotes a failed device.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4923,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'mdadm Detailed Array Inspection (--detail)',
    definition: 'Displays exhaustive runtime details of a software RAID device including superblock version, chunk size, array size, active/working/failed/spare device counts, UUID, and event counter.',
    example: 'mdadm --detail /dev/md0\nmdadm -D /dev/md0',
    exampleExplanation: 'Inspects array `/dev/md0` to verify redundancy health, degradation status, and individual component device states.',
    question: 'Which `mdadm` option displays complete diagnostic information, drive roles, and operational health of an active RAID device `/dev/md0`?',
    answer: '`mdadm --detail /dev/md0` (or `mdadm -D /dev/md0`).',
    category: 'Advanced RAID',
    examTip: 'Use `mdadm --examine /dev/sdb1` (or `-E`) to inspect the superblock metadata stored on an individual component partition rather than the assembled array.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4924,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Persistent Array Registration (/etc/mdadm/mdadm.conf)',
    definition: 'The configuration file used during system boot and initial ramdisk (initramfs) execution to identify, scan, and assemble software RAID devices using their persistent UUIDs.',
    example: 'mdadm --detail --scan >> /etc/mdadm/mdadm.conf\nupdate-initramfs -u',
    exampleExplanation: 'Scans active arrays, appends their ARRAY definitions and UUIDs to `mdadm.conf`, and rebuilds the initramfs to guarantee proper boot assembly.',
    question: 'What command generates compliant `ARRAY` definition lines from currently running RAID arrays to save into `/etc/mdadm/mdadm.conf`?',
    answer: '`mdadm --detail --scan` (or `mdadm -Ds`).',
    category: 'Advanced RAID',
    examTip: 'Relying solely on device node names (like `/dev/sdb1`) is fragile because SCSI device enumeration can change across reboots; `ARRAY` UUIDs guarantee deterministic assembly.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4925,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Internal Write-Intent Bitmaps (--bitmap=internal)',
    definition: 'A mechanism that tracks dirty/out-of-sync blocks in the RAID array superblock. Following an unexpected system crash, power outage, or node reboot, only blocks flagged in the bitmap are resynchronized rather than scanning the entire multi-terabyte disk.',
    example: 'mdadm --grow /dev/md0 --bitmap=internal\nmdadm --detail /dev/md0 | grep Bitmap',
    exampleExplanation: 'Adds an internal write-intent bitmap to active array `/dev/md0`, reducing subsequent post-crash resynchronization time from hours to seconds.',
    question: 'What feature of `mdadm` prevents lengthy full-array resynchronizations after an unclean shutdown by recording modified blocks in a metadata index?',
    answer: 'Write-intent bitmaps (enabled via `mdadm --grow /dev/md0 --bitmap=internal`).',
    category: 'Advanced RAID',
    examTip: 'Internal bitmaps reside within the RAID superblock; external bitmaps reside in a separate file or high-speed NVMe volume (`--bitmap=/path/to/bitmap_file`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4926,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Removing or Disabling a Write-Intent Bitmap (--bitmap=none)',
    definition: 'Removes the write-intent bitmap from an array, which is necessary before performing certain reshape operations (like altering chunk size) or to eliminate slight write performance overhead during continuous sequential writes.',
    example: 'mdadm --grow /dev/md0 --bitmap=none',
    exampleExplanation: 'Disables the bitmap on `/dev/md0` and cleans the corresponding superblock metadata area.',
    question: 'How do you remove an active write-intent bitmap from a software RAID array `/dev/md0`?',
    answer: '`mdadm --grow /dev/md0 --bitmap=none`.',
    category: 'Advanced RAID',
    examTip: 'Always remove the bitmap (`--bitmap=none`) before performing online RAID level migrations or growing chunk sizes, then re-enable it afterwards.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4927,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Online RAID Level Migration / Reshape (RAID 1 to RAID 5)',
    definition: 'The capability of `mdadm` to convert an array from one RAID level to another online without data loss or unmounting the active filesystem, such as reshaping a 2-disk RAID 1 into a 3-disk RAID 5.',
    example: 'mdadm --grow /dev/md0 --level=5 --raid-devices=3 --add /dev/sdd1',
    exampleExplanation: 'Adds third drive `/dev/sdd1` and reshapes the active 2-disk mirror into a 3-disk distributed parity RAID 5, doubling usable storage capacity.',
    question: 'Which command initiates an online reshape of a 2-disk RAID 1 array `/dev/md0` into a 3-disk RAID 5 array using newly added disk `/dev/sdd1`?',
    answer: '`mdadm --grow /dev/md0 --level=5 --raid-devices=3 --add /dev/sdd1`.',
    category: 'Advanced RAID',
    examTip: 'During online reshape, the array remains fully accessible for reads and writes, though I/O performance is throttled by the parity redistribution process.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4928,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Online RAID Level Migration / Reshape (RAID 5 to RAID 6)',
    definition: 'Converts a single-parity RAID 5 array into a dual-parity RAID 6 array to enhance fault tolerance against double disk failures by computing a second syndrome (Q parity).',
    example: 'mdadm --add /dev/md0 /dev/sde1\nmdadm --grow /dev/md0 --level=6 --raid-devices=5 --backup-file=/root/md0_reshape.bak',
    exampleExplanation: 'Adds a fifth disk, specifies a backup file for safety against power loss during critical stripe migration, and reshapes RAID 5 to RAID 6.',
    question: 'Why is the `--backup-file` option critical when growing or reshaping RAID arrays that change disk geometry or parity schemes?',
    answer: 'It stores critical in-flight stripe data during the transition, protecting against catastrophic data loss if power fails during the reshape.',
    category: 'Advanced RAID',
    examTip: 'The backup file MUST reside on a completely separate physical storage device, NOT on the array being reshaped.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4929,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Expanding Array Storage Capacity (Growing Array Size)',
    definition: 'After replacing all component disks in a RAID array with larger drives, the overall array storage capacity is expanded to consume the newly available physical capacity.',
    example: 'mdadm --grow /dev/md0 --size=max\nresize2fs /dev/md0\n# or for XFS: xfs_growfs /mnt/raid',
    exampleExplanation: 'Expands the MD device `/dev/md0` to fill the full size of its enlarged member drives, followed by expanding the filesystem online.',
    question: 'After replacing all drives in a RAID 1 array with larger capacity disks, what command expands the array to use all available space?',
    answer: '`mdadm --grow /dev/md0 --size=max` (followed by filesystem resizing).',
    category: 'Advanced RAID',
    examTip: 'Remember that growing the RAID array expands the underlying block device; you must subsequently run `resize2fs` (ext4) or `xfs_growfs` (xfs) to enlarge the filesystem.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4930,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Adding Disks to an Active Array (--raid-devices Expansion)',
    definition: 'Expanding an existing RAID 5 or RAID 6 array by increasing the active member disk count, adding raw capacity and distributing parity stripes across more spindles.',
    example: 'mdadm --add /dev/md0 /dev/sdf1\nmdadm --grow /dev/md0 --raid-devices=5',
    exampleExplanation: 'Adds partition `/dev/sdf1` as a spare, then commands the MD driver to grow the active device count from 4 to 5, triggering background restriping.',
    question: 'What two-step command sequence adds a new drive `/dev/sdf1` into RAID 5 array `/dev/md0` and reshapes the array to use 5 active drives?',
    answer: '`mdadm --add /dev/md0 /dev/sdf1` followed by `mdadm --grow /dev/md0 --raid-devices=5`.',
    category: 'Advanced RAID',
    examTip: 'Monitor restriping progress via `/proc/mdstat`. Depending on array size and drive speed, this process can take several hours to complete.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4931,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Handling Disk Failures: Marking a Device Faulty (--fail)',
    definition: 'Explicitly instructs the kernel MD driver that a specific component disk has failed or is reporting intermittent read/write errors, evicting it from active array I/O operations.',
    example: 'mdadm /dev/md0 --fail /dev/sdc1\n# or: mdadm --set-faulty /dev/md0 /dev/sdc1',
    exampleExplanation: 'Marks `/dev/sdc1` as faulty in array `/dev/md0`. If a hot spare is available, automatic rebuild onto the spare commences immediately.',
    question: 'Which `mdadm` command marks disk partition `/dev/sdc1` as faulty in array `/dev/md0`?',
    answer: '`mdadm /dev/md0 --fail /dev/sdc1` (or `mdadm -f /dev/md0 /dev/sdc1`).',
    category: 'Advanced RAID',
    examTip: 'A disk must be marked as faulty (`--fail`) before the MD subsystem permits you to remove it (`--remove`) from the array.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4932,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Handling Disk Replacements: Device Removal (--remove)',
    definition: 'Removes a faulty or detached block device from the array configuration, freeing the slot for a new physical replacement drive.',
    example: 'mdadm /dev/md0 --remove /dev/sdc1\n# or: mdadm -r /dev/md0 /dev/sdc1',
    exampleExplanation: 'Removes the failed device `/dev/sdc1` from array `/dev/md0` so the physical disk can be safely hot-swapped.',
    question: 'What command removes a failed drive `/dev/sdc1` from software array `/dev/md0` before physical disk extraction?',
    answer: '`mdadm /dev/md0 --remove /dev/sdc1` (or `mdadm -r /dev/md0 /dev/sdc1`).',
    category: 'Advanced RAID',
    examTip: 'Use `mdadm /dev/md0 --remove failed` to remove all failed component devices in one single command.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4933,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Handling Disk Replacements: Adding Replacement Disk (--add)',
    definition: 'Adds a newly inserted physical drive partition into an existing RAID array to replace a failed disk, immediately triggering automatic synchronization/rebuild.',
    example: 'mdadm /dev/md0 --add /dev/sdc1\n# or: mdadm -a /dev/md0 /dev/sdc1',
    exampleExplanation: 'Adds clean partition `/dev/sdc1` to array `/dev/md0`. The MD driver begins rebuilding data blocks onto the new drive.',
    question: 'Which command integrates replacement disk `/dev/sdc1` into degraded array `/dev/md0` to begin automatic data rebuild?',
    answer: '`mdadm /dev/md0 --add /dev/sdc1` (or `mdadm -a /dev/md0 /dev/sdc1`).',
    category: 'Advanced RAID',
    examTip: 'Ensure the partition table on the new disk matches the original drive exactly (e.g. using `sfdisk -d /dev/sdb | sfdisk /dev/sdc`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4934,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Configuring and Managing Hot Spares',
    definition: 'Drives added to a RAID array that remain idle and powered on in standby. If any active member drive fails, the MD driver instantly and automatically mobilizes the hot spare to reconstruct data without human intervention.',
    example: 'mdadm --create /dev/md0 --level=5 --raid-devices=3 /dev/sdb1 /dev/sdc1 /dev/sdd1 --spare-devices=1 /dev/sde1',
    exampleExplanation: 'Creates a 3-disk RAID 5 with 1 hot spare (`/dev/sde1`). If sdb1, sdc1, or sdd1 fails, sde1 immediately begins rebuilding.',
    question: 'How do you designate `/dev/sde1` as an idle hot spare when creating a software RAID array?',
    answer: 'By appending `--spare-devices=1 /dev/sde1` during array creation (or using `mdadm /dev/md0 --add /dev/sde1` later).',
    category: 'Advanced RAID',
    examTip: 'In `/proc/mdstat`, a hot spare is indicated with `(S)` (e.g. `sde1[3](S)`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4935,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'RAID Resync Speed Control (/proc/sys/dev/raid/)',
    definition: 'Kernel sysctl tunables controlling minimum and maximum I/O bandwidth allocated to RAID resynchronization and rebuild operations, balancing rebuild speed against active application latency.',
    example: 'cat /proc/sys/dev/raid/speed_limit_min\necho 50000 > /proc/sys/dev/raid/speed_limit_min\necho 200000 > /proc/sys/dev/raid/speed_limit_max',
    exampleExplanation: 'Sets the minimum rebuild speed floor to 50 MB/s (50,000 KB/s) and upper ceiling to 200 MB/s to accelerate array recovery.',
    question: 'Which kernel parameters control the minimum and maximum data transfer rates permitted during RAID rebuild and resync operations?',
    answer: '`/proc/sys/dev/raid/speed_limit_min` and `/proc/sys/dev/raid/speed_limit_max`.',
    category: 'Advanced RAID',
    examTip: 'If an array rebuild is running too slowly during off-peak hours, increasing `speed_limit_min` forces the kernel to prioritize RAID rebuilding over background tasks.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4936,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'mdadm Background Monitoring Daemon (--monitor / -F)',
    definition: 'Runs `mdadm` as a continuous background daemon that polls RAID status and executes custom alerting programs or dispatches email alerts upon degraded array events, drive failures, or rebuild completion.',
    example: 'mdadm --monitor --scan --daemonise --mail=root@localhost --program=/usr/local/bin/raid_alert.sh',
    exampleExplanation: 'Launches `mdadm` in daemon mode (`--daemonise`), scanning all arrays and alerting root upon critical drive failure events.',
    question: 'Which `mdadm` mode monitors array health continuously in the background and sends email alerts upon drive failures?',
    answer: '`mdadm --monitor` (or `mdadm -F` / `mdadm --monitor --scan --daemonise`).',
    category: 'Advanced RAID',
    examTip: 'The `MAILADDR` directive in `/etc/mdadm/mdadm.conf` defines the default recipient email address for the monitoring daemon.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4937,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Stopping and Deactivating RAID Arrays (--stop)',
    definition: 'Flushes dirty caches, unregisters the MD block device from the kernel, and deactivates the array safely for maintenance or decommissioning.',
    example: 'umount /mnt/raid\nmdadm --stop /dev/md0\n# or: mdadm -S /dev/md0',
    exampleExplanation: 'Unmounts the dependent filesystem and stops array `/dev/md0`, releasing all member disk partitions.',
    question: 'What command safely deactivates an assembled software RAID array `/dev/md0` after its filesystem has been unmounted?',
    answer: '`mdadm --stop /dev/md0` (or `mdadm -S /dev/md0`).',
    category: 'Advanced RAID',
    examTip: 'If `mdadm --stop` returns "device or resource busy", check for active mounts, open files with `lsof /dev/md0`, or LVM Physical Volumes on top of the array.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4938,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Zeroing Superblocks to Reuse Disks (--zero-superblock)',
    definition: 'Erases the MD superblock metadata from a physical disk partition, ensuring the kernel does not attempt to automatically detect or assemble the drive into an old RAID array.',
    example: 'mdadm --zero-superblock /dev/sdb1',
    exampleExplanation: 'Wipes the MD metadata magic headers from `/dev/sdb1` so the drive partition can be repurposed for standalone filesystems or LVM.',
    question: 'How can an administrator wipe stale RAID metadata headers from partition `/dev/sdb1` so it is no longer recognized as part of a RAID array?',
    answer: '`mdadm --zero-superblock /dev/sdb1`.',
    category: 'Advanced RAID',
    examTip: 'Always zero superblocks on retired member disks before adding them to new volume groups or distinct RAID arrays to prevent metadata collision errors.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4939,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'RAID 10 Layout Options (--layout n2, f2, o2)',
    definition: 'Linux software RAID 10 supports multiple layout architectures: `n2` (near copies, default), `f2` (far copies, excellent sequential read performance matching RAID 0), and `o2` (offset copies, interleaving stripe chunks).',
    example: 'mdadm --create /dev/md0 --level=10 --layout=f2 --raid-devices=4 /dev/sdb1 /dev/sdc1 /dev/sdd1 /dev/sde1',
    exampleExplanation: 'Creates a 4-disk RAID 10 using the far-layout (`f2`), where redundant mirrored blocks are placed on opposite drive tracks to maximize read throughput.',
    question: 'Which RAID 10 layout format in Linux places mirrored chunks at different physical offsets on member drives to achieve read performance comparable to RAID 0?',
    answer: 'The far layout (`--layout=f2`).',
    category: 'Advanced RAID',
    examTip: 'RAID 10 far (`f2`) provides higher read throughput on spinning HDDs, but write performance is lower because heads must seek across the disk surface during writes.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4940,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.2',
    command: 'Scrubbing and Consistency Checking (check / repair)',
    definition: 'Initiates a background integrity scrub on parity-based arrays (RAID 5/6/1) by reading all sectors and recalculating parity checksums to identify and correct silent bit-rot.',
    example: 'echo check > /sys/block/md0/md/sync_action\ncat /sys/block/md0/md/mismatch_cnt\necho repair > /sys/block/md0/md/sync_action',
    exampleExplanation: '`check` scans the array and records parity errors in `mismatch_cnt` without modifying data; `repair` updates faulty parity blocks.',
    question: 'How do you command the kernel to perform a non-destructive integrity read scan of parity on RAID device `/dev/md0`?',
    answer: '`echo check > /sys/block/md0/md/sync_action` (and inspect `mismatch_cnt`).',
    category: 'Advanced RAID',
    examTip: 'Many enterprise Linux distributions install a weekly cron job in `/etc/cron.d/raid-check` that automatically triggers this parity scrub.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },

  // -------------------------------------------------------------------------------------
  // Objective 364.3: Advanced LVM - Part 1: Thin Provisioning & Thin Pools (Cards 4941-4950)
  // -------------------------------------------------------------------------------------
  {
    id: 4941,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'LVM Thin Provisioning Concepts and Overprovisioning',
    definition: 'Thin provisioning allows administrators to create virtual Logical Volumes whose reported virtual size exceeds available physical storage (overprovisioning). Storage blocks are only allocated from a shared thin pool upon actual data write.',
    example: 'lvs -a\n# Notice: Data% and Meta% columns indicate consumption of the backing pool.',
    exampleExplanation: 'Displays thin pool and thin volume capacity utilization, verifying the divergence between virtual size (VSize) and physical data consumption.',
    question: 'What is the primary operational advantage of LVM Thin Provisioning compared to traditional thick-provisioned LVs?',
    answer: 'Storage is allocated on-demand upon writing rather than upfront, enabling capacity overprovisioning across multiple virtual volumes.',
    category: 'Advanced LVM',
    examTip: 'Thin provisioning enables instantaneous snapshot creation and thin volume cloning with zero initial storage consumption.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4942,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Creating an LVM Thin Pool (--thinpool)',
    definition: 'Creates a dedicated backing thin pool logical volume inside an existing Volume Group. A thin pool consists of two internal sub-LVs: a data pool LV (storing actual data) and a metadata pool LV (tracking block allocation mappings).',
    example: 'lvcreate -L 100G --thinpool tp_storage vg_data',
    exampleExplanation: 'Creates a 100GB physical thin pool named `tp_storage` inside volume group `vg_data` ready to host thin virtual volumes.',
    question: 'Which `lvcreate` syntax creates a 100GB thin pool named `tp_storage` in Volume Group `vg_data`?',
    answer: '`lvcreate -L 100G --thinpool tp_storage vg_data`.',
    category: 'Advanced LVM',
    examTip: 'Under the hood, LVM automatically creates `tp_storage_tdata` and `tp_storage_tmeta` to manage data blocks and allocation metadata.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4943,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Creating a Thin Volume (-V / --virtualsize)',
    definition: 'Creates a thinly provisioned virtual volume out of a designated thin pool, specifying a virtual capacity (`-V`) that can exceed the physical backing size of the pool.',
    example: 'lvcreate -V 500G --thin -n lv_app_data vg_data/tp_storage',
    exampleExplanation: 'Creates a 500GB virtual thin volume named `lv_app_data` carved out of the 100GB physical thin pool `tp_storage`.',
    question: 'Which `lvcreate` command creates a 500GB virtual thin volume named `lv_app_data` inside thin pool `tp_storage` in VG `vg_data`?',
    answer: '`lvcreate -V 500G --thin -n lv_app_data vg_data/tp_storage`.',
    category: 'Advanced LVM',
    examTip: 'Always specify `-V <virtual_size>` when creating thin volumes; using `-L` would attempt thick physical allocation.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4944,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Thin Pool Chunk Size Customization (-c / --chunksize)',
    definition: 'Specifies the allocation chunk size for the thin pool (from 64KB up to 1GB). Smaller chunk sizes reduce internal fragmentation for small random writes; larger chunk sizes reduce metadata overhead and optimize large streaming I/O.',
    example: 'lvcreate -L 200G -c 256K --thinpool tp_fast vg_nvme',
    exampleExplanation: 'Configures a thin pool with 256KB chunk size, balancing allocation efficiency and metadata scaling.',
    question: 'What parameter determines the allocation block unit size for a new LVM thin pool during `lvcreate`?',
    answer: '`-c` or `--chunksize` (e.g. `-c 128K` or `-c 1M`).',
    category: 'Advanced LVM',
    examTip: 'The chunk size must be a power of 2 between 64KB and 1GB. Default is typically calculated automatically based on pool size.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4945,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Thin Provisioning Zeroing (-Z / --zero)',
    definition: 'Controls whether newly allocated chunks in a thin volume are zero-filled before being handed to the application. Zeroing prevents sensitive data leakage from previously deleted blocks, but incurs slight write latency on first writes.',
    example: 'lvcreate -V 100G --thin -n lv_db --zero y vg_data/tp_storage\n# To disable zeroing for maximum write speed:\nlvcreate -V 100G --thin -n lv_bench -Z n vg_data/tp_storage',
    exampleExplanation: 'Enables (`-Z y`) or disables (`-Z n`) automatic block zeroing upon first block write.',
    question: 'Which `lvcreate` option prevents the kernel from zeroing newly allocated chunks in a thin volume to maximize initial write performance?',
    answer: '`-Z n` or `--zero n`.',
    category: 'Advanced LVM',
    examTip: 'Security consideration: Disabling zeroing (`-Z n`) can allow a user or virtual machine to read stale residual data previously stored in those physical blocks.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4946,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Thin Pool Automatic Extension in /etc/lvm/lvm.conf',
    definition: 'Directs the `dmeventd` monitoring daemon to automatically grow a thin pool when its data or metadata utilization crosses a configured percentage threshold, preventing emergency out-of-space write freezes.',
    example: '# In /etc/lvm/lvm.conf:\nthin_pool_autoextend_threshold = 75\nthin_pool_autoextend_percent = 20',
    exampleExplanation: 'Automatically extends the thin pool by 20% whenever data or metadata utilization exceeds 75%, provided free extents exist in the VG.',
    question: 'In `/etc/lvm/lvm.conf`, which two parameters configure automated expansion of a thin pool when it reaches 75% capacity?',
    answer: '`thin_pool_autoextend_threshold = 75` and `thin_pool_autoextend_percent = <percentage>`.',
    category: 'Advanced LVM',
    examTip: 'For auto-extension to function, the `dmeventd` monitoring daemon must be enabled and running (`systemctl enable --now lvm2-monitor`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4947,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Extending a Thin Pool Manually (lvextend)',
    definition: 'Expands the backing physical storage capacity of a thin pool or its separate metadata volume when physical space runs low.',
    example: 'lvextend -L +50G vg_data/tp_storage\nlvextend --poolmetadatasize +1G vg_data/tp_storage',
    exampleExplanation: 'Expands the data capacity of thin pool `tp_storage` by 50GB and increases its metadata allocation by 1GB.',
    question: 'How do you increase the metadata volume size of an existing thin pool `tp_storage` by 1GB?',
    answer: '`lvextend --poolmetadatasize +1G vg_data/tp_storage`.',
    category: 'Advanced LVM',
    examTip: 'If metadata space fills to 100%, all I/O to all thin volumes in the pool will fail; always monitor `Meta%` in the output of `lvs`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4948,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Thin Pool Exhaustion Consequences and Error Behaviors',
    definition: 'When a thin pool reaches 100% capacity and cannot auto-extend, writes to all hosted thin volumes will either be queued/blocked or return immediate I/O errors, depending on the configured error policy.',
    example: '# In /etc/lvm/lvm.conf:\nerror_when_full = 1',
    exampleExplanation: 'When `error_when_full = 1`, writes to a full thin pool immediately return EIO errors; when set to 0, writes are queued until space is added.',
    question: 'What happens to write operations when an LVM thin pool reaches 100% capacity with `error_when_full = 0`?',
    answer: 'Write I/O requests are queued/blocked until an administrator extends the thin pool.',
    category: 'Advanced LVM',
    examTip: 'Queuing writes (`error_when_full = 0`) prevents immediate filesystem corruption, but applications can hang indefinitely if the pool is not extended quickly.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4949,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Thin Provisioning Discard / TRIM Handling',
    definition: 'Allows the guest operating system or filesystem to notify the underlying thin pool that blocks have been deleted (via `fstrim` or the `discard` mount option), freeing physical blocks back to the thin pool.',
    example: 'fstrim -v /mnt/thin_vol\n# In /etc/lvm/lvm.conf:\nissue_discards = 1',
    exampleExplanation: 'Runs manual filesystem trim to reclaim unused blocks back into the shared thin pool and ensures LVM issues discard commands to physical SSDs.',
    question: 'Which command reclaims unused deleted filesystem blocks from a mounted thin volume and returns them to the LVM thin pool?',
    answer: '`fstrim -v <mountpoint>`.',
    category: 'Advanced LVM',
    examTip: 'Ensure `issue_discards = 1` is enabled in `lvm.conf` so underlying hardware SSDs receive TRIM notifications when thin volumes or pools are deleted.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4950,
    deck: 'LPIC-3 Topic 364',
    topicNumber: 364,
    objectiveId: '364.3',
    command: 'Thin Provisioning Repair and Dump Tools (thin_dump / thin_repair)',
    definition: 'Userspace utilities provided by the `thin-provisioning-tools` package to inspect, dump, check, and repair corrupted metadata structures within LVM thin pools.',
    example: 'thin_dump /dev/mapper/vg_data-tp_storage_tmeta\nthin_repair -i /dev/mapper/vg_data-tp_storage_tmeta -o /dev/mapper/vg_data-new_tmeta',
    exampleExplanation: 'Inspects raw XML metadata structures and repairs damaged metadata mappings after an unclean shutdown.',
    question: 'Which tool from `thin-provisioning-tools` extracts and repairs corrupted metadata structures from a damaged LVM thin pool metadata LV?',
    answer: '`thin_repair` (along with `thin_dump` and `thin_check`).',
    category: 'Advanced LVM',
    examTip: 'To safely repair a thin pool, the pool must be inactive (`lvchange -an`) to prevent concurrent metadata modifications.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
];
