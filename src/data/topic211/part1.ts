import { Flashcard } from '../../types';

/**
 * LPI LPIC-2 Exam 202 - Topic 211: E-Mail Services
 * Part 1: Cards 3101 - 3150
 * - Objective 211.1: Using E-Mail Servers (Postfix/Sendmail) [50 Cards] (Weight 4)
 */

export const topic211Part1: Flashcard[] = [
  // =========================================================================
  // OBJECTIVE 211.1: Using E-Mail Servers (Postfix/Sendmail) (Cards 3101-3150) [Weight 4]
  // =========================================================================
  {
    id: 3101,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'MTA Architecture',
    command: 'MTA vs MUA vs MDA vs MSA',
    question: 'What are the distinct roles of MTA, MUA, MDA, and MSA in the email delivery chain?',
    answer: 'MUA (Mail User Agent): Client application used to compose and read email (e.g. Thunderbird, Mutt). MSA (Mail Submission Agent): Ingests email from MUA on port 587 with authentication. MTA (Mail Transfer Agent): Routes and relays email between servers using SMTP on port 25 (e.g. Postfix, Sendmail). MDA (Mail Delivery Agent): Takes email from the MTA and writes it into local user storage (e.g. Dovecot LDA, Procmail).',
    definition: 'Standard architectural roles defined in Internet mail standards (RFC 5598) governing email transmission.',
    example: 'MUA (Thunderbird) -> MSA/MTA (Postfix :587/:25) -> MTA (Remote Postfix) -> MDA (Dovecot LDA) -> Maildir',
    exampleExplanation: 'Traces an email from composition through submission, internet transport, local delivery, and storage.',
    examTip: 'Postfix functions as an MTA/MSA, Dovecot operates as both an MDA (LDA/LMTP) and remote retrieval server (IMAP/POP3), and Procmail is strictly an MDA.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3102,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Architecture',
    command: 'Postfix modular daemon design',
    question: 'How does Postfix\'s modular security architecture differ from classic Sendmail?',
    answer: 'Unlike monolithic Sendmail which historically ran as a single setuid root process, Postfix consists of multiple small, specialized daemons managed by a central supervisor (`master`). Most daemons run unprivileged with minimal capabilities, often chrooted, communicating via internal IPC.',
    definition: 'A security-oriented design pattern engineered by Wietse Venema minimizing root privilege exposure and containment risk.',
    example: 'ps aux | grep postfix\n# master (root), smtpd (postfix), cleanup (postfix), qmgr (postfix), anvil (postfix)',
    exampleExplanation: 'Only the master supervisor runs as root to bind privileged ports (e.g. port 25); all worker daemons drop privileges to the postfix user.',
    examTip: 'Sendmail was monolithic; Postfix is multi-process and modular with least-privilege separation.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3103,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Configuration',
    command: 'main.cf and master.cf',
    question: 'What are the two primary configuration files in Postfix and what does each control?',
    answer: '`/etc/postfix/main.cf` specifies global operational parameters, domain names, network permissions, and lookup tables. `/etc/postfix/master.cf` defines which service daemons (smtpd, qmgr, local, cleanup) run, their process limits, socket types, and execution environments.',
    definition: 'The two foundational configuration files located in `/etc/postfix/` governing all Postfix mail operations.',
    example: 'ls -l /etc/postfix/main.cf /etc/postfix/master.cf',
    exampleExplanation: 'Both files are read by the master supervisor daemon upon startup and reloaded on `postfix reload`.',
    examTip: 'Parameters in `main.cf` follow `parameter = value` syntax; `master.cf` uses multi-column tabular formatting.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3104,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Configuration',
    command: 'postconf command',
    question: 'How do you inspect Postfix parameters using `postconf`, and what is the difference between `postconf -n` and `postconf -d`?',
    answer: '`postconf` without flags displays all currently active parameters. `postconf -n` (non-default) prints ONLY parameters explicitly overridden in `main.cf`. `postconf -d` (defaults) displays Postfix factory default parameters.',
    definition: 'The Postfix parameter query and configuration editing utility.',
    example: 'postconf -n\npostconf myhostname mydestination\npostconf -d mail_version',
    exampleExplanation: '`postconf -n` is standard for troubleshooting because it filters out hundreds of unchanged default settings.',
    examTip: 'Always remember `postconf -n` for LPIC-2 troubleshooting questions when inspecting customized settings.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3105,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Configuration',
    command: 'postconf -e',
    question: 'Which `postconf` option modifies or adds parameter settings directly in `/etc/postfix/main.cf` safely?',
    answer: '`postconf -e "parameter = value"`. It edits `main.cf` in place, creating backups and preventing syntax corruption.',
    definition: 'The safe command-line parameter modification switch for Postfix.',
    example: 'postconf -e "myorigin = example.com"\npostconf -e "inet_interfaces = loopback-only"\npostfix reload',
    exampleExplanation: 'Appends or updates the specified directive in `/etc/postfix/main.cf` without opening a text editor.',
    examTip: 'Use `-e` (edit). Multiple parameter changes can be combined in one command or chained.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3106,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Parameters',
    command: 'myhostname parameter',
    question: 'What is the purpose of the `myhostname` parameter in Postfix, and what does it default to?',
    answer: 'It defines the Fully Qualified Domain Name (FQDN) of the mail system host (e.g. `mail.example.com`). It defaults to the server\'s FQDN as returned by the `gethostname()` system call.',
    definition: 'The core identity parameter used by Postfix in SMTP HELO/EHLO greetings and internal address generation.',
    example: 'postconf -e "myhostname = mail.example.com"',
    exampleExplanation: 'Ensures Postfix identifies itself as mail.example.com when announcing itself to remote SMTP servers.',
    examTip: 'Remote mail servers verify reverse DNS (PTR) against the HELO hostname defined by `myhostname`. Mismatches cause delivery rejection.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3107,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Parameters',
    command: 'mydomain parameter',
    question: 'What does the `mydomain` parameter specify and what is its default value in Postfix?',
    answer: 'It specifies the parent internet domain name of the mail server (e.g. `example.com`). By default, it derives from `$myhostname` by stripping off the first subdomain component.',
    definition: 'The organizational internet domain used as a default building block for other Postfix parameters.',
    example: '# If myhostname = mail.example.com\n# mydomain defaults to example.com\npostconf -e "mydomain = example.com"',
    exampleExplanation: 'Allows other directives like `myorigin = $mydomain` to reference the organization\'s primary domain.',
    examTip: 'Variables in Postfix configuration files can reference other variables with a dollar sign prefix (e.g. `$mydomain`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3108,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Parameters',
    command: 'myorigin parameter',
    question: 'What is the function of the `myorigin` directive in `/etc/postfix/main.cf`?',
    answer: 'It specifies the domain name that locally submitted email appears to originate from when no domain is provided in the sender address. It also replaces missing recipient domains. It defaults to `$myhostname`, but is typically set to `$mydomain`.',
    definition: 'The domain appended to unqualified user addresses (e.g. `alice` becomes `alice@$myorigin`).',
    example: 'postconf -e "myorigin = $mydomain"',
    exampleExplanation: 'When a local user sends mail via `mail bob`, the sender address becomes `alice@example.com` rather than `alice@mail.example.com`.',
    examTip: 'Setting `myorigin = $mydomain` ensures users send email as `user@company.com` instead of revealing the specific server hostname.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3109,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Parameters',
    command: 'mydestination parameter',
    question: 'What does the `mydestination` parameter control, and what happens if a destination domain is omitted from it?',
    answer: 'It lists the domains that this Postfix system considers itself the final destination for and delivers locally via the local delivery agent. If an incoming domain is NOT in `mydestination` (and not in virtual domains or relay domains), Postfix treats it as an outbound relay attempt.',
    definition: 'The list of domains delivered to local mailboxes (e.g. `/var/mail/` or `~/Maildir/`).',
    example: 'mydestination = $myhostname, localhost.$mydomain, localhost, $mydomain',
    exampleExplanation: 'Tells Postfix to accept and locally store mail addressed to any user at mail.example.com, localhost, or example.com.',
    examTip: 'Never list the same domain in both `mydestination` and `virtual_mailbox_domains` or Postfix will trigger a configuration error.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3110,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Relay & Access',
    command: 'mynetworks parameter',
    question: 'What does `mynetworks` specify in Postfix, and why is it crucial for preventing open relays?',
    answer: 'It defines the list of trusted client IP addresses and subnets authorized to relay outbound mail through Postfix to remote external destinations without requiring SASL authentication. Restricting this list prevents the server from becoming an open relay for spammers.',
    definition: 'The trusted network access control list for unauthorized relaying in Postfix.',
    example: 'postconf -e "mynetworks = 127.0.0.0/8, [::1]/128, 192.168.1.0/24"',
    exampleExplanation: 'Allows only the local loopback and the 192.168.1.0/24 subnet to forward outbound email to third-party domains.',
    examTip: 'If `mynetworks` is set to `0.0.0.0/0`, anyone on the Internet can use your server to send spam (Open Relay), leading to immediate blacklisting.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3111,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Relay & Access',
    command: 'mynetworks_style parameter',
    question: 'What are the three values for `mynetworks_style` when `mynetworks` is not explicitly defined?',
    answer: '1. `host`: Trusts only the local machine (127.0.0.1/localhost). 2. `subnet`: Trusts all machines in the local IP subnets the host is directly connected to (Postfix default). 3. `class`: Trusts all machines in the legacy class A/B/C network (dangerous; never recommended).',
    definition: 'An automatic subnet computation setting used by Postfix when `mynetworks` is left unconfigured.',
    example: 'postconf -e "mynetworks_style = host"',
    exampleExplanation: 'Restricts automatic relaying strictly to the local host itself.',
    examTip: 'Explicitly defining `mynetworks` is always preferred over relying on `mynetworks_style`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3112,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Relay & Access',
    command: 'smtpd_relay_restrictions',
    question: 'Which parameter enforces relay restrictions in modern Postfix, and what is the standard secure configuration?',
    answer: '`smtpd_relay_restrictions`. The standard secure configuration is: `permit_mynetworks, permit_sasl_authenticated, defer_unauth_destination` (or `reject_unauth_destination`).',
    definition: 'Access restrictions applied before recipient evaluation to prevent unauthorized relaying.',
    example: 'smtpd_relay_restrictions = permit_mynetworks, permit_sasl_authenticated, reject_unauth_destination',
    exampleExplanation: 'Permits trusted networks and authenticated clients to send mail anywhere; rejects all other attempts to send mail to external domains.',
    examTip: '`reject_unauth_destination` blocks external clients from sending mail through your server to external recipients (open relay protection).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3113,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Parameters',
    command: 'relayhost parameter',
    question: 'What is the role of `relayhost` in Postfix and what do square brackets around the hostname mean?',
    answer: 'It directs Postfix to forward all outbound non-local mail through an upstream smart host/relay server instead of delivering directly to recipient MX servers. Square brackets (e.g. `[smtp.isp.com]`) suppress MX DNS lookups, forcing an A/AAAA record lookup directly.',
    definition: 'The upstream outbound mail gateway configuration directive.',
    example: 'postconf -e "relayhost = [smtp.sendgrid.net]:587"',
    exampleExplanation: 'Routes all outbound email through sendgrid on port 587, querying the A record directly without querying MX records.',
    examTip: 'Exam favorite: `[mail.isp.com]` disables MX lookup; `mail.isp.com` without brackets performs full MX lookup.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3114,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Parameters',
    command: 'inet_interfaces parameter',
    question: 'What does `inet_interfaces` configure in Postfix and how do you restrict Postfix to local-only mail delivery?',
    answer: 'It defines the network interface IP addresses Postfix listens on for incoming SMTP connections. Setting `inet_interfaces = loopback-only` restricts Postfix to 127.0.0.1 and ::1, preventing external incoming network connections.',
    definition: 'The network interface binding directive for the Postfix smtpd listener.',
    example: 'postconf -e "inet_interfaces = loopback-only"\n# Or for all interfaces:\npostconf -e "inet_interfaces = all"',
    exampleExplanation: 'Commonly configured on internal application or database servers that only need to send local notifications.',
    examTip: 'When changing `inet_interfaces`, a simple `postfix reload` is often not sufficient; a full `postfix restart` is required to rebind sockets.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3115,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Parameters',
    command: 'inet_protocols parameter',
    question: 'How do you force Postfix to use only IPv4 or both IPv4 and IPv6?',
    answer: 'Set `inet_protocols = ipv4` (IPv4 only) or `inet_protocols = all` (both IPv4 and IPv6).',
    definition: 'The IP protocol version selection parameter for incoming and outgoing connections in Postfix.',
    example: 'postconf -e "inet_protocols = ipv4"',
    exampleExplanation: 'Prevents Postfix from attempting IPv6 lookups or listening on IPv6 sockets on systems without IPv6 routing.',
    examTip: 'Setting `inet_protocols = ipv4` resolves common timeout delays caused by dead or unroutable IPv6 connections.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3116,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'master.cf Architecture',
    command: 'master.cf column structure',
    question: 'What are the eight columns defined for each service entry in `/etc/postfix/master.cf`?',
    answer: '1. `service name` 2. `type` (inet, unix, fifo) 3. `private` (y/n) 4. `unpriv` (y/n) 5. `chroot` (y/n) 6. `wakeup` (time) 7. `maxproc` (max processes) 8. `command + args`.',
    definition: 'The master daemon process blueprint format describing individual Postfix microservices.',
    example: '# service type  private unpriv  chroot  wakeup  maxproc command\nsmtp      inet  n       -       y       -       -       smtpd\nqmgr      fifo  n       -       n       300     1       qmgr',
    exampleExplanation: 'Defines the internet-facing SMTP listener and the queue manager daemon with their respective attributes.',
    examTip: 'A dash (`-`) in a column means "use the default value" defined in Postfix compiled defaults or main.cf.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3117,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'master.cf Architecture',
    command: 'submission (587) and smtps (465) in master.cf',
    question: 'How are client email submission (port 587) and SMTPS (port 465) enabled in `/etc/postfix/master.cf`?',
    answer: 'By uncommenting the `submission inet n - y - - smtpd` or `smtps inet n - y - - smtpd` lines and specifying sub-options like `-o smtpd_sasl_auth_enable=yes` and `-o smtpd_tls_security_level=encrypt`.',
    definition: 'Dedicated client submission endpoints isolating end-user mail injection from server-to-server port 25 traffic.',
    example: '# In /etc/postfix/master.cf:\nsubmission inet n - y - - smtpd\n  -o syslog_name=postfix/submission\n  -o smtpd_tls_security_level=encrypt\n  -o smtpd_sasl_auth_enable=yes\n  -o smtpd_client_restrictions=permit_sasl_authenticated,reject',
    exampleExplanation: 'Requires TLS and SASL authentication for MUAs submitting outbound email.',
    examTip: 'Port 587 is STARTTLS Submission; Port 465 is legacy implicit TLS (SMTPS) reintroduced as modern secure submission (RFC 8314).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3118,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Queues',
    command: 'Postfix queue directories',
    question: 'What are the main mail queue stages inside `/var/spool/postfix/` and what does each hold?',
    answer: '1. `maildrop`: Locally submitted mail waiting for pickup. 2. `incoming`: New mail from network or maildrop being processed by cleanup. 3. `active`: Messages currently opened by queue manager (`qmgr`) for delivery. 4. `deferred`: Messages that failed delivery temporarily and await retry. 5. `hold`: Messages manually frozen by admin. 6. `corrupt`: Unreadable/damaged queue files.',
    definition: 'The spool directory hierarchy where Postfix persists email messages throughout their delivery lifecycle.',
    example: 'ls -l /var/spool/postfix/\n# maildrop, incoming, active, deferred, hold, corrupt',
    exampleExplanation: 'Illustrates how Postfix segregates in-flight, queued, held, and problematic emails.',
    examTip: 'Messages move from `incoming` -> `active` -> (delivered OR `deferred`). Held messages reside in `hold`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3119,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Queues',
    command: 'mailq and postqueue -p',
    question: 'Which commands display the current Postfix mail queue, message IDs, sizes, arrival times, and delivery failure reasons?',
    answer: '`mailq` or `postqueue -p`. Both output the list of messages in the active, deferred, and hold queues with their queue IDs and failure diagnostics.',
    definition: 'The standard utilities for viewing pending and deferred outgoing mail queues.',
    example: 'mailq\n# Output:\n# -Queue ID- --Size-- ----Arrival Time---- -Sender/Recipient-------\n# 4SZVqL1jxyz 1240 Fri Sep 4 10:12:01 sender@example.com\n# (Connection timed out)\n#                    user@remote-domain.org\n# -- 1 Kbytes in 1 Request.',
    exampleExplanation: 'Shows a deferred message with its 11-character queue ID and the reason for delivery deferral.',
    examTip: '`mailq` is a symlink or compatibility wrapper calling `postqueue -p`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3110,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Queues',
    command: 'postqueue -f',
    question: 'What does `postqueue -f` do, and when would an administrator run it?',
    answer: 'It forces Postfix to flush the mail queue, immediately retrying delivery for all messages currently in the deferred queue rather than waiting for regular retry intervals.',
    definition: 'The administrative queue flush command.',
    example: 'postqueue -f',
    exampleExplanation: 'Attempts immediate delivery of all deferred messages after fixing an upstream network or DNS outage.',
    examTip: '`postqueue -f` flushes the entire deferred queue. To flush mail for a specific domain only, use `postqueue -s <domain>`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3121,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Queues',
    command: 'postqueue -s site',
    question: 'How do you flush deferred mail destined specifically for a single domain name?',
    answer: '`postqueue -s <domain>` (e.g. `postqueue -s example.com`).',
    definition: 'Targeted domain queue flush command in Postfix.',
    example: 'postqueue -s clientdomain.com',
    exampleExplanation: 'Instructs the queue manager to wake up and deliver messages queued specifically for clientdomain.com.',
    examTip: 'Requires `fast_flush_domains` to include the target domain (by default includes `$relay_domains`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3122,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Queue Management',
    command: 'postsuper -d',
    question: 'How do you delete a specific message or all messages from the Postfix mail queue?',
    answer: 'To delete a specific message: `postsuper -d <queue_id>`. To delete ALL messages in all queues: `postsuper -d ALL`. To delete all deferred messages only: `postsuper -d ALL deferred`.',
    definition: 'The superuser queue maintenance command for modifying and deleting queued messages.',
    example: 'postsuper -d 4SZVqL1jxyz\npostsuper -d ALL deferred',
    exampleExplanation: 'Deletes an individual stuck message or purges the deferred queue of undeliverable spam.',
    examTip: '`postsuper` requires root privileges. Notice that `ALL` must be capitalized when purging queues.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3123,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Queue Management',
    command: 'postsuper -h and -H',
    question: 'What do the `-h` and `-H` options do in `postsuper`?',
    answer: '`-h <queue_id>` puts a message ON HOLD (moves it to the `hold` queue). `-H <queue_id>` RELEASES a message from hold (moves it back to the deferred or incoming queue for delivery).',
    definition: 'The queue freeze and unfreeze commands in Postfix.',
    example: 'postsuper -h 4SZVqL1jxyz\npostsuper -H 4SZVqL1jxyz\npostsuper -H ALL',
    exampleExplanation: 'Puts a suspicious message on hold for inspection, then releases it once cleared.',
    examTip: 'Remember: lowercase `-h` puts on Hold; uppercase `-H` un-Holds (releases).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3124,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Queue Management',
    command: 'postsuper -r',
    question: 'What is the purpose of `postsuper -r <queue_id>` (or `postsuper -r ALL`)?',
    answer: 'It requeues messages by moving them to the `maildrop` queue. Postfix re-processes the messages through the `cleanup` daemon as if they had just arrived, recalculating headers, content filters, and address rewrites.',
    definition: 'The message requeue command in Postfix.',
    example: 'postsuper -r ALL',
    exampleExplanation: 'Requeues all messages across the mail system after updating transport maps or address rewrite tables.',
    examTip: 'Useful after updating a broken mail routing rule so stuck messages pick up the newly corrected route.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3125,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Aliases',
    command: '/etc/aliases format',
    question: 'What is the syntax of `/etc/aliases` and what targets can a local alias point to?',
    answer: 'Format: `alias_name: destination(s)`. Destinations can be: 1. Another local user (e.g. `root: admin`). 2. An external email (e.g. `sales: info@corp.com`). 3. An include file (e.g. `:include:/etc/mail/team.list`). 4. A program pipe (e.g. `| /usr/bin/procmail`). 5. A file for archiving (e.g. `/var/log/mailarchive`).',
    definition: 'The system-wide email aliasing table for local user accounts.',
    example: '# /etc/aliases\npostmaster: root\nroot:       sysadmin@company.com\nsupport:    alice, bob, charlie\nnotify:     | /usr/local/bin/notify.sh\nteam:       :include:/etc/postfix/lists/team.txt',
    exampleExplanation: 'Demonstrates all common alias destination types in Linux mail systems.',
    examTip: 'Always redirect `postmaster` and `root` to an active system administrator mailbox in `/etc/aliases`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3126,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Aliases',
    command: 'newaliases and postalias',
    question: 'Why must `newaliases` or `postalias` be executed after editing `/etc/aliases`?',
    answer: 'Because Postfix does not read flat text files directly for aliases. `newaliases` (or `postalias /etc/aliases`) compiles the text file into an indexed Berkeley DB format file named `/etc/aliases.db`.',
    definition: 'The alias database compiler utility.',
    example: 'vi /etc/aliases\nnewaliases\n# Or explicitly:\npostalias /etc/aliases',
    exampleExplanation: 'Updates `/etc/aliases.db` so Postfix immediately recognizes new or modified aliases.',
    examTip: 'Classic exam question: If changes to `/etc/aliases` have no effect, the administrator forgot to run `newaliases`!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3127,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Aliases',
    command: 'alias_maps vs alias_database',
    question: 'What is the difference between `alias_maps` and `alias_database` in `/etc/postfix/main.cf`?',
    answer: '`alias_maps` defines the lookup tables queried by the `local` delivery agent during mail routing. `alias_database` defines the tables that are rebuilt when running the `newaliases` command.',
    definition: 'The two complementary alias parameters in Postfix configuration.',
    example: 'alias_maps = hash:/etc/aliases\nalias_database = hash:/etc/aliases',
    exampleExplanation: 'Configures Postfix to query hash database `/etc/aliases.db` and rebuild it when `newaliases` is called.',
    examTip: 'Typically both parameters are set to `hash:/etc/aliases`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3128,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Virtual Aliases',
    command: 'virtual_alias_maps and postmap',
    question: 'What is `virtual_alias_maps` used for, and which command compiles the virtual alias table?',
    answer: 'It redirects addresses for both local and hosted virtual domains to local user mailboxes or external email addresses. The text file (e.g. `/etc/postfix/virtual`) must be compiled using `postmap /etc/postfix/virtual`.',
    definition: 'The Postfix table mapping virtual recipient addresses to actual destination accounts.',
    example: '# /etc/postfix/virtual\ninfo@example.com      alice\ncontact@example.com   bob@external.org\n@example.org          charlie\n\npostmap /etc/postfix/virtual',
    exampleExplanation: 'Compiles `/etc/postfix/virtual` into `/etc/postfix/virtual.db` for fast indexed lookups.',
    examTip: 'Use `postalias` for `/etc/aliases` and `postmap` for virtual/transport/canonical lookup tables!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3129,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Virtual Aliases',
    command: 'Catch-all virtual alias',
    question: 'How do you configure a catch-all email address in Postfix using `virtual_alias_maps`?',
    answer: 'In `/etc/postfix/virtual`, add an entry with `@domain.com destination_address` (omitting the username on the left-hand side).',
    definition: 'A wildcard address mapping that routes any unrecognized recipient at a domain to a designated mailbox.',
    example: '# /etc/postfix/virtual\n@example.com    admin@example.com\n\npostmap /etc/postfix/virtual\npostfix reload',
    exampleExplanation: 'Any email sent to `nonexistent@example.com` will be forwarded to `admin@example.com`.',
    examTip: 'Catch-alls attract immense volumes of spam and dictionary attack emails in production, but are frequently tested on LPIC exams.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3130,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Address Rewriting',
    command: 'canonical_maps',
    question: 'What is the purpose of `sender_canonical_maps` and `recipient_canonical_maps` in Postfix?',
    answer: 'They rewrite sender or recipient envelope addresses and headers from one format to another (e.g. converting internal login names `jsmith` into public standard addresses `john.smith@example.com`).',
    definition: 'Address rewriting tables that alter both the envelope address and the RFC 822 message headers.',
    example: '# /etc/postfix/sender_canonical\njsmith    john.smith@example.com\n\npostmap /etc/postfix/sender_canonical',
    exampleExplanation: 'Rewrites outgoing email from user jsmith so external recipients see the professional address.',
    examTip: 'Unlike virtual aliases (which only alter the envelope recipient), canonical maps rewrite both envelope addresses and message header lines.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3131,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Address Rewriting',
    command: 'smtp_generic_maps',
    question: 'What does `smtp_generic_maps` do when Postfix transmits mail to a remote SMTP server?',
    answer: 'It rewrites local internal domain addresses (e.g. `alice@internal.local`) to valid public internet addresses (e.g. `alice@company.com`) strictly on outbound SMTP connections, without affecting local delivery.',
    definition: 'An address masquerading lookup table for outbound mail sent via the `smtp` delivery agent.',
    example: '# In main.cf:\nsmtp_generic_maps = hash:/etc/postfix/generic\n\n# In /etc/postfix/generic:\n@localdomain.lan    @company.com\nroot@localdomain.lan alerts@company.com',
    exampleExplanation: 'Replaces internal non-routable .local addresses with registered public domains for outgoing mail.',
    examTip: 'Ideal when internal servers have fictitious domain names (like `.lan` or `.local`) and must send mail externally.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3132,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Routing',
    command: 'transport_maps',
    question: 'What is the function of `transport_maps` in Postfix and what is the syntax in `/etc/postfix/transport`?',
    answer: 'It overrides standard MX routing, directing mail for specific domains or addresses through dedicated transports (e.g. `smtp`, `lmtp`, `local`, `uucp`) or custom relay hosts. Syntax: `pattern transport:nexthop`.',
    definition: 'A routing override table dictating the delivery agent and destination gateway per domain.',
    example: '# /etc/postfix/transport\nexample.com        local:\nsub.example.com    smtp:[mailgate.sub.example.com]\npartner.org        smtp:mail.partner.org:2525\n\npostmap /etc/postfix/transport',
    exampleExplanation: 'Delivers example.com locally, routes sub.example.com to a specific IP without MX lookup, and forwards partner.org on port 2525.',
    examTip: 'To route via a specific host without MX lookup, wrap the hostname in square brackets: `smtp:[gateway.corp.com]`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3133,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Security',
    command: 'smtpd_sasl_auth_enable',
    question: 'How do you enable SASL authentication in Postfix to allow roaming users to send mail through the server?',
    answer: 'Set `smtpd_sasl_auth_enable = yes` in `/etc/postfix/main.cf`, configure the SASL type (e.g. `smtpd_sasl_type = dovecot`), and define the communication socket (`smtpd_sasl_path = private/auth`).',
    definition: 'The configuration parameters enabling Simple Authentication and Security Layer for inbound SMTP connections.',
    example: 'smtpd_sasl_auth_enable = yes\nsmtpd_sasl_type = dovecot\nsmtpd_sasl_path = private/auth\nsmtpd_sasl_authenticated_header = yes',
    exampleExplanation: 'Allows Postfix to authenticate remote client credentials against Dovecot\'s user authentication backend.',
    examTip: 'Once SASL is enabled, `permit_sasl_authenticated` in `smtpd_relay_restrictions` allows authenticated clients to relay mail.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3134,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix TLS',
    command: 'smtpd_tls_security_level',
    question: 'What are the main values for `smtpd_tls_security_level` and what does `may` signify?',
    answer: 'Values: `none` (no TLS), `may` (opportunistic TLS; offer STARTTLS to clients but allow unencrypted connections), `encrypt` (mandatory TLS; reject connections that do not negotiate TLS).',
    definition: 'The inbound server-side TLS encryption enforcement parameter in Postfix.',
    example: 'smtpd_tls_security_level = may\nsmtpd_tls_cert_file = /etc/ssl/certs/mail.crt\nsmtpd_tls_key_file = /etc/ssl/private/mail.key',
    exampleExplanation: 'Enables opportunistic TLS on port 25 so modern servers encrypt transit without breaking legacy unencrypted senders.',
    examTip: 'On public port 25 (MTA-to-MTA), `may` is standard. On port 587 (submission), `encrypt` is recommended.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3135,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix TLS',
    command: 'smtp_tls_security_level',
    question: 'What is the purpose of `smtp_tls_security_level` (without the \'d\') in Postfix?',
    answer: 'It controls client-side TLS encryption when Postfix SENDS outbound mail to remote mail servers. Setting it to `may` enables opportunistic TLS for outbound connections.',
    definition: 'The outbound SMTP client TLS policy parameter.',
    example: 'postconf -e "smtp_tls_security_level = may"',
    exampleExplanation: 'When connecting to remote MTAs, Postfix will send STARTTLS if offered by the recipient server.',
    examTip: 'Distinguish `smtpd_*` (d = daemon/server receiving mail) from `smtp_*` (client transmitting outbound mail).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3136,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Access Restrictions',
    command: 'reject_rbl_client (DNSBL)',
    question: 'How do you configure Postfix to block incoming mail from known spamming IP addresses listed in a Real-time Blackhole List (DNSBL)?',
    answer: 'Add `reject_rbl_client <dnsbl_zone>` (e.g. `reject_rbl_client zen.spamhaus.org`) inside `smtpd_recipient_restrictions` or `smtpd_client_restrictions`.',
    definition: 'A DNS-based query check that queries blacklists before accepting incoming mail.',
    example: 'smtpd_recipient_restrictions =\n  permit_mynetworks,\n  permit_sasl_authenticated,\n  reject_unauth_destination,\n  reject_rbl_client zen.spamhaus.org',
    exampleExplanation: 'Rejects SMTP connections originating from IP addresses flagged by Spamhaus.',
    examTip: 'DNSBL lookups should be placed AFTER `permit_mynetworks` and `permit_sasl_authenticated` so local and authenticated users are never checked against blacklists.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3137,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Access Restrictions',
    command: 'HELO restrictions',
    question: 'Which Postfix parameters reject incoming mail if the client provides an invalid or non-FQDN HELO/EHLO hostname?',
    answer: '`reject_invalid_helo_hostname` (rejects malformed syntax) and `reject_non_fqdn_helo_hostname` (rejects hostnames that are not fully qualified domains) inside `smtpd_helo_restrictions`.',
    definition: 'SMTP handshake validation checks filtering automated spam scripts and malformed bots.',
    example: 'smtpd_helo_restrictions = permit_mynetworks, reject_invalid_helo_hostname, reject_non_fqdn_helo_hostname',
    exampleExplanation: 'Blocks bots connecting with greetings like `HELO localhost` or `HELO workstation`.',
    examTip: '`smtpd_helo_required = yes` forces connecting clients to issue a HELO or EHLO greeting before any transaction.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3138,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Limits',
    command: 'message_size_limit and mailbox_size_limit',
    question: 'What do `message_size_limit` and `mailbox_size_limit` specify in `/etc/postfix/main.cf`?',
    answer: '`message_size_limit` sets the maximum total size of an individual email message in bytes (default 10240000 = ~10 MB). `mailbox_size_limit` defines the maximum size of a local mbox file (0 means unlimited).',
    definition: 'Resource allocation limits preventing disk exhaustion from oversized email messages.',
    example: 'postconf -e "message_size_limit = 20480000" # ~20 MB\npostconf -e "mailbox_size_limit = 0"',
    exampleExplanation: 'Allows messages up to 20MB and disables size limits on local mailboxes (often used with Maildir).',
    examTip: 'If `mailbox_size_limit` is smaller than `message_size_limit`, Postfix logs a configuration warning.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3139,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Management',
    command: 'postfix command actions',
    question: 'What are the primary management subcommands for the `postfix` administrative tool?',
    answer: '`postfix start` (starts the master daemon), `postfix stop` (stops daemons cleanly), `postfix reload` (re-reads configuration files without terminating connections), `postfix status` (checks if master is running), and `postfix check` (validates permissions and directory structure).',
    definition: 'The primary control utility for the Postfix mail subsystem.',
    example: 'postfix check\npostfix reload\npostfix status',
    exampleExplanation: 'Validates file permissions, reloads new configurations, and confirms running state.',
    examTip: 'Use `postfix reload` after editing `main.cf` or `master.cf`. Use `postfix check` to catch permission errors.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3140,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Sendmail Compatibility',
    command: 'sendmail command flags in Postfix',
    question: 'Which legacy Sendmail command-line flags are implemented by Postfix\'s `/usr/sbin/sendmail` wrapper?',
    answer: '`-t` (reads recipients from `To:`, `Cc:`, `Bcc:` headers in message body), `-bp` (view mail queue; same as `mailq`), `-q` (process/flush the queue), `-v` (verbose delivery output).',
    definition: 'Compatibility CLI options provided by Postfix to support scripts and web applications written for classic Sendmail.',
    example: 'sendmail -t <<EOF\nTo: admin@example.com\nSubject: Disk Full\n\nDisk usage exceeded 90%.\nEOF',
    exampleExplanation: 'Sends an email by piping text into sendmail, extracting the recipient from the To: header.',
    examTip: 'The `-t` flag is universally used by web frameworks (PHP `mail()`, Python) to send outgoing email.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3141,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Logging',
    command: 'Postfix mail logs',
    question: 'Where does Postfix write its operational logs, and what key message status fields appear in log entries?',
    answer: 'In `/var/log/mail.log` (Debian/Ubuntu) or `/var/log/maillog` (RHEL/CentOS). Key status entries: `status=sent` (successfully delivered), `status=deferred` (temporary failure; queued for retry), `status=bounced` (permanent fatal rejection).',
    definition: 'Syslog logging facility for tracking mail acceptance, queuing, delivery, and rejections.',
    example: 'tail -f /var/log/mail.log | grep "status="\n# postfix/smtp[1234]: 4SZVqL1: to=<user@remote.com>, relay=mail.remote.com[1.2.3.4]:25, delay=1.2, status=sent (250 2.0.0 Ok)',
    exampleExplanation: 'Traces message progress by matching queue ID across connection, cleanup, and smtp log lines.',
    examTip: 'Every message is assigned a unique Queue ID (e.g. `4SZVqL1`) upon acceptance. Grepping for this ID reveals its full history.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3142,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Troubleshooting',
    command: 'debug_peer_list and debug_peer_level',
    question: 'How do you enable verbose debugging for connections to or from a specific remote mail server or IP address?',
    answer: 'In `/etc/postfix/main.cf`, set `debug_peer_list = <hostname_or_ip>` (e.g. `debug_peer_list = mail.partner.com, 198.51.100.25`) and optionally set `debug_peer_level = 2` (or higher).',
    definition: 'Targeted logging parameters that produce detailed operational traces for specific hosts without overwhelming logs with all traffic.',
    example: 'postconf -e "debug_peer_list = 192.168.1.50"\npostfix reload',
    exampleExplanation: 'Causes smtpd and smtp daemons to write verbose debug logs for connections involving 192.168.1.50.',
    examTip: 'Much safer than global debugging because it avoids filling up disk partitions during troubleshooting.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3143,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Security',
    command: 'chroot column in master.cf',
    question: 'What does the `chroot` column in `/etc/postfix/master.cf` control, and what precaution is necessary when enabled?',
    answer: 'It determines whether the service daemon runs inside a chroot jail rooted at `/var/spool/postfix/` (`y`) or not (`n`). When enabled, required libraries, configuration files (like `/etc/resolv.conf`, `/etc/services`), and devices must be mirrored inside `/var/spool/postfix/`.',
    definition: 'Filesystem isolation security jail for Postfix child daemons.',
    example: '# In master.cf:\nsmtp      inet  n       -       y       -       -       smtpd',
    exampleExplanation: 'Runs the public smtpd daemon inside the chroot jail so compromised processes cannot traverse the root filesystem.',
    examTip: 'Debian traditionally enabled chroot (`y`) by default; RHEL traditionally disabled it (`n`) for simplicity.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3144,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Lookup Tables',
    command: 'hash vs lmdb vs btree tables',
    question: 'What are the common table database types used in Postfix and how are they declared?',
    answer: '`hash:` (Berkeley DB; e.g. `hash:/etc/aliases`), `lmdb:` (Lightning Memory-Mapped Database; modern fast default), `btree:` (binary tree for large indexed tables), `cidr:` (classless inter-domain routing IP ranges without compiling), and `pcre:` / `regexp:` (regular expression tables).',
    definition: 'Indexed database backends queried by Postfix for fast key-value lookups.',
    example: 'virtual_alias_maps = hash:/etc/postfix/virtual\nmynetworks = cidr:/etc/postfix/mynetworks.cidr',
    exampleExplanation: 'CIDR tables do not require `postmap` compilation and evaluate network blocks sequentially.',
    examTip: '`cidr:` and `pcre:` files are plain text and do NOT use `postmap`. `hash:` and `lmdb:` files MUST be compiled with `postmap`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3145,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Configuration',
    command: 'anvil daemon in Postfix',
    question: 'What is the role of the `anvil` daemon in Postfix architecture?',
    answer: '`anvil` tracks client connection rates, connection counts, and message rates per client IP address to prevent Denial of Service (DoS) attacks and brute-force password guessing.',
    definition: 'The connection rate-limiting and anti-abuse daemon in Postfix.',
    example: 'smtpd_client_connection_rate_limit = 60\nsmtpd_client_message_rate_limit = 100',
    exampleExplanation: 'Instructs anvil to throttle any IP connecting more than 60 times per minute or sending over 100 messages per minute.',
    examTip: 'Limits configured with `smtpd_client_*_limit` parameters rely on the `anvil` daemon.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3146,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Configuration',
    command: 'cleanup daemon',
    question: 'What does the Postfix `cleanup` daemon do when mail enters the system?',
    answer: 'It processes incoming mail before queue placement: canonicalizes addresses, adds missing `Message-Id:` or `Date:` headers, performs virtual alias transformations, and extracts recipient addresses into the queue file.',
    definition: 'The central message pre-processing and header normalization daemon in Postfix.',
    example: '# In master.cf:\ncleanup   unix  n       -       y       -       0       cleanup',
    exampleExplanation: 'Every message arriving via smtpd or maildrop passes through cleanup before reaching the incoming queue.',
    examTip: '`cleanup` ensures all queued messages follow strict RFC email standards regardless of how sloppy the sending client was.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3147,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Configuration',
    command: 'qmgr daemon',
    question: 'What is the responsibility of the `qmgr` (Queue Manager) daemon in Postfix?',
    answer: 'It is the heart of Postfix delivery: moves messages from `incoming` to `active`, assigns messages to appropriate delivery agents (local, smtp, pipe), handles concurrency limits, and moves failed deliveries to `deferred`.',
    definition: 'The central routing scheduler and queue orchestrator daemon.',
    example: 'postconf -e "default_destination_concurrency_limit = 20"',
    exampleExplanation: 'Directs qmgr to limit simultaneous connections to any single remote domain to 20.',
    examTip: '`qmgr` never contacts the network directly; it delegates network transmissions to the `smtp` delivery agent.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3148,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Sendmail Basics',
    command: 'Sendmail configuration (sendmail.cf & sendmail.mc)',
    question: 'In traditional Sendmail, what is the relationship between `sendmail.mc` and `sendmail.cf`?',
    answer: '`sendmail.cf` is the complex, low-level configuration file interpreted by Sendmail. Because its syntax is notoriously cryptic, administrators edit macro file `sendmail.mc` and compile it into `sendmail.cf` using the `m4` macro processor.',
    definition: 'The classic macro compilation workflow for Sendmail MTAs.',
    example: 'm4 /etc/mail/sendmail.mc > /etc/mail/sendmail.cf\n# Or via make:\nmake -C /etc/mail',
    exampleExplanation: 'Compiles high-level m4 macros into the low-level ruleset file sendmail.cf.',
    examTip: 'Never edit `sendmail.cf` directly. Always edit `sendmail.mc` and process with `m4`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3149,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Sendmail Basics',
    command: 'makemap command',
    question: 'Which Sendmail utility compiles access, mailertable, and virtusertable database files?',
    answer: '`makemap`. For example: `makemap hash /etc/mail/access.db < /etc/mail/access`.',
    definition: 'The Sendmail equivalent of Postfix\'s `postmap` tool for compiling lookup databases.',
    example: 'makemap hash /etc/mail/virtusertable < /etc/mail/virtusertable',
    exampleExplanation: 'Creates an indexed hash database from the plain text virtual user table in Sendmail.',
    examTip: '`postmap` is for Postfix; `makemap` is for Sendmail.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3150,
    deck: 'Topic 211: E-Mail Services',
    objectiveId: '211.1',
    topicNumber: 211,
    category: 'Postfix Architecture',
    command: 'bounce daemon',
    question: 'What is the role of the `bounce` daemon in Postfix?',
    answer: 'It generates non-delivery reports (NDRs) or Delivery Status Notifications (DSNs) and sends bounce messages back to the sender when mail delivery fails permanently.',
    definition: 'The bounce report generator service in Postfix.',
    example: '# /var/log/mail.log:\n# postfix/bounce[4567]: 4SZVqL: sender non-delivery notification: 5BTWrM',
    exampleExplanation: 'Generates a new message containing the original headers and failure explanation to inform the sender of delivery failure.',
    examTip: '`bounce_queue_lifetime` parameter in `main.cf` defines how long Postfix attempts retries before the bounce daemon generates a final failure report.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
];
