import { Flashcard } from '../../types';

/**
 * Topic 352: Container Virtualization (LPIC-3 Virtualization and Containerization Exam 305-300) - Part 2 (Cards 4451-4500)
 * 
 * Objectives Covered:
 * - 352.2: LXC (Linux Containers) - Part 2 (Cards 4451-4452) [2 Cards] (Weight 6)
 * - 352.3: Docker (Cards 4453-4488) [36 Cards] (Weight 9)
 * - 352.4: Container Orchestration Platforms (Cards 4489-4500) [12 Cards] (Weight 3)
 */
export const topic352Part2: Flashcard[] = [
  // -------------------------------------------------------------
  // Objective 352.2: LXC (Linux Containers) - Part 2 (Cards 4451-4452)
  // -------------------------------------------------------------
  {
    id: 4451,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.2',
    command: 'lxc.mount.entry Directive',
    definition: 'Directs LXC to bind-mount a directory, filesystem, or device from the host into the container rootfs, using standard fstab mount options.',
    example: 'lxc.mount.entry = /var/log/shared-logs var/log/shared-logs none bind,ro,create=dir 0 0',
    exampleExplanation: 'The `create=dir` option automatically creates the target mount directory inside the container if it does not already exist; `ro` mounts it read-only.',
    question: 'How do you bind-mount a host directory into an LXC container in a read-only state using its configuration file?',
    answer: 'Add `lxc.mount.entry = /host/path target/rel/path none bind,ro,create=dir 0 0` to the container config.',
    category: 'LXC Configuration',
    examTip: 'The target path in `lxc.mount.entry` is relative to the container\'s rootfs (omit leading slash for the target).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4452,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.2',
    command: 'lxc.cap.drop Directive',
    definition: 'Specifies a list of POSIX capabilities to permanently drop from the container\'s capability bounding set when it boots.',
    example: 'lxc.cap.drop = sys_admin net_raw mknod',
    exampleExplanation: 'Prevents the container from creating raw sockets (ping flooding), making device nodes, or executing administrative mounting calls.',
    question: 'Which LXC configuration directive restricts the privileges of a privileged container by stripping specific kernel capabilities?',
    answer: 'lxc.cap.drop',
    category: 'LXC Security',
    examTip: 'To drop all capabilities except a few, some administrators use `lxc.cap.keep` instead.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },

  // -------------------------------------------------------------
  // Objective 352.3: Docker (Cards 4453-4488)
  // -------------------------------------------------------------
  {
    id: 4453,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker Architecture Overview',
    definition: 'Client-server architecture comprising the Docker CLI (`docker`), the background Docker Daemon (`dockerd`), the high-level container runtime (`containerd`), and the low-level OCI reference runtime (`runc`) which interacts directly with kernel namespaces and cgroups.',
    example: 'Client (docker CLI) -> Daemon (dockerd) -> containerd -> runc -> Container processes',
    exampleExplanation: 'The CLI communicates with dockerd over a REST API via Unix socket or TCP; dockerd delegates lifecycle execution to containerd.',
    question: 'What is the low-level OCI-compliant runtime that Docker utilizes to directly invoke Linux namespaces and cgroups to execute containers?',
    answer: 'runc',
    category: 'Docker Architecture',
    examTip: 'Docker donated its runtime specifications and `runc` to the Open Container Initiative (OCI) for industry standardization.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4454,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: '/var/run/docker.sock',
    definition: 'The default local Unix domain socket used by the Docker CLI client to communicate with the background Docker daemon (dockerd).',
    example: 'sudo chmod 660 /var/run/docker.sock && sudo chown root:docker /var/run/docker.sock',
    exampleExplanation: 'Members of the "docker" group can interact with this socket without sudo, which is equivalent to full root access on the host system.',
    question: 'Why is granting a user membership in the host "docker" group considered equivalent to granting full passwordless root privileges?',
    answer: 'Access to `/var/run/docker.sock` allows running a container with `-v /:/host --privileged`, granting unhindered root control over the entire host filesystem.',
    category: 'Docker Security',
    examTip: 'Never expose `/var/run/docker.sock` into an untrusted container unless building CI/CD runners with strict access controls.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4455,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker run Lifecycle Flags',
    definition: 'Instantiates and starts a new container from an image. Essential flags: `-d` (detached background), `--name` (custom name), `-p` (port mapping), `-v` (volume mount), `-e` (env variable), `--restart` (restart policy), and `--rm` (auto-delete on exit).',
    example: 'docker run -d --name web -p 8080:80 --restart unless-stopped nginx:alpine',
    exampleExplanation: 'Launches an Nginx container named "web" listening on host port 8080 that persists across host reboots unless stopped manually.',
    question: 'Which `docker run` flag causes a container to be automatically deleted from disk immediately after its primary process exits?',
    answer: '--rm',
    category: 'Docker Operations',
    examTip: '`--restart unless-stopped` restarts the container if it crashes or the daemon restarts, but honors explicit `docker stop` commands.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4456,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker ps vs docker ps -a',
    definition: '`docker ps` lists only currently running containers; `docker ps -a` (all) lists all containers regardless of state, including exited, created, and dead containers with exit codes.',
    example: 'docker ps -a --filter "status=exited"',
    exampleExplanation: 'Displays all terminated containers along with their exit codes (e.g., Exited (137) indicates an OOM kill or SIGKILL).',
    question: 'How do you list all containers on the host system, including those that have stopped or crashed?',
    answer: 'docker ps -a (or docker container ls -a)',
    category: 'Docker Operations',
    examTip: 'Exit code 137 typically indicates killed by SIGKILL (128 + 9), frequently caused by the Linux kernel Out-Of-Memory (OOM) killer.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4457,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker stop vs docker kill',
    definition: '`docker stop` gracefully halts a container by sending SIGTERM to PID 1, waiting a timeout period (default 10 seconds), and sending SIGKILL if it does not exit; `docker kill` sends an immediate SIGKILL (or custom signal) without grace period.',
    example: 'docker stop -t 30 db-server vs docker kill -s SIGHUP nginx-proxy',
    exampleExplanation: 'Databases should always be stopped with `docker stop` to allow flushing transaction logs and closing database files safely.',
    question: 'What signal does `docker stop` initially send to the container\'s PID 1 process, and what signal follows if it fails to exit within the timeout?',
    answer: 'Initial signal: SIGTERM (15); Follow-up signal after timeout: SIGKILL (9).',
    category: 'Docker Operations',
    examTip: 'You can reload Nginx configurations without restarting the container using `docker kill -s HUP <nginx_container>`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4458,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker exec',
    definition: 'Runs a new command or starts an interactive shell inside the namespaces of an already running container.',
    example: 'docker exec -it web-srv /bin/sh',
    exampleExplanation: 'The `-i` (interactive) flag keeps STDIN open; `-t` allocates a pseudo-TTY, enabling a responsive interactive terminal shell.',
    question: 'Which command allows an administrator to run an interactive bash shell inside an active container named "web-srv"?',
    answer: 'docker exec -it web-srv /bin/bash',
    category: 'Docker Operations',
    examTip: 'To run a command as root inside a container whose primary process runs as an unprivileged user, use `docker exec -u 0 -it <container> sh`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4459,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker logs',
    definition: 'Fetches the stdout and stderr log streams generated by the container\'s entrypoint and application processes.',
    example: 'docker logs -f --tail 50 --timestamps web-srv',
    exampleExplanation: '`-f` follows (streams) log output live; `--tail 50` outputs only the last 50 lines; `--timestamps` prepends UTC ISO timestamps to every log line.',
    question: 'Which `docker logs` options stream log output continuously while displaying the last 100 historical lines with timestamps?',
    answer: 'docker logs -f --tail 100 -t (or --timestamps)',
    category: 'Docker Operations',
    examTip: '`docker logs` only works when the container uses the `json-file` or `journald` logging drivers.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4460,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker inspect',
    definition: 'Returns detailed low-level JSON configuration and runtime state information for containers, images, volumes, and networks, supporting Go template formatting via `--format`.',
    example: 'docker inspect -f "{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}" web-srv',
    exampleExplanation: 'Extracts and prints exclusively the IPv4 address assigned to the container on its Docker bridge network.',
    question: 'How can you extract only the IP address of container "web-srv" using `docker inspect` and a Go template?',
    answer: 'docker inspect -f "{{ .NetworkSettings.IPAddress }}" web-srv',
    category: 'Docker Operations',
    examTip: 'Use `docker inspect -f "{{ .State.Status }}" <name>` in health-check shell scripts to monitor state (running, paused, exited).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4461,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker stats',
    definition: 'Displays a live streaming resource usage statistics dashboard for all running containers, including CPU %, Memory Usage / Limit, Memory %, Net I/O, Block I/O, and PIDs.',
    example: 'docker stats --no-stream',
    exampleExplanation: 'The `--no-stream` flag prints a single instantaneous snapshot of current resource metrics and exits immediately, ideal for monitoring scripts.',
    question: 'Which Docker CLI tool provides a top-like live resource monitoring dashboard for all running containers?',
    answer: 'docker stats',
    category: 'Docker Monitoring',
    examTip: 'To format output for automation, use `docker stats --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}"`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4462,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker rm vs docker rmi',
    definition: '`docker rm` deletes stopped container instances and their writable layers; `docker rmi` (or `docker image rm`) deletes stored local container images from the host cache.',
    example: 'docker rm -f web-srv (force kills and removes container) vs docker rmi nginx:alpine',
    exampleExplanation: 'An image cannot be deleted with `docker rmi` if any existing container (even in stopped state) was created from that image.',
    question: 'Why will `docker rmi myapp:1.0` fail if there is a stopped container on the system that was created from that image?',
    answer: 'Docker preserves parent images to maintain container writable layers; you must delete the stopped container (`docker rm`) first.',
    category: 'Docker Operations',
    examTip: '`docker rm -v <container>` also deletes any anonymous volumes attached to that container.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4463,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker build & Build Context',
    definition: 'Constructs a new Docker container image from a specified Dockerfile and a local build context directory sent as a tar archive to the Docker daemon.',
    example: 'docker build -t company/web:1.0 -f Dockerfile .',
    exampleExplanation: 'The trailing dot `.` represents the build context path sent across the Docker socket to the daemon.',
    question: 'What is the "build context" in a `docker build` invocation, and what file prevents unnecessary files from being included?',
    answer: 'The directory tree sent to the Docker daemon containing files accessible to COPY/ADD; `.dockerignore` excludes unnecessary files.',
    category: 'Docker Images',
    examTip: 'Avoid running `docker build .` in the root `/` or home `~` directory, as it will package the entire disk into the build context!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4464,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Dockerfile FROM & scratch',
    definition: '`FROM` sets the base/parent image for subsequent build instructions. `FROM scratch` is a special reserved no-op image that indicates the build starts from an empty filesystem with zero OS files.',
    example: 'FROM scratch\nCOPY hello-binary /hello\nENTRYPOINT ["/hello"]',
    exampleExplanation: 'Allows building ultra-minimal container images (often < 10 MB) containing only a single statically compiled binary (e.g., in Go, Rust, or C).',
    question: 'What base image keyword in a Dockerfile creates an empty image containing zero pre-installed operating system files or libraries?',
    answer: 'scratch (e.g. `FROM scratch`)',
    category: 'Dockerfile Syntax',
    examTip: '`FROM` must always be the first non-comment instruction in a Dockerfile, with the exception of `ARG` instructions.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4465,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Dockerfile RUN Instruction',
    definition: 'Executes commands during the image build process and commits the resulting state as a new read-only image layer. Can be written in shell form (`RUN <command>`) or exec form (`RUN ["executable", "param1"]`).',
    example: 'RUN apt-get update && apt-get install -y --no-install-recommends nginx \\\n    && rm -rf /var/lib/apt/lists/*',
    exampleExplanation: 'Chaining commands with `&&` and cleaning apt lists in a single RUN instruction ensures temporary package caches are not permanently baked into the layer.',
    question: 'Why is it a Docker best practice to combine `apt-get update`, `apt-get install`, and `rm -rf /var/lib/apt/lists/*` into a single `RUN` instruction?',
    answer: 'Because each RUN instruction creates a permanent image layer; chaining them ensures package cache files are removed before the layer commits.',
    category: 'Dockerfile Syntax',
    examTip: 'Shell form runs inside `/bin/sh -c`; exec form executes the binary directly without a shell wrapper.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4466,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Dockerfile CMD vs ENTRYPOINT',
    definition: '`ENTRYPOINT` defines the executable binary that always runs when the container starts; `CMD` provides default arguments passed to the `ENTRYPOINT` which can be easily overridden by CLI arguments in `docker run`.',
    example: 'ENTRYPOINT ["nginx"]\nCMD ["-g", "daemon off;"]',
    exampleExplanation: 'Running `docker run my-nginx -t` overrides `CMD` with `-t`, executing `nginx -t` to test the configuration syntax.',
    question: 'How do `ENTRYPOINT` and `CMD` interact when both are defined in a Dockerfile using the JSON exec array format?',
    answer: '`ENTRYPOINT` specifies the fixed binary executable, and `CMD` supplies default arguments that can be overridden at runtime.',
    category: 'Dockerfile Syntax',
    examTip: 'To override a fixed ENTRYPOINT at runtime, use `docker run --entrypoint /bin/sh <image>`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4467,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Exec Form vs Shell Form',
    definition: 'Exec form syntax uses JSON array format `["executable", "param1", "param2"]`; Shell form syntax uses plain string format `executable param1 param2`.',
    example: 'Exec: ENTRYPOINT ["/app/server"] vs Shell: ENTRYPOINT /app/server',
    exampleExplanation: 'In shell form, the command runs as a child of `/bin/sh -c`, meaning signals like SIGTERM are sent to the shell rather than your app, preventing graceful shutdown.',
    question: 'Why is the JSON array exec form (`CMD ["node", "app.js"]`) strongly preferred over the shell form (`CMD node app.js`)?',
    answer: 'Exec form runs the binary directly as PID 1, allowing it to receive OS signals (SIGTERM) for graceful shutdown, whereas shell form spawns a shell parent.',
    category: 'Dockerfile Syntax',
    examTip: 'If your process doesn\'t stop cleanly within 10 seconds during `docker stop`, check whether shell form was mistakenly used.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4468,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Dockerfile COPY vs ADD',
    definition: '`COPY` transfers files and directories from the local build context into the container filesystem; `ADD` has additional capabilities: it can download files from remote URLs and automatically unpack recognized local tar archives (gzip, bzip2, xz) into the destination.',
    example: 'COPY config.json /etc/app/ vs ADD rootfs.tar.gz /',
    exampleExplanation: '`COPY` is strongly preferred for standard files because it is transparent and predictable; `ADD` is reserved when automatic tar extraction into rootfs is required.',
    question: 'What unique feature does `ADD` possess that `COPY` does not when transferring a local tarball into an image?',
    answer: '`ADD` automatically unpacks and extracts local compressed tar archives into the target destination directory.',
    category: 'Dockerfile Syntax',
    examTip: 'Best practice: Always use `COPY` unless you explicitly require automatic local tarball decompression.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4469,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Dockerfile ENV vs ARG',
    definition: '`ARG` defines variables available ONLY during the image build process (can be set via `docker build --build-arg VAR=val`); `ENV` defines environment variables that persist both during build time and into the running container environment.',
    example: 'ARG APP_VERSION=1.0\nENV PORT=8080',
    exampleExplanation: '`PORT=8080` remains accessible to running application processes via `process.env.PORT`, whereas `APP_VERSION` disappears after the build finishes.',
    question: 'Which Dockerfile instruction sets an environment variable that persists and remains accessible to processes inside running container instances?',
    answer: 'ENV',
    category: 'Dockerfile Syntax',
    examTip: 'Never store API keys or passwords in `ENV` or `ARG` as they are permanently viewable via `docker history` or `docker inspect`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4470,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Dockerfile WORKDIR & USER',
    definition: '`WORKDIR` sets the working directory for all subsequent RUN, CMD, ENTRYPOINT, COPY, and ADD instructions; `USER` switches the active UID/GID from root to a non-privileged user to enforce principle of least privilege.',
    example: 'WORKDIR /usr/src/app\nUSER node',
    exampleExplanation: 'Prevents container processes from running as root UID 0, mitigating container breakout risks.',
    question: 'Which Dockerfile instructions set the active working directory and drop root privileges to an unprivileged application user?',
    answer: '`WORKDIR` sets the working directory; `USER` sets the user UID/GID.',
    category: 'Dockerfile Syntax',
    examTip: 'If `WORKDIR` directory does not already exist, Docker automatically creates it with root ownership.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4471,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Dockerfile EXPOSE & VOLUME',
    definition: '`EXPOSE` documents the network ports the container service intends to listen on; `VOLUME` creates a managed mount point with the specified path and marks it as holding externally mounted storage.',
    example: 'EXPOSE 80/tcp 443/tcp\nVOLUME ["/var/lib/mysql"]',
    exampleExplanation: '`EXPOSE` is informational metadata that does not actually publish ports to the host unless `-P` is supplied at runtime.',
    question: 'Does the Dockerfile instruction `EXPOSE 80` automatically publish port 80 to the host network interface during `docker run`?',
    answer: 'No. `EXPOSE` is purely documentation; ports must be explicitly published using `-p` or `-P` at runtime.',
    category: 'Dockerfile Syntax',
    examTip: 'Any data written to a declared `VOLUME` path during subsequent `RUN` instructions in the Dockerfile will be discarded in final layers.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4472,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Dockerfile HEALTHCHECK',
    definition: 'Instructs Docker how to test if a container is functioning correctly beyond just checking if PID 1 is alive. Options include `--interval`, `--timeout`, `--start-period`, and `--retries`.',
    example: 'HEALTHCHECK --interval=30s --timeout=3s --retries=3 \\\n  CMD curl -f http://localhost:8080/health || exit 1',
    exampleExplanation: 'If the health check fails 3 consecutive times, Docker flags the container status as `unhealthy` in `docker ps`.',
    question: 'What status does Docker report for a container whose configured `HEALTHCHECK` command repeatedly exits with a non-zero return code?',
    answer: 'unhealthy (e.g. "Up 10 minutes (unhealthy)")',
    category: 'Dockerfile Syntax',
    examTip: 'To disable any inherited healthcheck from a parent image, write `HEALTHCHECK NONE`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4473,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: '.dockerignore File',
    definition: 'Text file located in the root of the build context that lists file and directory patterns to exclude from being packaged into the build context tarball sent to the Docker daemon.',
    example: '.git\nnode_modules\n*.log\n.env',
    exampleExplanation: 'Drastically accelerates build initiation times, avoids leaking local secrets (.env), and prevents cache invalidation caused by local log changes.',
    question: 'Which file prevents sensitive files like `.env` and large directories like `node_modules` from being sent in the Docker build context?',
    answer: '.dockerignore',
    category: 'Docker Images',
    examTip: '`.dockerignore` uses standard globbing syntax similar to `.gitignore`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4474,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Multi-Stage Docker Builds',
    definition: 'Build pattern using multiple `FROM` instructions in a single Dockerfile where artifacts compiled in heavy build stages are copied into a final minimal production runtime stage using `COPY --from=<stage>`.',
    example: 'FROM golang:1.22-alpine AS builder\nWORKDIR /app\nCOPY . .\nRUN go build -o server\n\nFROM scratch\nCOPY --from=builder /app/server /server\nENTRYPOINT ["/server"]',
    exampleExplanation: 'The final production image contains only the compiled binary and zero Go compilers or build dependencies, reducing image size from 800 MB to 10 MB.',
    question: 'How do multi-stage Docker builds reduce the final production container image size and improve security?',
    answer: 'They allow building code in a stage with heavy compilers/tools, then copying only the compiled artifacts into a clean, minimal runtime stage.',
    category: 'Docker Images',
    examTip: 'You can target a specific intermediate stage during testing using `docker build --target builder -t myapp:dev .`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4475,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker Layer Caching Optimization',
    definition: 'Docker caches the result of each instruction layer; if a layer\'s instruction and files are unchanged, Docker reuses the cached layer, invalidating the cache for all subsequent instructions once a change occurs.',
    example: 'COPY package.json package-lock.json ./\nRUN npm ci\nCOPY . .',
    exampleExplanation: 'Copying dependency manifests before source code ensures expensive `npm ci` dependencies are cached unless dependencies actually change.',
    question: 'Why should dependency files (like package.json or requirements.txt) be copied and installed before copying the rest of application source code?',
    answer: 'To maximize Docker layer cache reuse, so dependencies are only re-downloaded when manifests change rather than on every code edit.',
    category: 'Docker Images',
    examTip: 'To force rebuilding an image without using any cached layers, pass `--no-cache` to `docker build`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4476,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker Named Volumes',
    definition: 'Persistent storage managed entirely by Docker, stored on the host filesystem under `/var/lib/docker/volumes/`, decoupled from the container lifecycle.',
    example: 'docker volume create pg-data && docker run -v pg-data:/var/lib/postgresql/data postgres',
    exampleExplanation: 'Deleting the container does not delete the named volume, preserving critical state and database records indefinitely.',
    question: 'Where are Docker named volumes stored by default on the host Linux filesystem?',
    answer: '/var/lib/docker/volumes/<volume_name>/_data',
    category: 'Docker Storage',
    examTip: 'Use `docker volume ls` and `docker volume prune` to find and clean up orphaned, unattached volumes.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4477,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker Bind Mounts',
    definition: 'Mounts an explicit file or directory from anywhere on the host filesystem directly into the container filesystem tree.',
    example: 'docker run -d -v /opt/app/config:/etc/app/config:ro my-app',
    exampleExplanation: 'The `:ro` modifier mounts the directory read-only, preventing the container from modifying host files.',
    question: 'What is the syntax to mount the host directory `/var/www` into `/usr/share/nginx/html` inside a container in read-only mode using `-v`?',
    answer: '-v /var/www:/usr/share/nginx/html:ro',
    category: 'Docker Storage',
    examTip: 'Unlike named volumes, if the host path does not exist when using `--mount type=bind`, Docker throws an error rather than auto-creating a directory.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4478,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker tmpfs Mounts',
    definition: 'Mounts volatile storage residing entirely within host system RAM, never writing data to the host disk or container writable layer.',
    example: 'docker run -d --tmpfs /tmp:rw,noexec,nosuid,size=64m nginx',
    exampleExplanation: 'Ideal for storing sensitive security tokens, cryptographic keys, or high-throughput temporary caches that should vanish when the container stops.',
    question: 'Which Docker mount type stores data exclusively in host memory (RAM) and never writes blocks to the host physical disk?',
    answer: 'tmpfs mount (`--tmpfs` or `--mount type=tmpfs`)',
    category: 'Docker Storage',
    examTip: 'tmpfs mounts cannot be shared between multiple containers; they are strictly local to a single container.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4479,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker Storage Driver: overlay2',
    definition: 'The default Union Filesystem storage driver for Docker on modern Linux, combining multiple read-only image layers (`lowerdir`), a writable container layer (`upperdir`), an internal tracking directory (`workdir`), and presenting them as a single unified rootfs (`merged`).',
    example: '/var/lib/docker/overlay2/<layer-id>/',
    exampleExplanation: 'Modifying a file from a read-only lower layer performs a copy-on-write (CoW) operation, copying the file into the upperdir layer before saving changes.',
    question: 'What are the four key directories utilized by the `overlay2` Union Filesystem storage driver to construct a container filesystem?',
    answer: 'lowerdir (read-only layers), upperdir (read-write container layer), merged (unified view), and workdir (internal kernel staging).',
    category: 'Docker Storage',
    examTip: '`overlay2` is supported on top of ext4 and xfs (with ftype=1 enabled).',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4480,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker Default bridge Network',
    definition: 'The default virtual network created by Docker, represented on the host by interface `docker0` (typically on subnet 172.17.0.0/16).',
    example: 'docker run -d nginx (automatically attaches to default bridge network)',
    exampleExplanation: 'CRITICAL LIMITATION: The default `bridge` network does NOT support automatic container name resolution; containers can only communicate with each other via IP address or legacy `--link`.',
    question: 'What major DNS limitation exists on Docker\'s default `bridge` network that does not exist on custom user-defined bridge networks?',
    answer: 'Containers on the default bridge network cannot resolve each other by container name using Docker\'s embedded DNS server.',
    category: 'Docker Networking',
    examTip: 'Always create a custom user-defined bridge network in production environments to enable automatic service name resolution.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4481,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker User-Defined Bridge Networks',
    definition: 'Custom bridge networks created with `docker network create` that feature an embedded DNS server at 127.0.0.11 for automatic service discovery by container name or network alias.',
    example: 'docker network create my-net && docker run --net my-net --name db mysql',
    exampleExplanation: 'Another container on `my-net` can immediately connect to the database simply using hostname `db`.',
    question: 'Which command creates a custom user-defined bridge network with automatic internal DNS name resolution?',
    answer: 'docker network create --driver bridge <network_name>',
    category: 'Docker Networking',
    examTip: 'Docker\'s internal embedded DNS server listens on loopback IP `127.0.0.11` inside container namespaces.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4482,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker host & none Network Drivers',
    definition: '`--network host` disables network namespace isolation, binding container sockets directly to the host\'s physical network interfaces with zero NAT overhead; `--network none` provides only a loopback interface, completely isolating the container from all networking.',
    example: 'docker run --net host iperf3 -s vs docker run --net none isolated-worker',
    exampleExplanation: 'Host networking offers maximum I/O throughput for performance-critical services, but sacrifices port isolation.',
    question: 'Which Docker network driver strips away network namespace isolation entirely, placing container sockets directly on the host network interfaces?',
    answer: 'The `host` network driver (`--network host`)',
    category: 'Docker Networking',
    examTip: 'When using `--network host`, port mappings (`-p`) are completely ignored because the container binds directly to host ports.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4483,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker Port Publishing (-p vs -P)',
    definition: '`-p <host_port>:<container_port>` explicitly maps a specified host port to a container port; `-P` (or `--publish-all`) maps every port declared with `EXPOSE` in the Dockerfile to random ephemeral ports on the host (typically 32768-60999).',
    example: 'docker run -p 127.0.0.1:8080:80 nginx vs docker run -P nginx',
    exampleExplanation: 'Binding to `127.0.0.1:8080:80` ensures the container is only reachable from localhost and not exposed to external public interfaces.',
    question: 'How do you publish container port 80 exclusively to localhost interface 127.0.0.1 on host port 8080?',
    answer: '-p 127.0.0.1:8080:80',
    category: 'Docker Networking',
    examTip: 'Docker modifies host `iptables` NAT tables directly, which can inadvertently bypass UFW firewall rules on Ubuntu unless configured.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4484,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: '/etc/docker/daemon.json',
    definition: 'Main configuration file for the Docker daemon (dockerd), allowing persistent configuration of logging drivers, storage drivers, default address pools, metrics, live-restore, and insecure registries.',
    example: '{\n  "storage-driver": "overlay2",\n  "log-driver": "json-file",\n  "log-opts": { "max-size": "10m", "max-file": "3" },\n  "live-restore": true\n}',
    exampleExplanation: 'Enabling `"live-restore": true` keeps containers running even when the Docker daemon is being upgraded or restarted.',
    question: 'Which parameter in `/etc/docker/daemon.json` ensures running containers remain online when the dockerd daemon restarts or updates?',
    answer: '"live-restore": true',
    category: 'Docker Configuration',
    examTip: 'After editing `/etc/docker/daemon.json`, validate JSON syntax before reloading the daemon via `systemctl reload docker`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4485,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker Logging Drivers & Log Rotation',
    definition: 'Mechanisms governing how container stdout/stderr output is captured and stored. Supported drivers include `json-file` (default), `journald`, `syslog`, `fluentd`, `gelf`, and `none`.',
    example: 'docker run --log-driver json-file --log-opt max-size=10m --log-opt max-file=5 nginx',
    exampleExplanation: 'Prevents rogue container log output from filling up the host root disk by capping log files at 10 MB with 5 rotation files.',
    question: 'How do you prevent container log files from exhausting host disk space when using the default `json-file` logging driver?',
    answer: 'Configure log rotation using `--log-opt max-size=<size>` and `--log-opt max-file=<count>`.',
    category: 'Docker Operations',
    examTip: 'If containers use the `syslog` or `journald` drivers, `docker logs` might not display output if Docker cannot read the remote log stream.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4486,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'Docker Registry Commands: tag, push, pull',
    definition: 'Commands to manage image distribution across registries. `docker tag` assigns a registry repository name; `docker login` authenticates; `docker push` uploads; `docker pull` downloads.',
    example: 'docker tag webapp:latest registry.example.com:5000/team/webapp:v2.0 && docker push registry.example.com:5000/team/webapp:v2.0',
    exampleExplanation: 'Prefixing the image tag with the registry domain name instructs Docker to upload to the private registry instead of Docker Hub.',
    question: 'How does the Docker CLI know which registry to push an image to when executing `docker push`?',
    answer: 'It parses the first component of the image repository tag (e.g. `registry.example.com:5000/image:tag`).',
    category: 'Docker Registries',
    examTip: 'If pushing to an HTTP registry without TLS certificates, configure `"insecure-registries": ["registry.example.com:5000"]` in `/etc/docker/daemon.json`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4487,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker commit vs docker export/import',
    definition: '`docker commit` creates a new image layer from a container\'s current changes, retaining Dockerfile metadata (ENV, ENTRYPOINT); `docker export` serializes a container\'s filesystem into a flat tarball, discarding history and metadata; `docker import` creates an image from a flat tarball.',
    example: 'docker commit web-srv my-snapshot:v1 vs docker export web-srv > web.tar',
    exampleExplanation: 'Importing an exported tarball flattens all layers into a single layer, stripping sensitive layer history.',
    question: 'What is the key difference between creating an image via `docker commit` versus exporting/importing with `docker export` and `docker import`?',
    answer: '`docker commit` preserves image layers and metadata; `docker export/import` flattens the filesystem into a single layer and discards layer history.',
    category: 'Docker Operations',
    examTip: 'When using `docker import`, you must re-specify `ENTRYPOINT` or `CMD` because all previous container metadata is stripped.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4488,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.3',
    command: 'docker system prune',
    definition: 'Comprehensive cleanup utility that reclaims host disk space by removing stopped containers, unused networks, dangling images, and build caches.',
    example: 'docker system prune -a --volumes',
    exampleExplanation: 'The `-a` flag deletes all unreferenced images (not just dangling ones); `--volumes` deletes all unattached anonymous and named volumes.',
    question: 'Which command reclaims maximum host disk space by deleting all stopped containers, unused networks, unreferenced images, and unattached volumes?',
    answer: 'docker system prune -a --volumes',
    category: 'Docker Operations',
    examTip: 'Be cautious with `--volumes` in production as it permanently destroys unattached database volumes.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },

  // -------------------------------------------------------------
  // Objective 352.4: Container Orchestration Platforms (Cards 4489-4500)
  // -------------------------------------------------------------
  {
    id: 4489,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'Container Orchestration Core Concepts',
    definition: 'Automated management of container lifecycles across clusters of host nodes, providing service discovery, load balancing, health monitoring, automatic horizontal scaling, self-healing, rolling zero-downtime updates, and secret configuration management.',
    example: 'Docker Compose (single-host) vs Docker Swarm & Kubernetes (multi-node clusters)',
    exampleExplanation: 'Orchestrators continuously compare current cluster state to declared desired state and reconcile discrepancies automatically.',
    question: 'What is the primary role of a container orchestrator when a containerized service replica crashes on a cluster node?',
    answer: 'Self-healing: detecting the failure via health probes and automatically instantiating a new replica to match the desired state.',
    category: 'Orchestration Concepts',
    examTip: 'Compose is designed for local developer stacks; Swarm and Kubernetes are designed for distributed multi-host production environments.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4490,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'Docker Compose & compose.yaml',
    definition: 'Tool and specification for defining and running multi-container Docker applications via declarative YAML files (`compose.yaml` or `docker-compose.yml`), organizing containers into services, networks, and volumes.',
    example: 'services:\n  web:\n    image: nginx:alpine\n    ports: ["80:80"]\n  db:\n    image: postgres:15\n    volumes: ["db-data:/var/lib/postgresql/data"]\nvolumes:\n  db-data:',
    exampleExplanation: 'Compose automatically establishes a dedicated user-defined bridge network, allowing `web` to resolve `db` by its service name.',
    question: 'What is the standardized filename preferred by modern Docker Compose specifications for multi-container configurations?',
    answer: 'compose.yaml (or legacy docker-compose.yml)',
    category: 'Docker Compose',
    examTip: 'Services defined in the same Compose file automatically share a custom bridge network and can communicate using service names.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4491,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'docker compose CLI Commands',
    definition: 'Commands for managing the application stack lifecycle defined in Compose files: `docker compose up -d` (create and start), `docker compose down -v` (stop and delete containers/volumes), `docker compose ps`, `docker compose logs -f`, and `docker compose exec`.',
    example: 'docker compose up -d --build --scale web=3',
    exampleExplanation: 'Rebuilds images if Dockerfiles changed, boots services in background, and scales the `web` service to 3 container replicas.',
    question: 'Which Docker Compose command stops running containers, removes associated networks, and deletes named volumes created by the stack?',
    answer: 'docker compose down -v',
    category: 'Docker Compose',
    examTip: 'Note that modern syntax uses `docker compose` (Docker CLI plugin) instead of legacy hyphenated `docker-compose`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4492,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'Docker Compose depends_on with Healthchecks',
    definition: 'Controls startup and shutdown dependency ordering between services. When configured with `condition: service_healthy`, Compose waits for the dependency container\'s health check to succeed before starting dependent containers.',
    example: 'web:\n  image: my-web\n  depends_on:\n    db:\n      condition: service_healthy',
    exampleExplanation: 'Prevents the web service from crashing on startup due to connecting to PostgreSQL before the database is ready to accept connections.',
    question: 'How do you configure Docker Compose to wait until a database service is fully initialized and healthy before starting a dependent web application?',
    answer: 'Use `depends_on` with `condition: service_healthy` tied to the database\'s `healthcheck` definition.',
    category: 'Docker Compose',
    examTip: 'Plain `depends_on` without conditions only waits for the container to start, not for the service inside to be ready.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4493,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'Docker Swarm Architecture',
    definition: 'Docker\'s native clustering and orchestration engine built into the Docker daemon. Nodes are categorized as Managers (maintain cluster state using the Raft consensus algorithm, accept API calls, schedule tasks) or Workers (execute container tasks assigned by managers).',
    example: 'Managers (Raft quorum: 3 or 5 nodes) + Workers (execute tasks)',
    exampleExplanation: 'A cluster of 3 managers can tolerate the failure of 1 manager node while maintaining operational quorum.',
    question: 'What consensus protocol do Docker Swarm manager nodes use to maintain a consistent distributed cluster state?',
    answer: 'Raft consensus algorithm',
    category: 'Docker Swarm',
    examTip: 'Always deploy an odd number of Swarm managers (3, 5, or 7) to prevent split-brain election deadlocks.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4494,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'docker swarm init & join',
    definition: 'Commands to initialize and expand a Swarm cluster. `docker swarm init` transforms the current host into a Swarm manager; `docker swarm join-token worker` outputs the join command and cryptographic token for adding worker nodes.',
    example: 'docker swarm init --advertise-addr 192.168.1.100',
    exampleExplanation: 'Worker nodes join by running `docker swarm join --token <token> 192.168.1.100:2377`.',
    question: 'Which command initializes a new Docker Swarm cluster on a host and designates it as the first manager node?',
    answer: 'docker swarm init --advertise-addr <ip_address>',
    category: 'Docker Swarm',
    examTip: 'Docker Swarm cluster management communication occurs over TCP port 2377.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4495,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'docker service create & scale',
    definition: 'Commands for deploying and managing services across a Swarm cluster. A service defines the desired state (image, replicas, ports, update policy); `docker service scale` adjusts replica counts dynamically.',
    example: 'docker service create --name web --replicas 3 -p 80:80 nginx:alpine\ndocker service scale web=6',
    exampleExplanation: 'Swarm managers automatically distribute the 6 tasks evenly across all healthy worker nodes in the cluster.',
    question: 'Which command scales an active Docker Swarm service named "web" to 6 running container replicas?',
    answer: 'docker service scale web=6 (or docker service update --replicas 6 web)',
    category: 'Docker Swarm',
    examTip: 'Use `docker service update --image nginx:1.25 web` to trigger a rolling update with zero downtime.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4496,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'Docker Swarm Routing Mesh & Overlay Networks',
    definition: 'The Routing Mesh enables every node in a Swarm cluster to accept connections on published service ports and route traffic transparently across a VXLAN overlay network to an active container on any node.',
    example: 'Traffic hitting Worker-A:80 is forwarded across ingress overlay network to container running on Worker-B',
    exampleExplanation: 'Provides built-in layer 4 load balancing without requiring external hardware load balancers for basic ingress.',
    question: 'What Docker Swarm feature allows an external client to connect to a published port on ANY cluster node and reach the service container?',
    answer: 'The Ingress Routing Mesh',
    category: 'Docker Swarm',
    examTip: 'Swarm overlay networks encapsulate Ethernet packets in UDP packets over port 4789 using VXLAN.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4497,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'Kubernetes Control Plane Components',
    definition: 'The management brain of a Kubernetes cluster: `kube-apiserver` (central REST API frontend), `etcd` (distributed key-value datastore for cluster state), `kube-scheduler` (assigns unscheduled pods to optimal worker nodes), and `kube-controller-manager` (runs control loops reconciling desired state).',
    example: 'kube-apiserver, etcd, kube-scheduler, kube-controller-manager, cloud-controller-manager',
    exampleExplanation: 'All components communicate exclusively through `kube-apiserver`; no component talks directly to `etcd` except the API server.',
    question: 'Which component of the Kubernetes Control Plane is the sole component that reads from and writes to the `etcd` cluster datastore?',
    answer: 'kube-apiserver',
    category: 'Kubernetes Architecture',
    examTip: 'Worker nodes never communicate directly with `etcd`; all queries and updates route through `kube-apiserver`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4498,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'Kubernetes Worker Node Components',
    definition: 'Components running on every Kubernetes worker node: `kubelet` (primary node agent communicating with API server, driving the container runtime), `kube-proxy` (maintains network rules and Service IP forwarding), and the Container Runtime Interface (CRI runtime, e.g. containerd or CRI-O).',
    example: 'kubelet, kube-proxy, Container Runtime (containerd)',
    exampleExplanation: 'The `kubelet` receives PodSpecs from the API server and instructs containerd via the CRI gRPC interface to instantiate containers.',
    question: 'Which agent runs on each Kubernetes worker node to ensure that the containers described in PodSpecs are running and healthy?',
    answer: 'kubelet',
    category: 'Kubernetes Architecture',
    examTip: '`kube-proxy` configures iptables or IPVS rules on the node to implement Kubernetes Service VIP routing.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4499,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'Kubernetes Core API Objects',
    definition: 'Basic resource abstractions in Kubernetes: `Pod` (smallest deployable unit containing 1+ co-located containers), `Deployment` (manages declarative updates and rollbacks of ReplicaSets), and `Service` (stable network endpoint with ClusterIP, NodePort, or LoadBalancer).',
    example: 'Deployment -> ReplicaSet -> Pods (containers) <- Service (load balanced VIP)',
    exampleExplanation: 'Containers within the same Pod share the same Network namespace (localhost IP) and can share storage Volumes.',
    question: 'What is the smallest deployable atomic computing unit in Kubernetes, consisting of one or more co-located containers?',
    answer: 'A Pod',
    category: 'Kubernetes Architecture',
    examTip: 'A `ClusterIP` Service is only accessible within the cluster; `NodePort` exposes the service on each node\'s IP at a static port (30000-32767).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4500,
    deck: 'LPIC-3 Topic 352',
    topicNumber: 352,
    objectiveId: '352.4',
    command: 'Essential kubectl Commands',
    definition: 'CLI tool for controlling Kubernetes clusters: `kubectl apply -f <manifest>` (declarative deployment), `kubectl get pods,svc,deploy -o wide` (list resources), `kubectl describe <type> <name>` (detailed diagnostic events), `kubectl logs -f <pod>` (stream container logs), and `kubectl exec -it <pod> -- /bin/sh`.',
    example: 'kubectl apply -f deployment.yaml && kubectl get pods -l app=web -o wide',
    exampleExplanation: '`kubectl describe` shows the chronological event history at the bottom of output, which is the primary method to diagnose CrashLoopBackOff or ImagePullBackOff errors.',
    question: 'Which `kubectl` command displays detailed operational diagnostics and system events (such as image pull failures or OOM kills) for a Pod?',
    answer: 'kubectl describe pod <pod_name>',
    category: 'Kubernetes Operations',
    examTip: '`kubectl apply` performs a three-way merge between the local file, the live configuration, and the last applied configuration stored in annotations.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
];
