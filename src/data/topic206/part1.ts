import { Flashcard } from '../../types';

/**
 * LPI LPIC-2 Exam 201 - Topic 206: System Maintenance
 * Part 1: Cards 2601 - 2650
 * 
 * Coverage:
 * - 206.1: Make and Install Programs from Source (Cards 2601-2635) [35 Cards]
 * - 206.2: Backup Operations (Part 1, Cards 2636-2650) [15 Cards]
 */
export const topic206Part1: Flashcard[] = [
  // =========================================================================
  // OBJECTIVE 206.1: Make and Install Programs from Source (Cards 2601-2635)
  // =========================================================================
  {
    id: 2601,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Source Archive Extraction',
    command: 'tar -xvf <archive>',
    question: 'Which compression autodetection and flag options are standard when unpacking source tarballs in tar?',
    answer: 'Use `tar -xvf <file>` (auto-decompression in modern tar) or explicit flags: `-z` (gzip), `-j` (bzip2), `-J` (xz), or `--zstd`.',
    definition: 'Extracts archive contents to the current directory while decompressing the stream based on the compression format.',
    example: 'tar -xzvf nginx-1.24.0.tar.gz\ntar -xjvf coreutils-9.3.tar.bz2\ntar -xJvf linux-6.1.tar.xz',
    exampleExplanation: 'Unpacks gzip (.tar.gz), bzip2 (.tar.bz2), and xz (.tar.xz) compressed tar archives while displaying extracted file names.',
    examTip: 'Remember: modern GNU tar automatically detects compression format with `tar -xf <file>`, but LPIC-2 specifically tests `-z` (gzip), `-j` (bzip2), and `-J` (xz).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2602,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Source Archive Conventions',
    command: '/usr/src vs /usr/local/src',
    question: 'According to Linux FHS conventions, where should source code for local system software and kernel sources be placed?',
    answer: '`/usr/src` is conventionally used for kernel and system source trees, while `/usr/local/src` is designated for local admin-compiled application sources.',
    definition: 'Standard Filesystem Hierarchy Standard (FHS) locations separating distribution-managed source trees from locally compiled administrative software packages.',
    example: 'cd /usr/local/src\nsudo tar -xzvf haproxy-2.8.0.tar.gz\ncd haproxy-2.8.0',
    exampleExplanation: 'Navigates to `/usr/local/src` to extract and build source code for local administration.',
    examTip: 'FHS rules: System software installed outside the package manager from source belongs under `/usr/local/` or `/opt/`, and its unpacked source typically resides in `/usr/local/src`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2603,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'GNU Autotools',
    command: './configure && make && make install',
    question: 'What is the standard three-step pipeline for compiling and installing software built with the GNU Autotools framework?',
    answer: '1. `./configure` (checks dependencies & generates Makefile), 2. `make` (compiles source into binaries), 3. `sudo make install` (copies binaries and assets into system paths).',
    definition: 'The classic build lifecycle generated by GNU Autoconf and Automake tools to adapt C/C++ codebases to heterogeneous Unix/Linux environments.',
    example: './configure\nmake -j$(nproc)\nsudo make install',
    exampleExplanation: 'Configures the build environment, compiles the source code in parallel using all CPU cores, and copies the resulting files to target installation directories.',
    examTip: 'Note the privilege boundary: `./configure` and `make` should run as a non-privileged unprivileged user; only `make install` requires superuser (root) privileges.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2604,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'GNU Autotools',
    command: './configure',
    question: 'What primary file does the `./configure` script inspect as a template to produce the final `Makefile`?',
    answer: 'It inspects `Makefile.in` (which was generated by Automake from `Makefile.am`).',
    definition: 'Shell script created by GNU Autoconf that probes the system for compilers, headers, and library dependencies, translating `Makefile.in` into a tailored `Makefile` and generating `config.h`.',
    example: './configure\n# Checks for gcc, header files, libraries, and writes Makefile and config.status',
    exampleExplanation: 'Performs environmental tests and substitutes architecture-specific paths and variables into `Makefile.in` to produce `Makefile`.',
    examTip: 'Understand the Autotools chain: `configure.ac` (Autoconf) -> `configure`; `Makefile.am` (Automake) -> `Makefile.in`; `./configure` + `Makefile.in` -> `Makefile`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2605,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Configure Options',
    command: './configure --prefix=<path>',
    question: 'What is the function of the `--prefix` argument when running `./configure`, and what is its default value if omitted?',
    answer: '`--prefix=<path>` defines the top-level installation directory for architecture-independent and dependent files. Its default is `/usr/local`.',
    definition: 'Directs the build system where to place the final binaries (`<prefix>/bin`), libraries (`<prefix>/lib`), headers (`<prefix>/include`), and documentation (`<prefix>/share`).',
    example: './configure --prefix=/opt/custom-app\nmake && sudo make install',
    exampleExplanation: 'Installs the resulting application into `/opt/custom-app/bin`, `/opt/custom-app/lib`, etc., preventing overwrites of system files.',
    examTip: 'Essential exam question: Default prefix is always `/usr/local`. To replace an existing distribution package, administrators often set `--prefix=/usr`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2606,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Configure Options',
    command: './configure --sysconfdir=<path>',
    question: 'Which configure flag relocates configuration files (like `.conf` files) away from the default `<prefix>/etc`?',
    answer: '`--sysconfdir=<path>` (e.g. `--sysconfdir=/etc`).',
    definition: 'Overrides the target directory for read-only single-machine system configuration files, often pointing to `/etc` instead of `/usr/local/etc`.',
    example: './configure --prefix=/usr/local --sysconfdir=/etc/custom-app',
    exampleExplanation: 'Places application executables in `/usr/local/bin` while placing its config files in `/etc/custom-app/`.',
    examTip: 'LPIC-2 tests standard directory override parameters: `--sysconfdir` (configuration), `--localstatedir` (`/var` data), and `--mandir` (man pages).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2607,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Configure Options',
    command: './configure --bindir & --libdir',
    question: 'How do you customize the destination directories for user binaries and shared library objects in `./configure`?',
    answer: 'Use `--bindir=<path>` for user executables and `--libdir=<path>` for object code libraries.',
    definition: 'Configure directory variables specifying exact locations for user executables (default `<prefix>/bin`) and dynamic/static libraries (default `<prefix>/lib` or `<prefix>/lib64`).',
    example: './configure --prefix=/usr --bindir=/usr/bin --libdir=/usr/lib64',
    exampleExplanation: 'Ensures 64-bit libraries are installed into `/usr/lib64` on multi-lib distributions like RHEL/CentOS.',
    examTip: 'For administrative/system-only binaries, use `--sbindir=<path>` (default `<prefix>/sbin`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2608,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Configure Options',
    command: './configure --enable-FEATURE / --disable-FEATURE',
    question: 'How do you toggle built-in features of a package inside `./configure`?',
    answer: 'Use `--enable-FEATURE` to turn on an optional internal feature, and `--disable-FEATURE` to turn it off.',
    definition: 'Autoconf mechanism for conditionally compiling internal source code modules without requiring external library detection.',
    example: './configure --enable-debug --disable-ipv6',
    exampleExplanation: 'Compiles the binary with debugging symbols enabled while stripping out internal IPv6 networking support.',
    examTip: 'Distinction: `--enable/--disable` controls features within the package source itself. `--with/--without` controls linkages to external third-party libraries.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2609,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Configure Options',
    command: './configure --with-PACKAGE / --without-PACKAGE',
    question: 'How do you specify linking against or omitting third-party external libraries during `./configure`?',
    answer: 'Use `--with-PACKAGE[=DIR]` to link against an external package, or `--without-PACKAGE` to omit it.',
    definition: 'Instructs the compiler/linker to look for external headers and libraries in custom paths or disable dependencies on external third-party software.',
    example: './configure --with-ssl=/usr/local/openssl --without-ldap',
    exampleExplanation: 'Builds with OpenSSL headers/libraries from `/usr/local/openssl` and excludes LDAP support.',
    examTip: 'To discover all available `--enable-*` and `--with-*` options for a software package, run `./configure --help`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2610,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Compiler Environment Variables',
    command: 'CFLAGS="-O2 -g" ./configure',
    question: 'Which environment variables pass custom compiler options, C++ options, and linker flags to `./configure`?',
    answer: '`CFLAGS` (C compiler options), `CXXFLAGS` (C++ options), `LDFLAGS` (linker options), and `CC` / `CXX` (specifying compiler binary).',
    definition: 'Standard shell environment variables recognized by Autotools to modify optimization levels, debugging information, include paths (`-I`), and library paths (`-L`).',
    example: 'CC=clang CFLAGS="-O3 -march=native" LDFLAGS="-L/opt/lib" ./configure',
    exampleExplanation: 'Instructs configure to use the Clang compiler, apply maximum CPU-native optimizations, and append `/opt/lib` to linker search paths.',
    examTip: 'Passing environment variables preceding `./configure` (or as arguments like `./configure CFLAGS="..."`) stores them inside the generated `Makefile`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2611,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Configure Troubleshooting',
    command: 'config.log',
    question: 'If `./configure` aborts with an error such as "C compiler cannot create executables", which file contains the exact compiler commands and diagnostics?',
    answer: '`config.log` in the build directory.',
    definition: 'Detailed log generated by Autoconf recording every test invocation, failed test code snippets, exit codes, and compiler stderr outputs.',
    example: 'tail -n 50 config.log\ngrep -i "error" config.log',
    exampleExplanation: 'Inspects `config.log` to reveal whether the compiler failed due to a missing header, missing libc development files, or invalid CFLAGS.',
    examTip: 'Crucial exam concept: When configure fails, the console error message is often generic. `config.log` is the definitive diagnostic file.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2612,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Build Dependencies',
    command: 'pkg-config --cflags --libs <package>',
    question: 'What command-line tool helps configure scripts and Makefiles query the compiler and linker flags for installed development libraries?',
    answer: '`pkg-config` (inspects `.pc` metadata files in `/usr/lib/pkgconfig`).',
    definition: 'Helper tool that outputs the exact include paths (`-I`) and library flags (`-l`, `-L`) required to compile software against installed shared libraries.',
    example: 'pkg-config --cflags --libs libpcre\n# Output: -I/usr/include -lpcre',
    exampleExplanation: 'Returns compilation and linking arguments for PCRE, verifying that developer header files (`.h`) and libraries are present.',
    examTip: 'If `./configure` complains about a missing library despite the runtime package being installed, ensure the development package (`-dev` on Debian or `-devel` on RHEL) is installed.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2613,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Make Utility',
    command: 'make',
    question: 'How does the `make` utility decide which source files must be recompiled into object files (`.o`)?',
    answer: 'It compares the file modification timestamps (mtime) of source files (`.c`) against their target object files (`.o`).',
    definition: 'Dependency tracking engine that only executes compiler commands for target files whose prerequisites are newer than the target or targets that do not exist.',
    example: 'make\n# Compiles only modified C files and links the binary',
    exampleExplanation: 'Reads the rules in `Makefile` and initiates compilation based on target dependencies and filesystem timestamps.',
    examTip: 'To specify an alternate Makefile name, use `make -f <filename>` or `make --file=<filename>`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2614,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Make Utility',
    command: 'make -j <N>',
    question: 'Which option instructs `make` to execute jobs in parallel across multiple CPU cores?',
    answer: '`make -j <jobs>` (e.g. `make -j 8` or `make -j$(nproc)`).',
    definition: 'Specifies the number of simultaneous recipe jobs make can run concurrently, dramatically reducing compilation time on multi-core architectures.',
    example: 'make -j$(nproc)',
    exampleExplanation: 'Detects available processing units with `nproc` and compiles independent source files simultaneously across all cores.',
    examTip: 'If omitted, `make` runs sequentially (`-j 1`). If `-j` is used without an argument, make spawns unbounded parallel tasks, which can exhaust RAM.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2615,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Make Targets',
    command: 'make all',
    question: 'What is the standard convention for the default target when running `make` without target arguments?',
    answer: 'The `all` target (or the first target rule defined in the `Makefile`).',
    definition: 'Compiles all libraries, binaries, and documentation in the project without performing any installation to system directories.',
    example: 'make all\n# Equivalent to executing "make" in standard GNU projects',
    exampleExplanation: 'Invokes the top-level compilation target defined in the Makefile.',
    examTip: 'Make executes the first non-pattern target encountered in the Makefile if no target name is supplied on the CLI.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2616,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Make Targets',
    command: 'make clean vs make distclean',
    question: 'What is the critical difference between `make clean` and `make distclean`?',
    answer: '`make clean` deletes compiled object files (`.o`) and binaries; `make distclean` deletes compiled files PLUS files generated by `./configure` (like `Makefile` and `config.status`).',
    definition: 'Standard cleanup targets: `clean` leaves the build configuration intact, while `distclean` resets the source tree to its pristine pre-configure state.',
    example: 'make clean\n# Removes .o files\nmake distclean\n# Removes .o, Makefile, config.cache, config.log',
    exampleExplanation: 'Use `make clean` before recompiling with different compiler flags; use `make distclean` before re-running `./configure` with different prefix options.',
    examTip: 'High-probability exam question: `distclean` is more destructive than `clean`—it requires you to re-run `./configure` from scratch.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2617,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Make Targets',
    command: 'sudo make install',
    question: 'What actions are carried out by `make install`, and why is root escalation typically needed?',
    answer: 'It copies compiled binaries, shared libraries, headers, and man pages to their destination paths (e.g. `/usr/local/bin`, `/usr/local/lib`), which require write access.',
    definition: 'Executes the installation rules from `Makefile`, invoking commands like `install` or `cp` into system-wide protected directories.',
    example: 'sudo make install',
    exampleExplanation: 'Installs executables into prefix directories with root permissions.',
    examTip: 'You can test installation without modifying the root filesystem using `make DESTDIR=/tmp/staging install`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2618,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Make Targets',
    command: 'make uninstall',
    question: 'Why is `make uninstall` not always a reliable method for removing software compiled from source?',
    answer: 'The developer is not required to implement an `uninstall` target in the `Makefile`, and if missing, make returns an error: "No rule to make target \'uninstall\'".',
    definition: 'Optional Makefile target to delete installed files; if omitted or if files were added after configuration, removal cannot be tracked cleanly.',
    example: 'sudo make uninstall\n# If supported: deletes files installed by make install',
    exampleExplanation: 'Removes files previously placed in prefix paths, provided the Makefile author defined the target.',
    examTip: 'Because `make uninstall` is often missing or incomplete, best practice is to use `checkinstall` or package managers instead of raw `make install`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2619,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Package Creation',
    command: 'checkinstall',
    question: 'What is the purpose of the `checkinstall` utility when compiling software from source?',
    answer: 'It intercepts the files written by `make install` and builds a native distribution package (`.deb`, `.rpm`, or Slackware `.tgz`) for clean package management and removal.',
    definition: 'Wrapper utility that monitors file writes during installation, creates a native binary package, and installs it via `dpkg` or `rpm`.',
    example: './configure && make\nsudo checkinstall',
    exampleExplanation: 'Replaces `sudo make install` with a process that packages the compiled software into an installed .deb or .rpm package.',
    examTip: 'Major LPIC-2 topic: `checkinstall` allows administrators to query installed source software via `dpkg -l` or `rpm -qa` and remove it via `apt remove` or `dnf remove`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2620,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Package Creation',
    command: 'checkinstall -D / -R',
    question: 'Which `checkinstall` options explicitly force generation of a Debian (.deb) or RPM (.rpm) package?',
    answer: '`-D` (or `--type=debian`) forces a Debian package; `-R` (or `--type=rpm`) forces an RPM package; `-S` forces Slackware.',
    definition: 'Format selection flags in checkinstall overriding auto-detection based on the host distribution.',
    example: 'sudo checkinstall -D --pkgname=custom-nginx --pkgversion=1.24.0 --default',
    exampleExplanation: 'Builds a Debian package named `custom-nginx_1.24.0-1_amd64.deb` and accepts all interactive defaults without prompts.',
    examTip: 'The `--default` (or `-y`) flag accepts all default answers non-interactively, ideal for automation scripts.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2621,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Shared Libraries',
    command: 'soname vs real name vs linker name',
    question: 'Explain the three naming conventions for shared libraries on Linux (e.g. for libssl).',
    answer: '1. **Real name**: contains full version (`libssl.so.1.1.1`). 2. **soname**: contains major version for ABI compatibility (`libssl.so.1.1`). 3. **Linker name**: versionless symlink for compilation (`libssl.so`).',
    definition: 'Linux library versioning scheme using symlinks to ensure that binaries dynamically link to backward-compatible ABI releases.',
    example: 'ls -l /usr/lib64/libssl.so*\n# libssl.so -> libssl.so.1.1 (Linker name)\n# libssl.so.1.1 -> libssl.so.1.1.1k (soname -> real name)\n# libssl.so.1.1.1k (Real name)',
    exampleExplanation: 'Executables bind to the soname at compile time, resolving to the specific real name version at runtime.',
    examTip: 'Exam note: When a minor bugfix is released, only the real name file updates; the soname symlink is updated to point to the new real name, maintaining compatibility.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2622,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Dynamic Linker',
    command: 'ld.so / ld-linux.so',
    question: 'What is the role of `/lib64/ld-linux-x86-64.so.2` (or `ld.so`) when a dynamically linked executable is executed?',
    answer: 'It is the dynamic linker/loader that locates, loads shared library dependencies into memory, and resolves symbolic function addresses before execution begins.',
    definition: 'The runtime dynamic linker program specified in the ELF binary `INTERP` header that sets up the execution environment for shared-object programs.',
    example: 'readelf -l /bin/ls | grep interpreter\n# Output: [Requesting program interpreter: /lib64/ld-linux-x86-64.so.2]',
    exampleExplanation: 'Shows that `/bin/ls` invokes the kernel-assisted dynamic loader at execution startup.',
    examTip: 'Dynamic linking occurs at program execution time via `ld-linux.so`, whereas static linking bundles all library code into the binary at compile time via `ld`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2623,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Dynamic Linker Configuration',
    command: '/etc/ld.so.conf & /etc/ld.so.conf.d/*.conf',
    question: 'Where are permanent system-wide directories for shared library searches configured in Linux?',
    answer: 'In `/etc/ld.so.conf` and modular configuration files placed in `/etc/ld.so.conf.d/*.conf`.',
    definition: 'Text configuration files specifying directories (one per line) outside standard `/lib` and `/usr/lib` where the dynamic linker should search for libraries.',
    example: 'echo "/usr/local/customlib/lib" | sudo tee /etc/ld.so.conf.d/customlib.conf',
    exampleExplanation: 'Registers `/usr/local/customlib/lib` as an official system library directory.',
    examTip: 'Creating a file in `/etc/ld.so.conf.d/` does NOT take effect immediately; you must run `ldconfig` to re-generate the binary cache.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2624,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Dynamic Linker Configuration',
    command: '/etc/ld.so.cache',
    question: 'Why does the dynamic linker read `/etc/ld.so.cache` instead of searching `/etc/ld.so.conf` paths sequentially at program launch?',
    answer: 'Performance: `/etc/ld.so.cache` is a pre-indexed binary cache containing sorted library names and exact filesystem paths for fast memory lookup.',
    definition: 'Compiled binary cache mapping shared library sonames to filesystem paths, built by `ldconfig`.',
    example: 'file /etc/ld.so.cache\n# Output: /etc/ld.so.cache: data',
    exampleExplanation: 'Shows that `/etc/ld.so.cache` is a binary file optimized for rapid read operations during binary initialization.',
    examTip: 'If `/etc/ld.so.cache` is missing or corrupted, programs launch noticeably slower as the linker falls back to scanning directories manually.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2625,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Dynamic Linker Configuration',
    command: 'ldconfig',
    question: 'Which command must be executed after installing new shared libraries into `/usr/local/lib` or editing `/etc/ld.so.conf.d/*.conf`?',
    answer: '`ldconfig` (as root).',
    definition: 'Scans directories listed in `/etc/ld.so.conf` and standard `/lib`, `/usr/lib` paths, updates soname symlinks, and rebuilds `/etc/ld.so.cache`.',
    example: 'sudo ldconfig',
    exampleExplanation: 'Regenerates `/etc/ld.so.cache` to make newly installed libraries immediately visible to dynamic binaries.',
    examTip: 'Frequent fill-in-the-blank exam question: Name the command that updates `/etc/ld.so.cache`: `ldconfig`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2626,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Dynamic Linker Configuration',
    command: 'ldconfig -v',
    question: 'What does the `-v` (verbose) flag provide when executing `ldconfig`?',
    answer: 'It displays every directory scanned, existing libraries found, and all soname symlinks created or modified.',
    definition: 'Verbose execution mode reporting directory traversals and link resolutions.',
    example: 'sudo ldconfig -v | grep -A 5 "/usr/local/lib"',
    exampleExplanation: 'Shows libraries discovered under `/usr/local/lib` and reports newly created soname symlinks.',
    examTip: 'Use `ldconfig -v` to confirm whether your custom directory was processed or if syntax errors in `ld.so.conf` prevented reading.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2627,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Dynamic Linker Configuration',
    command: 'ldconfig -p',
    question: 'Which option causes `ldconfig` to print the entire list of cached libraries stored in `/etc/ld.so.cache`?',
    answer: '`ldconfig -p` (or `--print-cache`).',
    definition: 'Dumps the contents of the current binary dynamic linker cache to stdout in human-readable format.',
    example: 'ldconfig -p | grep libssl\n# Output: libssl.so.3 (libc6,x86-64) => /lib64/libssl.so.3',
    exampleExplanation: 'Verifies whether `libssl.so.3` is indexed in the cache and identifies its resolved filesystem path.',
    examTip: 'Exam favorite: To inspect what libraries the runtime loader can currently find without running an executable, use `ldconfig -p`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2628,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Dynamic Linker Configuration',
    command: 'ldconfig -n <directory>',
    question: 'How do you create soname symlinks in a specific directory without rebuilding the global `/etc/ld.so.cache`?',
    answer: '`ldconfig -n <directory>` (processes only the specified directory without updating `/etc/ld.so.cache`).',
    definition: 'Limits ldconfig operations to command-line specified directories without scanning system directories or updating the global cache file.',
    example: 'sudo ldconfig -n /opt/myapp/lib',
    exampleExplanation: 'Inspects `/opt/myapp/lib` and creates missing soname symlinks (e.g. `libmyapp.so.1 -> libmyapp.so.1.0.0`) locally.',
    examTip: 'The `-X` flag tells ldconfig not to update symlinks, whereas `-N` tells it not to rebuild the cache. `-n` implies `-N`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2629,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Library Inspection',
    command: 'ldd <executable>',
    question: 'Which command prints the shared libraries required by a given binary program and how they resolve?',
    answer: '`ldd` (List Dynamic Dependencies).',
    definition: 'Invokes the dynamic linker with simulated environment settings to print each required shared library, its resolved path, and load memory address.',
    example: 'ldd /usr/bin/python3\n# Output: libm.so.6 => /lib64/libm.so.6 (0x...)\n# libc.so.6 => /lib64/libc.so.6 (0x...)',
    exampleExplanation: 'Lists every shared object `.so` file Python 3 depends upon and the paths from which they will be loaded.',
    examTip: 'Security note: `ldd` may execute certain untrusted binaries in order to inspect dependencies. Never run `ldd` on an untrusted or suspicious binary; use `objdump -p` or `readelf -d` instead.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2630,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Library Inspection',
    command: 'ldd output: "not found"',
    question: 'If `ldd` outputs `libfoo.so.2 => not found`, what does this signify and how do you resolve it?',
    answer: 'The binary requires `libfoo.so.2`, but the dynamic linker cannot locate it in `/etc/ld.so.cache`, `LD_LIBRARY_PATH`, or standard library paths. Resolve by installing the library, adding its directory to `/etc/ld.so.conf.d/`, and running `ldconfig`.',
    definition: 'Indicates a broken runtime dependency that will cause execution of the binary to immediately fail with "cannot open shared object file: No such file or directory".',
    example: 'ldd /usr/local/bin/app\n# libcustom.so.1 => not found\necho "/usr/local/lib" | sudo tee -a /etc/ld.so.conf.d/local.conf && sudo ldconfig',
    exampleExplanation: 'Diagnoses the missing dependency and adds the library path to the dynamic linker cache to fix startup.',
    examTip: 'A statically linked binary evaluated with `ldd` returns "not a dynamic executable".',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2631,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Dynamic Linker Environment',
    command: 'LD_LIBRARY_PATH',
    question: 'How can a non-root user temporarily instruct a program to search a custom folder for shared libraries before searching system paths?',
    answer: 'Set the `LD_LIBRARY_PATH` environment variable: `export LD_LIBRARY_PATH=/path/to/libs:$LD_LIBRARY_PATH`.',
    definition: 'Colon-separated list of directories searched by the dynamic linker before `/etc/ld.so.cache` and standard paths (`/lib`, `/usr/lib`).',
    example: 'LD_LIBRARY_PATH=/home/user/lib:/opt/testing/lib ./myprogram',
    exampleExplanation: 'Launches `./myprogram` using shared libraries found in `/home/user/lib` in preference to system-wide libraries.',
    examTip: 'Security safeguard: For security reasons, `LD_LIBRARY_PATH` is ignored by the dynamic linker for setuid (SUID) and setgid (SGID) programs.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2632,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Dynamic Linker Environment',
    command: 'LD_PRELOAD',
    question: 'What is the purpose of the `LD_PRELOAD` environment variable and `/etc/ld.so.preload` file?',
    answer: 'Specifies shared libraries to be loaded BEFORE any other libraries, allowing selective overriding and hooking of standard functions (e.g. malloc, open).',
    definition: 'Mechanism to intercept library calls by giving specified shared objects the highest symbol resolution precedence.',
    example: 'LD_PRELOAD=/usr/lib64/libjemalloc.so ./high_load_app',
    exampleExplanation: 'Forces `high_load_app` to use jemalloc for memory allocations instead of glibc standard malloc.',
    examTip: 'Like `LD_LIBRARY_PATH`, `LD_PRELOAD` is ignored for SUID binaries unless the library is in a standard directory and has the SUID bit cleared.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2633,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Source Code Patching',
    command: 'patch < <patchfile>',
    question: 'How do you apply a patch file containing source code differences to an existing source tree?',
    answer: 'Use the `patch` command: `patch -p1 < bugfix.patch`.',
    definition: 'Takes unified or context diff output and modifies original source files in place to reflect code updates, bugfixes, or security patches.',
    example: 'cd /usr/src/linux\npatch -p1 < /tmp/patch-6.1.2.patch',
    exampleExplanation: 'Applies kernel patch file `patch-6.1.2.patch` from the root of the kernel source tree.',
    examTip: 'If a patch was created with `diff -u old/file.c new/file.c`, inside the directory you strip one leading directory component using `-p1`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2634,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Source Code Patching',
    command: 'patch -p<num>',
    question: 'What does the `-p` (strip level) option in `patch` control?',
    answer: 'It indicates how many leading slashes and directory path segments to strip from filenames listed in the patch file header.',
    definition: 'Path pruning option allowing patches created from arbitrary directory structures to be applied regardless of the recipient local working directory.',
    example: '# In patch: --- a/src/main.c\n# With -p0: looks for "a/src/main.c"\n# With -p1: strips "a/" and looks for "src/main.c"',
    exampleExplanation: 'Demonstrates how `-p1` strips the first path component `a/` commonly generated by `git diff`.',
    examTip: 'Exam essential: `-p0` uses the entire path as written in the patch. `-p1` removes the first component up to the first slash. Git diffs almost always require `-p1`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2635,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.1',
    topicNumber: 206,
    category: 'Source Code Patching',
    command: 'patch -R / --reverse & diff -u',
    question: 'How do you generate a unified diff patch file, and how can a previously applied patch be reversed?',
    answer: 'Generate with `diff -u[rN] old_dir new_dir > update.patch`. Reverse an applied patch using `patch -R -p1 < update.patch`.',
    definition: 'Unified diff produces a standardized patch with line number context. `-R` (or `--reverse`) swaps original and new sections to undo modifications.',
    example: 'diff -u original.c modified.c > fix.patch\npatch -p0 < fix.patch   # Apply\npatch -R -p0 < fix.patch # Revert back',
    exampleExplanation: 'Creates a unified patch between two C files, applies it, and then reverses it using `-R`.',
    examTip: 'The `-b` (or `--backup`) flag instructs `patch` to create backup copies of modified files (e.g. `file.c.orig`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },

  // =========================================================================
  // OBJECTIVE 206.2: Backup Operations (Cards 2636-2650: Part 1)
  // =========================================================================
  {
    id: 2636,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'Backup Strategies',
    command: 'Full Backup (Level 0)',
    question: 'What is a Full Backup (Level 0), and what are its primary advantages and disadvantages?',
    answer: 'Copies ALL selected files regardless of when they were last modified. **Advantage**: Fast, simple single-archive restore. **Disadvantage**: Consumes maximum storage and takes the longest time to execute.',
    definition: 'The foundational baseline backup in all rotation schemes containing 100% of target data.',
    example: 'tar -cvpzf /backup/full_backup_$(date +%F).tar.gz /home /etc /var/www',
    exampleExplanation: 'Archives all contents of `/home`, `/etc`, and `/var/www` into a standalone baseline archive.',
    examTip: 'In Unix `dump` terminology, a full backup is always Level 0.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2637,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'Backup Strategies',
    command: 'Differential Backup',
    question: 'What is a Differential Backup, and how does its scope differ from an Incremental Backup?',
    answer: 'A Differential Backup captures ALL files created or modified since the **LAST FULL BACKUP**. It does not reset modification markers.',
    definition: 'Cumulative archive capturing all changes since the last Full baseline. Size grows each subsequent day until the next Full backup.',
    example: '# Sunday: Full (Level 0)\n# Monday: Changes since Sun\n# Tuesday: Changes since Sun (includes Mon + Tue changes)\n# Wednesday: Changes since Sun',
    exampleExplanation: 'Illustrates that a differential backup continuously archives all changes made relative to Sunday baseline.',
    examTip: 'To restore from a Differential backup, you only ever need TWO archives: the last Full backup + the most recent Differential backup.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2638,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'Backup Strategies',
    command: 'Incremental Backup',
    question: 'What is an Incremental Backup, and what are its operational trade-offs?',
    answer: 'Captures only files modified since the **LAST BACKUP OF ANY KIND** (full or incremental). **Advantage**: Fastest backup execution, minimal storage. **Disadvantage**: Slowest, multi-step restore.',
    definition: 'Non-cumulative archive capturing delta changes since the immediately preceding backup event.',
    example: '# Sunday: Full (Level 0)\n# Monday: Changes since Sun (Level 1)\n# Tuesday: Changes since Mon (Level 2)\n# Wednesday: Changes since Tue (Level 3)',
    exampleExplanation: 'Each daily incremental archive contains only the changes made on that specific day.',
    examTip: 'To restore an Incremental chain: You must restore the Full baseline + EVERY incremental backup sequentially up to the target date. If one is corrupt, later increments may be unrecoverable.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2639,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'Backup Strategies',
    command: 'Differential vs Incremental Restore',
    question: 'Compare the restore time and disaster recovery complexity of Differential versus Incremental backups.',
    answer: 'Differential is significantly faster and simpler to restore (requires only Full + last Differential: 2 tapes/files). Incremental requires restoring Full + every sequential incremental slice (N archives).',
    definition: 'Core disaster recovery metric evaluating Recovery Time Objective (RTO) against storage overhead.',
    example: '# Restoring Wednesday failure:\n# Differential: Restore Sunday Full + Tuesday Diff (2 steps)\n# Incremental: Restore Sunday Full + Mon Inc + Tue Inc (3 steps)',
    exampleExplanation: 'Differential minimizes restoration steps at the expense of larger daily backup files.',
    examTip: 'LPIC-2 scenario question: "A system requires minimal downtime during emergency recovery (low RTO)." Solution: Prefer Differential backups over Incremental.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2640,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'Backup Best Practices',
    command: '3-2-1 Backup Rule',
    question: 'What is the industry-standard 3-2-1 backup rule?',
    answer: 'Maintain at least **3** total copies of data, stored on **2** different storage media types (e.g. disk and tape/optical), with at least **1** copy kept offsite/remote.',
    definition: 'Fundamental disaster recovery principle safeguarding against hardware failure, facility disasters (fire/flood), and ransomware.',
    example: 'Copy 1: Production SSD array\nCopy 2: Local NAS backup storage\nCopy 3: Offsite cloud bucket / remote data center',
    exampleExplanation: 'Protects business continuity across localized physical and digital failure modes.',
    examTip: 'Always remember: 3 copies, 2 different media formats, 1 offsite location.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2641,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'Backup Rotation',
    command: 'Grandfather-Father-Son (GFS)',
    question: 'Describe the Grandfather-Father-Son (GFS) media rotation scheme.',
    answer: '**Son**: Daily incremental/differential backups. **Father**: Weekly full backups. **Grandfather**: Monthly full archives retained long-term for compliance.',
    definition: 'Hierarchical tape/disk rotation scheme balancing retention period coverage against media pool costs.',
    example: 'Daily tapes rotated every week (Son);\nWeekly tapes rotated every month (Father);\nMonthly tapes archived for years (Grandfather).',
    exampleExplanation: 'Enables point-in-time recovery for recent days while maintaining historical checkpoints over decades.',
    examTip: 'LPIC-2 tests familiarity with classic rotation schemes: Grandfather-Father-Son (hierarchical calendar) and Tower of Hanoi (exponential interval rotation).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2642,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'Backup Scope',
    command: 'Essential Directories to Backup',
    question: 'Which Linux system directories contain critical unique data and configurations that must be included in system backups?',
    answer: '`/etc` (configuration), `/home` and `/root` (user data), `/var/www`, `/var/mail`, `/var/log` (audit trails), and database directories (e.g. `/var/lib/mysql`).',
    definition: 'Non-reproducible, stateful filesystem locations holding administrative settings, credentials, databases, and user work.',
    example: 'tar -czf sys_backup.tar.gz /etc /home /var/spool/cron /var/www',
    exampleExplanation: 'Focuses backup storage strictly on essential configurations and dynamic data.',
    examTip: 'Exam question: Backing up `/bin`, `/sbin`, `/lib`, and `/usr` is rarely necessary for routine backups because they can be reinstalled from distribution packages.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2643,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'Backup Scope',
    command: 'Directories to Exclude from Backups',
    question: 'Which virtual or temporary directories should always be excluded from filesystem backup routines?',
    answer: '`/proc`, `/sys`, `/dev`, `/run`, `/tmp`, `/var/tmp`, and `/lost+found`.',
    definition: 'Pseudo-filesystems (virtual files generated dynamically by the kernel), temporary volatile storage (tmpfs in RAM), and device nodes.',
    example: 'tar --exclude=/proc --exclude=/sys --exclude=/dev --exclude=/run --exclude=/tmp -cvpzf full.tar.gz /',
    exampleExplanation: 'Excludes in-memory virtual trees which cannot be meaningfully archived and could cause tar to hang on FIFO pipes.',
    examTip: 'Backing up `/proc` or `/sys` can cause backup software to loop infinitely or crash due to dynamic zero-length files and infinite virtual symlinks.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2644,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'rsync Utility',
    command: 'rsync <options> <src> <dest>',
    question: 'Why is `rsync` preferred over `scp` or `cp` for network synchronization and recurring backup routines?',
    answer: 'It employs the **rsync remote delta-transfer algorithm**, transmitting only byte-level differences for modified files rather than re-sending whole files.',
    definition: 'Fast, versatile file copying and synchronization tool that compares file modification times and sizes (or checksums) to minimize network traffic.',
    example: 'rsync -avz /data/ backupuser@backupserver:/remote/data/',
    exampleExplanation: 'Transfers new or modified files under `/data/` to `backupserver`, skipping unchanged files entirely.',
    examTip: 'For encrypted network transport, rsync uses SSH by default in modern Linux distributions.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2645,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'rsync Utility',
    command: 'rsync -a / --archive',
    question: 'What exact individual flags are bundled into the commonly used `rsync -a` (archive mode) switch?',
    answer: '`-r` (recursive), `-l` (preserve symlinks), `-p` (preserve permissions), `-t` (preserve modification times), `-g` (preserve group), `-o` (preserve owner), `-D` (preserve device nodes and special files).',
    definition: 'Meta-flag providing an exact mirror copy preserving directory structures, file metadata, ownership, and permissions.',
    example: 'rsync -a /etc/ /backup/etc/',
    exampleExplanation: 'Recursively copies `/etc/` while preserving file permissions, ownership, symlinks, and timestamps.',
    examTip: 'Guaranteed exam question: Does `-a` preserve hard links? NO. To preserve hard links in rsync, you must explicitly add the `-H` (`--hard-links`) flag.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2646,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'rsync Utility',
    command: 'rsync: trailing slash on source',
    question: 'What is the critical behavioral difference between `rsync -a /src/ /dst/` and `rsync -a /src /dst/`?',
    answer: 'With trailing slash (`/src/`): copies the **CONTENTS** of `/src` into `/dst`. Without trailing slash (`/src`): copies the **DIRECTORY ITSELF**, resulting in `/dst/src/`.',
    definition: 'Fundamental rsync syntax rule dictating whether the directory container or its contained files are copied.',
    example: 'rsync -a /var/www/ /backup/www/\n# Result: /backup/www/index.html\nrsync -a /var/www /backup/www/\n# Result: /backup/www/www/index.html',
    exampleExplanation: 'Omitting the trailing slash creates an extra nested directory level at the destination.',
    examTip: 'High-frequency LPIC-2 exam trick: Look closely at whether the source argument has a trailing slash.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2647,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'rsync Utility',
    command: 'rsync -z & -v',
    question: 'What do the `-z` and `-v` options do in `rsync`?',
    answer: '`-z` (`--compress`) compresses file data in transit over the network. `-v` (`--verbose`) outputs filenames and transfer statistics.',
    definition: 'Data reduction and logging switches in rsync.',
    example: 'rsync -avz /home/ remotehost:/backup/home/',
    exampleExplanation: 'Enables archive mode, verbose output, and gzip in-flight network compression to save WAN bandwidth.',
    examTip: 'Do not use `-z` when syncing across high-speed LANs with already-compressed files (like .mp4 or .gz), as CPU compression overhead exceeds network transmission gains.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2648,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'rsync Utility',
    command: 'rsync -e / --rsh',
    question: 'How do you configure `rsync` to use a custom SSH port (e.g. 2222) or custom identity key?',
    answer: 'Use the `-e` (or `--rsh`) option: `rsync -avz -e "ssh -p 2222 -i /root/.ssh/id_backup" /data/ user@host:/backup/`.',
    definition: 'Specifies the remote shell program and arguments to invoke for transport.',
    example: 'rsync -avz -e "ssh -p 2200" /etc/ backupuser@192.168.1.100:/backups/etc/',
    exampleExplanation: 'Connects to the backup server via SSH on non-standard port 2200.',
    examTip: 'Alternatively, you can set the `RSYNC_RSH="ssh -p 2222"` environment variable.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2649,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'rsync Utility',
    command: 'rsync --delete',
    question: 'What is the purpose of the `--delete` flag in `rsync`, and what risk does it carry?',
    answer: 'It deletes files from the destination directory that no longer exist in the source directory, maintaining an exact replica. **Risk**: If run with wrong source path, it will erase target backup files.',
    definition: 'Mirroring flag that purges orphaned destination files deleted on the source system.',
    example: 'rsync -av --delete /var/www/ /backup/www/',
    exampleExplanation: 'Syncs `/var/www/` and purges obsolete files in `/backup/www/` that were deleted in source.',
    examTip: 'Always test commands with `--dry-run` (`-n`) before executing `rsync --delete` to preview which files would be erased without modifying anything.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2650,
    deck: 'Topic 206: System Maintenance',
    objectiveId: '206.2',
    topicNumber: 206,
    category: 'rsync Utility',
    command: 'rsync --exclude & --exclude-from',
    question: 'How do you exclude temporary files or entire directories from an `rsync` backup job?',
    answer: 'Use `--exclude="<pattern>"` for single patterns, or `--exclude-from=<file>` to specify a text file containing exclusion patterns.',
    definition: 'Filtering options that skip matching paths during file traversal.',
    example: 'rsync -av --exclude="*.tmp" --exclude="/cache" /src/ /dst/\nrsync -av --exclude-from=/etc/rsync_exclude.txt / /backup/',
    exampleExplanation: 'Excludes all `.tmp` files and `/cache` directory from synchronization.',
    examTip: 'An exclude pattern with a leading slash (e.g. `/tmp`) matches only at the root of the transfer; without leading slash (`tmp`), it matches anywhere in the tree.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
];
