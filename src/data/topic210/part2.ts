import { Flashcard } from '../../types';

/**
 * LPI LPIC-2 Exam 202 - Topic 210: Network Client Management
 * Part 2: Cards 3051 - 3100
 * - Objective 210.2: PAM Authentication - Part 2 (Cards 3051-3070) [20 Cards] (Weight 3)
 * - Objective 210.3: LDAP Client Usage (Cards 3071-3100) [30 Cards] (Weight 2)
 */

export const topic210Part2: Flashcard[] = [
  // =========================================================================
  // OBJECTIVE 210.2: PAM Authentication - Part 2 (Cards 3051-3070) [Weight 3]
  // =========================================================================
  {
    id: 3051,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'PAM extended control syntax',
    question: 'How does PAM extended control syntax work, and what does `[success=1 default=ignore]` mean?',
    answer: 'Extended syntax uses bracketed key-value pairs `[value1=action1 value2=action2 ...]`. `[success=1 default=ignore]` means: if the module succeeds, SKIP the next 1 module in the stack; for any other return code, ignore the result and proceed to the next module.',
    definition: 'A sophisticated control mechanism providing fine-grained branching, module jumping, and explicit handling of specific PAM return values.',
    example: 'auth [success=2 default=ignore] pam_unix.so\nauth [success=1 default=ignore] pam_ldap.so\nauth requisite pam_deny.so\nauth required pam_permit.so',
    exampleExplanation: 'Implements conditional module bypass: if pam_unix succeeds, it jumps past the next 2 modules directly to pam_permit.',
    examTip: 'Numerical actions like `success=N` skip N modules downward. Named actions include `ok`, `done`, `bad`, `die`, `ignore`, and `reset`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3052,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_access.so and access.conf',
    question: 'What is `pam_access.so` and what configuration file does it parse to authorize or deny user logins?',
    answer: '`pam_access.so` provides logdaemon-style login access control based on `/etc/security/access.conf`. It allows or denies users and groups based on origin tty, host name, domain, or network IP range.',
    definition: 'An account management module that enforces host- and tty-based login policies before granting a session.',
    example: '# In /etc/security/access.conf:\n# format: <permission> : <users/groups> : <origins>\n+ : root : 192.168.1.0/24 LOCAL\n- : root : ALL\n+ : @admin : ALL\n- : ALL : ALL',
    exampleExplanation: 'Allows root only from the local console and 192.168.1.0/24 subnet; allows admin group from anywhere; blocks everyone else.',
    examTip: 'Format in `access.conf` is strictly: `permission : users : origins`, where permission is `+` (allow) or `-` (deny).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3053,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_env.so and pam_env.conf',
    question: 'What is the role of `pam_env.so` and which default files does it consult to set environment variables?',
    answer: 'It sets or unsets environment variables for the user session upon login. By default, it parses `/etc/security/pam_env.conf` and `/etc/environment`.',
    definition: 'A session module that establishes standard system-wide environment variables (such as PATH, LANG, or custom variables) during login.',
    example: '# In /etc/security/pam_env.conf:\nJAVA_HOME DEFAULT=/usr/lib/jvm/default-java\nEDITOR    DEFAULT=vim OVERRIDE=nano',
    exampleExplanation: 'Sets JAVA_HOME and default EDITOR for all authenticated user sessions.',
    examTip: '`pam_env.so` does NOT evaluate shell syntax like `export VAR=val`; it reads key-value assignment pairs directly.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3054,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_pwquality.so / pam_cracklib.so',
    question: 'Which PAM modules enforce password strength and complexity rules when users update passwords, and what does `minlen=12` mean?',
    answer: '`pam_pwquality.so` (modern) or `pam_cracklib.so` (legacy). They evaluate candidate passwords against dictionary words, permutations, and character classes. `minlen=12` sets the minimum required password length to 12 characters.',
    definition: 'Password validation modules called in the `password` management group to reject weak or easily guessable passwords.',
    example: 'password required pam_pwquality.so retry=3 minlen=12 dcredit=-1 ucredit=-1 lcredit=-1 ocredit=-1',
    exampleExplanation: 'Requires at least 12 characters including at least 1 digit, 1 uppercase, 1 lowercase, and 1 special character.',
    examTip: 'In pwquality, negative credit values (e.g. `dcredit=-1`) mean that character class is MANDATORY (at least 1 required), whereas positive credit reduces required length.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3055,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_pwquality enforce_for_root',
    question: 'By default, does `pam_pwquality.so` enforce password complexity requirements on the root user? How can this be changed?',
    answer: 'No, root is exempt from password quality checks by default. Adding the `enforce_for_root` option forces root to also comply with all password complexity rules.',
    definition: 'A security hardening option ensuring administrative password updates meet the same complexity standards as regular user accounts.',
    example: 'password required pam_pwquality.so retry=3 minlen=14 enforce_for_root',
    exampleExplanation: 'Forces root to also pick passwords containing at least 14 characters.',
    examTip: 'Configuration can also be managed centrally in `/etc/security/pwquality.conf` without modifying PAM service files directly.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3056,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_time.so and time.conf',
    question: 'How do you restrict user logins to specific days of the week or hours of the day using PAM?',
    answer: 'Use `account required pam_time.so` in the PAM service stack and define access schedules in `/etc/security/time.conf`.',
    definition: 'A time-based account control module that restricts when individual users or groups can log in across specific services and terminals.',
    example: '# In /etc/security/time.conf:\n# <services> ; <ttys> ; <users> ; <times>\nsshd ; * ; !root & student ; Wk0800-1700',
    exampleExplanation: 'Permits student users to access SSH only during weekdays between 08:00 and 17:00.',
    examTip: 'Days in `time.conf`: `Mo`, `Tu`, `We`, `Th`, `Fr`, `Sa`, `Su`, `Wk` (weekdays), `Wd` (weekend). Time is 24-hour HHMM-HHMM.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3057,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_listfile.so',
    question: 'What is `pam_listfile.so` and what arguments does it require to allow or deny users listed in an external file?',
    answer: 'It grants or denies access based on the contents of a flat text file. Required arguments: `item={user|group|tty|rhost|ruser}`, `sense={allow|deny}`, `file=/path/to/file`, and `onerr={succeed|fail}`.',
    definition: 'A versatile PAM module allowing services (like FTP or SSH) to consult a simple list of names (e.g. `/etc/ftpusers` or `/etc/vsftpd.denied_users`).',
    example: 'auth required pam_listfile.so item=user sense=deny file=/etc/ftpusers onerr=succeed',
    exampleExplanation: 'Denies FTP login to any username listed inside `/etc/ftpusers`.',
    examTip: 'The `onerr=succeed` or `onerr=fail` argument tells PAM what to do if the specified file cannot be opened or read.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3058,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_securetty.so and /etc/securetty',
    question: 'What is the purpose of `pam_securetty.so` and which file does it check?',
    answer: 'It restricts direct `root` logins to only secure virtual consoles (ttys) listed inside `/etc/securetty`. If root attempts to log in on a tty not listed (e.g. pseudo-terminals `pts/0` used by SSH/telnet), login is denied.',
    definition: 'A classic security module preventing direct root logins over unencrypted or network-facing terminal lines.',
    example: '# /etc/securetty contents:\nconsole\ntty1\ntty2\ntty3',
    exampleExplanation: 'Root can only log in physically on virtual terminals 1 through 3 and the physical console.',
    examTip: '`pam_securetty.so` only affects root; normal users are completely ignored by this module.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3059,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'include and substack directives',
    question: 'What is the difference between `@include` (or `include`) and `substack` in modern PAM configuration files?',
    answer: '`include` inlines the contents of the target configuration directly into the current chain. `substack` creates an isolated nested sub-chain: a `sufficient` or `die` action inside a substack only terminates the substack, returning control back to the parent stack.',
    definition: 'Directives that promote modularity and DRY (Don\'t Repeat Yourself) design across PAM service files.',
    example: '# In /etc/pam.d/sshd:\nauth substack password-auth\nauth include  postlogin',
    exampleExplanation: 'Executes password-auth as an isolated sub-stack so its internal jumps do not derail sshd-specific rules.',
    examTip: 'Debian/Ubuntu commonly use `@include common-auth`, `@include common-account`, `@include common-password`, `@include common-session`. Red Hat uses `include system-auth` or `substack password-auth`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3060,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_succeed_if.so',
    question: 'What is the purpose of `pam_succeed_if.so` in complex PAM configurations?',
    answer: 'It evaluates specific conditions (such as checking if a user\'s UID is greater than 1000, user is in a specific group, or user belongs to a service) and returns success or failure based on the test.',
    definition: 'A conditional testing module used to dynamically branch or skip authentication checks based on user attributes.',
    example: 'account sufficient pam_succeed_if.so uid < 1000 quiet\naccount required   pam_ldap.so',
    exampleExplanation: 'If the user is a system account (UID < 1000), bypasses remote LDAP account checks immediately.',
    examTip: 'Common flags: `quiet` (suppresses logging), `user in ...`, `uid >= ...`, `ingroup ...`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3061,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_motd.so',
    question: 'Which module displays `/etc/motd` upon interactive login and in which management group is it placed?',
    answer: '`pam_motd.so`, placed in the `session` management group.',
    definition: 'A session setup module that reads and displays message-of-the-day text banners and dynamic motd directory scripts (`/etc/update-motd.d/`).',
    example: 'session optional pam_motd.so motd=/etc/motd',
    exampleExplanation: 'Outputs the banner message to the user\'s terminal when the login session opens.',
    examTip: 'Can accept `motd=/path/to/file` or `noupdate` (disables running `/etc/update-motd.d/` dynamic scripts).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3062,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_systemd.so',
    question: 'What role does `pam_systemd.so` play in modern Linux distributions?',
    answer: 'It registers newly opened user sessions with the systemd login manager (`systemd-logind`), setting up runtime directories (`/run/user/$UID`), cgroups, environment variables (like `XDG_RUNTIME_DIR`), and user slice tracking.',
    definition: 'A session integration module bridging PAM logins to systemd-logind session management.',
    example: '# In /etc/pam.d/system-auth:\nsession optional pam_systemd.so',
    exampleExplanation: 'Registers the login session with `loginctl`, enabling features like graphical seat management and per-user service management.',
    examTip: 'You can verify active sessions registered by pam_systemd using the command `loginctl list-sessions`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3063,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_umask.so',
    question: 'How does `pam_umask.so` control file creation permissions for user sessions?',
    answer: 'It sets the process file mode creation mask (`umask`) for user sessions upon login, reading values from `umask=` module arguments, `/etc/login.defs`, or user GECOS fields.',
    definition: 'A session module that enforces default permissions for new files and directories created during user sessions.',
    example: 'session optional pam_umask.so umask=0027',
    exampleExplanation: 'Ensures new files created in the user session have maximum permissions 0640 and directories 0750.',
    examTip: 'Overrides the static default umask configured in `/etc/profile` or `/etc/bashrc`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3064,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_group.so and group.conf',
    question: 'What is `pam_group.so` and what does it configure based on `/etc/security/group.conf`?',
    answer: 'It dynamically assigns temporary group memberships to a user during session establishment without modifying the permanent `/etc/group` file.',
    definition: 'An authentication/session module granting transient secondary group privileges based on time, terminal, service, or user.',
    example: '# In /etc/security/group.conf:\n# <services> ; <ttys> ; <users> ; <times> ; <groups>\nlogin ; tty* ; * ; Al0800-1800 ; floppy,audio,video',
    exampleExplanation: 'Grants audio and video device group access to console users during daytime hours.',
    examTip: 'Because group assignments are strictly in-memory for the duration of the session, no changes are written to disk.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3065,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_keyinit.so',
    question: 'What is the function of `pam_keyinit.so` in session setup?',
    answer: 'It initializes the Linux kernel session keyring for the newly logged-in user, creating an isolated session keyring or linking to the user\'s personal keyring.',
    definition: 'A session module managing kernel cryptographic keys, Kerberos ticket caches, and encrypted filesystem keys.',
    example: 'session optional pam_keyinit.so force revoke',
    exampleExplanation: 'Ensures a clean session keyring is created on login and revoked upon logout.',
    examTip: 'Commonly used in Kerberos and NFSv4/CIFS credential management environments.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3066,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'Debugging PAM with debug option',
    question: 'How do you enable verbose debug output for a specific PAM module when diagnosing authentication failures?',
    answer: 'Append the `debug` argument to the module line in the `/etc/pam.d/` service file and inspect `/var/log/auth.log` (Debian/Ubuntu) or `/var/log/secure` (RHEL/CentOS).',
    definition: 'A universal module debugging argument supported by nearly all Linux PAM modules to output detailed operational traces.',
    example: '# In /etc/pam.d/sshd:\nauth required pam_unix.so debug',
    exampleExplanation: 'Instructs pam_unix to log verbose execution details, salt values, and internal return codes to the system log.',
    examTip: 'Remember to remove `debug` after troubleshooting, as debug traces can leak sensitive authentication metadata.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3067,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam-auth-update (Debian/Ubuntu)',
    question: 'Which tool on Debian/Ubuntu systems manages central PAM profiles automatically without requiring manual editing of `/etc/pam.d/common-*` files?',
    answer: '`pam-auth-update` (reads profile definitions from `/usr/share/pam-configs/`).',
    definition: 'A menu-driven and non-interactive configuration tool that automatically rebuilds `/etc/pam.d/common-auth`, `common-account`, `common-password`, and `common-session`.',
    example: 'pam-auth-update --package\npam-auth-update',
    exampleExplanation: 'Launches interactive terminal UI to enable or disable SSSD, LDAP, winbind, or local Unix authentication modules.',
    examTip: 'Manual edits to `common-*` files on Debian can be overwritten when `pam-auth-update` runs during package updates unless configured otherwise.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3068,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'authselect (RHEL/CentOS/Fedora)',
    question: 'What is `authselect` and what replaces the legacy `authconfig` utility in modern Red Hat-based systems?',
    answer: '`authselect` is the modern profile-based tool for configuring system authentication and PAM stacks (replacing legacy `authconfig`). It deploys pre-tested profiles like `sssd` or `winbind`.',
    definition: 'A utility that validates and creates PAM and NSS configurations from template profiles.',
    example: 'authselect current\nauthselect select sssd with-mkhomedir --force',
    exampleExplanation: 'Checks the active profile and applies the SSSD profile with automatic home directory creation enabled.',
    examTip: 'LPIC-2 tests both modern `authselect` and legacy `authconfig`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3069,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'PAM security best practices: preventing lockout',
    question: 'What precaution should an administrator always take before editing files in `/etc/pam.d/`?',
    answer: 'Keep an active, authenticated root shell session open in a separate terminal. Never close your working session until you have verified successful login in a new, independent test session.',
    definition: 'A critical operational safety rule to prevent accidental administrative lockout due to PAM syntax or module configuration errors.',
    example: '# Keep root shell open in Window 1\n# In Window 2:\nssh testuser@localhost\nsu - testuser',
    exampleExplanation: 'Verifies that login, su, and sudo still function before terminating the only authenticated session.',
    examTip: 'If all sessions are locked out due to a broken PAM stack, recovery requires rebooting into single-user mode or rescue media (`init=/bin/sh`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3070,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_echo.so',
    question: 'What does `pam_echo.so` do in a PAM configuration stack?',
    answer: 'It prints a static text message or the contents of a file to the user\'s terminal during the PAM interaction phase.',
    definition: 'A notification module used to output warnings, policy reminders, or custom instructions to users during authentication or password changes.',
    example: 'password required pam_echo.so file=/etc/security/password-policy.txt\npassword required pam_pwquality.so',
    exampleExplanation: 'Displays password policy guidelines to the user before prompting them to change their password.',
    examTip: 'Accepts arguments `file=/path` to read from a file, or text strings directly. Always succeeds unless file cannot be read.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },

  // =========================================================================
  // OBJECTIVE 210.3: LDAP Client Usage (Cards 3071-3100) [Weight 2]
  // =========================================================================
  {
    id: 3071,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'LDAP architecture & DIT',
    question: 'What does LDAP stand for, and what is the Directory Information Tree (DIT)?',
    answer: 'Lightweight Directory Access Protocol. The DIT is the hierarchical, inverted tree data structure used by LDAP to organize directory entries, starting from a root/suffix down to individual object leaves.',
    definition: 'An open, vendor-neutral protocol running over TCP/IP (port 389/636) optimized for rapid read, query, and lookup operations.',
    example: '# Root: dc=example,dc=com\n#   -> ou=People\n#        -> uid=jdoe,ou=People,dc=example,dc=com\n#   -> ou=Groups\n#        -> cn=developers,ou=Groups,dc=example,dc=com',
    exampleExplanation: 'Illustrates the inverted tree hierarchy of a typical organizational LDAP directory.',
    examTip: 'Unlike relational databases designed for frequent transactions and writes, LDAP is heavily optimized for fast reads, hierarchical searches, and authentication lookups.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3072,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'DN vs RDN',
    question: 'What is the difference between a Distinguished Name (DN) and a Relative Distinguished Name (RDN) in LDAP?',
    answer: 'An RDN is the single, leftmost component that uniquely identifies an entry relative to its immediate parent (e.g. `uid=jdoe`). A DN is the complete, globally unique path from the root of the DIT to that specific entry (e.g. `uid=jdoe,ou=People,dc=example,dc=com`).',
    definition: 'The addressing conventions in LDAP analogous to a filename (RDN) versus its full absolute filesystem path (DN).',
    example: '# RDN: cn=Alice Smith\n# DN:  cn=Alice Smith,ou=Engineering,dc=corp,dc=net',
    exampleExplanation: 'Shows how an RDN forms the final leaf of an absolute Distinguished Name.',
    examTip: 'DN components are comma-separated and evaluated from left to right (leaf to root).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3073,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'Common LDAP attributes (dc, ou, cn, uid)',
    question: 'What do the common LDAP attribute abbreviations `dc`, `ou`, `cn`, `sn`, and `uid` represent?',
    answer: '`dc`: Domain Component (e.g., `dc=example`). `ou`: Organizational Unit (e.g., `ou=Sales`). `cn`: Common Name (e.g., `cn=Jane Doe`). `sn`: Surname / Last Name (e.g., `sn=Doe`). `uid`: User Identifier (e.g., `uid=jdoe`).',
    definition: 'Standard LDAP attribute types defined in RFC 4519 representing standard naming and directory properties.',
    example: 'dn: uid=bwayne,ou=Security,dc=wayne,dc=enterprises\ncn: Bruce Wayne\nsn: Wayne\nuid: bwayne\nmail: bwayne@wayne.enterprises',
    exampleExplanation: 'A sample LDAP entry exhibiting standard organizational attributes.',
    examTip: 'Remember: `sn` is Surname (Last Name), NOT Social Security Number or Serial Number! `givenName` is First Name.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3074,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'posixAccount and posixGroup objectClasses',
    question: 'Which objectClasses are required to represent standard Linux user accounts and groups in an LDAP directory?',
    answer: '`posixAccount` for user accounts (requires `cn`, `uid`, `uidNumber`, `gidNumber`, `homeDirectory`) and `posixGroup` for groups (requires `cn`, `gidNumber`).',
    definition: 'Schema definitions defined in RFC 2307 allowing Unix/Linux operating systems to store user and group database records inside LDAP.',
    example: 'objectClass: posixAccount\nuid: alice\nuidNumber: 1050\ngidNumber: 1050\nhomeDirectory: /home/alice\nloginShell: /bin/bash\nuserPassword: {SSHA}xyz...',
    exampleExplanation: 'Displays all mandatory POSIX attributes needed for Linux to authenticate and provide an identity for user alice.',
    examTip: '`shadowAccount` is an auxiliary objectClass used if shadow password aging parameters (`shadowLastChange`, `shadowMax`, etc.) are also stored in LDAP.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3075,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: '/etc/ldap/ldap.conf / /etc/openldap/ldap.conf',
    question: 'Where are OpenLDAP client configuration files located, and what do the `URI` and `BASE` directives specify?',
    answer: '`/etc/ldap/ldap.conf` (Debian/Ubuntu) or `/etc/openldap/ldap.conf` (RHEL/CentOS). `URI` specifies the LDAP server connection endpoints (e.g. `ldap://host:389` or `ldaps://host:636`). `BASE` sets the default search base DN.',
    definition: 'The default configuration file controlling default parameters for OpenLDAP client command-line utilities (like ldapsearch, ldapadd).',
    example: 'URI  ldap://ldap.example.com ldaps://ldap2.example.com\nBASE dc=example,dc=com\nTLS_CACERT /etc/ssl/certs/ca-certificates.crt',
    exampleExplanation: 'Configures client tools to connect to ldap.example.com with a default search base of dc=example,dc=com and trusted CA certificate.',
    examTip: 'Multiple space-separated URIs can be specified for failover redundancy.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3076,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'TLS_REQCERT in ldap.conf',
    question: 'What does the `TLS_REQCERT` directive in `ldap.conf` do, and what are its four possible values?',
    answer: 'Specifies how strictly the LDAP client verifies the server\'s SSL/TLS certificate. Values: `never` (do not request or check cert), `allow` (request cert; accept if invalid/missing), `try` (request cert; accept if missing, reject if bad), `demand` / `hard` (strict: reject if missing or invalid).',
    definition: 'A security configuration parameter in OpenLDAP governing certificate validation for TLS sessions.',
    example: 'TLS_REQCERT demand\nTLS_CACERT /etc/ssl/certs/corp-ca.pem',
    exampleExplanation: 'Enforces strict certificate validation: connection fails if server certificate is self-signed or untrusted.',
    examTip: 'In production environments, `demand` (or `hard`) is mandatory to prevent Man-in-the-Middle (MITM) attacks during authentication.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3077,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'ldapsearch -x',
    question: 'What does the `-x` flag do in `ldapsearch` and other OpenLDAP client utilities?',
    answer: '`-x` forces Simple Authentication (username/password or anonymous bind), bypassing SASL (Simple Authentication and Security Layer) negotiation.',
    definition: 'The option to disable SASL mechanisms (GSSAPI/Kerberos, DIGEST-MD5) and use basic LDAP protocol bind authentication.',
    example: 'ldapsearch -x -b "dc=example,dc=com" "(uid=john)"',
    exampleExplanation: 'Performs a simple anonymous or basic query without initiating SASL negotiation.',
    examTip: 'Always remember `-x` on LPIC-2 exams. If omitted, `ldapsearch` defaults to SASL and may fail with "SASL/GSSAPI Error" if Kerberos is not configured.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3078,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'ldapsearch -b and -s',
    question: 'What do the `-b` and `-s` options specify in `ldapsearch`?',
    answer: '`-b <basedn>` sets the starting point (base DN) in the DIT for the search. `-s {base|one|sub}` specifies the search scope: `base` (searches only the base entry itself), `one` (searches one level beneath the base), `sub` (searches the entire subtree below the base; default).',
    definition: 'Core scoping parameters controlling the depth and boundaries of an LDAP query.',
    example: 'ldapsearch -x -b "ou=People,dc=example,dc=com" -s sub "(objectClass=posixAccount)"',
    exampleExplanation: 'Searches all entries located within and below the `ou=People` container for user accounts.',
    examTip: 'Search scopes: `base` = depth 0 (just the target DN); `one` = depth 1 (immediate children); `sub` = entire tree downward.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3079,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'ldapsearch -D and -W / -w',
    question: 'How do you bind as an authenticated user when executing `ldapsearch`?',
    answer: 'Use `-D "<bind-dn>"` to specify the user DN, combined with `-W` (prompts interactively for password) or `-w <password>` (supplies password directly on command line) or `-y <file>` (reads password from file).',
    definition: 'Authentication arguments identifying the caller to the directory server for access-controlled lookups.',
    example: 'ldapsearch -x -H ldap://ldap.example.com -D "cn=admin,dc=example,dc=com" -W -b "dc=example,dc=com"',
    exampleExplanation: 'Prompts the administrator for their password securely and queries the directory as cn=admin.',
    examTip: 'Avoid `-w <password>` in scripts or command history because passwords appear in `ps` process lists and shell history files. Prefer `-W` or `-y <file>`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3080,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'ldapsearch filter syntax',
    question: 'How do you write boolean LDAP search filters using AND (`&`), OR (`|`), and NOT (`!`) operators?',
    answer: 'Operators use prefix Polish notation: `(&(filter1)(filter2))` for AND, `(|(filter1)(filter2))` for OR, and `(!(filter))` for NOT.',
    definition: 'The standard search filter format defined in RFC 4515 used to query LDAP directory entries.',
    example: 'ldapsearch -x -b "dc=example,dc=com" "(&(objectClass=posixAccount)(|(uid=alice)(uid=bob))(!(loginShell=/bin/false)))"',
    exampleExplanation: 'Searches for posixAccount entries where username is alice OR bob, AND their shell is NOT /bin/false.',
    examTip: 'Parentheses around every expression are mandatory. Notice prefix placement: `(&(cond1)(cond2))` not `(cond1 & cond2)`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3081,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'ldapsearch attribute filtering',
    question: 'How do you restrict `ldapsearch` to only display specific attributes (such as email and full name) rather than the entire entry?',
    answer: 'List the desired attribute names at the very end of the `ldapsearch` command, after the search filter.',
    definition: 'Attribute projection in LDAP queries that reduces bandwidth and cleans up terminal output.',
    example: 'ldapsearch -x -b "dc=example,dc=com" "(uid=john)" cn mail telephoneNumber',
    exampleExplanation: 'Queries for user john and only prints their common name, email address, and phone number.',
    examTip: 'To retrieve operational/system attributes (like `createTimestamp` or `entryUUID`), list `+` or specify the attribute name explicitly.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3082,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'ldapsearch -ZZ',
    question: 'What is the purpose of the `-ZZ` flag in `ldapsearch`?',
    answer: '`-ZZ` forces the client to negotiate StartTLS over a standard LDAP connection (port 389). If TLS cannot be established, the command exits immediately with an error.',
    definition: 'An option that guarantees encryption on port 389 by upgrading an unencrypted TCP channel to TLS.',
    example: 'ldapsearch -x -ZZ -H ldap://ldap.example.com -b "dc=example,dc=com"',
    exampleExplanation: 'Connects to port 389, sends StartTLS extended operation, and encrypts communication before sending credentials.',
    examTip: 'A single `-Z` attempts StartTLS but allows fallback to unencrypted if unsupported; `-ZZ` strictly requires StartTLS or fails.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3083,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'LDIF format',
    question: 'What does LDIF stand for, and what format rules govern LDIF files?',
    answer: 'LDAP Data Interchange Format. Plain text format where entries are separated by blank lines, starting with a `dn:` line followed by `attribute: value` pairs. Comments start with `#`.',
    definition: 'The standard ASCII file format (RFC 2849) used to import, export, and modify directory information in LDAP.',
    example: 'dn: uid=clark,ou=People,dc=krypton,dc=org\nobjectClass: top\nobjectClass: person\nobjectClass: posixAccount\ncn: Clark Kent\nsn: Kent\nuid: clark\nuidNumber: 2001\ngidNumber: 2000\nhomeDirectory: /home/clark\nloginShell: /bin/bash',
    exampleExplanation: 'A valid LDIF entry ready for import into an LDAP directory.',
    examTip: 'Lines that begin with a space are treated as continuations of the previous line (line folding).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3084,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'ldapadd',
    question: 'Which utility imports new entries into an LDAP directory from an LDIF file, and what option specifies the input file?',
    answer: '`ldapadd` (or `ldapmodify -a`). The `-f <filename.ldif>` option specifies the file to read.',
    definition: 'The client utility for adding new directory records into an LDAP database.',
    example: 'ldapadd -x -D "cn=admin,dc=example,dc=com" -W -f new_users.ldif',
    exampleExplanation: 'Authenticates as directory administrator and creates all entries defined inside new_users.ldif.',
    examTip: '`ldapadd` is effectively a hard link or wrapper to `ldapmodify -a`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3085,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'ldapmodify changetypes',
    question: 'What are the four `changetype` operations supported in LDIF records when using `ldapmodify`?',
    answer: '1. `add`: Adds a new entry. 2. `delete`: Deletes an existing entry. 3. `modify`: Adds, deletes, or replaces specific attributes within an entry. 4. `modrdn` (or `moddn`): Renames an entry or moves it in the DIT.',
    definition: 'Operation types specified in LDIF modification records to instruct `ldapmodify` how to alter existing directory data.',
    example: 'dn: uid=alice,ou=People,dc=example,dc=com\nchangetype: modify\nreplace: mail\nmail: alice.new@example.com\n-\nadd: telephoneNumber\ntelephoneNumber: +1-555-0199',
    exampleExplanation: 'Replaces the email attribute and adds a phone number to alice\'s directory entry.',
    examTip: 'In an LDIF `changetype: modify` block, individual attribute operations within the same entry must be separated by a hyphen line (`-`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3086,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'ldapdelete',
    question: 'Which utility deletes an entry from an LDAP directory, and how is the target entry specified?',
    answer: '`ldapdelete`. The target is specified by passing its Distinguished Name (DN) as a command argument or in an input file (`-f`).',
    definition: 'The OpenLDAP client tool for removing entries from a directory tree.',
    example: 'ldapdelete -x -D "cn=admin,dc=example,dc=com" -W "uid=baduser,ou=People,dc=example,dc=com"',
    exampleExplanation: 'Removes the specified user entry permanently from the directory.',
    examTip: 'By default, `ldapdelete` cannot delete an entry if it has child entries (sub-tree); you must delete children first or use recursive delete extensions.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3087,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'ldappasswd',
    question: 'Which utility is used to set or change an LDAP user\'s `userPassword` attribute, and what does `-s` do?',
    answer: '`ldappasswd`. The `-s <new-password>` flag sets the new password directly from the command line, while `-S` prompts interactively for the new password.',
    definition: 'A dedicated OpenLDAP tool implementing the LDAP Password Modify extended operation (RFC 3062).',
    example: 'ldappasswd -x -D "cn=admin,dc=example,dc=com" -W -S "uid=alice,ou=People,dc=example,dc=com"',
    exampleExplanation: 'Prompts the admin for their own password (-W), then prompts for alice\'s new password (-S) and hashes it into userPassword.',
    examTip: '`ldappasswd` automatically handles server-side password hashing schemes (e.g. SSHA, SSHA512).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3088,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: '/etc/nsswitch.conf',
    question: 'What is the role of `/etc/nsswitch.conf` in integrating Linux clients with central LDAP directory services?',
    answer: 'It defines the Name Service Switch lookup order for databases such as `passwd`, `group`, and `shadow`. Adding `sss` or `ldap` instructs the C library to look up accounts in LDAP when not found in local `/etc/passwd`.',
    definition: 'The configuration file for GNU C Library name lookups, dictating how system databases query files, DNS, SSSD, or NIS.',
    example: 'passwd: files sss\ngroup:  files sss\nshadow: files sss\nhosts:  files dns',
    exampleExplanation: 'Tells the operating system to check local `/etc/passwd` first, then query SSSD (LDAP) if the user is not found locally.',
    examTip: 'Order matters! `files` should almost always precede `sss` or `ldap` so essential system accounts (root, daemon, bin) remain accessible during network outages.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3089,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'getent command',
    question: 'Which command queries administrative databases (like `passwd` or `group`) respecting `/etc/nsswitch.conf` to verify LDAP user resolution?',
    answer: '`getent passwd <username>` (or `getent group <groupname>`).',
    definition: 'Get entries from Name Service Switch libraries, verifying whether remote LDAP accounts are visible to the local system.',
    example: 'getent passwd alice\n# Output:\n# alice:*:1050:1050:Alice Smith:/home/alice:/bin/bash',
    exampleExplanation: 'Confirms that the system successfully resolved user alice through NSS via local files or remote directory service.',
    examTip: 'Unlike `cat /etc/passwd`, `getent passwd` queries all configured NSS sources including LDAP, SSSD, and Active Directory.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3090,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'SSSD architecture',
    question: 'What is SSSD (System Security Services Daemon) and what primary advantages does it offer over legacy `nss_ldap`?',
    answer: 'SSSD acts as a central caching broker between local identity consumers (PAM/NSS) and remote identity providers (LDAP, Active Directory, Kerberos). Its primary advantage is OFFLINE CREDENTIAL CACHING, connection pooling, and resilient failover.',
    definition: 'A modern Linux system service managing identity and authentication against remote directories with local caching.',
    example: 'systemctl status sssd\ncat /etc/sssd/sssd.conf',
    exampleExplanation: 'Verifies the SSSD daemon is active and managing identity lookups for the system.',
    examTip: 'With SSSD, a laptop user can log in with their corporate LDAP/AD credentials even when disconnected from the corporate network, because SSSD caches credentials locally.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3091,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: '/etc/sssd/sssd.conf permissions',
    question: 'What strict file permission requirement is enforced on `/etc/sssd/sssd.conf`, and what happens if it is not met?',
    answer: '`/etc/sssd/sssd.conf` must be owned by `root:root` and have permissions `0600` (`-rw-------`). If world or group accessible, the SSSD daemon will REFUSE TO START.',
    definition: 'A security safeguard preventing unprivileged users from reading directory service credentials and bind passwords stored in sssd.conf.',
    example: 'chown root:root /etc/sssd/sssd.conf\nchmod 600 /etc/sssd/sssd.conf',
    exampleExplanation: 'Applies mandatory security permissions before starting the SSSD daemon.',
    examTip: 'Classic exam question: If SSSD fails to start immediately after installation, check the file permissions of `/etc/sssd/sssd.conf` first!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3092,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'sssd.conf core parameters',
    question: 'What are the primary configuration directives inside an `[domain/<NAME>]` section of `/etc/sssd/sssd.conf` for an LDAP backend?',
    answer: '`id_provider = ldap`, `auth_provider = ldap`, `ldap_uri = ldap://...`, `ldap_search_base = dc=...`, and `cache_credentials = true`.',
    definition: 'Configuration settings declaring how SSSD queries user identity and verifies credentials against an LDAP directory.',
    example: '[domain/CORP]\nid_provider = ldap\nauth_provider = ldap\nldap_uri = ldap://ldap.example.com\nldap_search_base = dc=example,dc=com\ncache_credentials = true',
    exampleExplanation: 'Configures a domain named CORP that uses LDAP for identity and authentication with offline credential caching.',
    examTip: 'Setting `cache_credentials = true` is what enables offline logins when the LDAP server is unreachable.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3093,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'sssctl command',
    question: 'Which utility inspects SSSD domain status, checks user identity paths, and flushes SSSD cache on modern systems?',
    answer: '`sssctl`. Common subcommands: `sssctl domain-status <domain>`, `sssctl user-checks <user>`, `sssctl cache-expire -E` (clears all cached entries).',
    definition: 'The primary command-line tool for controlling, querying, and managing the System Security Services Daemon.',
    example: 'sssctl domain-status LDAP\nsssctl cache-expire -E\nsssctl user-checks alice',
    exampleExplanation: 'Flushes the SSSD identity cache and verifies user resolution and authentication rules for alice.',
    examTip: 'Older systems used `sss_cache -E` to invalidate the cache; `sssctl` is the modern unified tool.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3094,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'nslcd and nss-pam-ldapd',
    question: 'What is `nslcd` and how does it fit into Linux LDAP client architecture?',
    answer: '`nslcd` is the Name Service LDAP Connection Daemon (from the `nss-pam-ldapd` package). It runs as a local unprivileged daemon that services NSS and PAM requests by querying LDAP according to `/etc/nslcd.conf`.',
    definition: 'A popular alternative to SSSD for forwarding NSS and PAM queries to LDAP without requiring each application process to open its own LDAP connection.',
    example: 'systemctl status nslcd\ncat /etc/nslcd.conf',
    exampleExplanation: 'Displays status of the local nslcd proxy daemon connecting NSS/PAM to LDAP.',
    examTip: 'In `/etc/nsswitch.conf`, `nslcd` uses the `ldap` service keyword (e.g. `passwd: files ldap`), whereas SSSD uses `sss` (e.g. `passwd: files sss`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3095,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'LDAP standard network ports',
    question: 'What are the standard TCP and UDP port numbers for plain LDAP (with StartTLS) versus LDAPS (LDAP over SSL/TLS)?',
    answer: 'Standard LDAP (plain text and StartTLS) uses TCP/UDP port 389. LDAPS (LDAP over SSL/TLS wrapper) uses TCP port 636.',
    definition: 'IANA registered network transport port assignments for directory services.',
    example: 'ss -tulpn | grep -E "389|636"',
    exampleExplanation: 'Checks if directory service ports 389 or 636 are active on the network.',
    examTip: 'StartTLS runs on port 389, starting in plain text before initiating TLS encryption. LDAPS connects directly over TLS on port 636.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3096,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'pam_ldap.so vs pam_sss.so',
    question: 'What is the key difference between `pam_ldap.so` and `pam_sss.so` in PAM configuration files?',
    answer: '`pam_ldap.so` connects directly to the LDAP server for each authentication request (requiring network availability). `pam_sss.so` communicates with the local SSSD daemon via a local UNIX socket, supporting offline caching and unified policy management.',
    definition: 'The two PAM modules used for directory authentication: direct LDAP connection vs SSSD middleware broker.',
    example: '# In /etc/pam.d/common-auth (SSSD):\nauth [success=1 default=ignore] pam_sss.so use_first_pass\nauth required pam_deny.so',
    exampleExplanation: 'Uses SSSD to authenticate users locally via socket rather than opening raw TCP sockets to LDAP.',
    examTip: 'Modern enterprise Linux systems strongly favor `pam_sss.so` because of offline caching and connection resilience.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3097,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'SASL authentication in LDAP',
    question: 'What is SASL (Simple Authentication and Security Layer) and which authentication mechanisms does it support in LDAP?',
    answer: 'SASL is a framework for authentication and data security in network protocols (RFC 4422). Supported mechanisms include `GSSAPI` (Kerberos ticket), `DIGEST-MD5`, `EXTERNAL` (TLS client certificates), and `PLAIN`.',
    definition: 'An authentication negotiation layer enabling advanced multi-protocol authentication schemes in LDAP.',
    example: 'ldapsearch -Y GSSAPI -H ldap://ldap.corp.com -b "dc=corp,dc=com"',
    exampleExplanation: 'Authenticates to the LDAP directory using the user\'s active Kerberos credential ticket via SASL GSSAPI without sending passwords.',
    examTip: 'Use `-Y <mechanism>` to specify a SASL mechanism, or `-x` to bypass SASL and use simple authentication.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3098,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'Anonymous vs Authenticated bind',
    question: 'What is an "anonymous bind" in LDAP, and why do many modern directories restrict or disable it?',
    answer: 'An anonymous bind connects to the directory without supplying a bind DN or password. Modern directories restrict it to prevent unauthorized enumeration of user accounts, email addresses, and organizational structures by attackers.',
    definition: 'An unauthenticated directory session used for public address book lookups or initial authentication discovery.',
    example: 'ldapsearch -x -H ldap://ldap.example.com -b "dc=example,dc=com"',
    exampleExplanation: 'Attempts to query the directory without providing credentials (anonymous bind).',
    examTip: 'If anonymous bind is disabled on the server, `ldapsearch -x` without `-D` will return `LDAP_INAPPROPRIATE_AUTH` (error code 48).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3099,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'pam_mkhomedir with LDAP logins',
    question: 'Why is `pam_mkhomedir.so` almost always configured on Linux LDAP clients?',
    answer: 'Because LDAP users exist on the central directory server and do not have local user home directories pre-created on the client machine. `pam_mkhomedir.so` creates `/home/<username>` on the fly when the LDAP user first logs in.',
    definition: 'The automatic provisioning bridge between centralized identity and local filesystem storage.',
    example: '# In /etc/pam.d/common-session:\nsession optional pam_mkhomedir.so skel=/etc/skel umask=0077',
    exampleExplanation: 'Automatically scaffolds the home directory from `/etc/skel` when an LDAP user logs in for the first time.',
    examTip: 'Without `pam_mkhomedir.so`, an LDAP user will log in to the fallback directory `/` and see "Could not chdir to home directory /home/alice: No such file or directory".',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3100,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.3',
    topicNumber: 210,
    category: 'LDAP Client Usage',
    command: 'Testing LDAP connectivity: nc / openssl s_client',
    question: 'How can you test network connectivity and SSL certificate validity to an LDAPS server from the command line?',
    answer: '`openssl s_client -connect <ldap-server>:636 -showcerts` for LDAPS, or `openssl s_client -connect <ldap-server>:389 -starttls ldap` for StartTLS.',
    definition: 'Diagnostic commands to troubleshoot TLS handshakes, certificate chains, and port reachability before debugging LDAP authentication.',
    example: 'openssl s_client -connect ldap.example.com:636\nopenssl s_client -connect ldap.example.com:389 -starttls ldap',
    exampleExplanation: 'Validates that the server\'s TLS certificate is valid, matches the hostname, and was issued by a trusted CA.',
    examTip: 'If `ldapsearch -ZZ` fails with "TLS: can\'t connect", testing with `openssl s_client` immediately reveals expired certificates or untrusted CA issues.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
];
