import { Flashcard } from '../../types';

/**
 * LPI LPIC-2 Exam 202 - Topic 210: Network Client Management
 * Part 1: Cards 3001 - 3050
 * - Objective 210.1: DHCP Configuration (Cards 3001-3030) [30 Cards] (Weight 2)
 * - Objective 210.2: PAM Authentication - Part 1 (Cards 3031-3050) [20 Cards] (Weight 3)
 */

export const topic210Part1: Flashcard[] = [
  // =========================================================================
  // OBJECTIVE 210.1: DHCP Configuration (Cards 3001-3030) [Weight 2]
  // =========================================================================
  {
    id: 3001,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'dhcpd',
    question: 'What is the primary function of the `dhcpd` daemon and which UDP ports does it utilize for client communication?',
    answer: '`dhcpd` is the ISC Dynamic Host Configuration Protocol daemon that automatically assigns IP addresses and network parameters to clients. It listens on UDP port 67 (DHCP server) and sends responses to UDP port 68 (DHCP client).',
    definition: 'The server-side daemon for the Internet Systems Consortium (ISC) DHCP suite, providing IP address leasing, DNS servers, routers, and PXE boot parameters across IPv4 networks.',
    example: 'systemctl status isc-dhcp-server\nss -ulnp | grep 67',
    exampleExplanation: 'Checks the running state of the DHCP server daemon and verifies that it is actively bound to UDP port 67.',
    examTip: 'Remember the port asymmetry: Server listens on UDP 67; Client listens on UDP 68. For DHCPv6, server is UDP 547 and client is UDP 546.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3002,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: '/etc/dhcp/dhcpd.conf',
    question: 'Where is the main configuration file located for the ISC DHCP server, and what is its basic block syntax for subnets?',
    answer: '`/etc/dhcp/dhcpd.conf` (or `/etc/dhcpd.conf` on older systems). Subnets are defined using the syntax `subnet <network-address> netmask <subnet-mask> { ... }`.',
    definition: 'The primary configuration file containing global declarations, subnet definitions, address pools, option parameters, and static host reservations for `dhcpd`.',
    example: 'subnet 192.168.10.0 netmask 255.255.255.0 {\n  range 192.168.10.100 192.168.10.200;\n  option routers 192.168.10.1;\n}',
    exampleExplanation: 'Defines an address pool for 192.168.10.0/24 offering dynamically assignable addresses from .100 to .200 and setting the default gateway.',
    examTip: 'Every statement inside a configuration block in dhcpd.conf MUST end with a semicolon (`;`). Missing semicolons are the #1 cause of syntax errors.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3003,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'range',
    question: 'How do you define an allocatable dynamic pool of IP addresses within a subnet block in `dhcpd.conf`?',
    answer: 'Using the `range [dynamic-bootp] <start-ip> <end-ip>;` directive inside the corresponding `subnet` declaration.',
    definition: 'The directive specifying the contiguous or bounded span of IPv4 addresses that `dhcpd` can dynamically allocate to unreserved requesting clients.',
    example: 'subnet 10.0.0.0 netmask 255.255.0.0 {\n  range 10.0.1.50 10.0.1.250;\n  range 10.0.2.50 10.0.2.250;\n}',
    exampleExplanation: 'Demonstrates configuring multiple non-overlapping IP address pools within the same subnet definition.',
    examTip: 'You can declare multiple `range` statements inside a single subnet block if you want to partition pool allocations or skip reserved server IPs.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3004,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'option routers',
    question: 'Which `dhcpd.conf` directive supplies clients with the default gateway IP address for their network?',
    answer: '`option routers <ip-address> [, <ip-address> ...];`',
    definition: 'Standard DHCP option (Option 3) that informs the client which default router/gateway to install into its local routing table.',
    example: 'subnet 172.16.0.0 netmask 255.255.255.0 {\n  option routers 172.16.0.1, 172.16.0.2;\n}',
    exampleExplanation: 'Configures 172.16.0.1 as the primary default gateway for clients on the subnet, with .0.2 as secondary router.',
    examTip: 'Note the plural form: `option routers` (not `option router` or `default-gateway`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3005,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'option domain-name-servers',
    question: 'Which directive in `dhcpd.conf` specifies the recursive DNS nameservers that clients should write into their `/etc/resolv.conf`?',
    answer: '`option domain-name-servers <ip1> [, <ip2> ...];`',
    definition: 'DHCP option (Option 6) that delivers a comma-separated list of DNS server IP addresses to clients.',
    example: 'option domain-name-servers 192.168.1.1, 8.8.8.8, 1.1.1.1;',
    exampleExplanation: 'Declares global or per-subnet DNS resolver addresses that clients will configure upon receiving a lease.',
    examTip: 'Ensure you write `domain-name-servers` (plural with hyphens). When set at the top-level of dhcpd.conf, it applies globally to all subnets unless overridden locally.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3006,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'option domain-name',
    question: 'What directive sets the default DNS search domain name given to clients in `dhcpd.conf`?',
    answer: '`option domain-name "<domain.name>";`',
    definition: 'DHCP option (Option 15) specifying the primary domain suffix clients append to unqualified single-label hostnames during DNS resolution.',
    example: 'option domain-name "corp.example.com";',
    exampleExplanation: 'Causes client machines to populate `search corp.example.com` or `domain corp.example.com` into their resolver settings.',
    examTip: 'The domain name string must be enclosed in double quotes (e.g., `"example.com"`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3007,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'default-lease-time / max-lease-time',
    question: 'What is the difference between `default-lease-time` and `max-lease-time` in `dhcpd.conf`?',
    answer: '`default-lease-time` is the lease duration assigned when a client does not request a specific duration. `max-lease-time` is the upper limit cap dhcpd permits even if a client explicitly requests a longer lease. Both are measured in seconds.',
    definition: 'Time configuration parameters controlling the validity period of dynamic DHCP address leases.',
    example: 'default-lease-time 86400; # 24 hours\nmax-lease-time 172800;     # 48 hours',
    exampleExplanation: 'Grants standard clients a 24-hour lease while capping any client requesting a custom lease time at 48 hours.',
    examTip: 'All lease time units in dhcpd.conf are strictly in SECONDS. 86400 = 1 day, 600 = 10 minutes.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3008,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'host reservation',
    question: 'How do you configure a static IP address reservation for a specific host based on its MAC address in `dhcpd.conf`?',
    answer: 'Use a `host <name> { hardware ethernet <mac>; fixed-address <ip>; }` block.',
    definition: 'A static binding between a network interface hardware MAC address and a specific static IPv4 address, ensuring the device always receives the same IP.',
    example: 'host web-printer {\n  hardware ethernet 00:1a:2b:3c:4d:5e;\n  fixed-address 192.168.1.50;\n  option host-name "printer01";\n}',
    exampleExplanation: 'Ensures the network interface with MAC 00:1a:2b:3c:4d:5e always receives 192.168.1.50.',
    examTip: 'The hardware type is almost always `hardware ethernet`. The reserved IP given by `fixed-address` should ideally fall outside any dynamic `range` pool to avoid potential collisions.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3009,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: '/var/lib/dhcp/dhcpd.leases',
    question: 'Which file stores active and expired DHCP client lease records on an ISC DHCP server?',
    answer: '`/var/lib/dhcp/dhcpd.leases` (or `/var/lib/dhcpd/dhcpd.leases` on RHEL/CentOS systems).',
    definition: 'The persistent state database maintained by `dhcpd` recording all issued dynamic leases, client hostnames, MAC addresses, lease start/end timestamps, and binding states.',
    example: 'cat /var/lib/dhcp/dhcpd.leases\n# Look for active leases:\ngrep -E "^lease|client-hostname|hardware ethernet" /var/lib/dhcp/dhcpd.leases',
    exampleExplanation: 'Inspects active lease records recorded by the server daemon.',
    examTip: 'Never edit `dhcpd.leases` while `dhcpd` is running! `dhcpd` periodically rewrites this file from memory and will overwrite external edits.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3010,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'DORA process',
    question: 'What is the four-step handshake sequence used during standard DHCP IPv4 lease negotiation?',
    answer: 'DORA: 1. Discover (DHCPDISCOVER) -> 2. Offer (DHCPOFFER) -> 3. Request (DHCPREQUEST) -> 4. Acknowledge (DHCPACK).',
    definition: 'The standardized client-server message flow defined in RFC 2131 for discovering available servers, reserving an IP, and finalizing a binding lease.',
    example: '# Client broadcasts: DHCPDISCOVER to 255.255.255.255:67\n# Server responds:  DHCPOFFER with offered IP\n# Client broadcasts: DHCPREQUEST accepting the offer\n# Server responds:  DHCPACK confirming lease',
    exampleExplanation: 'Illustrates the four phases of the DORA state machine.',
    examTip: 'The client broadcasts the DHCPREQUEST so other DHCP servers that also sent DHCPOFFERs know their offers were declined and can return their offered IPs to the pool.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3011,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'DHCPNAK / DHCPDECLINE / DHCPRELEASE',
    question: 'What are the purposes of the DHCPNAK, DHCPDECLINE, and DHCPRELEASE message types in DHCP?',
    answer: 'DHCPNAK: Server rejects client request (e.g., requested invalid subnet/expired lease). DHCPDECLINE: Client rejects offered IP because it detected an IP collision via ARP. DHCPRELEASE: Client voluntarily terminates its lease and frees the IP.',
    definition: 'Supplemental DHCP protocol control messages managing lease rejections, address conflicts, and graceful shutdowns.',
    example: 'dhclient -r eth0 # Sends DHCPRELEASE to the server',
    exampleExplanation: 'Releases the currently held IP lease on eth0 so the server can reassign it immediately.',
    examTip: 'If a server receives a DHCPREQUEST for an IP that does not match the subnet or is already taken, it responds with a DHCPNAK (Negative Acknowledgment), forcing the client to restart DORA.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3012,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'DHCPINFORM',
    question: 'What is the purpose of the `DHCPINFORM` message type in DHCP?',
    answer: 'A client sends `DHCPINFORM` when it already has an IP address configured (e.g. static IP) but needs other network configuration parameters from DHCP, such as DNS servers, domain search paths, or NTP servers.',
    definition: 'A DHCP message allowing statically configured or pre-addressed clients to obtain site-wide configuration options without negotiating a dynamic address lease.',
    example: 'dhclient -i eth0 # Can be used to issue an inform query',
    exampleExplanation: 'Prompts the DHCP server to reply with DHCPACK containing configuration options without recording an IP lease.',
    examTip: 'The DHCP server replies to DHCPINFORM with a unicast DHCPACK containing network options without allocating an IP address from its pool.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3013,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'dhclient',
    question: 'Which utility is the standard ISC DHCP client on Linux, and what does the `-r` option accomplish?',
    answer: '`dhclient`. The `-r` flag releases the current lease by transmitting a `DHCPRELEASE` packet to the DHCP server and clearing the interface IP configuration.',
    definition: 'The command-line DHCP client responsible for configuring network interfaces according to leases received from a DHCP server.',
    example: 'dhclient -r eth0\ndhclient -v eth0',
    exampleExplanation: 'First releases any active lease on interface eth0, then requests a fresh lease with verbose output displayed to standard error.',
    examTip: 'To release and immediately re-acquire an address on eth0: `dhclient -r eth0 && dhclient eth0`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3014,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'dhclient -v',
    question: 'What is the benefit of using `dhclient -v` when troubleshooting client network autoconfiguration?',
    answer: 'The `-v` (verbose) flag forces `dhclient` to output detailed log messages directly to standard error, displaying each step of the DORA transaction and the lease details received.',
    definition: 'Verbose execution option for `dhclient` showing real-time DHCPDISCOVER, DHCPOFFER, DHCPREQUEST, and DHCPACK message transmissions.',
    example: 'dhclient -v eth1',
    exampleExplanation: 'Runs dhclient interactively with real-time logs, showing packet exchanges and timeout counters.',
    examTip: 'By default, `dhclient` runs quietly and logs strictly via syslog. Use `-v` in an interactive shell to see immediate progress.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3015,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: '/etc/dhcp/dhclient.conf',
    question: 'Which file configures behavior, requested options, overrides, and fallback leases for the `dhclient` utility?',
    answer: '`/etc/dhcp/dhclient.conf` (or `/etc/dhclient.conf`).',
    definition: 'The client configuration file controlling which DHCP options are requested, required, prepended, superseded, or appended by `dhclient`.',
    example: 'prepend domain-name-servers 127.0.0.1;\nrequest subnet-mask, broadcast-address, time-offset, routers,\n        domain-name, domain-name-servers, host-name;',
    exampleExplanation: 'Directs dhclient to always place 127.0.0.1 at the top of the nameserver list and specifies which options to request in DORA packets.',
    examTip: 'Key directives: `supersede` (overrides server-supplied option), `prepend` (adds values before server options), `append` (adds values after), and `request` (lists requested option codes).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3016,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: '/var/lib/dhcp/dhclient.leases',
    question: 'Where does `dhclient` store its active and past lease history on Debian/Ubuntu and RHEL systems?',
    answer: '`/var/lib/dhcp/dhclient.leases` (or `/var/lib/dhclient/dhclient-<iface>.leases` on Red Hat/CentOS).',
    definition: 'The local client-side lease database recording the client\'s current and previous lease contracts.',
    example: 'cat /var/lib/dhcp/dhclient.leases',
    exampleExplanation: 'Displays lease records containing the assigned IP, server identifier, lease start/expiry times, and subnet options.',
    examTip: 'When rebooting or re-running `dhclient`, the client checks this file and attempts to re-request the previously held IP address first.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3017,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'dhcrelay',
    question: 'What is the purpose of the `dhcrelay` agent in multi-subnet enterprise networks?',
    answer: 'It forwards DHCP broadcast packets across subnets/routers where no local DHCP server exists, relaying them as unicast UDP packets to a centralized DHCP server on another network.',
    definition: 'A DHCP Relay Agent (RFC 3046 / Option 82 capable) that listens for broadcast DHCP traffic on local interfaces and unicasts it to specified DHCP server addresses.',
    example: 'dhcrelay -i eth1 -i eth2 192.168.1.10',
    exampleExplanation: 'Listens for DHCP client broadcasts on eth1 and eth2 and forwards them as unicast packets to the central DHCP server at 192.168.1.10.',
    examTip: 'Because DHCPDISCOVER uses IP broadcast (255.255.255.255) which routers do not forward, `dhcrelay` is required unless the router itself supports "DHCP Helper / IP Helper".',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3018,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'shared-network',
    question: 'When must the `shared-network` declaration be used in `dhcpd.conf`?',
    answer: 'When multiple logical IP subnets share the same physical network segment (broadcast domain / switch VLAN) on a single physical server interface.',
    definition: 'A configuration block enclosing two or more `subnet` definitions that coexist on the same physical link.',
    example: 'shared-network Floor2 {\n  subnet 192.168.1.0 netmask 255.255.255.0 {\n    range 192.168.1.50 192.168.1.250;\n  }\n  subnet 10.0.0.0 netmask 255.255.0.0 {\n    range 10.0.1.1 10.0.1.254;\n  }\n}',
    exampleExplanation: 'Tells dhcpd that both subnets exist on the same physical wire, allowing it to allocate leases from either pool.',
    examTip: 'If `dhcpd` detects an interface configured for a subnet within a shared-network, it knows it can service requests for all subnets declared inside that block.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3019,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'option subnet-mask / option broadcast-address',
    question: 'Which directives in `dhcpd.conf` explicitly provide the client with its subnet mask and broadcast address?',
    answer: '`option subnet-mask <mask-ip>;` and `option broadcast-address <broadcast-ip>;`.',
    definition: 'DHCP options 1 and 28 supplying network layer boundary parameters to the client interface.',
    example: 'subnet 192.168.5.0 netmask 255.255.255.0 {\n  option subnet-mask 255.255.255.0;\n  option broadcast-address 192.168.5.255;\n}',
    exampleExplanation: 'Sets the subnet mask and broadcast address for clients on the 192.168.5.0/24 subnet.',
    examTip: '`dhcpd` will automatically compute these options from the `subnet ... netmask ...` statement if omitted, but explicit declaration allows customization.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3020,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'option ntp-servers',
    question: 'How do you supply network time synchronization server addresses to clients in `dhcpd.conf`?',
    answer: 'Using the directive `option ntp-servers <ip1> [, <ip2> ...];`.',
    definition: 'DHCP Option 42 delivering a list of Network Time Protocol (NTP) servers for clock synchronization.',
    example: 'option ntp-servers 192.168.1.5, 192.168.1.6;',
    exampleExplanation: 'Provides clients with local NTP server IP addresses to configure their system time daemons (e.g. chrony, ntpd, systemd-timesyncd).',
    examTip: 'NTP servers must be specified by IPv4 address, not by DNS hostname, unless special option formats are configured.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3021,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'next-server / filename',
    question: 'Which directives in `dhcpd.conf` are required to support PXE network booting of clients via TFTP?',
    answer: '`next-server <tftp-server-ip>;` (specifies TFTP server) and `filename "<boot-file-path>";` (specifies bootloader file name, such as `pxelinux.0` or `ipxe.efi`).',
    definition: 'BOOTP/DHCP parameters that inform a PXE network-booting BIOS/UEFI client where to download its initial bootstrap loader via TFTP.',
    example: 'subnet 192.168.1.0 netmask 255.255.255.0 {\n  next-server 192.168.1.20;\n  filename "pxelinux.0";\n}',
    exampleExplanation: 'Directs PXE clients to query the TFTP service at 192.168.1.20 for the boot file `pxelinux.0`.',
    examTip: 'If `next-server` is omitted, the PXE client defaults to trying the DHCP server\'s own IP address for TFTP.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3022,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'authoritative',
    question: 'What is the purpose of placing the `authoritative;` directive at the top of `dhcpd.conf`?',
    answer: 'It declares that this DHCP server is the authoritative server for the local network. If a client requests an unassigned or invalid IP for the subnet, an authoritative server immediately sends a DHCPNAK to reset the client configuration.',
    definition: 'A directive asserting official management of the subnet, preventing stale clients from waiting through prolonged timeouts.',
    example: 'ddns-update-style none;\nauthoritative;\n\nsubnet 10.10.0.0 netmask 255.255.255.0 { ... }',
    exampleExplanation: 'Configures dhcpd to reject rogue or out-of-subnet client requests aggressively with DHCPNAK.',
    examTip: 'If `authoritative` is omitted (unauthoritative mode), `dhcpd` will ignore invalid requests from unknown clients instead of sending DHCPNAK, causing long client timeouts.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3023,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'dhcpd -t',
    question: 'Which command checks `dhcpd.conf` for syntax errors without actually starting the DHCP daemon or binding to network ports?',
    answer: '`dhcpd -t` (or `dhcpd -t -cf /path/to/dhcpd.conf`).',
    definition: 'Syntax test mode option for the ISC DHCP server daemon.',
    example: 'dhcpd -t\necho $?',
    exampleExplanation: 'Validates configuration syntax and returns exit status 0 on success, or prints line-number errors on syntax failures.',
    examTip: 'Always run `dhcpd -t` before restarting `isc-dhcp-server` to avoid taking down dynamic network addressing due to typos or missing semicolons.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3024,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'Limiting network interfaces',
    question: 'Where do you specify which physical network interfaces `dhcpd` should listen on in Debian and RHEL systems?',
    answer: 'In Debian/Ubuntu: `/etc/default/isc-dhcp-server` via `INTERFACESv4="eth0"`. In RHEL/CentOS: `/etc/sysconfig/dhcpd` via `DHCPDARGS="eth0"` (or via systemd service unit arguments).',
    definition: 'System environment files used by service startup scripts to restrict `dhcpd` to designated network interfaces.',
    example: '# In /etc/default/isc-dhcp-server:\nINTERFACESv4="eth1 eth2"',
    exampleExplanation: 'Instructs the DHCP daemon to bind and process DHCP traffic only on eth1 and eth2, ignoring eth0.',
    examTip: 'If dhcpd starts on a multi-homed machine without an interface bound to an IP within a declared subnet in dhcpd.conf, it will exit with a fatal error.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3025,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'T1 and T2 timers',
    question: 'What are the DHCP lease renewal timer (T1) and rebind timer (T2), and at what percentages of the lease time do they trigger by default?',
    answer: 'T1 (Renewal timer) triggers at 50% of the lease duration: client unicasts DHCPREQUEST to the leasing server. T2 (Rebind timer) triggers at 87.5% (7/8) of lease: client broadcasts DHCPREQUEST to ANY available DHCP server if T1 failed.',
    definition: 'Standard RFC 2131 timers governing the progression of lease extension attempts before an IP lease expires.',
    example: '# For an 86400s (24h) lease:\n# T1 = 43200s (12h): unicast renewal request\n# T2 = 75600s (21h): broadcast rebind request',
    exampleExplanation: 'Shows when the client begins attempting renewal and when it broadens its request to any reachable DHCP server.',
    examTip: 'Remember: T1 = 50% (unicast to original server), T2 = 87.5% (broadcast to all servers). If lease expires without renewal, client loses the IP.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3026,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'supersede / prepend / append in dhclient.conf',
    question: 'What is the exact distinction between `supersede`, `prepend`, and `append` in `dhclient.conf`?',
    answer: '`supersede`: Replaces the server-supplied option value completely. `prepend`: Places local values before the server-provided list. `append`: Places local values after the server-provided list.',
    definition: 'Option modification statements allowing client-side customization of DHCP-delivered network settings.',
    example: 'supersede domain-name "local.lan";\nprepend domain-name-servers 127.0.0.1;\nappend domain-name-servers 8.8.8.8;',
    exampleExplanation: 'Forces domain name to local.lan, puts 127.0.0.1 at the head of resolv.conf, and adds 8.8.8.8 at the end.',
    examTip: 'Use `prepend domain-name-servers 127.0.0.1;` when running a local caching resolver like unbound or dnsmasq on the client.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3027,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'lease state in dhcpd.leases',
    question: 'What `binding state` values can an address record have inside `/var/lib/dhcp/dhcpd.leases`?',
    answer: '`active` (currently leased to a client), `free` (available for allocation), `abandoned` (client sent DHCPDECLINE indicating IP is already in use by another device), and `backup` (in failover configurations).',
    definition: 'Status tracking indicators in the dhcpd lease database reflecting the lifecycle stage of an IP allocation.',
    example: 'lease 192.168.1.105 {\n  starts 4 2026/09/04 12:00:00;\n  ends 5 2026/09/05 12:00:00;\n  binding state active;\n  hardware ethernet 00:11:22:33:44:55;\n}',
    exampleExplanation: 'A lease entry showing an active binding with its hardware address and validity period.',
    examTip: 'An `abandoned` lease means dhcpd will not re-offer that address until all other addresses are exhausted or ping-check succeeds.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3028,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'dhcpd ping-check',
    question: 'How does the ISC DHCP server verify that an IP address is genuinely unassigned before issuing a DHCPOFFER?',
    answer: 'Using ICMP Echo (ping) check (`ping-check true;`, which is the default). `dhcpd` sends an ICMP echo request to the target IP; if a response is received, the IP is marked as `abandoned` and not offered.',
    definition: 'A pre-allocation collision prevention mechanism used by dhcpd to prevent duplicate IP address assignment.',
    example: 'ping-check false; # Disables ICMP check for faster lease issuance',
    exampleExplanation: 'Shows how to disable ping checks in environments where latency during address assignment must be minimized.',
    examTip: 'If a static device ignores ICMP or firewall drops pings, dhcpd may still offer the IP, causing the client to detect collision and send DHCPDECLINE.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 3029,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'deny unknown-clients',
    question: 'How can you restrict a DHCP server pool so that only devices with explicit `host` declarations receive leases?',
    answer: 'Place the `deny unknown-clients;` directive inside the `subnet` or `pool` declaration in `dhcpd.conf`.',
    definition: 'An access control directive that instructs `dhcpd` to ignore DHCPDISCOVER requests from clients whose MAC addresses do not have a corresponding `host` block.',
    example: 'subnet 10.0.0.0 netmask 255.255.255.0 {\n  deny unknown-clients;\n  range 10.0.0.100 10.0.0.200;\n}',
    exampleExplanation: 'Prevents unauthorized laptops or rogue devices from obtaining a dynamic IP on the 10.0.0.0/24 subnet.',
    examTip: 'Pairs with `allow unknown-clients;` which is the default behavior allowing any device to receive an available IP.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3030,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.1',
    topicNumber: 210,
    category: 'DHCP Configuration',
    command: 'dhclient custom config file',
    question: 'How do you run `dhclient` pointing to a non-standard configuration file and specific lease file?',
    answer: '`dhclient -cf <config-file> -lf <lease-file> <interface>`',
    definition: 'Command-line overrides for `dhclient` designating alternate paths for the configuration file (`-cf`) and lease database file (`-lf`).',
    example: 'dhclient -cf /etc/dhcp/test-client.conf -lf /tmp/test.leases eth0',
    exampleExplanation: 'Runs dhclient on eth0 using test-client.conf and writing lease information into /tmp/test.leases.',
    examTip: '`-cf` = Configuration File; `-lf` = Lease File; `-pf` = PID File; `-r` = Release.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },

  // =========================================================================
  // OBJECTIVE 210.2: PAM Authentication - Part 1 (Cards 3031-3050) [Weight 3]
  // =========================================================================
  {
    id: 3031,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'PAM architecture',
    question: 'What does PAM stand for, and what core problem does it solve in Linux system security?',
    answer: 'Pluggable Authentication Modules. It decouples authentication mechanisms from individual applications, allowing system administrators to modify authentication policies (passwords, 2FA, LDAP, biometric) across all services without recompiling application binaries.',
    definition: 'A modular, layered authentication framework (RFC 86.0) used by programs such as `login`, `sshd`, `su`, `sudo`, and `passwd` to authenticate users.',
    example: 'ls /etc/pam.d/\n# Shows service-specific PAM configuration files like sshd, sudo, login',
    exampleExplanation: 'Each service has a file in `/etc/pam.d/` defining the stack of modules executed during authentication.',
    examTip: 'Applications link against `libpam.so` and make API calls (`pam_authenticate`, etc.), while PAM dynamically loads modules from `/lib/security/` or `/lib64/security/`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3032,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'PAM module types / management groups',
    question: 'What are the four PAM module interface types (management groups), and what does each handle?',
    answer: '1. `auth`: Verifies user identity (password prompt, 2FA token) and sets credentials. 2. `account`: Checks non-auth account validity (expiration, login time, pam_access). 3. `password`: Updates authentication tokens (changing password). 4. `session`: Prepares/tears down user session environment (mounting home dir, resource limits, motd).',
    definition: 'The four distinct operational phases within the PAM framework that a service can invoke.',
    example: '# In /etc/pam.d/sshd:\nauth    required pam_unix.so\naccount required pam_nologin.so\npassword required pam_pwquality.so\nsession required pam_limits.so',
    exampleExplanation: 'Illustrates how the four types correspond to authentication, access rules, credential changing, and environment setup.',
    examTip: 'A single `.so` file (e.g., `pam_unix.so`) can implement multiple module types (auth, account, password, session) with distinct functions in each.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3033,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'required control flag',
    question: 'What is the behavior of the `required` PAM control flag when a module succeeds and when it fails?',
    answer: 'If the module succeeds, execution continues down the stack. If it fails, PAM records a failure status, but STILL CONTINUES executing the remaining modules in the chain before returning failure to the application.',
    definition: 'A control flag indicating that the module must succeed, but failure does not abort the chain immediately (preventing attackers from guessing which check failed based on timing).',
    example: 'auth required pam_unix.so\nauth required pam_warn.so',
    exampleExplanation: 'Even if pam_unix.so fails, pam_warn.so is still called to log the attempt before PAM rejects the user.',
    examTip: 'Contrast `required` with `requisite`: `required` continues testing the rest of the stack after failure; `requisite` aborts immediately.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3034,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'requisite control flag',
    question: 'How does the `requisite` control flag differ from the `required` flag upon module failure?',
    answer: 'When a module marked `requisite` fails, PAM immediately aborts the stack and returns failure to the calling application without executing any subsequent modules in that interface group.',
    definition: 'A control flag requiring immediate success; failure terminates execution of the module chain instantly.',
    example: 'auth requisite pam_nologin.so\nauth required  pam_unix.so',
    exampleExplanation: 'If pam_nologin detects /etc/nologin, it fails immediately—the user is not prompted for a password at all.',
    examTip: 'Use `requisite` when continuing the stack is pointless or computationally expensive (e.g. checking account lockout or system shutdown state before prompting password).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3035,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'sufficient control flag',
    question: 'What occurs when a module marked with the `sufficient` control flag returns SUCCESS?',
    answer: 'If no prior module marked `required` has failed, PAM immediately terminates the stack and returns SUCCESS to the application. If the module fails, its failure is ignored and execution continues down the stack.',
    definition: 'A control flag granting immediate authentication approval upon success provided no previous required module registered a failure.',
    example: 'auth sufficient pam_rootok.so\nauth required   pam_unix.so',
    exampleExplanation: 'If root executes the command (pam_rootok succeeds), access is granted immediately without asking for a password.',
    examTip: 'CRITICAL: If an earlier `required` module in the chain already failed, a succeeding `sufficient` module does NOT grant access; the stack will still eventually return failure.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3036,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'optional control flag',
    question: 'What is the role of the `optional` control flag in a PAM stack?',
    answer: 'The success or failure of the module does not determine the overall result of the chain, UNLESS it is the only module defined for that interface group.',
    definition: 'A control flag used for secondary or logging actions where failure should not block user authentication or access.',
    example: 'session optional pam_motd.so',
    exampleExplanation: 'Displays the message of the day if possible, but failure to read motd does not prevent the user from logging in.',
    examTip: '`optional` modules are commonly used for notifications, environment setting, or statistics collection.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3037,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: '/etc/pam.d/ vs /etc/pam.conf',
    question: 'What is the difference between `/etc/pam.conf` and the `/etc/pam.d/` directory structure?',
    answer: '`/etc/pam.conf` is the legacy monolithic file where the first field specifies the service name (`service type control module args`). `/etc/pam.d/` uses separate per-service files where the file name is the service name and lines omit the service column (`type control module args`).',
    definition: 'The two alternative configuration file conventions used by the PAM subsystem.',
    example: '# In /etc/pam.conf:\nlogin auth required pam_unix.so\n\n# In /etc/pam.d/login:\nauth required pam_unix.so',
    exampleExplanation: 'Contrasts the 4-column syntax in /etc/pam.d/ with the 5-column syntax in /etc/pam.conf.',
    examTip: 'If `/etc/pam.d/` exists, modern PAM implementations ignore `/etc/pam.conf`. Each application has its own file named after the service.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3038,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_unix.so',
    question: 'What is `pam_unix.so` and what commonly used options can be passed to it in `auth` or `password` blocks?',
    answer: 'It is the standard Unix module that authenticates against `/etc/passwd` and `/etc/shadow`. Common options: `nullok` (permits empty passwords), `sha512` (uses SHA-512 hashing), `obscure` (enforces password checks), `remember=N` (prevents reusing past N passwords).',
    definition: 'The traditional standard authentication module providing classic Linux user/password verification, aging, and shadow password management.',
    example: 'auth    required pam_unix.so nullok\npassword required pam_unix.so sha512 shadow remember=5',
    exampleExplanation: 'Allows empty passwords if nullok is specified, and enforces SHA-512 hashing and history tracking on password changes.',
    examTip: 'Without `nullok`, accounts with empty passwords in `/etc/shadow` are automatically blocked from authenticating.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3039,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_deny.so and pam_permit.so',
    question: 'What are `pam_deny.so` and `pam_permit.so` used for in PAM configurations?',
    answer: '`pam_deny.so` always returns failure (used as a fallback lock or default reject rule). `pam_permit.so` always returns success (used for testing or granting unconditional pass for specific groups).',
    definition: 'Primitive stub modules providing unconditional failure or unconditional success across all four PAM interfaces.',
    example: '# /etc/pam.d/other (default fallback service):\nauth    required pam_deny.so\naccount required pam_deny.so',
    exampleExplanation: 'Ensures that any service without an explicit `/etc/pam.d/` configuration file is denied access by default.',
    examTip: 'The `/etc/pam.d/other` file typically invokes `pam_deny.so` to ensure security through default-deny behavior.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3040,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_rootok.so',
    question: 'What does the `pam_rootok.so` module do and where is it commonly used?',
    answer: 'It checks if the real UID of the calling process is 0 (root). If root, it returns SUCCESS; otherwise, it returns failure. Commonly used in `su` and `cron` so root is not prompted for passwords.',
    definition: 'An authentication module verifying root identity to allow passwordless administrative switching.',
    example: '# In /etc/pam.d/su:\nauth sufficient pam_rootok.so\nauth required   pam_unix.so',
    exampleExplanation: 'Allows the root user to run `su username` without having to type the target user\'s password.',
    examTip: 'Notice the control flag: `auth sufficient pam_rootok.so` ensures root gets immediate access without proceeding to pam_unix.so.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3041,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_wheel.so',
    question: 'How do you restrict the use of `su` so that only members of the administrative `wheel` group can become root?',
    answer: 'In `/etc/pam.d/su`, add `auth required pam_wheel.so use_uid`.',
    definition: 'A PAM module that restricts access to the root account to users belonging to the `wheel` (or specified) group.',
    example: '# In /etc/pam.d/su:\nauth required pam_wheel.so group=admin use_uid',
    exampleExplanation: 'Restricts su access to members of the `admin` group using the real UID of the calling user.',
    examTip: 'The `use_uid` parameter instructs `pam_wheel` to check the UID of the user who ran `su`, rather than the terminal/login UID.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3042,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_nologin.so',
    question: 'Which file does `pam_nologin.so` check, and what is the behavior when that file exists?',
    answer: 'It checks for the existence of `/etc/nologin`. If the file exists, non-root users are denied login and the file\'s contents are displayed as the rejection message. Root is still allowed to log in.',
    definition: 'An account management module used during maintenance windows to temporarily block normal users from logging in.',
    example: 'echo "System under emergency maintenance until 14:00" > /etc/nologin\n# Any user trying to login sees this message and is disconnected',
    exampleExplanation: 'Blocks all standard user logins while displaying the maintenance message, leaving root access intact.',
    examTip: 'To restore normal user logins, simply delete `/etc/nologin` (`rm -f /etc/nologin`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3043,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_mkhomedir.so',
    question: 'What is the purpose of `pam_mkhomedir.so` and what arguments does it accept?',
    answer: 'It automatically creates a user\'s home directory upon their first login if it does not already exist. It accepts `skel=<path>` (template directory, default `/etc/skel`) and `umask=<octal>` (permission mask, e.g. `0022` or `0077`).',
    definition: 'A session management module essential in centralized LDAP/Active Directory environments where user directories do not pre-exist locally.',
    example: '# In /etc/pam.d/common-session:\nsession required pam_mkhomedir.so skel=/etc/skel umask=0077',
    exampleExplanation: 'Creates the user home directory on first login with private 0700 permissions copying files from /etc/skel.',
    examTip: 'Must be placed in the `session` interface stack because home directory creation happens as part of setting up the user session.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3044,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_limits.so',
    question: 'Which configuration file is read by `pam_limits.so` to enforce system resource constraints on users and groups?',
    answer: '`/etc/security/limits.conf` and files ending in `.conf` inside `/etc/security/limits.d/`.',
    definition: 'A session module that configures process, memory, open file, and login limits on user logins based on `/etc/security/limits.conf`.',
    example: '# In /etc/pam.d/login:\nsession required pam_limits.so',
    exampleExplanation: 'Applies resource limits when a user logs in via console or SSH.',
    examTip: 'If `pam_limits.so` is missing from `/etc/pam.d/sshd` or `/etc/pam.d/login`, settings in `limits.conf` will NOT take effect!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3045,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: '/etc/security/limits.conf syntax',
    question: 'What is the 4-column syntax used in `/etc/security/limits.conf`?',
    answer: '`<domain> <type> <item> <value>`\n- `domain`: username, `@groupname`, or `*` (everyone)\n- `type`: `soft`, `hard`, or `-` (both)\n- `item`: resource being limited (e.g. `nofile`, `nproc`, `maxlogins`)\n- `value`: numerical limit',
    definition: 'The format for defining resource usage boundaries per user or group in Linux.',
    example: '@developers  hard  nproc   500\n*            soft  nofile  4096\n*            hard  nofile  65536',
    exampleExplanation: 'Limits developers to 500 processes and gives all users a soft limit of 4096 open files with an absolute hard ceiling of 65536.',
    examTip: 'Remember that `@` denotes a group (e.g. `@student`), whereas no prefix means a specific user (e.g. `john`). `*` matches all users except root.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3046,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'soft vs hard limits',
    question: 'What is the difference between a `soft` limit and a `hard` limit in `/etc/security/limits.conf`?',
    answer: 'A `soft` limit is the active default limit currently enforced, which the user can increase up to the hard limit ceiling. A `hard` limit is the maximum ceiling that can only be increased by the root user.',
    definition: 'The two tiers of resource governance in Linux: advisory/flexible threshold (soft) versus absolute administrative ceiling (hard).',
    example: 'ulimit -Sn 2048 # Raises soft open file limit\nulimit -Hn 8192 # Fails for unprivileged user',
    exampleExplanation: 'Regular users can raise their soft limit up to the hard limit, but cannot exceed or raise the hard limit.',
    examTip: 'Using `-` as the type in limits.conf (e.g., `* - nofile 65535`) sets both the soft and hard limits simultaneously.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3047,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'limits.conf items: nofile, nproc, maxlogins',
    question: 'What do the `nofile`, `nproc`, and `maxlogins` items control in `/etc/security/limits.conf`?',
    answer: '`nofile`: Maximum number of open file descriptors per process. `nproc`: Maximum number of processes available to the user. `maxlogins`: Maximum number of concurrent logins allowed for that user/group.',
    definition: 'Common system resource parameters controlled via pam_limits.',
    example: '* hard maxlogins 3\n* soft nproc 1024\n* hard nofile 65535',
    exampleExplanation: 'Restricts users to at most 3 simultaneous sessions, 1024 processes, and 65535 file handles.',
    examTip: 'For database and web servers (PostgreSQL, MySQL, Nginx), increasing `nofile` is often essential to prevent "Too many open files" errors.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3048,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'ulimit',
    question: 'Which shell builtin command views and modifies resource limits for the current shell session, and what do `-a`, `-n`, and `-u` do?',
    answer: '`ulimit`. `-a` displays all current limits; `-n` views/sets the maximum open file descriptors (`nofile`); `-u` views/sets the maximum user process count (`nproc`).',
    definition: 'A shell builtin command for querying and manipulating process resource limits defined by the kernel and pam_limits.',
    example: 'ulimit -a\nulimit -n 8192\nulimit -Hn',
    exampleExplanation: 'Lists all limits, increases soft file limit to 8192, and inspects the current hard file limit.',
    examTip: 'Use `-S` for soft limit and `-H` for hard limit. E.g., `ulimit -Sn` prints the soft file descriptor limit.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 3049,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'pam_tally2 / faillock',
    question: 'What PAM modules are used to track failed login attempts and lock accounts to prevent brute-force attacks?',
    answer: '`pam_tally2.so` (older systems) and `pam_faillock.so` (modern systems). They record failed authentication attempts and temporarily lock accounts after reaching a threshold.',
    definition: 'Security modules that defend against credential brute-forcing by locking accounts after repeated password failures.',
    example: '# /etc/pam.d/system-auth (faillock):\nauth required pam_faillock.so preauth silent deny=5 unlock_time=900\nauth required pam_unix.so\nauth [default=die] pam_faillock.so authfail deny=5 unlock_time=900',
    exampleExplanation: 'Locks account for 900 seconds (15 mins) after 5 consecutive failed login attempts.',
    examTip: 'On LPIC-2, know both utilities: `pam_tally2 --user <name> --reset` and `faillock --user <name> --reset`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 3050,
    deck: 'Topic 210: Network Client Management',
    objectiveId: '210.2',
    topicNumber: 210,
    category: 'PAM Authentication',
    command: 'faillock command',
    question: 'Which command displays failed login attempts and resets account lockouts under `pam_faillock`?',
    answer: '`faillock --user <username>` displays failed attempts and lock status; `faillock --user <username> --reset` clears the failure counter and unlocks the account.',
    definition: 'The administrative command-line utility for querying and resetting the authentication failure record for pam_faillock.',
    example: 'faillock --user alice\nfaillock --user alice --reset',
    exampleExplanation: 'Audits recent failed login attempts for user alice and subsequently unlocks her account.',
    examTip: 'Under legacy `pam_tally2`, the syntax was `pam_tally2 -u <username>` to view and `pam_tally2 -u <username> -r` to reset.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
];
