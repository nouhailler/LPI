import { Flashcard } from '../types';

/**
 * 100 Comprehensive Interactive Flashcards for LPI Exam 102 - Topic 110: Security
 * 
 * Sub-objectives:
 * - 110.1: Perform security administration tasks (Cards 1001-1035) [35 Cards]
 * - 110.2: Setup host security (Cards 1036-1070) [35 Cards]
 * - 110.3: Securing data with encryption (Cards 1071-1100) [30 Cards]
 * Total: 100 Interactive Cards
 */

export const topic110Flashcards: Flashcard[] = [
  // =========================================================================
  // OBJECTIVE 110.1: Perform security administration tasks (Cards 1001-1035) [35 Cards]
  // =========================================================================
  {
    id: 1001,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo & Privilege Delegation',
    command: 'visudo',
    definition: 'Safely edits the `/etc/sudoers` file and `/etc/sudoers.d/` drop-in files with syntax checking and file locking to prevent corruption and accidental administrative lockouts.',
    example: 'sudo visudo\n# Or to edit a specific drop-in file:\nsudo visudo -f /etc/sudoers.d/devops',
    exampleExplanation: 'Locks the sudoers file, opens the configured default editor (via $EDITOR or $VISUAL), and checks syntax upon saving before replacing the active file.',
    examTip: 'NEVER edit `/etc/sudoers` directly with standard text editors like nano or vim without visudo; a single syntax error will break all sudo access across the system.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1002,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo & Privilege Delegation',
    command: 'visudo -c / visudo -f',
    definition: '`visudo -c` checks the syntax of `/etc/sudoers` and included files without editing; `-f <file>` specifies an alternate sudoers file to check or edit.',
    example: 'sudo visudo -c\n# Output: /etc/sudoers: parsed OK\n# /etc/sudoers.d/99-admins: parsed OK\nsudo visudo -c -f /etc/sudoers.d/mycustom',
    exampleExplanation: 'Useful in automated deployment scripts (e.g., Ansible, shell scripts) to validate generated sudoers snippets before applying them.',
    examTip: 'The `-c` (check-only) flag returns exit status 0 if the configuration parses cleanly, and non-zero on syntax errors.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1003,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo Configuration',
    command: '/etc/sudoers Grammar & User Specification',
    definition: 'The standard line syntax in `/etc/sudoers` is: `user/group hosts = (run_as_users : run_as_groups) commands`.',
    example: '# Allow user alice on all hosts as root to run all commands\nalice ALL=(ALL:ALL) ALL\n\n# Allow members of group wheel or sudo full access\n%wheel ALL=(ALL:ALL) ALL\n%sudo  ALL=(ALL:ALL) ALL',
    exampleExplanation: 'Leading `%` signifies a Unix group. `(ALL:ALL)` allows executing commands as any user and any group.',
    examTip: 'Exam question staple: Identify the meaning of `%admin ALL=(root) /usr/bin/apt-get`. It allows members of group `admin` on all hosts to run only `/usr/bin/apt-get` as user `root`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1004,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo Configuration',
    command: 'NOPASSWD Tag in /etc/sudoers',
    definition: 'The `NOPASSWD:` tag disables password authentication prompting for the specific command(s) that follow it in a sudoers rule.',
    example: 'backup ALL=(ALL) NOPASSWD: /usr/bin/rsync, /usr/sbin/tar\n%monitoring ALL=(ALL) NOPASSWD: ALL',
    exampleExplanation: 'Allows automated maintenance accounts or designated groups to execute specific privileged commands without requiring an interactive password.',
    examTip: '`NOPASSWD:` applies to all subsequent commands in the comma-separated list unless overridden by a later `PASSWD:` tag on the same line.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1005,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo Configuration',
    command: 'sudoers Aliases (User_Alias, Runas_Alias, Host_Alias, Cmnd_Alias)',
    definition: 'Sudoers aliases group users, hosts, runas identities, or commands into reusable uppercase identifiers for clean, scalable privilege management.',
    example: 'User_Alias WEBMASTERS = bob, carol, %webdev\nCmnd_Alias WEBOPS = /usr/sbin/nginx, /usr/bin/systemctl restart nginx\nWEBMASTERS ALL=(root) WEBOPS',
    exampleExplanation: 'Aliases MUST be capitalized. Defining commands with absolute paths prevents path spoofing.',
    examTip: 'All Alias names in `/etc/sudoers` MUST consist of uppercase letters, numbers, and underscores (e.g. `NETWORKING_CMDS`, `SERVERS_EAST`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1006,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo Configuration',
    command: '/etc/sudoers.d/ Drop-in Directory',
    definition: 'Directory parsed by `/etc/sudoers` via `#includedir /etc/sudoers.d` (or `@includedir`) to allow modular, package-specific sudo privilege files.',
    example: '# In /etc/sudoers:\n#includedir /etc/sudoers.d\n\n# In /etc/sudoers.d/custom-ansible:\nansible ALL=(ALL) NOPASSWD:ALL',
    exampleExplanation: 'Files in `/etc/sudoers.d/` should NOT have file extensions ending in `~` or containing `.`, or visudo/sudo will ignore them as backup files.',
    examTip: 'Note that `#includedir` is NOT a comment! Even though it begins with `#`, it is an active sudo configuration directive.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1007,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo Configuration',
    command: 'Defaults secure_path, env_reset & env_keep',
    definition: 'Global Defaults directives in `/etc/sudoers` that control the environment variables and execution search path of commands executed via sudo.',
    example: 'Defaults env_reset\nDefaults secure_path="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"\nDefaults env_keep += "SSH_AUTH_SOCK HTTP_PROXY"',
    exampleExplanation: '`secure_path` prevents trojan horse binaries in untrusted user directories; `env_reset` clears potentially malicious user environment variables.',
    examTip: '`env_reset` resets the environment to a minimal set; `env_keep += "VAR"` explicitly preserves listed variables when transitioning to sudo.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 1008,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo Usage',
    command: 'sudo -l',
    definition: 'Lists the allowed (and forbidden) commands and sudo privileges for the invoking user (or a specified user via `-U <username>`).',
    example: 'sudo -l\n# Or to inspect another user:\nsudo -l -U bob',
    exampleExplanation: 'Prints matching User_Alias, Host_Alias, and effective privileges parsed from `/etc/sudoers` and `/etc/sudoers.d/`.',
    examTip: '`sudo -l` is the first command to run when verifying whether a user has been granted proper administrative privileges.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1009,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo Usage',
    command: 'sudo -u / sudo -g / sudo -i / sudo -s',
    definition: 'Options to control the target execution user, group, and shell environment when running commands with sudo.',
    example: 'sudo -u postgres psql\nsudo -g developers touch /var/shared/file\nsudo -i  # Login root shell (reads /root/.profile, /root/.bashrc, sets PWD to /root)\nsudo -s  # Non-login shell ($SHELL, keeps current directory)',
    exampleExplanation: '`-u` runs as specified user; `-i` simulates an initial root login with a clean environment; `-s` runs the user shell specified by $SHELL.',
    examTip: 'Difference between `sudo -i` and `sudo -s`: `-i` simulates a full login shell (loads /root login scripts and changes cwd to /root); `-s` runs the shell without login startup files and preserves current directory.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1010,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo Usage',
    command: 'sudo -k / sudo -K / sudo -v',
    definition: 'Flags controlling the sudo timestamp caching mechanism.',
    example: 'sudo -v  # Validate/refresh user credentials, updating the timestamp without running a command\nsudo -k  # Invalidate/kill the current cached credentials (requires password next run)\nsudo -K  # Fully removes the cached timestamp file completely',
    exampleExplanation: 'By default, sudo caches valid credentials for 5 or 15 minutes. `sudo -k` immediately revokes this cache for security when leaving a workstation.',
    examTip: '`sudo -k` (kill cache) ensures subsequent sudo invocations will prompt for a password immediately.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1011,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo vs Su',
    command: 'su vs su - (su -l / su --login)',
    definition: '`su` (substitute user) switches identity to another account (root by default). `su -` creates a full login shell with the target user’s environment.',
    example: 'su          # Switches to root, but keeps caller environment ($PATH, $HOME, $USER)\nsu -        # Full login shell for root (sets $HOME=/root, $PATH to root system paths)\nsu - alice  # Full login shell for user alice',
    exampleExplanation: 'Using `su` without `-` retains the unprivileged user’s PATH and environment variables, which can lead to command execution hazards.',
    examTip: 'Always prefer `su -` over plain `su` so that root receives standard administrative paths like `/sbin` and `/usr/sbin` and home directory `/root`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1012,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Sudo vs Su',
    command: 'su -c',
    definition: 'Passes a single command string to the target user’s shell for execution, returning immediately without launching an interactive shell session.',
    example: 'su - postgres -c "createuser myapp"\nsu -c "systemctl restart nginx" root',
    exampleExplanation: 'Runs the command under the target user credentials and exits with the target command’s exit code.',
    examTip: '`-c` allows scripting privileged commands using `su` when sudo is not configured or in non-interactive batch scripts.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1013,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Special Permissions',
    command: 'SUID (Set Owner User ID) Bit',
    definition: 'When set on an executable binary (octal `4000`, displayed as `s` in owner execute position `rwsr-xr-x`), the program executes with the privileges of the file owner (typically root) rather than the invoking user.',
    example: 'ls -l /usr/bin/passwd\n# -rwsr-xr-x 1 root root 68208 May 28 2023 /usr/bin/passwd\nchmod u+s /usr/local/bin/mytool\nchmod 4755 /usr/local/bin/mytool',
    exampleExplanation: 'Allows standard users to update `/etc/shadow` through `/usr/bin/passwd` safely without granting them direct root write access.',
    examTip: 'Capital `S` (e.g. `rwSr-xr-x`) indicates SUID is set but the owner does NOT have execute (`x`) permission, which is an ineffective misconfiguration.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1014,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Special Permissions',
    command: 'SGID (Set Group ID) Bit',
    definition: 'On executable files (octal `2000`, `rwxr-sr-x`), runs with the owning group’s privileges. On directories (`drwxrwsr-x`), new files created inside automatically inherit the directory’s group ownership.',
    example: 'chmod g+s /var/shared/devops\nchmod 2775 /var/shared/devops',
    exampleExplanation: 'Crucial for shared group collaboration directories so that all new files created by different users share the same team group ID.',
    examTip: 'SGID on directories ensures group inheritance; SUID on directories has no effect on Linux.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1015,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Special Permissions',
    command: 'Sticky Bit',
    definition: 'When set on a directory (octal `1000`, displayed as `t` in others execute position `drwxrwxrwt`), only the file owner, directory owner, or root can delete or rename files inside.',
    example: 'ls -ld /tmp /var/tmp\n# drwxrwxrwt 24 root root 4096 Sep  2 12:00 /tmp\nchmod +t /shared/public\nchmod 1777 /shared/public',
    exampleExplanation: 'Protects world-writable directories like `/tmp` from malicious users deleting other users’ files.',
    examTip: 'Capital `T` indicates the sticky bit is set on a directory that lacks others execute (`x`) permission.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1016,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Auditing Permissions',
    command: 'find -perm /4000 (Finding SUID Files)',
    definition: 'Uses the `find` utility to scan filesystems for all executable binaries configured with SUID permissions for security auditing.',
    example: 'find / -perm /4000 -type f 2>/dev/null\n# Or finding both SUID and SGID:\nfind / -type f \\( -perm -4000 -o -perm -2000 \\) -exec ls -ld {} \\; 2>/dev/null',
    exampleExplanation: '`-perm /4000` (or `-perm +4000` on older versions) matches if any of the SUID bits are set; redirecting stderr to `/dev/null` hides permission denied noise.',
    examTip: 'Syntax difference in `find -perm`: `4000` (exact match only), `-4000` (all specified bits must match), `/4000` (any of the specified bits match).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1017,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Auditing Permissions',
    command: 'find -nouser / find -nogroup (Orphaned Files)',
    definition: 'Searches for files and directories that do not belong to any valid user ID or group ID registered in `/etc/passwd` or `/etc/group`.',
    example: 'find / -nouser -o -nogroup 2>/dev/null\n# To take action:\nfind /home -nouser -exec ls -ld {} \\;',
    exampleExplanation: 'When accounts are deleted without removing their files (`userdel` without `-r`), orphaned files remain. If a new user is assigned the recycled UID, they gain ownership.',
    examTip: 'Finding `-nouser` or `-nogroup` files is a critical post-user-deletion security audit requirement tested on LPIC-1.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1018,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Auditing Permissions',
    command: 'find -perm /002 (World-Writable Files)',
    definition: 'Searches for files or directories that can be modified by any local user on the system.',
    example: 'find / -type f -perm /002 2>/dev/null\n# Find world-writable directories without sticky bit:\nfind / -type d -perm -0002 ! -perm -1000 2>/dev/null',
    exampleExplanation: 'World-writable files outside of `/tmp` pose serious security risks because unauthorized users or processes can alter configuration files or inject code.',
    examTip: 'A world-writable directory without the sticky bit (`! -perm -1000`) allows any local user to delete critical system files in that folder.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1019,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Resource Limits',
    command: '/etc/security/limits.conf',
    definition: 'The PAM `pam_limits.so` configuration file setting per-user, per-group, or global system resource boundaries (process counts, open files, memory, core dumps).',
    example: '# <domain>    <type>  <item>     <value>\n*             soft    nofile     1024\n*             hard    nofile     65536\n@developers   hard    nproc      200\nbob           hard    maxlogins  2',
    exampleExplanation: 'Domain can be username, group (`@groupname`), or `*` for all. Types are `soft` (user adjustable up to hard limit), `hard` (enforced by kernel/root), or `-` (both).',
    examTip: 'Items in `limits.conf`: `nofile` (open file descriptors), `nproc` (max processes), `core` (core dump size), `maxlogins` (concurrent logins), `fsize` (max file size), `as` (address space memory).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1020,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Resource Limits',
    command: 'limits.conf Drop-in Directory (/etc/security/limits.d/)',
    definition: 'Modular drop-in directory where packages and system administrators place custom `.conf` resource limitation files override or extend `/etc/security/limits.conf`.',
    example: 'cat /etc/security/limits.d/99-nofile.conf\n# File contents:\n* soft nofile 4096\n* hard nofile 65536',
    exampleExplanation: 'Files in `/etc/security/limits.d/` are parsed in alphabetical order, taking precedence over default `/etc/security/limits.conf` entries.',
    examTip: 'Changes in `limits.conf` or `limits.d/` require the user to log out and log back in for PAM to apply new session limits.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1021,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Resource Limits',
    command: 'ulimit',
    definition: 'Bash built-in command used to display or modify user resource limits for the current shell session and its child processes.',
    example: 'ulimit -a         # Display all active limits for current shell\nulimit -n 4096     # Set max open file descriptors to 4096\nulimit -u 500      # Set max process count (nproc) to 500\nulimit -c unlimited # Enable unlimited core dumps for debugging',
    exampleExplanation: 'Regular users can decrease their limits or raise soft limits up to the hard limit; only root can increase hard limits.',
    examTip: 'Key `ulimit` flags tested: `-n` (open files), `-u` (max user processes), `-c` (core file size), `-f` (max file size), `-v` (virtual memory), `-a` (all limits).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1022,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Resource Limits',
    command: 'ulimit -S vs ulimit -H',
    definition: '`-S` selects or sets the soft limit (the current warning/enforced threshold); `-H` selects or sets the hard limit (the absolute ceiling managed by root).',
    example: 'ulimit -Sn   # Show soft limit for open files\nulimit -Hn   # Show hard limit for open files\nulimit -S -n 2048 # Set soft limit to 2048',
    exampleExplanation: 'An unprivileged user can freely change their soft limit between 0 and the hard limit, but cannot exceed or raise the hard limit.',
    examTip: 'Default behavior of `ulimit` with no flag (or when setting values) is to modify the soft limit.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1023,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'User Session Auditing',
    command: 'who',
    definition: 'Displays information about all users currently logged into the Linux system by reading `/var/run/utmp` (or `/run/utmp`).',
    example: 'who\n# Output: alice   pts/0   2026-09-02 08:30 (192.168.1.50)\n#         bob     tty1    2026-09-02 07:15\nwho -b   # Time of last system boot\nwho -r   # Current system runlevel',
    exampleExplanation: 'Shows username, terminal line (tty or pts/N), login timestamp, and remote hostname or IP address.',
    examTip: '`who -b` shows system boot time; `who -r` displays current runlevel / target.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1024,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'User Session Auditing',
    command: 'w',
    definition: 'Displays detailed information about logged-in users and what processes they are currently running, combined with system load averages and uptime.',
    example: 'w\n# 09:15:02 up 10 days,  2:14,  2 users,  load average: 0.08, 0.03, 0.01\n# USER     TTY      FROM             LOGIN@   IDLE   JCPU   PCPU WHAT\n# alice    pts/0    192.168.1.50     08:30    1.00s  0.05s  0.01s vim app.py',
    exampleExplanation: 'Provides richer insight than `who` by including IDLE time, JCPU (time used by all processes on tty), PCPU (CPU time of current WHAT process), and the active command.',
    examTip: 'The `WHAT` column in the `w` command output shows the command currently being executed by each user.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1025,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'User Session Auditing',
    command: 'last',
    definition: 'Reads the binary `/var/log/wtmp` log file to display a chronological history of previous successful user logins, logouts, reboot events, and session durations.',
    example: 'last -n 5\nlast reboot\nlast -f /var/log/wtmp.1  # Read archived wtmp log\nlast -i                  # Display remote IP addresses instead of hostnames',
    exampleExplanation: 'Searches `/var/log/wtmp` backwards to show most recent logins first. Special pseudo-users like `reboot` and `shutdown` track system restarts.',
    examTip: '`last` reads `/var/log/wtmp`. If `/var/log/wtmp` does not exist or is corrupted, `last` will show no login history.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1026,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'User Session Auditing',
    command: 'lastb',
    definition: 'Reads `/var/log/btmp` to display a log of all bad (failed/unsuccessful) login attempts for security intrusion and brute-force auditing.',
    example: 'sudo lastb\n# Output: root     ssh:notty   203.0.113.10   Wed Sep  2 04:12 - 04:12  (00:00)\n#         admin    ssh:notty   203.0.113.10   Wed Sep  2 04:12 - 04:12  (00:00)\nsudo lastb -n 20',
    exampleExplanation: 'Access to `/var/log/btmp` requires root privileges to protect sensitive mis-typed passwords entered as usernames.',
    examTip: 'Exam distinction: `last` reads `/var/log/wtmp` (successful logins), while `lastb` reads `/var/log/btmp` (bad/failed logins).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1027,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'User Session Auditing',
    command: 'lastlog',
    definition: 'Formats and prints the contents of the `/var/log/lastlog` database file showing the most recent login date, time, and port for all local system accounts.',
    example: 'lastlog\n# Show last login for user alice:\nlastlog -u alice\n# Show users who logged in within the last 5 days:\nlastlog -b 5',
    exampleExplanation: 'Displays `**Never logged in**` for system service accounts or dormant users who have never opened an interactive session.',
    examTip: '`lastlog` reads `/var/log/lastlog` (indexed by UID), whereas `last` reads `/var/log/wtmp` (sequential append log).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1028,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Log File Types',
    command: '/var/run/utmp vs /var/log/wtmp vs /var/log/btmp',
    definition: 'The three core binary login accounting database files maintained by glibc on Linux systems.',
    example: '# utmp (/run/utmp): Currently logged in users (read by `who`, `w`, `users`)\n# wtmp (/var/log/wtmp): Historical login/logout/reboot log (read by `last`)\n# btmp (/var/log/btmp): Failed login attempts log (read by `lastb`)',
    exampleExplanation: 'These files are binary format and cannot be viewed directly with `cat` or `less`; dedicated utilities must be used.',
    examTip: 'High-frequency exam matching question: Which file records current sessions (`utmp`), past sessions (`wtmp`), and failed logins (`btmp`)?',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1029,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Account Locking & Management',
    command: 'faillock / pam_faillock (or pam_tally2 / faillog)',
    definition: 'PAM modules and command-line utilities used to track and lock user accounts after excessive consecutive failed password authentication attempts.',
    example: '# Inspect locked status for user alice:\nsudo faillock --user alice\n# Reset failed counters and unlock alice:\nsudo faillock --user alice --reset\n# Older distributions:\nsudo faillog -u alice -r',
    exampleExplanation: 'Defends against local and remote brute-force password guessing attacks by locking the account for a configurable timeout.',
    examTip: '`faillock --reset --user <user>` clears the lockout counter after a user has resolved their password authentication issue.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1030,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Socket Auditing',
    command: 'ss -tulpen / netstat -tulpen',
    definition: 'Displays all active listening TCP and UDP network sockets with PID, process name, numeric port, and user UID for security auditing.',
    example: 'sudo ss -tulpen\n# Flags:\n# -t = TCP\n# -u = UDP\n# -l = Listening sockets only\n# -p = Process name and PID\n# -e = Extended details (UID, inode)\n# -n = Numeric ports/IPs (no DNS/service name resolution)',
    exampleExplanation: 'Auditing listening sockets reveals unauthorized or rogue daemons listening on public network interfaces (`0.0.0.0` or `::`).',
    examTip: 'Look for daemons binding to `0.0.0.0` (all interfaces) vs `127.0.0.1` (localhost only). Services intended for internal use only must not bind to external IPs.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1031,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Process Auditing',
    command: 'lsof -i / lsof -p / fuser',
    definition: '`lsof` (list open files) and `fuser` identify which processes, users, or network connections have opened specific files, ports, or filesystems.',
    example: 'sudo lsof -i :22         # Find process listening or connected on port 22\nsudo lsof -i TCP:80      # Find HTTP processes\nsudo lsof -u alice       # List all files opened by user alice\nsudo fuser -v 80/tcp     # Find PID using port 80\nsudo fuser -km /mnt/usb  # Kill all processes holding /mnt/usb open',
    exampleExplanation: 'Essential forensic commands when investigating suspicious socket connections or unmounting locked filesystem mounts.',
    examTip: '`lsof -i :<port>` quickly determines the exact process name, PID, and user bound to a specific network port.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1032,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Account Password Security',
    command: 'chage / /etc/shadow Password Aging Fields',
    definition: 'Manages user password expiration, warning periods, inactivity locking, and account disabling dates in `/etc/shadow`.',
    example: 'sudo chage -l alice          # List password aging policy for alice\nsudo chage -M 90 -m 7 -W 14 alice # Max 90 days, min 7 days, 14 days warning\nsudo chage -E 2026-12-31 bob # Set explicit account expiration date',
    exampleExplanation: '`/etc/shadow` fields: `username:password:lastchanged:min:max:warn:inactive:expire:reserved`.',
    examTip: '`chage -l <user>` displays current aging values in human-readable format without having to manually parse `/etc/shadow` epoch timestamps.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1033,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Account Password Security',
    command: 'passwd -l / passwd -u / passwd -S',
    definition: 'Locks, unlocks, and inspects account password status using the `passwd` command.',
    example: 'sudo passwd -l alice  # Locks account (prepends ! to password hash in /etc/shadow)\nsudo passwd -u alice  # Unlocks account (removes leading !)\nsudo passwd -S alice  # Displays password status (P=active, L=locked, NP=no password)',
    exampleExplanation: 'Prepend `!` in `/etc/shadow` invalidates the password hash so password authentication fails, but SSH public key logins may still function unless disabled.',
    examTip: '`usermod -L` and `passwd -l` lock passwords; `usermod -U` and `passwd -u` unlock passwords.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1034,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'Account Shell Security',
    command: '/sbin/nologin vs /bin/false',
    definition: 'Restricted shell binaries assigned to service accounts or locked users in `/etc/passwd` to prevent interactive shell logins.',
    example: 'sudo usermod -s /sbin/nologin nginx\nsudo usermod -s /bin/false ftpuser',
    exampleExplanation: '`/sbin/nologin` prints a polite notification ("This account is currently not available.") before exiting with code 1; `/bin/false` immediately exits silently with status 1.',
    examTip: 'Both `/sbin/nologin` (or `/usr/sbin/nologin`) and `/bin/false` reject shell access, but `nologin` displays a message from `/etc/nologin.txt` if configured.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1035,
    deck: 'Topic 110: Security',
    objectiveId: '110.1',
    topicNumber: 110,
    category: 'System Login Banners',
    command: '/etc/issue vs /etc/issue.net vs /etc/motd',
    definition: 'Text banner files displayed to users at different stages of local and remote system access.',
    example: '# /etc/issue: Displayed before login on local physical consoles (TTYs)\n# /etc/issue.net: Displayed before login over Telnet / SSH (if configured)\n# /etc/motd: Message of the Day displayed AFTER successful authentication',
    exampleExplanation: 'Security compliance policies use `/etc/issue` and `/etc/issue.net` to display legal authorization warnings prior to credential entry.',
    examTip: 'Key distinction: `/etc/issue` and `/etc/issue.net` appear BEFORE login; `/etc/motd` appears AFTER successful login.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },

  // =========================================================================
  // OBJECTIVE 110.2: Setup host security (Cards 1036-1070) [35 Cards]
  // =========================================================================
  {
    id: 1036,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'TCP Wrappers',
    command: 'TCP Wrappers Architecture (/etc/hosts.allow & /etc/hosts.deny)',
    definition: 'Host-based access control subsystem (using `libwrap.so`) that filters incoming network connection requests for wrapper-compiled server daemons based on client IP or hostname.',
    example: '# Check if sshd supports TCP wrappers:\nldd $(which sshd) | grep libwrap',
    exampleExplanation: 'When a connection arrives, libwrap intercepts the call and checks `/etc/hosts.allow` and `/etc/hosts.deny` before passing control to the daemon.',
    examTip: 'TCP wrappers operate at Layer 7 (Application) by inspecting client IP/hostname before accepting client interaction.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1037,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'TCP Wrappers',
    command: 'TCP Wrappers Evaluation Order Rule',
    definition: 'Strict 3-step decision flow evaluated in sequence for every incoming connection request.',
    example: '# 1. Check /etc/hosts.allow -> If match found, GRANT ACCESS immediately.\n# 2. Check /etc/hosts.deny  -> If match found, DENY ACCESS immediately.\n# 3. If no match in either  -> GRANT ACCESS by default.',
    exampleExplanation: 'Evaluation halts at the first matching rule. `hosts.allow` always overrides `hosts.deny`.',
    examTip: 'Critical exam rule: If a client IP matches a rule in BOTH `/etc/hosts.allow` and `/etc/hosts.deny`, ACCESS IS GRANTED because `hosts.allow` is evaluated first.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1038,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'TCP Wrappers',
    command: 'TCP Wrappers Default-Deny Configuration',
    definition: 'Industry standard hardening practice that denies all traffic by default and explicitly whitelists only trusted clients.',
    example: '# In /etc/hosts.deny:\nALL : ALL\n\n# In /etc/hosts.allow:\nsshd : 192.168.1.0/255.255.255.0, 10.0.0.5',
    exampleExplanation: 'Setting `ALL : ALL` in `hosts.deny` blocks all connections except those explicitly permitted in `hosts.allow`.',
    examTip: 'In TCP wrappers subnet specifications, use full netmask notation (e.g. `192.168.1.0/255.255.255.0` or `192.168.1.`) rather than CIDR slash notation (`/24`), as classic libwrap does not support CIDR `/24`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1039,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'TCP Wrappers',
    command: 'TCP Wrappers Rule Syntax & Wildcards',
    definition: 'Line structure: `daemon_list : client_list [: option : option...]` with special wildcards `ALL`, `LOCAL`, `KNOWN`, `UNKNOWN`, and `PARANOID`.',
    example: '# In /etc/hosts.allow:\nsshd, vsftpd : .example.com EXCEPT bad.example.com\nin.tftpd : LOCAL\nALL : PARANOID',
    exampleExplanation: '`LOCAL` matches any host without a dot in its hostname. `PARANOID` matches when forward DNS lookup does not match reverse DNS (PTR) lookup.',
    examTip: '`EXCEPT` operator creates exceptions: `ALL : 192.168.1. EXCEPT 192.168.1.50` allows all IPs in 192.168.1.0/24 except 192.168.1.50.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1040,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'TCP Wrappers',
    command: 'TCP Wrappers spawn & twist Directives',
    definition: 'Advanced options executed upon matching a TCP wrapper rule.',
    example: '# In /etc/hosts.deny:\nsshd : ALL : spawn (echo "Unauthorized access attempt from %h (%a) to %d" | mail -s "Alert" admin@example.com)\n\nin.telnetd : ALL : twist (echo "Telnet is disabled. Connect via SSH.")',
    exampleExplanation: '`spawn` executes a shell command in the background and continues; `twist` replaces the service request with specified message/command.',
    examTip: 'Expansion tokens in TCP wrappers: `%a` (client IP address), `%h` (client hostname), `%d` (daemon process name), `%u` (client username via ident).',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 1041,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Firewall Architecture',
    command: 'Netfilter Architecture (Kernel Packet Filtering)',
    definition: 'The Linux kernel subsystem that provides packet filtering, network address translation (NAT), and packet mangling hooks.',
    example: '# User-space tools that interact with Netfilter:\n# 1. iptables (legacy)\n# 2. nftables / nft (modern replacement)\n# 3. ufw / firewalld (high-level frontends)',
    exampleExplanation: 'Netfilter inspects network packets at the kernel level across five hooks: PREROUTING, INPUT, FORWARD, OUTPUT, POSTROUTING.',
    examTip: '`iptables` and `nftables` are user-space frontends; `netfilter` is the underlying Linux kernel packet processing engine.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1042,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'iptables Tables',
    command: 'iptables Tables (filter, nat, mangle, raw, security)',
    definition: 'The default functional tables in `iptables` that group specific packet inspection chains.',
    example: 'iptables -t filter -L  # Default table: packet filtering (INPUT, OUTPUT, FORWARD)\niptables -t nat -L     # Network Address Translation (PREROUTING, POSTROUTING, OUTPUT)\niptables -t mangle -L  # Packet header modification (TTL, TOS, MARK)',
    exampleExplanation: 'If no `-t <table>` flag is specified, `iptables` defaults to the `filter` table.',
    examTip: 'Default iptables table is `filter`. Memorize its 3 built-in chains: `INPUT`, `OUTPUT`, and `FORWARD`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1043,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'iptables Chains',
    command: 'filter Table Built-in Chains (INPUT, OUTPUT, FORWARD)',
    definition: 'The 3 core chains of the `filter` table that determine packet routing direction.',
    example: '# INPUT: Packets addressed directly to the local host machine\n# OUTPUT: Packets originated locally by processes on the host\n# FORWARD: Packets routed through the host from one interface to another',
    exampleExplanation: 'A standard desktop or server evaluates `INPUT` and `OUTPUT`; a Linux router forwarding packets between subnets evaluates `FORWARD`.',
    examTip: 'Packets passing through a Linux machine configured as a router or gateway are processed by the `FORWARD` chain, NOT `INPUT`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1044,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'iptables Targets',
    command: 'iptables Targets (ACCEPT, DROP, REJECT, LOG)',
    definition: 'Actions assigned to matching packets using the `-j` (jump) parameter in iptables rules.',
    example: 'iptables -A INPUT -p tcp --dport 22 -j ACCEPT\niptables -A INPUT -p tcp --dport 23 -j DROP\niptables -A INPUT -p tcp --dport 21 -j REJECT --reject-with tcp-reset\niptables -A INPUT -j LOG --log-prefix "FIREWALL-DROP: "',
    exampleExplanation: '`DROP` silently discards packets (client times out); `REJECT` discards packet and returns an ICMP port-unreachable or TCP RST error to the sender.',
    examTip: 'Difference between DROP and REJECT: DROP sends no response (stealth), while REJECT immediately informs the sender with an error packet.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1045,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'iptables Management',
    command: 'iptables -L / -v / -n / --line-numbers',
    definition: 'Commands to list and inspect active iptables firewall rules.',
    example: 'sudo iptables -L\nsudo iptables -L -v -n\nsudo iptables -L INPUT -n --line-numbers',
    exampleExplanation: '`-v` shows packet and byte counters; `-n` prints IP addresses and ports numerically (avoids slow reverse DNS lookups); `--line-numbers` adds rule index numbers for easy deletion.',
    examTip: 'Always use `-n` with `-L` in production to prevent `iptables` from freezing while attempting reverse DNS lookups on unresolvable IPs.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1046,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'iptables Management',
    command: 'iptables -P (Default Chain Policy)',
    definition: 'Sets the default policy (target action) for a chain when a packet traverses the entire chain without matching any specific rule.',
    example: 'sudo iptables -P INPUT DROP\nsudo iptables -P FORWARD DROP\nsudo iptables -P OUTPUT ACCEPT',
    exampleExplanation: 'A secure firewall sets the default `INPUT` policy to `DROP` (default-deny) and explicitly allows only required inbound ports.',
    examTip: 'Warning: Running `iptables -P INPUT DROP` over an SSH connection without first creating an ACCEPT rule for port 22 will instantly lock you out!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1047,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'iptables Management',
    command: 'iptables -F / -X / -Z',
    definition: 'Commands used to flush rules, delete custom user chains, and zero packet counters.',
    example: 'sudo iptables -F         # Flush (delete) all rules in the current table\nsudo iptables -t nat -F  # Flush rules in the NAT table\nsudo iptables -X         # Delete user-defined chains\nsudo iptables -Z         # Zero packet and byte counters',
    exampleExplanation: '`iptables -F` removes all rules within chains but does NOT reset the chain default policy (`-P`).',
    examTip: 'Flushing an `INPUT` chain whose default policy is set to `DROP` will block all inbound traffic including SSH.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1048,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'iptables Rule Manipulation',
    command: 'iptables -A vs -I vs -D vs -R',
    definition: 'Flags controlling rule placement within an iptables chain.',
    example: 'sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT  # Append to end of chain\nsudo iptables -I INPUT 1 -p tcp --dport 22 -j ACCEPT # Insert as Rule #1 (top)\nsudo iptables -D INPUT 3                             # Delete rule number 3\nsudo iptables -R INPUT 2 -p tcp --dport 443 -j ACCEPT # Replace rule number 2',
    exampleExplanation: '`iptables` evaluates rules top-to-bottom and stops at the first match. Use `-I` to place high-priority rules at the beginning.',
    examTip: '`-A` appends to the bottom; `-I <chain> <pos>` inserts at specified index (defaults to index 1, top of chain if index omitted).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1049,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'iptables Rule Syntax',
    command: 'iptables Filtering Options (-p, -s, -d, -i, -o, --dport, --sport)',
    definition: 'Packet header matching parameters used when constructing iptables rules.',
    example: 'sudo iptables -A INPUT -i eth0 -p tcp -s 192.168.1.0/24 --dport 22 -j ACCEPT\nsudo iptables -A OUTPUT -o eth0 -p udp -d 8.8.8.8 --dport 53 -j ACCEPT',
    exampleExplanation: '`-i` (in-interface), `-o` (out-interface), `-s` (source IP/CIDR), `-d` (destination IP/CIDR), `-p` (protocol: tcp, udp, icmp), `--dport` (destination port), `--sport` (source port).',
    examTip: 'You cannot use `--dport` or `--sport` without first specifying the protocol with `-p tcp` or `-p udp`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1050,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Stateful Filtering',
    command: 'iptables Connection Tracking (-m conntrack / -m state)',
    definition: 'Enables stateful inspection by tracking connection states (NEW, ESTABLISHED, RELATED, INVALID).',
    example: 'sudo iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT\nsudo iptables -A INPUT -m conntrack --ctstate INVALID -j DROP\n# Older syntax:\nsudo iptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT',
    exampleExplanation: 'Allows return traffic for established outgoing sessions (e.g. web browsing, DNS responses) without opening all incoming ports.',
    examTip: '`ESTABLISHED` matches packets belonging to an existing 2-way session; `RELATED` matches packets initiating a new connection associated with an existing session (e.g. FTP data or ICMP errors).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1051,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'iptables Persistence',
    command: 'iptables-save & iptables-restore',
    definition: 'Dumps active memory-resident iptables rules to standard output or restores rules from a text file, ensuring firewall rules survive reboots.',
    example: 'sudo iptables-save > /etc/iptables/rules.v4\nsudo ip6tables-save > /etc/iptables/rules.v6\nsudo iptables-restore < /etc/iptables/rules.v4',
    exampleExplanation: 'Changes made with `iptables` live only in volatile kernel RAM. On Debian/Ubuntu, the `iptables-persistent` package automates restore during boot.',
    examTip: '`iptables-save` exports to stdout; `iptables-restore` reads from stdin. Both require redirection (`>` or `<`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1052,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Modern Firewall',
    command: 'nftables & nft Command',
    definition: 'The modern successor to iptables, ip6tables, arptables, and ebtables, providing a unified syntax and higher-performance kernel bytecode engine.',
    example: 'sudo nft list ruleset\nsudo nft add table inet my_table\nsudo nft add chain inet my_table my_input { type filter hook input priority 0 \\; }\nsudo nft add rule inet my_table my_input tcp dport 22 accept\nsudo nft -f /etc/nftables.conf',
    exampleExplanation: 'The `inet` address family handles both IPv4 and IPv6 rules simultaneously in a single table.',
    examTip: '`nftables` replaces the separate `iptables` (IPv4) and `ip6tables` (IPv6) tools with a unified `nft` utility.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1053,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'UFW Firewall',
    command: 'ufw (Uncomplicated Firewall)',
    definition: 'User-friendly CLI frontend for netfilter/iptables primarily used on Debian and Ubuntu distributions.',
    example: 'sudo ufw status verbose\nsudo ufw default deny incoming\nsudo ufw default allow outgoing\nsudo ufw allow 22/tcp\nsudo ufw allow http\nsudo ufw enable\nsudo ufw disable',
    exampleExplanation: 'Simplifies complex iptables syntax into intuitive high-level commands like `ufw allow ssh`.',
    examTip: '`ufw enable` starts the firewall and enables the systemd service to start on subsequent system boots.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1054,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'UFW Firewall',
    command: 'ufw Rule Management (delete, reject, from, app profiles)',
    definition: 'Advanced rule configuration and management with UFW.',
    example: 'sudo ufw status numbered\nsudo ufw delete 3\nsudo ufw allow from 192.168.1.100 to any port 22 proto tcp\nsudo ufw deny proto tcp from 10.0.0.0/8 to any port 23\nsudo ufw app list\nsudo ufw allow "Nginx Full"',
    exampleExplanation: '`ufw status numbered` allows precise rule deletion by index number.',
    examTip: 'To delete a rule by number: `ufw status numbered` followed by `ufw delete <number>`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1055,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'firewalld',
    command: 'firewalld & firewall-cmd',
    definition: 'Dynamically managed firewall daemon with D-Bus interface and zone-based network abstraction used on RHEL, CentOS, Fedora, and openSUSE.',
    example: 'sudo firewall-cmd --state\nsudo firewall-cmd --get-active-zones\nsudo firewall-cmd --zone=public --list-all\nsudo firewall-cmd --permanent --zone=public --add-service=https\nsudo firewall-cmd --permanent --zone=public --add-port=8080/tcp\nsudo firewall-cmd --reload',
    exampleExplanation: 'Rules added without `--permanent` take effect immediately in runtime memory but are lost on reload/reboot. `--permanent` plus `--reload` ensures persistence.',
    examTip: 'To make firewalld changes permanent: Use `--permanent` AND reload the daemon with `firewall-cmd --reload`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1056,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'firewalld Zones',
    command: 'firewalld Predefined Zones',
    definition: 'Preconfigured security trust levels assigned to network interfaces or source IP subnets.',
    example: '# Predefined zones from most restrictive to least:\n# drop: Drops all incoming packets silently\n# block: Rejects all incoming packets with ICMP errors\n# public: Untrusted public networks (minimal allowed services)\n# dmz: Demilitarized zone with isolated publicly accessible services\n# internal: Internal trusted LAN networks\n# trusted: All incoming traffic is ACCEPTED',
    exampleExplanation: 'Interfaces (e.g. `eth0`) are assigned to zones (`--zone=internal --change-interface=eth0`).',
    examTip: 'The default zone for newly detected network interfaces in firewalld is `public`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1057,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Service Hardening',
    command: 'systemctl disable vs systemctl mask',
    definition: 'Hardening techniques to prevent unneeded, obsolete, or insecure daemons from starting.',
    example: 'sudo systemctl stop telnet.socket\nsudo systemctl disable telnet.socket\n# Strongest hardening prevention:\nsudo systemctl mask telnet.socket\n# Links /etc/systemd/system/telnet.socket -> /dev/null',
    exampleExplanation: 'A disabled service can still be started manually or pulled in as a dependency by another service; a `masked` service links to `/dev/null` and cannot start under any condition.',
    examTip: '`systemctl mask <unit>` creates a symlink to `/dev/null`, rendering it impossible to start even if activated manually or by another dependency.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1058,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Service Hardening',
    command: 'systemctl unmask',
    definition: 'Removes the `/dev/null` symlink for a masked unit, restoring normal start and enable capabilities.',
    example: 'sudo systemctl unmask nginx.service\nsudo systemctl enable --now nginx.service',
    exampleExplanation: 'Deletes the override symlink in `/etc/systemd/system/` that pointed to `/dev/null`.',
    examTip: 'To restore a masked service: `systemctl unmask <unit_name>`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1059,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Super-Server Security',
    command: 'xinetd / inetd & Socket Activation',
    definition: 'Internet super-servers that listen on multiple network ports on behalf of dormant daemons, launching the specific service process only when an incoming connection arrives.',
    example: '# Sample /etc/xinetd.d/tftp file:\nservice tftp\n{\n    socket_type = dgram\n    protocol    = udp\n    wait        = yes\n    user        = root\n    server      = /usr/sbin/in.tftpd\n    disable     = yes  # Hardening: keep disabled\n}',
    exampleExplanation: 'Reduces memory footprint and attack surface by keeping daemons inactive until needed. Modern systemd achieves this with `.socket` units.',
    examTip: 'In xinetd service configuration files, `disable = yes` turns the service OFF, while `disable = no` turns the service ON.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1060,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Super-Server Security',
    command: 'xinetd Security Directives (only_from, no_access, cps, instances)',
    definition: 'Built-in security attributes configured inside `/etc/xinetd.conf` or `/etc/xinetd.d/*` service files.',
    example: 'only_from = 192.168.1.0/24 10.0.0.5\nno_access = 192.168.1.50\ninstances = 10     # Max total concurrent daemon processes\ncps       = 25 30  # Max 25 connections per second, or disable for 30 seconds',
    exampleExplanation: '`only_from` and `no_access` provide host-level filtering; `cps` provides rate-limiting against DoS attacks.',
    examTip: '`only_from` specifies allowed client addresses; `no_access` specifies blocked client addresses.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1061,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Port Scanning & Auditing',
    command: 'nmap (Network Mapper)',
    definition: 'Security scanner used to discover live hosts, open network ports, running services, and OS versions across networks.',
    example: 'nmap -sT 192.168.1.1       # TCP connect scan\nnmap -sS 192.168.1.1       # SYN stealth scan (default as root)\nnmap -sU 192.168.1.1       # UDP port scan\nnmap -p 1-1024 192.168.1.1 # Scan specific port range\nnmap -sV 192.168.1.1       # Service version detection',
    exampleExplanation: 'Used by system administrators to audit their own perimeter firewalls and ensure no unintended ports are accessible.',
    examTip: '`-sS` (SYN scan) is faster and stealthier because it sends a SYN and resets (RST) before completing the 3-way TCP handshake.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1062,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Kernel Hardening',
    command: 'sysctl / /etc/sysctl.conf (Network Security Parameters)',
    definition: 'Configures runtime Linux kernel parameters related to network hardening, IP forwarding, and ICMP flood/redirect defense.',
    example: '# In /etc/sysctl.conf (or /etc/sysctl.d/99-security.conf):\nnet.ipv4.ip_forward = 0                     # Disable packet routing\nnet.ipv4.icmp_echo_ignore_broadcasts = 1   # Prevent Smurf DDoS attacks\nnet.ipv4.conf.all.accept_source_route = 0  # Ignore source-routed packets\nnet.ipv4.conf.all.accept_redirects = 0     # Ignore ICMP redirects\nnet.ipv4.tcp_syncookies = 1                 # Defend against SYN flood attacks\n\n# Apply immediately without reboot:\nsudo sysctl -p',
    exampleExplanation: 'Modifies active settings in `/proc/sys/net/ipv4/`. `sysctl -p` loads changes from `/etc/sysctl.conf`.',
    examTip: 'To apply sysctl configuration file changes immediately without rebooting: `sysctl -p`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1063,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Insecure Protocols',
    command: 'Insecure Legacy Protocols to Disable / Replace',
    definition: 'Cleartext, unencrypted legacy protocols that transmit credentials and data in plain text, making them vulnerable to network sniffing and man-in-the-middle attacks.',
    example: '# Insecure Protocol -> Secure Encrypted Replacement:\n# Telnet (Port 23)   -> SSH (Port 22)\n# rsh / rlogin / rcp -> SSH / SCP / SFTP (Port 22)\n# FTP (Port 21)      -> SFTP (Port 22) or FTPS\n# HTTP (Port 80)     -> HTTPS (Port 443)\n# POP3 (110) / IMAP (143) -> POP3S (995) / IMAPS (993)',
    exampleExplanation: 'Disabling legacy `r-commands` (`rsh`, `rlogin`, `rcp`, `rexec`) and Telnet is a core requirement of server hardening.',
    examTip: '`rsh`, `rlogin`, `rcp`, and `telnet` transmit all usernames and passwords across the network in unencrypted cleartext.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1064,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Insecure Authentication',
    command: '/etc/hosts.equiv & ~/.rhosts',
    definition: 'Legacy configuration files used by `rlogin` and `rsh` to trust remote hosts and users without requiring passwords.',
    example: '# /etc/hosts.equiv (System-wide):\nserver1.example.com\n+@trusted_hosts\n\n# ~/.rhosts (Per-user):\nhost1.example.com bob',
    exampleExplanation: 'Highly insecure because authentication relies purely on unverified IP/hostname and source port (<1024), making IP spoofing trivial.',
    examTip: 'For secure systems, `/etc/hosts.equiv` and `~/.rhosts` files must be deleted or empty.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1065,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Intrusion Detection',
    command: 'Fail2ban & Intrusion Prevention',
    definition: 'Log-parsing daemon that monitors application logs (e.g. `/var/log/auth.log` or `/var/log/secure`) and dynamically inserts firewall rules to block IPs exhibiting malicious authentication behavior.',
    example: 'sudo fail2ban-client status\nsudo fail2ban-client status sshd\nsudo fail2ban-client set sshd unbanip 192.168.1.105',
    exampleExplanation: 'Scans for repeated failed password attempts within a time window (maxretry) and temporarily or permanently bans offending IP addresses via iptables/nftables/firewalld.',
    examTip: 'Configured via `/etc/fail2ban/jail.conf` and `/etc/fail2ban/jail.local`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1066,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Host Integrity',
    command: 'File Integrity Monitoring (AIDE & Tripwire)',
    definition: 'Host-based intrusion detection systems (HIDS) that create a cryptographic hash database of system files to detect unauthorized modifications, trojans, and rootkits.',
    example: 'sudo aideinit            # Initialize the baseline database\nsudo cp /var/lib/aide/aide.db.new /var/lib/aide/aide.db\nsudo aide --check        # Audit current files against baseline database',
    exampleExplanation: 'Stores SHA256 hashes, file sizes, permissions, and timestamps of binaries and configuration files in a database to detect unauthorized modifications.',
    examTip: '`aide --check` compares the current filesystem against the baseline database to report modified, added, or deleted files.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1067,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Rootkit Detection',
    command: 'chkrootkit & rkhunter',
    definition: 'Security audit tools that scan local Linux systems for known rootkits, backdoors, hidden processes, modified binaries, and compromised kernel modules.',
    example: 'sudo chkrootkit\nsudo rkhunter --update\nsudo rkhunter --check --sk',
    exampleExplanation: 'Scans system binaries (e.g. `ls`, `ps`, `netstat`, `top`) to verify they have not been trojaned to conceal hidden malicious processes.',
    examTip: '`chkrootkit` and `rkhunter` are standard open-source rootkit scanners tested on LPIC security objectives.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1068,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Mandatory Access Control',
    command: 'SELinux & AppArmor Concepts',
    definition: 'Kernel security modules implementing Mandatory Access Control (MAC) policies that restrict process capabilities beyond standard Discretionary Access Control (DAC) permissions.',
    example: '# SELinux (RHEL/CentOS):\nsestatus\ngetenforce\nsetenforce 0  # Permissive\nsetenforce 1  # Enforcing\n\n# AppArmor (Ubuntu/Debian/SUSE):\nsudo aa-status',
    exampleExplanation: 'Even if root or a compromised daemon (e.g. Apache) is exploited, MAC policies prevent the process from accessing unauthorized files or sockets.',
    examTip: 'SELinux modes: `Enforcing` (enforce policy and log), `Permissive` (log only without blocking), `Disabled` (turned off completely).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1069,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Network Auditing',
    command: 'tcpdump for Security Inspection',
    definition: 'Command-line packet analyzer used to capture and inspect live network traffic on an interface for unauthorized communication or plaintext credential leakage.',
    example: 'sudo tcpdump -i eth0 -n -c 10\nsudo tcpdump -i eth0 port 21 or port 23 -X\nsudo tcpdump -i eth0 -w /tmp/capture.pcap',
    exampleExplanation: 'Captures raw frames passing through the network adapter. `-X` displays packet contents in both hex and ASCII.',
    examTip: '`-w <file>` writes captured raw packets to a `.pcap` file for later analysis in Wireshark; `-r <file>` reads packets back.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1070,
    deck: 'Topic 110: Security',
    objectiveId: '110.2',
    topicNumber: 110,
    category: 'Physical Security',
    command: 'GRUB Password Protection',
    definition: 'Securing the GRUB2 bootloader with a password to prevent unauthorized physical users from editing kernel parameters (such as `init=/bin/bash` or `single`) to bypass authentication.',
    example: '# Generate PBKDF2 password hash:\ngrub-mkpasswd-pbkdf2\n\n# In /etc/grub.d/40_custom:\nset superusers="admin"\npassword_pbkdf2 admin grub.pbkdf2.sha512.10000...\n\n# Update GRUB configuration:\nsudo update-grub',
    exampleExplanation: 'Protects the system against physical access single-user root shell recovery exploits.',
    examTip: '`grub-mkpasswd-pbkdf2` generates an encrypted hash for securing GRUB boot menu access.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },

  // =========================================================================
  // OBJECTIVE 110.3: Securing data with encryption (Cards 1071-1100) [30 Cards]
  // =========================================================================
  {
    id: 1071,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'OpenSSH Architecture',
    command: 'OpenSSH Suite Overview & Components',
    definition: 'The standard suite of secure networking utilities providing encrypted communication, remote command execution, key management, and secure file transfer over insecure networks.',
    example: '# Core client tools: ssh, scp, sftp, ssh-copy-id, ssh-agent, ssh-add, ssh-keyscan\n# Server daemon: sshd (/usr/sbin/sshd)\n# Key generator: ssh-keygen\n# Default TCP Port: 22',
    exampleExplanation: 'Replaces insecure legacy suites like Telnet, rsh, rcp, and rlogin with end-to-end cryptographic security.',
    examTip: 'OpenSSH operates on standard TCP port 22 by default.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1072,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'Asymmetric Cryptography',
    command: 'Public Key vs Private Key Cryptography',
    definition: 'Asymmetric encryption uses a mathematically linked pair of keys: a Public Key (freely shared with servers/recipients) and a Private Key (kept strictly confidential on the client).',
    example: '# Private key: ~/.ssh/id_ed25519 (NEVER share, password protected)\n# Public key:  ~/.ssh/id_ed25519.pub (copied to remote servers)',
    exampleExplanation: 'Data encrypted with the public key can only be decrypted by the matching private key; signatures generated by the private key can be verified by anyone holding the public key.',
    examTip: 'The public key always has the `.pub` extension (e.g. `id_rsa.pub`, `id_ed25519.pub`). The private key has NO file extension.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1073,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Key Generation',
    command: 'ssh-keygen -t',
    definition: 'Generates public and private authentication key pairs for SSH using specified cryptographic algorithms.',
    example: '# Generate modern high-security Ed25519 key:\nssh-keygen -t ed25519 -C "admin@example.com"\n\n# Generate 4096-bit RSA key:\nssh-keygen -t rsa -b 4096 -C "alice@workstation"',
    exampleExplanation: '`-t` specifies key type (`ed25519`, `rsa`, `ecdsa`); `-b` specifies bit length (for RSA); `-C` adds an informational comment appended to the public key.',
    examTip: 'Ed25519 (Edwards-curve Digital Signature Algorithm) is the modern recommended standard for SSH keys due to its compact size, speed, and high cryptographic strength.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1074,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Key Generation',
    command: 'ssh-keygen Options (-f, -p, -N, -y)',
    definition: 'Key management flags for changing passphrases, specifying output filenames, or extracting public keys.',
    example: 'ssh-keygen -f ~/.ssh/id_deploy -N ""   # Generate key with empty passphrase\nssh-keygen -p -f ~/.ssh/id_rsa         # Change passphrase of existing private key\nssh-keygen -y -f ~/.ssh/id_rsa > id.pub # Recreate public key from private key',
    exampleExplanation: '`-f` specifies file path; `-p` prompts to change passphrase; `-N` supplies new passphrase; `-y` reads private key and outputs public key.',
    examTip: '`ssh-keygen -p` allows changing the passphrase of an existing private key without regenerating the key pair or modifying remote authorized_keys.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1075,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Key Distribution',
    command: 'ssh-copy-id',
    definition: 'Installs your local public key onto a remote server by appending it to the target user’s `~/.ssh/authorized_keys` file.',
    example: 'ssh-copy-id user@remote.example.com\n# Specify exact public key file:\nssh-copy-id -i ~/.ssh/id_ed25519.pub -p 2222 user@remote.example.com',
    exampleExplanation: 'Logs into the remote machine using password authentication and automatically ensures proper directory permissions (`700` for `~/.ssh`, `600` for `authorized_keys`).',
    examTip: '`ssh-copy-id` appends the public key to `~/.ssh/authorized_keys` on the remote host.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1076,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Permissions',
    command: 'SSH Filesystem Permissions Rules',
    definition: 'Strict filesystem permission requirements enforced by OpenSSH to prevent unauthorized users from tampering with keys.',
    example: 'chmod 700 ~/.ssh\nchmod 600 ~/.ssh/authorized_keys\nchmod 600 ~/.ssh/id_ed25519      # Private key\nchmod 644 ~/.ssh/id_ed25519.pub  # Public key\nchmod 644 ~/.ssh/known_hosts\nchmod 644 ~/.ssh/config',
    exampleExplanation: 'If permissions on `~/.ssh` or private keys are too permissive (e.g. group or world writable), ssh and sshd will refuse to use them and abort authentication.',
    examTip: 'High-frequency exam question: `~/.ssh` must be `700` (`drwx------`), and private keys must be `600` (`-rw-------`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1077,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Files',
    command: '~/.ssh/authorized_keys File Format',
    definition: 'Server-side file in each user’s home directory containing the list of public keys authorized to log in as that user.',
    example: '# Format: <key-type> <base64-encoded-key> <comment>\nssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIG... alice@workstation\n\n# With security restrictions:\nfrom="192.168.1.0/24",no-pty ssh-ed25519 AAAAC3... backup-job',
    exampleExplanation: 'Each line contains one public key. Optional prefix options can restrict source IPs (`from=`), disable pseudo-terminals (`no-pty`), or enforce commands (`command=`).',
    examTip: 'Multiple clients can authenticate to the same account by placing their public keys on separate lines inside `~/.ssh/authorized_keys`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1078,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Files',
    command: '~/.ssh/known_hosts File & Host Key Verification',
    definition: 'Client-side database file storing the public host keys of remote servers to prevent Man-in-the-Middle (MITM) attacks and DNS spoofing.',
    example: '# When connecting to a new host:\n# The authenticity of host "server.example.com" cannot be established.\n# ED25519 key fingerprint is SHA256:4kG...\n# Are you sure you want to continue connecting (yes/no)? yes\n# Warning: Permanently added "server.example.com" (ED25519) to the list of known hosts.',
    exampleExplanation: 'If the remote server’s host key changes later, SSH displays a severe `WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED!` warning and blocks the connection.',
    examTip: 'Host keys verify the IDENTITY OF THE SERVER to the client, preventing MITM impersonation.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1079,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Utilities',
    command: 'ssh-keyscan & ssh-keygen -R',
    definition: 'Utilities to gather host keys remotely or remove outdated/changed host keys from `known_hosts`.',
    example: '# Gather host key without interactive prompt:\nssh-keyscan -t ed25519 server.example.com >> ~/.ssh/known_hosts\n\n# Remove old/rebuilt host from known_hosts after key change:\nssh-keygen -R server.example.com\n# Or by IP:\nssh-keygen -R 192.168.1.50',
    exampleExplanation: '`ssh-keyscan` helps pre-populate `known_hosts` for automation; `ssh-keygen -R` removes previous host entries when a server has been reinstalled.',
    examTip: '`ssh-keygen -R <hostname/IP>` removes the specified host entry from `~/.ssh/known_hosts`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1080,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Client Configuration',
    command: '~/.ssh/config (User SSH Client Config)',
    definition: 'User-level client configuration file creating convenient host aliases, custom ports, usernames, and identity key bindings.',
    example: '# ~/.ssh/config\nHost webprod\n    HostName 203.0.113.50\n    User deploy\n    Port 2222\n    IdentityFile ~/.ssh/id_deploy\n    ForwardAgent yes\n\n# Usage:\nssh webprod  # Automatically connects to deploy@203.0.113.50:2222',
    exampleExplanation: 'System-wide client settings reside in `/etc/ssh/ssh_config`; user settings in `~/.ssh/config` take precedence.',
    examTip: '`~/.ssh/config` configures the SSH CLIENT; `/etc/ssh/sshd_config` configures the SSH SERVER daemon.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1081,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Server Hardening',
    command: '/etc/ssh/sshd_config Core Hardening Directives',
    definition: 'Configuration file for the OpenSSH server daemon controlling authentication methods, network binding, and access policies.',
    example: '# In /etc/ssh/sshd_config:\nPort 2222\nPermitRootLogin no\nPasswordAuthentication no\nPubkeyAuthentication yes\nPermitEmptyPasswords no\nX11Forwarding no\nMaxAuthTries 3\n\n# Reload daemon:\nsudo systemctl reload sshd',
    exampleExplanation: 'Disabling `PermitRootLogin` and `PasswordAuthentication` forces all users to authenticate using SSH key pairs as regular users before escalating.',
    examTip: 'Settings tested on exam: `PermitRootLogin no` (blocks direct root login), `PasswordAuthentication no` (disables password guessing), `PubkeyAuthentication yes` (enables key auth).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1082,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Server Hardening',
    command: 'AllowUsers, AllowGroups, DenyUsers, DenyGroups',
    definition: 'Directives in `/etc/ssh/sshd_config` used to whitelist or blacklist specific users and groups permitted to log in via SSH.',
    example: 'AllowUsers alice bob@192.168.1.* admin\nAllowGroups sysadmin devops\nDenyUsers guest test\nDenyGroups contractors',
    exampleExplanation: 'If `AllowUsers` is specified, ONLY the listed users are allowed to log in; all other accounts are rejected.',
    examTip: 'Evaluation order: `DenyUsers` first, then `AllowUsers`, then `DenyGroups`, then `AllowGroups`. If a user matches `DenyUsers`, login is denied immediately.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1083,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Server Keys',
    command: '/etc/ssh/ssh_host_*_key Files',
    definition: 'Server private and public host key pairs generated during OpenSSH server installation to prove server identity to connecting clients.',
    example: 'ls -l /etc/ssh/ssh_host_*\n# -rw------- 1 root root  505 Sep  2 00:00 /etc/ssh/ssh_host_ed25519_key\n# -rw-r--r-- 1 root root   92 Sep  2 00:00 /etc/ssh/ssh_host_ed25519_key.pub\n# -rw------- 1 root root 2602 Sep  2 00:00 /etc/ssh/ssh_host_rsa_key\n# -rw-r--r-- 1 root root  564 Sep  2 00:00 /etc/ssh/ssh_host_rsa_key.pub',
    exampleExplanation: 'The private host keys must have strict `600` permissions and be owned by root.',
    examTip: 'To regenerate all missing host keys: `ssh-keygen -A` or `dpkg-reconfigure openssh-server`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1084,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Agent',
    command: 'ssh-agent & eval $(ssh-agent -s)',
    definition: 'Background authentication agent that holds unencrypted private keys in memory so users only enter their key passphrase once per session.',
    example: '# Start the agent in the current shell:\neval $(ssh-agent -s)\n# Output: Agent pid 14820\n\n# Verify environment variable:\necho $SSH_AUTH_SOCK',
    exampleExplanation: '`ssh-agent -s` outputs shell commands that export `SSH_AUTH_SOCK` and `SSH_AGENT_PID`, which must be evaluated into the current environment via `eval`.',
    examTip: '`eval $(ssh-agent)` is required because the agent runs as a background child process and needs to set environment variables in the parent shell.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1085,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Agent',
    command: 'ssh-add & Options (-l, -L, -d, -D, -t)',
    definition: 'Adds private keys to the running `ssh-agent`, lists loaded keys, or removes keys from agent memory.',
    example: 'ssh-add ~/.ssh/id_ed25519     # Prompts for passphrase and loads key\nssh-add -l                    # List fingerprints of currently loaded keys\nssh-add -L                    # List full public key parameters of loaded keys\nssh-add -t 1h ~/.ssh/id_rsa   # Load key with a 1-hour time limit\nssh-add -d ~/.ssh/id_rsa      # Delete specific key from agent\nssh-add -D                    # Delete ALL keys from agent cache',
    exampleExplanation: '`-t <time>` automatically expires the cached key in agent memory after specified seconds or duration (e.g. `2h`, `30m`).',
    examTip: '`ssh-add -l` lists loaded key fingerprints; `ssh-add -D` purges all identities from the agent memory.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1086,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Agent Forwarding',
    command: 'Agent Forwarding (ssh -A / ForwardAgent yes)',
    definition: 'Allows a remote server to access your local `ssh-agent` to authenticate to a second remote machine without copying your private key to intermediate servers.',
    example: 'ssh -A user@bastion.example.com\n# From bastion host:\nssh user@internal-database.local',
    exampleExplanation: 'The bastion host forwards authentication requests back through the secure SSH tunnel to your local workstation agent.',
    examTip: 'Agent forwarding should only be used with trusted intermediate servers; an administrator on the intermediate server can access the forwarded agent socket while connected.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 1087,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Port Forwarding',
    command: 'Local Port Forwarding (ssh -L)',
    definition: 'Forwards a local port on the client workstation through the encrypted SSH tunnel to a destination host and port reachable from the remote SSH server.',
    example: 'ssh -L 8080:127.0.0.1:80 user@remote-server.com\n# Or to access internal database behind firewall:\nssh -L 5432:db.internal.lan:5432 user@bastion.com',
    exampleExplanation: 'Connecting to `localhost:8080` on your client workstation tunnels traffic through `remote-server.com` and connects to `127.0.0.1:80` on the server.',
    examTip: 'Syntax formula for `-L`: `ssh -L [local_bind_ip:]local_port:target_host:target_port user@ssh_server`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1088,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'SSH Port Forwarding',
    command: 'Remote & Dynamic Port Forwarding (ssh -R / ssh -D)',
    definition: '`ssh -R` forwards a port on the remote server back to the local client; `ssh -D` creates a local SOCKS5 proxy tunnel.',
    example: '# Remote port forwarding (exposes local port 3000 on remote port 8000):\nssh -R 8000:localhost:3000 user@remote.com\n\n# Dynamic SOCKS5 proxy on local port 1080:\nssh -D 1080 -N -C user@proxy-server.com',
    exampleExplanation: '`-N` tells SSH not to execute a remote command (tunnel only); `-C` enables compression.',
    examTip: '`-L` is Local forwarding (Local port -> Remote destination); `-R` is Remote forwarding (Remote port -> Local destination); `-D` is Dynamic SOCKS proxy.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 1089,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'Secure File Transfer',
    command: 'scp & sftp',
    definition: 'Encrypted file copy utilities that operate over the secure SSH protocol on port 22.',
    example: '# Copy local file to remote server:\nscp -P 2222 /local/file.txt user@remote:/remote/dir/\n# Recursive copy of directory:\nscp -r /local/dir user@remote:/remote/\n\n# Interactive secure FTP session:\nsftp -P 2222 user@remote.example.com',
    exampleExplanation: '`scp` is designed for quick command-line copies; `sftp` provides an interactive shell supporting `get`, `put`, `ls`, `mkdir`, and resume capabilities.',
    examTip: 'Notice the flag difference for custom ports: `ssh` and `sftp` use lowercase `-p <port>`, while `scp` uses capital `-P <port>`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1090,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GnuPG Architecture',
    command: 'GnuPG / OpenPGP Overview (gpg / gpg2)',
    definition: 'Complete, free implementation of the OpenPGP standard (RFC 4880) used for encrypting, decrypting, and digitally signing files, emails, and software packages.',
    example: 'gpg --version\n# Key storage location:\nls -ld ~/.gnupg\n# drwx------ 3 alice alice 4096 Sep  2 12:00 /home/alice/.gnupg',
    exampleExplanation: 'Supports both asymmetric key pairs (RSA, Ed25519) and symmetric ciphers (AES256, Camellia). `~/.gnupg` must have `700` permissions.',
    examTip: '`gpg` and `gpg2` are equivalent on modern Linux distributions (gpg is a symlink to gpg2).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1091,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GPG Key Generation',
    command: 'gpg --gen-key / gpg --full-generate-key',
    definition: 'Generates a new OpenPGP public and private key pair interactively.',
    example: 'gpg --gen-key\n# For advanced algorithm and expiration options:\ngpg --full-generate-key',
    exampleExplanation: 'Prompts for Real Name, Email Address, key type (RSA/ECC), key size (e.g. 4096 bits), expiration duration, and a passphrase to protect the secret key.',
    examTip: '`gpg --full-generate-key` allows customizing key type, key size, and exact expiration dates.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1092,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GPG Key Listing',
    command: 'gpg --list-keys & gpg --list-secret-keys',
    definition: 'Displays public and private keys stored in the user’s local GPG keyring along with Key IDs, algorithms, user IDs, and fingerprints.',
    example: 'gpg --list-keys\ngpg --list-secret-keys\n# Display full cryptographic fingerprints:\ngpg --fingerprint alice@example.com',
    exampleExplanation: '`pub` indicates a public key; `sub` indicates a subkey; `sec` indicates a secret (private) key; `ssb` indicates a secret subkey.',
    examTip: '`gpg -k` is shorthand for `--list-keys`; `gpg -K` is shorthand for `--list-secret-keys`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1093,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GPG Key Export & Import',
    command: 'gpg --export / gpg --import / gpg -a (--armor)',
    definition: 'Exports public or secret keys to a file or imports keys from external files or keyservers.',
    example: '# Export public key in ASCII-armored format (text):\ngpg --armor --export alice@example.com > alice_pubkey.asc\n\n# Import someone else’s public key:\ngpg --import bob_pubkey.asc',
    exampleExplanation: '`-a` or `--armor` outputs ASCII-armored text (`-----BEGIN PGP PUBLIC KEY BLOCK-----`) instead of raw binary format, making it safe for email and web posting.',
    examTip: 'The `--armor` (or `-a`) option creates ASCII-armored plain text output suitable for pasting into emails or web pages.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1094,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GPG Revocation',
    command: 'gpg --gen-revoke / gpg --output revoke.asc',
    definition: 'Generates a revocation certificate to invalidate your public key on keyservers if your private key is compromised or passphrase lost.',
    example: 'gpg --output revoke.asc --gen-revoke alice@example.com',
    exampleExplanation: 'Should be generated immediately after key creation and stored in a secure offline location (e.g. printed or on encrypted USB).',
    examTip: '`gpg --gen-revoke` creates a revocation certificate used to mark a key as revoked if compromised.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1095,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GPG Keyservers',
    command: 'gpg Keyserver Operations (--send-keys / --recv-keys / --search-keys)',
    definition: 'Distributes and retrieves public keys via public PGP keyserver networks (HKPS protocol).',
    example: '# Send public key to keyserver:\ngpg --keyserver hkps://keyserver.ubuntu.com --send-keys 0x12345678\n\n# Retrieve someone’s public key by Key ID:\ngpg --keyserver hkps://keyserver.ubuntu.com --recv-keys 0x12345678\n\n# Search for key by email:\ngpg --keyserver hkps://keyserver.ubuntu.com --search-keys alice@example.com',
    exampleExplanation: 'Keyservers synchronize globally, allowing anyone in the world to download your public key using your email or Key ID.',
    examTip: '`--recv-keys` downloads and imports a public key from the keyserver directly into your local keyring.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1096,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GPG Encryption',
    command: 'gpg --encrypt --recipient (Asymmetric Encryption)',
    definition: 'Encrypts a document using the recipient’s public key so only the recipient holding the matching private key can decrypt it.',
    example: 'gpg --encrypt --recipient bob@example.com secret_report.txt\n# Produces secret_report.txt.gpg\n\n# With ASCII armor:\ngpg -e -a -r bob@example.com secret_report.txt\n# Produces secret_report.txt.asc',
    exampleExplanation: '`-e` is shorthand for `--encrypt`; `-r` specifies the recipient identifier (email, key ID, or fingerprint).',
    examTip: 'To encrypt for multiple recipients, repeat the `-r` option: `gpg -e -r alice@work.com -r bob@work.com document.pdf`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1097,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GPG Encryption',
    command: 'gpg --symmetric / gpg -c (Symmetric Encryption)',
    definition: 'Encrypts a file using a shared passphrase and symmetric cipher (AES256 by default) without requiring public/private key pairs.',
    example: 'gpg --symmetric financial_data.tar.gz\n# Or shorthand:\ngpg -c financial_data.tar.gz\n# Produces financial_data.tar.gz.gpg',
    exampleExplanation: 'Prompts for a passphrase twice. Decryption requires entering the exact same passphrase.',
    examTip: '`gpg -c` (or `--symmetric`) performs symmetric encryption using a shared passphrase rather than asymmetric public/private keys.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1098,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GPG Decryption',
    command: 'gpg --decrypt (gpg -d)',
    definition: 'Decrypts an encrypted file (asymmetric or symmetric) and outputs the plaintext to stdout or a redirected file.',
    example: 'gpg --decrypt secret_report.txt.gpg > secret_report.txt\n# Or with -o (output file):\ngpg --output secret_report.txt --decrypt secret_report.txt.gpg',
    exampleExplanation: 'Automatically determines whether the file is symmetrically or asymmetrically encrypted, selects the matching private key from `~/.gnupg`, and prompts for the passphrase.',
    examTip: '`gpg -d` (or `--decrypt`) prints the decrypted plaintext to STDOUT unless `-o <file>` is specified.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 1099,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GPG Digital Signatures',
    command: 'gpg --sign vs --clearsign vs --detach-sign',
    definition: 'Methods for digitally signing documents using your private key to guarantee origin authenticity and document integrity.',
    example: '# 1. gpg --sign file.txt\n# Creates compressed binary file file.txt.gpg (contains doc + signature)\n\n# 2. gpg --clearsign announcement.txt\n# Creates readable ASCII text file announcement.txt.asc with signature block\n\n# 3. gpg --detach-sign archive.tar.gz\n# Creates separate signature file archive.tar.gz.sig without modifying archive',
    exampleExplanation: '`--clearsign` is ideal for human-readable plain text announcements; `--detach-sign` is standard for signing large software distribution tarballs and ISOs.',
    examTip: 'Key difference: `--detach-sign` creates a separate `.sig` file, leaving the original data file completely untouched.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 1100,
    deck: 'Topic 110: Security',
    objectiveId: '110.3',
    topicNumber: 110,
    category: 'GPG Signature Verification',
    command: 'gpg --verify',
    definition: 'Verifies the cryptographic digital signature of a file using the signer’s public key to confirm authenticity and detect any file tampering.',
    example: '# Verify signed or clearsigned file:\ngpg --verify announcement.txt.asc\n\n# Verify detached signature against original file:\ngpg --verify software-1.0.tar.gz.sig software-1.0.tar.gz',
    exampleExplanation: 'Outputs `gpg: Good signature from "Alice <alice@example.com>"` if valid, or `BAD signature` if the file has been modified in transit.',
    examTip: 'To verify a detached signature: `gpg --verify <sig_file> <data_file>`. A "Good signature" guarantees the file was not altered since signing.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
];
