import { Flashcard } from '../../types';

/**
 * LPI LPIC-2 Exam 201 - Topic 203: Filesystem and Devices
 * Part 1: Cards 2301 - 2350
 * - Objective 203.1: Operating the Linux Filesystem (Cards 2301-2340) [40 Cards]
 * - Objective 203.2: Maintaining a Linux Filesystem - Part 1 (Cards 2341-2350) [10 Cards]
 */

export const topic203Part1: Flashcard[] = [
  // =========================================================================
  // OBJECTIVE 203.1: Operating the Linux Filesystem (Cards 2301-2340)
  // =========================================================================
  {
    id: 2301,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: '/etc/fstab Anatomy & Fields',
    question: 'What are the six positional fields in an `/etc/fstab` configuration line, in order?',
    answer: '1. Device/spec (`fs_spec`), 2. Mount point (`fs_file`), 3. Filesystem type (`fs_vfstype`), 4. Mount options (`fs_mntops`), 5. Dump frequency (`fs_freq`), 6. Fsck pass number (`fs_passno`).',
    definition: 'Static filesystem configuration file read at system boot and by the `mount` command.',
    example: 'UUID=a1b2c3d4-5678-90ef-1234-567890abcdef  /data  ext4  defaults,noatime  0  2',
    exampleExplanation: 'Specifies device UUID, mounted at /data as ext4 with defaults and noatime, not dumped (0), checked in pass 2.',
    examTip: 'Exam question: Remember the last two numbers: dump is 5th, passno is 6th. 0 0 means no dump and no fsck check.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2302,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'UUID vs PARTUUID vs LABEL in fstab',
    question: 'Why is referencing partitions by `UUID=` or `LABEL=` in `/etc/fstab` preferred over traditional device nodes like `/dev/sdb1`?',
    answer: 'Device node letters (`/dev/sdX`) can change unpredictably across reboots if drive detection order shifts; UUIDs and LABELs are persistent filesystem identifiers.',
    definition: 'Persistent naming prevents mounting the wrong volume if storage controllers initialize in a different order.',
    example: 'UUID="e5b12850-77a8-452f-9811-66778899aabb" /home ext4 defaults 0 2\nLABEL="BACKUP" /mnt/backup xfs defaults 0 0',
    exampleExplanation: 'Matches the specific filesystem header regardless of whether it appears on /dev/sdb1 or /dev/sdc1.',
    examTip: 'PARTUUID identifies the partition table entry (GPT partition GUID), whereas UUID identifies the filesystem inside the partition.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2303,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'blkid utility',
    question: 'How do you display the UUID, PARTUUID, and filesystem TYPE for all block devices using `blkid`?',
    answer: '`blkid` without arguments (or `blkid /dev/sda1` for a specific partition).',
    definition: 'Command-line utility to locate/print block device attributes from the libblkid library cache.',
    example: 'blkid\n/dev/sda1: UUID="9f8e7d6c-5b4a-3210-fedc-ba9876543210" TYPE="ext4" PARTUUID="12345678-01"\n/dev/sda2: UUID="7a8b9c0d-1e2f-3a4b-5c6d-7e8f9a0b1c2d" TYPE="swap" PARTUUID="12345678-02"',
    exampleExplanation: 'Queries the blkid cache file (`/etc/blkid.tab`) or scans devices directly to output UUID, label, and filesystem type.',
    examTip: 'Use `blkid -s UUID -o value /dev/sda1` to extract solely the raw UUID string for scripting.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2304,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'findfs utility',
    question: 'Which utility finds a block device node given its filesystem UUID or volume LABEL?',
    answer: '`findfs UUID=<uuid>` or `findfs LABEL=<label>`.',
    definition: 'Searches block devices for the one matching the specified UUID or LABEL tag.',
    example: 'findfs UUID=9f8e7d6c-5b4a-3210-fedc-ba9876543210\n# Output: /dev/sda1\nfindfs LABEL=DATA_VOL\n# Output: /dev/sdb2',
    exampleExplanation: 'Returns the corresponding system device path currently assigned to that volume.',
    examTip: '`findfs` will return non-zero exit code and error if the UUID or LABEL does not exist.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2305,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'lsblk with filesystem tags',
    question: 'Which `lsblk` command options display device topology along with filesystem TYPE, UUID, and MOUNTPOINT?',
    answer: '`lsblk -f` (or `lsblk -o NAME,FSTYPE,LABEL,UUID,MOUNTPOINT`).',
    definition: 'Lists information about all available or specified block devices in a tree-like hierarchy.',
    example: 'lsblk -f\nNAME   FSTYPE FSVER LABEL UUID                                 FSAVAIL FSUSE% MOUNTPOINTS\nsda                                                                           \n├─sda1 ext4   1.0         9f8e7d6c-5b4a-3210-fedc-ba9876543210   32.4G    35% /\n└─sda2 swap   1           7a8b9c0d-1e2f-3a4b-5c6d-7e8f9a0b1c2d                [SWAP]',
    exampleExplanation: '`-f` enables the filesystem output view, listing file systems, labels, UUIDs, and mount points.',
    examTip: '`lsblk` reads directly from sysfs and udev, working without needing root privileges unlike raw `blkid` on older kernels.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2306,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'noexec mount option',
    question: 'What restriction does the `noexec` mount option enforce on a filesystem?',
    answer: 'It prohibits the direct execution of any binary executable files located on that filesystem.',
    definition: 'Security hardening option commonly applied to temporary and user-writable partitions like `/tmp`, `/var/tmp`, or `/dev/shm`.',
    example: 'mount -o remount,noexec /tmp\n/tmp/malware.sh\n# Output: -bash: /tmp/malware.sh: Permission denied',
    exampleExplanation: 'Prevents binaries and scripts from running directly, mitigating arbitrary code execution from world-writable directories.',
    examTip: 'Note: An interpreter can still read and execute a script (e.g. `python /tmp/script.py` or `bash /tmp/test.sh`), but binary `execve()` calls fail.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2307,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'nosuid mount option',
    question: 'What is the security effect of the `nosuid` mount option?',
    answer: 'It disables set-user-identifier (SUID) and set-group-identifier (SGID) execution bits on the mounted filesystem.',
    definition: 'Causes binaries with SUID/SGID bits to execute with the privileges of the calling user instead of the file owner.',
    example: 'mount -o remount,nosuid /home',
    exampleExplanation: 'If a user leaves a SUID-root binary in `/home/user/exploit`, executing it will NOT elevate privileges to root.',
    examTip: 'Exam favorite: High-security partitions mounted by non-root users should always specify `nosuid`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2308,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'nodev mount option',
    question: 'What does the `nodev` mount option prevent?',
    answer: 'It prevents character and block special device files located on that filesystem from functioning or being interpreted by the kernel.',
    definition: 'Security measure blocking attackers from creating unauthorized device nodes (e.g., via `mknod`) to read raw disk devices.',
    example: 'mount -o remount,nodev /var',
    exampleExplanation: 'Even if an attacker creates `/var/tmp/rawdisk` with major/minor numbers of `/dev/sda`, accessing it returns Permission Denied.',
    examTip: 'Frequently combined on unprivileged filesystems: `noexec,nosuid,nodev`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2309,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'user vs users mount options',
    question: 'What is the crucial difference between the `user` and `users` mount options in `/etc/fstab`?',
    answer: '`user` allows any ordinary user to mount the filesystem, but ONLY that same user (or root) can unmount it; `users` allows ANY user to unmount it.',
    definition: 'Options enabling non-root users to mount devices without requiring `sudo`.',
    example: '/dev/sr0  /media/cdrom  iso9660  user,noauto,ro  0 0\n/dev/sdb1 /mnt/usb      vfat     users,noauto    0 0',
    exampleExplanation: 'With `user`, if Alice mounts `/media/cdrom`, Bob cannot unmount it. With `users`, Bob can unmount Alice\'s USB drive.',
    examTip: 'CRITICAL: The `user` option automatically implies `noexec,nosuid,nodev` unless explicitly overridden afterward (e.g. `user,exec`).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2310,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'owner & group mount options',
    question: 'How do the `owner` and `group` mount options restrict non-root mounting in `/etc/fstab`?',
    answer: '`owner` allows mounting only if the user owns the underlying device node; `group` allows mounting only if the user belongs to the device node\'s group.',
    definition: 'More restrictive variants of the `user` option binding mount permissions to device node ownership.',
    example: '/dev/fd0 /mnt/floppy auto owner,noauto 0 0',
    exampleExplanation: 'Only the user who owns `/dev/fd0` can mount it.',
    examTip: 'Both `owner` and `group` also automatically enforce `nosuid,nodev` for security.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2311,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'noatime vs relatime vs strictatime',
    question: 'How does `noatime` differ from Linux\'s default `relatime` mount option, and why is `noatime` used on high-performance servers?',
    answer: '`noatime` disables updating file access timestamps entirely on read operations; `relatime` updates atime only if the previous atime is older than mtime/ctime or >24 hours old.',
    definition: 'Timestamp optimization preventing every file read operation from generating a metadata disk write.',
    example: 'mount -o remount,noatime /data',
    exampleExplanation: 'Eliminates read I/O overhead on database, web server, and SSD volumes by completely skipping atime updates.',
    examTip: '`nodiratime` disables atime updates solely for directories. `noatime` implies `nodiratime`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2312,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'sync vs async mount options',
    question: 'What are the trade-offs between `sync` and `async` mount options?',
    answer: '`sync` forces all I/O writes immediately to disk (slow, high wear, but safe against data loss on sudden power/USB pull); `async` buffers writes in RAM (fast, default).',
    definition: 'Controls whether metadata and file data write operations execute synchronously or asynchronously.',
    example: 'mount -o sync /dev/sdb1 /mnt/flashdrive',
    exampleExplanation: 'Ensures data is physically written to the flash drive immediately without needing an explicit `sync` command before removal.',
    examTip: '`dirsync` forces synchronous updates specifically for directory operations (creating, linking, unlinking files) without slowing all file data writes.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2313,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'mount -o remount',
    question: 'How do you switch the root filesystem `/` from read-only mode to read-write mode without unmounting or rebooting?',
    answer: '`mount -o remount,rw /`',
    definition: 'Changes the mount options of an already mounted filesystem on-the-fly.',
    example: 'mount -o remount,ro /var/log\nmount -o remount,rw /',
    exampleExplanation: 'Commonly executed in single-user recovery mode when root boots read-only for initial fsck checking.',
    examTip: 'Exam favorite: You can also combine options during remount: `mount -o remount,noatime,ro /data`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2314,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Bind Mounts (mount --bind)',
    question: 'What is a bind mount, and what command mounts `/var/www/html` to `/home/user/public_html`?',
    answer: 'A bind mount makes an existing directory subtree visible at another location in the filesystem hierarchy; command: `mount --bind /var/www/html /home/user/public_html`.',
    definition: 'Re-mounts part of an already mounted directory structure elsewhere without creating symlinks.',
    example: 'mount --bind /var/log /mnt/testlog\n# In /etc/fstab:\n/var/www/html  /home/user/public_html  none  bind  0  0',
    exampleExplanation: 'Unlike symlinks, chroot jails and services with path restrictions can access files inside bind mounts seamlessly.',
    examTip: '`--rbind` recursively binds all sub-mounts within the tree. `mount -o remount,ro,bind /target` makes the bind mount read-only.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2315,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Loop Devices & losetup',
    question: 'How do you manually associate a disk image file `disk.img` with the next free loop device, scan its partition table, and mount partition 1?',
    answer: '`losetup -fP disk.img` then `mount /dev/loop0p1 /mnt`.',
    definition: 'Loop devices expose regular disk image files as block devices so filesystems inside them can be partitioned and mounted.',
    example: 'losetup -fP ubuntu.iso\nlosetup -a\n# Shows: /dev/loop0: [66306]:12345 (/home/user/ubuntu.iso)\nlosetup -d /dev/loop0',
    exampleExplanation: '`-f` finds the first unused loop device; `-P` forces the kernel to scan the image for partition tables creating `/dev/loopXpY`.',
    examTip: '`losetup -d /dev/loop0` detaches the loop device when finished.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2316,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'mount -a',
    question: 'What does `mount -a` do, and which filesystems in `/etc/fstab` does it exclude by default?',
    answer: 'Mounts all filesystems mentioned in `/etc/fstab` marked for automatic mounting; excludes entries with the `noauto` option and already mounted filesystems.',
    definition: 'Executes bulk mounting during bootup or after manually editing `/etc/fstab`.',
    example: 'mount -a\nmount -a -t ext4',
    exampleExplanation: '`-t ext4` restricts the bulk mount operation exclusively to ext4 entries in `/etc/fstab`.',
    examTip: 'Always run `mount -a` after editing `/etc/fstab` before rebooting to catch syntax errors immediately!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2317,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'umount -l (Lazy Unmount)',
    question: 'What is the purpose of `umount -l <mountpoint>`, and when should an administrator use it?',
    answer: 'Performs a lazy unmount: detaches the filesystem from the namespace immediately, preventing new access, and cleans up open references once busy processes finish.',
    definition: 'Safe detach mechanism for busy filesystems that cannot be unmounted normally with `target is busy`.',
    example: 'umount -l /mnt/backup',
    exampleExplanation: 'Instantly makes `/mnt/backup` appear unmounted to other commands while pending file operations complete.',
    examTip: 'Do not confuse `-l` (lazy) with `-f` (force). `-f` is used for unreachable network filesystems (e.g. hung NFS servers).',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2318,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'fuser for busy mounts',
    question: 'Which `fuser` commands identify and forcefully terminate all processes holding open files on `/mnt/data`?',
    answer: '`fuser -vm /mnt/data` to view processes, and `fuser -km /mnt/data` to terminate them.',
    definition: 'Tool that displays and signals processes accessing specified files, filesystems, or sockets.',
    example: 'fuser -vm /mnt/usb\n# Output:\n#                     USER        PID ACCESS COMMAND\n# /mnt/usb:           alice      14522 ..c.. bash\nfuser -km -9 /mnt/usb',
    exampleExplanation: '`-v` provides verbose PID/user/command details; `-m` targets the entire mounted filesystem; `-k` sends SIGKILL to free the mount.',
    examTip: 'Access codes in `fuser`: `c` = current directory, `e` = executable running, `f` = open file, `r` = root directory.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2319,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'lsof +D for open files in directory',
    question: 'How do you list all open files and active processes within `/mnt/storage` using `lsof`?',
    answer: '`lsof +D /mnt/storage` (recursively checks the entire directory tree).',
    definition: 'Lists open files held by processes across the specified path.',
    example: 'lsof +D /mnt/storage\n# COMMAND   PID USER   FD   TYPE DEVICE SIZE/OFF NODE NAME\n# nano    18291 john    3u   REG   8,17     1024   12 /mnt/storage/notes.txt',
    exampleExplanation: 'Identifies John running `nano` on `notes.txt`, explaining why `umount /mnt/storage` fails.',
    examTip: '`+d` checks only the top-level directory; `+D` searches the full hierarchy recursively.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2320,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: '/proc/mounts vs /etc/mtab',
    question: 'What is the relationship between `/etc/mtab` and `/proc/mounts` on modern Linux systems?',
    answer: '`/etc/mtab` is historically a flat file updated by `mount`, but on modern systems it is a symbolic link to `/proc/self/mounts`, reflecting real-time kernel mount state.',
    definition: 'Tracks currently mounted filesystems.',
    example: 'ls -l /etc/mtab\n# lrwxrwxrwx 1 root root 19 /etc/mtab -> /proc/self/mounts',
    exampleExplanation: 'Symlinking prevents stale state when unmounts occur via namespaces, autofs, or container runtimes.',
    examTip: 'If `/etc/mtab` is corrupted or desynchronized on older systems, running `mount` can yield inaccurate active mount views.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2321,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'autofs Concept & Architecture',
    question: 'What is `autofs`, and what advantages does it provide over static `/etc/fstab` entries?',
    answer: 'It is a kernel automounter daemon that mounts filesystems on-demand only when accessed, and automatically unmounts them after a period of inactivity.',
    definition: 'Conserves network and server resources by mounting remote NFS/SMB shares or removable media only during active usage.',
    example: 'systemctl status autofs\n# Accessing /net/nfs.server/export automatically triggers mount',
    exampleExplanation: 'Prevents boot hangs if remote servers are offline and reduces stale network connections.',
    examTip: 'Autofs is managed by the user-space daemon `automount` working in tandem with the `autofs` kernel filesystem driver.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2322,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: '/etc/auto.master Syntax',
    question: 'What is the syntax of an entry in the master map file `/etc/auto.master`?',
    answer: '`<mount-point>  <map-name>  [options]`',
    definition: 'Primary configuration file for autofs designating base mount directories and their associated map files.',
    example: '/misc     /etc/auto.misc     --timeout=60\n/share    /etc/auto.share    --ghost\n/-        /etc/auto.direct',
    exampleExplanation: 'Directs `automount` to manage `/misc` using definitions in `/etc/auto.misc` with an inactivity timeout of 60 seconds.',
    examTip: 'Exam essential: Autofs configuration files can also be placed inside `/etc/auto.master.d/*.autofs`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2323,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Indirect Autofs Maps',
    question: 'How do indirect autofs maps work, and what is the syntax of an entry inside an indirect map file (e.g. `/etc/auto.misc`)?',
    answer: 'Indirect maps manage dynamic subdirectories under a shared base mount point; syntax: `<key>  [-options]  <location>`.',
    definition: 'Map file where subdirectories are dynamically mounted under the master map\'s parent directory.',
    example: '# In /etc/auto.master:\n/mnt/nfs  /etc/auto.nfs\n# In /etc/auto.nfs:\nproject   -rw,soft,intr  192.168.1.10:/exports/project',
    exampleExplanation: 'When a user accesses `/mnt/nfs/project`, autofs mounts `192.168.1.10:/exports/project` at that path.',
    examTip: 'The key `project` does NOT have a leading slash! It is relative to the master mount point `/mnt/nfs`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2324,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Direct Autofs Maps (/-)',
    question: 'How do direct maps differ from indirect maps in autofs, and what special mount point is declared in `/etc/auto.master`?',
    answer: 'Direct maps mount directories anywhere in the filesystem using full absolute paths; declared using `/-` as the mount point in `/etc/auto.master`.',
    definition: 'Map type allowing independent, non-grouped mount points without a common parent directory.',
    example: '# In /etc/auto.master:\n/-   /etc/auto.direct\n# In /etc/auto.direct:\n/usr/local/software  -ro,soft  nfs.example.com:/opt/software\n/var/data            -rw       nfs.example.com:/srv/data',
    exampleExplanation: 'Every key inside `/etc/auto.direct` MUST start with an absolute path (`/`).',
    examTip: 'High exam frequency: Direct maps use `/-` in `auto.master` and absolute paths in the map file. Indirect maps use relative keys.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2325,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Autofs Wildcard & Ampersand (&)',
    question: 'What do the wildcard `*` and substitution character `&` mean inside an indirect autofs map?',
    answer: '`*` matches any requested directory key, and `&` substitutes the exact matched key string into the remote path.',
    definition: 'Enables automatic mounting of hundreds of remote user home directories with a single rule.',
    example: '# In /etc/auto.home:\n*  -fstype=nfs,rw,nosuid  filer.example.com:/exports/home/&',
    exampleExplanation: 'When user `alice` logs in and accesses `/home/alice`, `*` matches `alice`, and `&` maps to `/exports/home/alice`.',
    examTip: 'Saves administrators from having to define separate map lines for every user or project share.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2326,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Autofs Ghosting (--ghost / browse)',
    question: 'What does the `--ghost` (or `browse`) option do in autofs configuration?',
    answer: 'It creates visible empty placeholder directories for all keys in the map before they are actually mounted.',
    definition: 'Ensures that running `ls` or file managers shows available directories, even when not currently mounted.',
    example: '/misc   /etc/auto.misc   --ghost\n# Or inside /etc/autofs.conf: browse_mode = yes',
    exampleExplanation: 'Without `--ghost`, `/misc` looks empty until a process explicitly accesses `/misc/cdrom` directly.',
    examTip: 'Exam question: `--ghost` option prevents directories from appearing invisible in `ls` listings.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2327,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Autofs Executable Maps',
    question: 'How does an executable map work in autofs?',
    answer: 'If the map file has executable permissions (`chmod +x`), `automount` executes it with the requested key as an argument (`$1`) and uses the stdout output as the mount specification.',
    definition: 'Dynamic programmatic map for querying LDAP, SQL, or custom APIs at runtime.',
    example: '# /etc/auto.dynamic is an executable bash script:\n#!/bin/bash\nKEY="$1"\necho "-fstype=nfs,rw nfs.corp.internal:/data/$KEY"',
    exampleExplanation: 'The script outputs mount options and server target on standard output for automount to consume.',
    examTip: 'To enable executable maps, simply grant execute permissions: `chmod 755 /etc/auto.script`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2328,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Linux Swap Space Fundamentals',
    question: 'What is the purpose of swap space in Linux, and what types of storage can be used as swap?',
    answer: 'Swap extends physical RAM by paging inactive anonymous memory pages to disk; it can be provisioned as dedicated disk partitions, swap files, or zram compressed RAM blocks.',
    definition: 'Virtual memory subsystem mechanism preventing Out-Of-Memory kernel panics when physical memory is exhausted.',
    example: 'swapon --show',
    exampleExplanation: 'Lists all active swap devices, types, sizes, usage, and assigned priority.',
    examTip: 'Linux supports swapping to both dedicated raw partitions (`/dev/sda2`) and regular files on an existing filesystem (`/swapfile`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2329,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'mkswap utility',
    question: 'Which command initializes a partition or file as a Linux swap area, and how do you label it?',
    answer: '`mkswap -L <label> <device_or_file>` (e.g. `mkswap -L SWAP_VOL /dev/sdb2`).',
    definition: 'Writes the swap area header signature and initializes page slot tables.',
    example: 'mkswap -L SWAP_PART /dev/nvme0n1p3\n# Output: Setting up swapspace version 1, size = 8 GiB (8589930496 bytes)\n# LABEL=SWAP_PART, UUID=3f4e5d6c-7b8a-9012-3456-7890abcdef12',
    exampleExplanation: 'Prepares the partition for use by `swapon`.',
    examTip: '`mkswap` supports the `-U <UUID>` flag to assign a custom UUID.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2330,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'swapon command & flags',
    question: 'What command enables all swap partitions and files declared in `/etc/fstab`, and which flag displays a summary of active swap devices?',
    answer: '`swapon -a` enables all; `swapon -s` (or `swapon --show`) displays active swap summary.',
    definition: 'Activates swap devices and files for kernel memory management.',
    example: 'swapon -a\nswapon --show\n# NAME      TYPE      SIZE USED PRIO\n# /dev/sda2 partition   8G   0B   -2\n# /swapfile file        4G   0B   -3',
    exampleExplanation: '`-a` parses `/etc/fstab` for all entries with filesystem type `swap`.',
    examTip: 'Exam favorite: `swapon -s` reads directly from `/proc/swaps`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2331,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'swapoff utility',
    question: 'What happens when `swapoff /dev/sda2` is executed, and what must the system ensure beforehand?',
    answer: 'The kernel reads all swapped-out pages from the swap device back into physical RAM and deactivates it; physical RAM must have enough free space to hold all swapped pages.',
    definition: 'Disables swap on specified devices or files.',
    example: 'swapoff /dev/sda2\nswapoff -a # Deactivates all swap devices',
    exampleExplanation: 'If RAM is full, `swapoff` will fail or invoke the Out-Of-Memory (OOM) killer.',
    examTip: 'Before deactivating swap, always check `free -m` to verify available memory exceeds used swap.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2332,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: '/proc/swaps file format',
    question: 'What columns are displayed in `/proc/swaps`?',
    answer: '`Filename`, `Type`, `Size`, `Used`, and `Priority`.',
    definition: 'Kernel virtual file reporting real-time swap configuration and allocation.',
    example: 'cat /proc/swaps\nFilename\t\t\t\tType\t\tSize\t\tUsed\t\tPriority\n/dev/zram0                              partition\t4194300\t\t0\t\t100\n/dev/sda2                               partition\t8388604\t\t2456\t\t-2',
    exampleExplanation: 'Shows `/dev/zram0` has priority 100 and will be filled before `/dev/sda2` (priority -2).',
    examTip: 'Sizes in `/proc/swaps` are reported in 1 KiB blocks.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2333,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Swap Priorities (pri=N)',
    question: 'How do swap priorities work, and how are devices with equal priorities treated by the kernel?',
    answer: 'Higher numbers have higher priority and are filled first; devices with equal priority are striped in round-robin fashion across both devices for parallel performance.',
    definition: 'Configurable weighting via `pri=<val>` in fstab or `swapon -p <val>`. Negative priorities (-1, -2) are automatically assigned by default.',
    example: '# In /etc/fstab:\n/dev/sda2  none  swap  sw,pri=10  0 0\n/dev/sdb2  none  swap  sw,pri=10  0 0\n/dev/sdc2  none  swap  sw,pri=5   0 0',
    exampleExplanation: 'sda2 and sdb2 are interleaved round-robin first; sdc2 is only used when sda2 and sdb2 are completely full.',
    examTip: 'Priority range is -1 to 32767. User-set priorities are 0 through 32767.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2334,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Creating a Swap File step-by-step',
    question: 'What is the complete sequence of commands to create, secure, format, and activate a 2GB swap file at `/swapfile`?',
    answer: '1. `fallocate -l 2G /swapfile` (or `dd if=/dev/zero of=/swapfile bs=1M count=2048`)\n2. `chmod 600 /swapfile`\n3. `mkswap /swapfile`\n4. `swapon /swapfile`',
    definition: 'Creates a swap space as a file on an existing filesystem without repartitioning disks.',
    example: 'fallocate -l 2G /swapfile\nchmod 600 /swapfile\nmkswap /swapfile\nswapon /swapfile\necho "/swapfile none swap defaults 0 0" >> /etc/fstab',
    exampleExplanation: 'Setting permissions to `600` is mandatory; permissions greater than 0600 create security vulnerabilities exposing memory dumps.',
    examTip: 'On Btrfs, swap files require `nodatacow` attribute (`chattr +C /swapfile`) before allocating.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2335,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'vm.swappiness sysctl',
    question: 'What does the `vm.swappiness` sysctl parameter control, and what do values of 0 versus 100 represent?',
    answer: 'It defines the kernel\'s preference to swap anonymous memory vs reclaiming page cache (file cache); 0 minimizes swapping (swaps only under memory pressure), while 100 aggressively swaps memory.',
    definition: 'Virtual memory tuning knob in `/proc/sys/vm/swappiness` (default is typically 60).',
    example: 'sysctl vm.swappiness=10\n# Permanent in /etc/sysctl.d/99-swappiness.conf:\nvm.swappiness = 10',
    exampleExplanation: 'Lowering swappiness to 10 on database servers keeps active process memory in RAM, favoring page cache eviction.',
    examTip: 'Exam question: `vm.swappiness` does NOT specify a percentage of RAM at which swapping starts; it is a relative ratio.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2336,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'ZRAM vs Zswap',
    question: 'What is the architectural difference between ZRAM and Zswap in Linux memory management?',
    answer: 'ZRAM creates a standalone compressed RAM block device formatted as swap; Zswap is an in-kernel compressed write-back cache that intercepts swap pages before they hit physical disk swap.',
    definition: 'Memory compression technologies providing faster swapping and extending usable RAM.',
    example: '# Enable zram swap:\nzramctl --find --size 4G --algorithm zstd\nmkswap /dev/zram0\nswapon -p 100 /dev/zram0',
    exampleExplanation: 'ZRAM provides swap without needing a disk partition; Zswap requires an underlying physical disk swap device.',
    examTip: 'ZRAM is ideal for embedded systems and laptops; Zswap accelerates existing physical SSD/HDD swap.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 2337,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'OOM Killer & oom_score_adj',
    question: 'How do you protect a critical daemon process (e.g. PID 1234) from being terminated by the Out-Of-Memory (OOM) killer?',
    answer: '`echo -1000 > /proc/1234/oom_score_adj` (or set `OOMScoreAdjust=-1000` in its systemd unit).',
    definition: 'Mechanism allowing administrators to tune process survivability during memory exhaustion.',
    example: 'cat /proc/1234/oom_score\necho -1000 > /proc/1234/oom_score_adj',
    exampleExplanation: 'Range is -1000 to +1000. A value of -1000 completely disables OOM killing for that process.',
    examTip: 'Older kernels used `/proc/<pid>/oom_adj` (range -17 to +15; -17 disabled OOM killing). Modern Linux uses `oom_score_adj`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2338,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'fstab fs_passno Field (Field 6)',
    question: 'What do the numbers 0, 1, and 2 mean in the 6th field (`fs_passno`) of `/etc/fstab`?',
    answer: '`0` = Do not check with `fsck` at boot;\n`1` = Root filesystem (`/`), checked first;\n`2` = All other filesystems checked after root.',
    definition: 'Determines the order in which `fsck` checks filesystems during system startup.',
    example: '/dev/sda1  /      ext4  defaults  1  1\n/dev/sda2  /home  ext4  defaults  1  2\n/dev/sdb1  /mnt   xfs   defaults  0  0',
    exampleExplanation: 'Root is checked first (1). /home is checked second (2). XFS does not use fsck at boot, so it is set to 0.',
    examTip: 'Exam question: Only the root filesystem should have `1` in field 6. All other filesystems use `2` or `0`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2339,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'fstab fs_freq Field (Field 5)',
    question: 'What is the purpose of the 5th field (`fs_freq`) in `/etc/fstab`?',
    answer: 'It indicates whether the filesystem needs to be backed up by the classic `dump` utility (`1` = backup candidate, `0` = ignore).',
    definition: 'Legacy backup frequency indicator rarely used in modern environments, typically set to `0`.',
    example: '/dev/sda1  /  ext4  defaults  1  1\n/dev/sda3  /data  ext4  defaults  0  2',
    exampleExplanation: 'If set to 1, `dump` archives this filesystem; 0 means `dump` ignores it.',
    examTip: 'Modern systems almost universally set this field to `0`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2340,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.1',
    topicNumber: 203,
    category: 'Operating the Linux Filesystem',
    command: 'Systemd .mount & .automount Units',
    question: 'How does systemd handle `/etc/fstab` entries internally, and what naming convention is used for mount units?',
    answer: 'The `systemd-fstab-generator` translates `/etc/fstab` into native `.mount` and `.automount` units; paths are converted by replacing `/` with `-` (e.g. `/var/log` becomes `var-log.mount`).',
    definition: 'Systemd integration dynamically mapping filesystem table entries into systemd dependency units.',
    example: 'systemctl status var-log.mount\nsystemctl status mnt-data.automount',
    exampleExplanation: 'Leading slash is omitted, and internal slashes become dashes. Root `/` becomes `-.mount`.',
    examTip: 'If a directory has a literal dash in its path, systemd escapes it as `\\x2d`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },

  // =========================================================================
  // OBJECTIVE 203.2: Maintaining a Linux Filesystem - Part 1 (Cards 2341-2350)
  // =========================================================================
  {
    id: 2341,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.2',
    topicNumber: 203,
    category: 'Maintaining a Linux Filesystem',
    command: 'fsck Command & Exit Codes',
    question: 'What does exit code 1 vs exit code 4 mean when `fsck` finishes scanning a filesystem?',
    answer: 'Exit code 1 = Errors were found and successfully corrected; Exit code 4 = Filesystem errors were left UNCORRECTED (manual repair required).',
    definition: 'Bitmask exit codes returned by `fsck` to inform init scripts of repair status.',
    example: 'fsck /dev/sda1\necho $?\n# 0 = No errors\n# 1 = Errors corrected\n# 2 = Reboot needed\n# 4 = Errors left uncorrected',
    exampleExplanation: 'If bit 2 is set (exit code 2 or 3), the system MUST be rebooted immediately.',
    examTip: 'Bitmask values: 0=Clean, 1=Errors corrected, 2=Reboot suggested, 4=Errors uncorrected, 8=Operational error, 16=Usage error, 32=Canceled, 128=Shared-lib error.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2342,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.2',
    topicNumber: 203,
    category: 'Maintaining a Linux Filesystem',
    command: 'fsck Common Flags (-A, -C, -f, -M, -p, -y)',
    question: 'Match the `fsck` flags: `-A`, `-C`, `-f`, `-M`, `-p`, and `-y` to their functions.',
    answer: '`-A`: Check all filesystems in fstab;\n`-C`: Display progress bar;\n`-f`: Force check clean filesystems;\n`-M`: Do not check mounted filesystems;\n`-p`: Preen/auto-repair safe errors;\n`-y`: Assume "yes" to all repair prompts.',
    definition: 'Standard command-line options passed through the fsck front-end wrapper.',
    example: 'fsck -C -f /dev/sda1\nfsck -p -A',
    exampleExplanation: '`-f` forces verification even when the superblock clean flag is set.',
    examTip: 'NEVER run `fsck -y` on a mounted read-write filesystem, as it will corrupt data structures!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2343,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.2',
    topicNumber: 203,
    category: 'Maintaining a Linux Filesystem',
    command: 'e2fsck & Superblock Recovery (-b)',
    question: 'How do you repair a corrupted ext4 filesystem when the primary superblock is unreadable, using `e2fsck`?',
    answer: 'Use an alternate backup superblock: `e2fsck -b <block_number> /dev/sdX` (e.g. `e2fsck -b 32768 /dev/sda1`).',
    definition: 'Ext2/3/4 filesystems store redundant backup superblocks throughout the partition for disaster recovery.',
    example: 'mke2fs -n /dev/sda1 # Lists where backup superblocks would reside\ne2fsck -b 32768 /dev/sda1',
    exampleExplanation: '`mke2fs -n` simulates creation without writing, displaying exact block locations of backup superblocks.',
    examTip: 'Exam favorite: `-b` specifies the backup superblock block number.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2344,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.2',
    topicNumber: 203,
    category: 'Maintaining a Linux Filesystem',
    command: 'dumpe2fs utility',
    question: 'What information does `dumpe2fs /dev/sda1` provide for ext2/3/4 filesystems?',
    answer: 'Displays comprehensive superblock details and block group descriptor information including inode counts, block size, UUID, mount count, check intervals, and backup superblock locations.',
    definition: 'Dumps filesystem metadata for ext2/3/4 partitions.',
    example: 'dumpe2fs -h /dev/sda1 | grep -E "Filesystem state|Mount count|Block size"',
    exampleExplanation: 'The `-h` option displays only the primary superblock header information, omitting voluminous block group descriptor listings.',
    examTip: 'Exam question: Use `dumpe2fs -h` to quickly view superblock parameters without flooding terminal scrollback.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2345,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.2',
    topicNumber: 203,
    category: 'Maintaining a Linux Filesystem',
    command: 'tune2fs: Mount Counts (-c & -C)',
    question: 'How do you set the maximum mount count between checks to 30 mounts, and reset the current mount counter to 0 on an ext4 partition?',
    answer: '`tune2fs -c 30 /dev/sda1` (sets maximum count to 30) and `tune2fs -C 0 /dev/sda1` (resets current counter to 0).',
    definition: 'Adjusts tunable filesystem parameters on ext2/ext3/ext4 filesystems.',
    example: 'tune2fs -c 50 -C 0 /dev/sda1\ntune2fs -c 0 /dev/sda1 # Disables check based on mount count',
    exampleExplanation: 'Setting `-c 0` or `-c -1` disables periodic fsck checks triggered by mount counter increments.',
    examTip: '`-c` (lowercase) sets maximum mount count. `-C` (uppercase) sets current mount count.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2346,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.2',
    topicNumber: 203,
    category: 'Maintaining a Linux Filesystem',
    command: 'tune2fs: Time Intervals (-i)',
    question: 'How do you configure an ext4 filesystem to force a check every 180 days using `tune2fs`?',
    answer: '`tune2fs -i 180d /dev/sda1` (or `tune2fs -i 6m /dev/sda1`).',
    definition: 'Sets the maximum time interval between forced filesystem checks.',
    example: 'tune2fs -i 180d /dev/sda1\ntune2fs -i 0 /dev/sda1 # Disables time-based checks',
    exampleExplanation: 'Suffixes supported: `d` (days), `m` (months), `w` (weeks). Value `0` disables time-based checks.',
    examTip: 'Exam question: Disabling both `-c 0` and `-i 0` prevents automatic boot-time fsck delays on production servers.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2347,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.2',
    topicNumber: 203,
    category: 'Maintaining a Linux Filesystem',
    command: 'tune2fs: Reserved Blocks (-m)',
    question: 'Why does ext4 reserve 5% of blocks by default, and how do you reduce this reserved space to 1% on a 2TB data partition?',
    answer: 'Reserved blocks prevent fragmentation and allow root daemons to function if the disk fills up; reduce to 1% using `tune2fs -m 1 /dev/sdb1`.',
    definition: 'Reclaims tens of gigabytes of disk space on large non-root storage volumes.',
    example: 'tune2fs -m 1 /dev/sdb1\n# Frees 80GB on a 2TB disk for regular users',
    exampleExplanation: 'On a 2TB disk, 5% reserves ~100GB. Reducing to 1% frees ~80GB for user storage while keeping a safety buffer.',
    examTip: '`-r <blocks>` sets the reserved count as an absolute number of blocks instead of a percentage.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 2348,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.2',
    topicNumber: 203,
    category: 'Maintaining a Linux Filesystem',
    command: 'tune2fs: Error Behavior (-e)',
    question: 'What command configures an ext4 filesystem to automatically remount as read-only whenever a kernel filesystem error is detected?',
    answer: '`tune2fs -e remount-ro /dev/sda1`',
    definition: 'Defines kernel behavior when errors are encountered in the filesystem.',
    example: 'tune2fs -e remount-ro /dev/sda1\ntune2fs -e panic /dev/sda1\ntune2fs -e continue /dev/sda1',
    exampleExplanation: '`remount-ro` stops further writes immediately to protect data integrity from catastrophic corruption.',
    examTip: 'The three supported options for `-e` are: `continue`, `remount-ro`, and `panic`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2349,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.2',
    topicNumber: 203,
    category: 'Maintaining a Linux Filesystem',
    command: 'tune2fs: Journal Management (-j & -O)',
    question: 'How do you add an ext3/ext4 journal to an existing ext2 filesystem, and what feature flag is enabled?',
    answer: '`tune2fs -j /dev/sda1` (enables the `has_journal` feature, converting ext2 to ext3).',
    definition: 'Converts non-journaled ext2 into journaled ext3/ext4 in-place without data loss.',
    example: 'tune2fs -j /dev/sda1\ntune2fs -O has_journal /dev/sda1\ntune2fs -O ^has_journal /dev/sda1 # Removes journal',
    exampleExplanation: 'The caret `^` prefix in `-O ^feature` removes or disables a filesystem feature.',
    examTip: 'To remove a journal, the filesystem MUST be unmounted and clean before running `tune2fs -O ^has_journal`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 2350,
    deck: 'Topic 203: Filesystem and Devices',
    objectiveId: '203.2',
    topicNumber: 203,
    category: 'Maintaining a Linux Filesystem',
    command: 'resize2fs online vs offline',
    question: 'Can `resize2fs` grow and shrink an ext4 filesystem while mounted?',
    answer: 'Ext4 can be GROWN online while mounted, but it CANNOT be SHRUNK online (shrinking strictly requires unmounting the filesystem first).',
    definition: 'Resizes ext2/3/4 filesystems to fit expanded or reduced partition boundaries.',
    example: '# Growing online:\nlvextend -L +10G /dev/vg0/lv_data\nresize2fs /dev/vg0/lv_data\n# Shrinking offline:\numount /data\ne2fsck -f /dev/vg0/lv_data\nresize2fs /dev/vg0/lv_data 20G\nlvreduce -L 20G /dev/vg0/lv_data',
    exampleExplanation: 'Running `resize2fs` without a size argument expands the filesystem to occupy the entire block device capacity.',
    examTip: 'CRITICAL: Always run `e2fsck -f` before shrinking, and ALWAYS shrink the filesystem with `resize2fs` BEFORE reducing the partition/LV!',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
];
