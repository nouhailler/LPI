import { Flashcard } from '../../types';

/**
 * Topic 363: High Availability Distributed Storage (LPIC-3 High Availability and Storage Clusters Exam 306-300) - Part 2 (Cards 4851-4900)
 * 
 * Objectives Covered:
 * - 363.2: Ceph Storage Clusters - Part 2: Daemons, Pools, PGs, CRUSH, RBD, CephFS, Monitoring & Troubleshooting (Cards 4851-4900) [50 Cards] (Weight 8)
 */
export const topic363Part2: Flashcard[] = [
  // --------------------------------------------------------------------------------------------------------
  // Objective 363.2: Ceph Storage Clusters - Part 2 (Cards 4851-4900)
  // --------------------------------------------------------------------------------------------------------
  {
    id: 4851,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph Metadata Server (ceph-mds) Role & CephFS',
    definition: 'A specialized daemon required exclusively for CephFS. It manages POSIX filesystem directory hierarchies, file ownership, permissions, and timestamps in memory, while storing raw file block payloads in RADOS data pools.',
    example: 'systemctl status ceph-mds@node1\nceph mds stat\nceph fs status',
    exampleExplanation: 'Verifies active/standby MDS daemons managing the CephFS POSIX filesystem namespace.',
    question: 'Which Ceph storage interface requires the deployment of `ceph-mds` (Metadata Server) daemons?',
    answer: 'CephFS (Ceph File System). RBD block devices and RGW object gateways do not use MDS daemons.',
    category: 'Ceph Daemons',
    examTip: 'MDS daemons do NOT store file data; they only manage directory hierarchies, inodes, and file layout metadata in RAM and write it to the CephFS metadata pool.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4852,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'RADOS Gateway (ceph-rgw) Role & S3/Swift API',
    definition: 'An HTTP/REST gateway service daemon (built using FastCGI/Civetweb or Beast) providing Amazon S3-compatible and OpenStack Swift-compatible object storage APIs on top of a Ceph cluster.',
    example: 'systemctl status ceph-radosgw@rgw.node1\nradosgw-admin user create --uid="appuser" --display-name="App User"',
    exampleExplanation: 'Checks RGW service status and creates an S3 user with access key and secret key credentials.',
    question: 'Which Ceph service daemon translates Amazon S3 and OpenStack Swift HTTP REST API requests into native RADOS object calls?',
    answer: '`ceph-rgw` (RADOS Gateway).',
    category: 'Ceph Daemons',
    examTip: 'Use `radosgw-admin` for administrative tasks such as creating users, buckets, subusers, and access policies.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4853,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph Authentication (cephx) and Keyrings',
    definition: 'The default cryptographic authentication protocol (`cephx`) that authenticates clients and daemons using shared secret keys stored in keyring files, protecting against man-in-the-middle attacks.',
    example: 'ceph auth list\nceph auth get client.admin\nceph-authtool -C -n client.test --gen-key /etc/ceph/ceph.client.test.keyring',
    exampleExplanation: 'Lists cluster authentication capabilities, extracts admin credentials, and creates a custom client keyring file.',
    question: 'Where is the default administrative client keyring stored on a Ceph management node?',
    answer: '`/etc/ceph/ceph.client.admin.keyring`',
    category: 'Ceph Security',
    examTip: 'The `client.admin` user has full root privileges across the cluster (`caps: [mon] allow *, [osd] allow *, [mgr] allow *`).',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4854,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph Configuration File (/etc/ceph/ceph.conf)',
    definition: 'The primary INI-style configuration file parsed by Ceph tools and daemons on startup, defining cluster FSID, monitor host IP addresses, network subnets, and daemon sections (`[global]`, `[mon]`, `[osd]`, `[mgr]`).',
    example: '[global]\nfsid = b892d19f-7bf6-4b2a-b9c1-7f98b671a067\nmon_initial_members = node1, node2, node3\nmon_host = 192.168.10.11, 192.168.10.12, 192.168.10.13\npublic_network = 192.168.10.0/24\ncluster_network = 192.168.20.0/24',
    exampleExplanation: 'Standard ceph.conf showing dual networks: public for clients and cluster for OSD replication/heartbeats.',
    question: 'What is the purpose of defining separate `public_network` and `cluster_network` directives in `ceph.conf`?',
    answer: '`public_network` carries client read/write traffic and monitor communications, while `cluster_network` is a dedicated high-bandwidth subnet for OSD-to-OSD data replication and heartbeat traffic.',
    category: 'Ceph Configuration',
    examTip: 'Separating the cluster replication network prevents heavy client I/O from starving internal OSD replication and health heartbeats.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4855,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph Storage Pools: Replicated vs. Erasure-Coded',
    definition: 'Logical partitions used to store objects, defining isolation, ownership, access permissions, and data protection strategies: Replicated pools (full N-way copies, faster, higher storage cost) and Erasure-Coded pools (parity-based, cost-effective for large cold data).',
    example: 'ceph osd lspools\nceph osd pool ls detail',
    exampleExplanation: 'Lists all defined pools along with their pool ID, pool type (replicated or erasure), and assigned CRUSH ruleset.',
    question: 'What are the two types of storage pools available in Ceph for data redundancy?',
    answer: 'Replicated pools (default, mirror-based) and Erasure-Coded (EC) pools (parity-based).',
    category: 'Ceph Pools & PGs',
    examTip: 'RBD and CephFS metadata require replicated pools; erasure-coded pools are commonly used for RGW object storage or CephFS data pools.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4856,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'ceph osd pool create',
    definition: 'CLI command used to create a new storage pool, specifying the pool name, initial Placement Group count (`pg_num`), and optionally the pool type and CRUSH rule.',
    example: 'ceph osd pool create rbd_data 128 128\nceph osd pool create cold_archive 64 64 erasure',
    exampleExplanation: 'Creates a replicated pool named rbd_data with 128 PGs, and an erasure-coded pool cold_archive with 64 PGs.',
    question: 'Write the command to create a standard replicated pool named `vms_pool` with 128 placement groups.',
    answer: '`ceph osd pool create vms_pool 128 128`',
    category: 'Ceph Pools & PGs',
    examTip: 'Historically, `pg_num` and `pgp_num` (placement groups for placement) were specified separately; in modern Ceph, setting `pg_num` can auto-set `pgp_num`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4857,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'ceph osd pool application enable',
    definition: 'Tags a pool for a specific client workload type (`rbd`, `cephfs`, or `rgw`), allowing management tools and Ceph health checks to validate pool configuration and prevent accidental misusage.',
    example: 'ceph osd pool application enable rbd_data rbd\nceph osd pool application get rbd_data',
    exampleExplanation: 'Marks rbd_data as an active RBD pool, silencing Ceph health warnings (POOL_APP_NOT_ENABLED).',
    question: 'Which command must be executed on a newly created pool `mypool` to silence the `POOL_APP_NOT_ENABLED` warning for block device storage?',
    answer: '`ceph osd pool application enable mypool rbd`',
    category: 'Ceph Pools & PGs',
    examTip: 'If a pool is created without enabling an application, Ceph status will display a HEALTH_WARN warning.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4858,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph Pool Replication Parameters: size vs. min_size',
    definition: '`size` defines the total number of object replicas written across different OSDs (default: 3). `min_size` defines the minimum number of healthy replicas required to accept client write I/O requests (default: 2).',
    example: 'ceph osd pool set rbd_data size 3\nceph osd pool set rbd_data min_size 2\nceph osd pool get rbd_data size\nceph osd pool get rbd_data min_size',
    exampleExplanation: 'Configures 3-copy replication with a minimum write threshold of 2 copies.',
    question: 'In a Ceph replicated pool, what is the critical operational difference between `size` and `min_size`?',
    answer: '`size` is the target number of total replicas maintained; `min_size` is the minimum number of available replicas required for Ceph to allow I/O writes to proceed.',
    category: 'Ceph Pools & PGs',
    examTip: 'Never set `min_size 1` in production on a `size 3` pool; if the single remaining OSD dies, all acknowledged writes since the partition are lost.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4859,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'The Critical Impact of min_size on Quorum and I/O Blocking',
    definition: 'When the number of active, peering OSDs for a placement group drops below `min_size`, Ceph blocks all client read/write I/O to that PG to prevent silent data loss or divergence until missing OSDs recover or data is backfilled.',
    example: 'ceph health detail\n# Shows: 1 pgs undersized+peered',
    exampleExplanation: 'Indicates that the placement group has fewer replicas than `min_size` and client operations are currently blocked.',
    question: 'What happens to client applications trying to write to an object if its placement group has fewer active OSDs than `min_size`?',
    answer: 'The client write operations block (hang/freeze) until sufficient OSD replicas become available to satisfy `min_size`.',
    category: 'Ceph Pools & PGs',
    examTip: 'Blocking writes under degraded conditions is an intentional design to enforce strong consistency (CP in CAP theorem) over availability.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4860,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Placement Groups (PGs) Concept & CRUSH Mapping',
    definition: 'Logical aggregates (buckets) of objects within a pool. Ceph hashes objects into Placement Groups, and the CRUSH algorithm maps Placement Groups onto specific acting sets of OSDs, preventing the cluster from tracking millions of individual objects.',
    example: 'ceph pg stat\nceph pg dump pgs_brief',
    exampleExplanation: 'Displays the total number of placement groups and their current acting sets (e.g., PG 1.a -> [2, 7, 14]).',
    question: 'Why does Ceph group objects into Placement Groups (PGs) instead of tracking individual object locations directly on OSDs?',
    answer: 'Grouping millions of objects into thousands of PGs dramatically reduces cluster map size and monitor memory overhead while enabling efficient parallel rebalancing.',
    category: 'Ceph Pools & PGs',
    examTip: 'CRUSH calculation order: Object Name -> Hash -> Placement Group ID -> CRUSH -> Set of OSD IDs.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4861,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Calculating Target PG Numbers (pg_num Formula)',
    definition: 'Rule of thumb: aim for approximately 100 to 200 PGs per OSD across all pools. The standard formula for a single pool is: `(Total OSDs * 100) / pool_replica_size`, rounded up to the nearest power of 2.',
    example: '# For 20 OSDs with replica size 3:\n# (20 * 100) / 3 = 666 -> nearest power of 2 = 512 or 1024\nceph osd pool create mypool 512 512',
    exampleExplanation: 'Calculates the optimal PG count to ensure balanced data distribution without overwhelming CPU/RAM with peering traffic.',
    question: 'If a Ceph cluster has 30 OSDs and a pool uses `size 3`, what is a reasonable power-of-two `pg_num` calculation aiming for ~100 PGs per OSD?',
    answer: '1024 (Formula: (30 * 100) / 3 = 1000 -> rounded to nearest power of 2: 1024).',
    category: 'Ceph Pools & PGs',
    examTip: 'Too few PGs causes uneven disk space distribution; too many PGs wastes CPU/memory tracking peering heartbeats.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4862,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph PG Autoscaler (ceph-mgr module)',
    definition: 'An automated manager daemon feature that monitors pool data growth and automatically scales `pg_num` up or down dynamically, eliminating manual PG calculations.',
    example: 'ceph osd pool autoscale-status\nceph osd pool set mypool pg_autoscale_mode on\nceph osd pool set mypool pg_autoscale_mode warn',
    exampleExplanation: 'Enables automatic PG autoscaling on mypool, or sets it to warn to emit recommendations without auto-applying.',
    question: 'Which Ceph feature automatically monitors pool utilization and adjusts `pg_num` dynamically without manual administrator intervention?',
    answer: 'The Ceph PG Autoscaler (`pg_autoscale_mode on`).',
    category: 'Ceph Pools & PGs',
    examTip: 'Use `ceph osd pool autoscale-status` to inspect current PG count vs. recommended PG count per pool.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4863,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Erasure-Coded (EC) Pools Architecture (k+m Chunks)',
    definition: 'A space-efficient storage method that breaks an object into `k` data chunks and generates `m` parity coding chunks stored across `k+m` different OSDs. Can tolerate the simultaneous failure of any `m` OSDs with lower storage overhead than replication.',
    example: 'ceph osd erasure-code-profile set myprofile k=4 m=2\nceph osd pool create ec_pool 64 64 erasure myprofile',
    exampleExplanation: 'Deploys a k=4, m=2 erasure code pool (6 chunks total, tolerates 2 OSD failures, 66.6% storage efficiency).',
    question: 'In an erasure-coded pool configured with `k=4` and `m=2`, how many simultaneous OSD failures can the pool tolerate without data loss?',
    answer: '2 simultaneous OSD failures (tolerates up to `m` failures).',
    category: 'Ceph Pools & PGs',
    examTip: 'Storage overhead for k+m is `(k + m) / k`. For k=4, m=2: 6/4 = 1.5x (compared to 3.0x for `size 3` replication).',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4864,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'ceph osd erasure-code-profile',
    definition: 'Defines plugin algorithms (such as `jerasure` or `isa`), plugin parameters (`k`, `m`), and failure domain rules for erasure-coded pools.',
    example: 'ceph osd erasure-code-profile ls\nceph osd erasure-code-profile get default\nceph osd erasure-code-profile set ec42 k=4 m=2 crush-failure-domain=host',
    exampleExplanation: 'Defines an EC profile ensuring data and coding chunks reside on separate physical host servers.',
    question: 'Which command parameter in an erasure-code profile guarantees that no two chunks of the same object are placed on the same physical server?',
    answer: '`crush-failure-domain=host`',
    category: 'Ceph Pools & PGs',
    examTip: 'Once a pool is created using an erasure-code profile, its k and m parameters cannot be changed; a new pool must be created.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4865,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'CRUSH Map Hierarchy and Bucket Types',
    definition: 'A tree structure representing physical datacenter topology. Internal nodes are "buckets" representing physical fault zones (`root`, `datacenter`, `room`, `row`, `pod`, `pdu`, `rack`, `chassis`, `host`), and leaf nodes are individual OSD daemons.',
    example: 'ceph osd crush tree\nceph osd tree',
    exampleExplanation: 'Displays the physical hierarchy showing which OSDs reside in which hosts and racks.',
    question: 'What are the leaf nodes (endpoints) in a Ceph CRUSH map hierarchy?',
    answer: 'Individual Ceph OSD daemons (e.g., `osd.0`, `osd.1`).',
    category: 'Ceph CRUSH',
    examTip: 'Buckets can be nested: e.g., `root default` -> `rack rack1` -> `host storage1` -> `osd.0, osd.1`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4866,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'CRUSH Failure Domains (host vs. rack Protection)',
    definition: 'Rules specifying that replicas of an object must be isolated across different failure boundaries (such as different physical hosts, server racks, or power circuits) to survive entire hardware component outages.',
    example: '# In CRUSH rule definition:\nstep chooseleaf firstn 0 type host\n# Or for rack-level isolation:\nstep chooseleaf firstn 0 type rack',
    exampleExplanation: 'Instructs CRUSH to place each replica in a distinct physical server host or rack.',
    question: 'If a CRUSH rule specifies `step chooseleaf firstn 0 type rack`, what hardware failure can occur without causing any data unavailability?',
    answer: 'An entire server rack (including its top-of-rack switches and power distribution units) can lose power without data loss.',
    category: 'Ceph CRUSH',
    examTip: 'Default CRUSH rules isolate across `type host`. If you run multiple OSDs on one host, CRUSH places replicas on different hosts.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4867,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'ceph osd tree Command',
    definition: 'Prints an ASCII representation of the CRUSH map hierarchy showing host associations, OSD IDs, CRUSH weights, state (up/down), and availability (in/out).',
    example: 'ceph osd tree\nceph osd tree down',
    exampleExplanation: 'Displays all cluster nodes and OSDs, or filters to only display OSDs currently in a down state.',
    question: 'Which Ceph command displays the hierarchical layout of hosts and OSDs along with their current status (`up`/`down` and `in`/`out`)?',
    answer: '`ceph osd tree`',
    category: 'Ceph CRUSH',
    examTip: '`ceph osd tree` is the single most useful command for quickly locating which physical server hosts a failing OSD.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4868,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Decompiling and Compiling CRUSH Maps (crushtool)',
    definition: '`crushtool` extracts, decompiles binary CRUSH maps into human-readable text, and recompiles edited text files back into binary format for upload into the running cluster.',
    example: 'ceph osd getcrushmap -o /tmp/crush.bin\ncrushtool -d /tmp/crush.bin -o /tmp/crush.txt\n# Edit /tmp/crush.txt\ncrushtool -c /tmp/crush.txt -o /tmp/crush_new.bin\nceph osd setcrushmap -i /tmp/crush_new.bin',
    exampleExplanation: 'Standard 4-step workflow for modifying and testing CRUSH rules safely.',
    question: 'Which utility is used to decompile a binary CRUSH map into a human-readable text file and recompile it?',
    answer: '`crushtool` (using `-d` to decompile and `-c` to compile).',
    category: 'Ceph CRUSH',
    examTip: 'Always test compiled CRUSH maps with `crushtool --test` before injecting them into a live production cluster.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4869,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph OSD Status Flags: UP vs. DOWN, IN vs. OUT',
    definition: '`up` means the OSD daemon process is running and connected via network. `down` means the daemon is stopped or unreachable. `in` means the OSD is assigned Placement Groups to store data. `out` means the OSD is excluded from data assignment.',
    example: 'ceph osd stat\n# Returns: 12 osds: 12 up, 12 in',
    exampleExplanation: 'Confirms all 12 OSD daemons are active (up) and participating in cluster data storage (in).',
    question: 'In Ceph, what is the difference between an OSD being marked `down` versus being marked `out`?',
    answer: '`down` means the daemon process or network is unreachable; `out` means the cluster has removed it from the CRUSH map and rebalanced its data onto other OSDs.',
    category: 'Ceph OSD Management',
    examTip: 'When an OSD goes `down`, Ceph waits `mon_osd_down_out_interval` (default 600 seconds) before automatically marking it `out` to begin rebalancing.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4870,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'ceph osd out & ceph osd in',
    definition: 'Administrative commands to manually exclude an OSD from data storage (triggering backfill off the device) or return an OSD back to active participation in CRUSH data allocation.',
    example: 'ceph osd out 3\nceph osd in 3',
    exampleExplanation: 'Evacuates data from OSD.3 prior to physical disk replacement, or re-includes it after maintenance.',
    question: 'Which command safely evacuates all placement groups and data off `osd.5` before taking the disk offline for maintenance?',
    answer: '`ceph osd out 5` (or `ceph osd out osd.5`)',
    category: 'Ceph OSD Management',
    examTip: 'Always wait for cluster recovery to reach `HEALTH_OK` after running `ceph osd out` before physically unmounting the drive.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4871,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph OSD Reweighting (reweight vs. crush reweight)',
    definition: '`ceph osd crush reweight` changes the relative physical capacity weight in the CRUSH map (based on disk size in TB: 1.0 = 1TB). `ceph osd reweight` is an override factor (0.0 to 1.0) used temporarily to adjust for uneven data distribution.',
    example: 'ceph osd crush reweight osd.2 3.64\nceph osd reweight 2 0.85',
    exampleExplanation: 'Sets the permanent CRUSH capacity weight for a 4TB drive, and throttles its relative utilization to 85%.',
    question: 'What metric does `ceph osd crush reweight` typically represent in the CRUSH map?',
    answer: 'The physical capacity of the storage disk in Terabytes (e.g., a 2TB disk has a weight of approximately 2.0).',
    category: 'Ceph OSD Management',
    examTip: 'To rebalance unevenly filled OSDs automatically, run `ceph osd reweight-by-utilization`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4872,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'ceph-volume Utility & OSD Provisioning',
    definition: 'The modern modular tool used to deploy, format, and activate OSD devices using LVM (Logical Volume Manager) tags, replacing the legacy `ceph-disk` tool.',
    example: 'ceph-volume lvm create --data /dev/sdb\nceph-volume lvm create --data /dev/sdb --block.db /dev/nvme0n1p1\nceph-volume lvm list',
    exampleExplanation: 'Provisions a new BlueStore OSD on /dev/sdb with external RocksDB metadata on NVMe.',
    question: 'What modern tool has replaced `ceph-disk` for provisioning and activating OSDs using LVM?',
    answer: '`ceph-volume` (specifically `ceph-volume lvm`).',
    category: 'Ceph OSD Management',
    examTip: '`ceph-volume lvm prepare` prepares the disk and LVM tags; `ceph-volume lvm activate` starts the systemd service. `create` does both in one step.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4873,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Decommissioning and Purging a Failed OSD',
    definition: 'The procedure to permanently remove a dead or decommissioned OSD from the cluster: stop the daemon, mark it out, purge its keys and CRUSH entries using `ceph osd purge`, and wipe LVM signatures using `ceph-volume lvm zap`.',
    example: 'systemctl stop ceph-osd@4\nceph osd out 4\nceph osd purge 4 --yes-i-really-mean-it\nceph-volume lvm zap /dev/sdd --destroy',
    exampleExplanation: 'Safely and completely purges OSD 4 from the monitor maps, CRUSH tree, and auth keyring, then wipes the drive.',
    question: 'Which single command removes a dead OSD (e.g. `osd.4`) from the CRUSH map, deletes its authentication keys, and removes its OSD ID?',
    answer: '`ceph osd purge 4 --yes-i-really-mean-it`',
    category: 'Ceph OSD Management',
    examTip: 'Before running `purge`, ensure all PGs previously hosted by that OSD have finished backfilling and cluster health is `HEALTH_OK`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4874,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph Block Device (RBD) Architecture & librbd',
    definition: 'Provides virtual, thin-provisioned, resizable network block devices striped across hundreds of RADOS objects. Can be attached via Linux kernel driver (`krbd`) or user-space hypervisors (QEMU/KVM via `librbd`).',
    example: 'rbd ls -p rbd_pool\nmodprobe rbd',
    exampleExplanation: 'Lists RBD images in a pool and loads the Linux kernel RBD client module.',
    question: 'How does Ceph RBD store a virtual 100GB disk image inside RADOS?',
    answer: 'It strips the virtual disk into thousands of small 4MB binary objects and distributes them across the cluster using CRUSH.',
    category: 'Ceph RBD',
    examTip: 'Because RBD images are broken into 4MB objects distributed across all OSDs, disk read/write bandwidth is aggregated across the entire cluster.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4875,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'rbd create and rbd ls',
    definition: 'Commands used to create a new virtual block device image with a defined capacity and list existing images within a pool.',
    example: 'rbd create --size 20G --pool rbd_data vm-disk1\nrbd ls -p rbd_data\nrbd ls -l -p rbd_data',
    exampleExplanation: 'Creates a 20GB thinly provisioned virtual disk image named vm-disk1 in pool rbd_data.',
    question: 'Write the command to create a 50GB virtual block image named `disk01` in the pool `pool_vms`.',
    answer: '`rbd create --size 50G --pool pool_vms disk01` (or `rbd create pool_vms/disk01 --size 50G`)',
    category: 'Ceph RBD',
    examTip: 'RBD images are thin-provisioned by default: creating a 1TB image consumes zero storage until data blocks are written.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4876,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'rbd info Command',
    definition: 'Displays detailed metadata for an RBD image, including image size, object size (default 4MB), object prefix, order, features (layering, exclusive-lock, journaling), and parent snapshot (if cloned).',
    example: 'rbd info rbd_data/vm-disk1',
    exampleExplanation: 'Shows image format version, size, object prefix, and enabled feature flags.',
    question: 'Which command displays the size, object prefix, and enabled feature flags (such as layering or exclusive-lock) of image `vol1` in pool `mypool`?',
    answer: '`rbd info mypool/vol1`',
    category: 'Ceph RBD',
    examTip: 'If mapping an image fails on older Linux kernels, use `rbd feature disable <image> layering,exclusive-lock` to remove unsupported features.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4877,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Mapping RBD Images (rbd map)',
    definition: 'Maps an RBD image to a local Linux kernel block device node (`/dev/rbd0`, `/dev/rbd/pool/image`), allowing standard Linux filesystem creation (`mkfs.ext4`, `mkfs.xfs`) and mounting.',
    example: 'rbd map --pool rbd_data vm-disk1\n# Output: /dev/rbd0\nmkfs.ext4 /dev/rbd0\nmount /dev/rbd0 /mnt/vmdisk',
    exampleExplanation: 'Maps vm-disk1, formats it with ext4, and mounts it locally.',
    question: 'Which command maps the Ceph block device `rbd_pool/image01` to a local Linux kernel block device?',
    answer: '`rbd map rbd_pool/image01` (or `rbd map --pool rbd_pool image01`)',
    category: 'Ceph RBD',
    examTip: 'To view all currently mapped RBD devices on the local system, run `rbd showmapped`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4878,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Unmapping RBD Images (rbd unmap)',
    definition: 'Detaches a mapped kernel block device from the local operating system. The filesystem mounted on the device must be unmounted first.',
    example: 'umount /mnt/vmdisk\nrbd unmap /dev/rbd0\nrbd unmap rbd_data/vm-disk1',
    exampleExplanation: 'Unmounts the filesystem and releases kernel device /dev/rbd0.',
    question: 'What must be done before successfully unmapping a kernel RBD device with `rbd unmap /dev/rbd0`?',
    answer: 'Any active filesystem mounted on `/dev/rbd0` must first be unmounted (`umount`).',
    category: 'Ceph RBD',
    examTip: 'If unmap reports device busy, ensure no processes or LVM volume groups have open references to `/dev/rbd0`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4879,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Resizing RBD Images (rbd resize)',
    definition: 'Expands or shrinks the virtual capacity of an RBD image. Expanding can be done online; shrinking requires the `--allow-shrink` safety flag.',
    example: 'rbd resize --size 50G rbd_data/vm-disk1\nrbd resize --size 10G rbd_data/vm-disk1 --allow-shrink',
    exampleExplanation: 'Expands image to 50GB, and forcibly shrinks image to 10GB.',
    question: 'Which flag must be explicitly passed to `rbd resize` when reducing the size of an RBD block device to prevent accidental data loss?',
    answer: '`--allow-shrink`',
    category: 'Ceph RBD',
    examTip: 'After expanding an RBD image, use `resize2fs /dev/rbd0` or `xfs_growfs /mnt` to expand the filesystem on top of it.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4880,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'RBD Snapshots (rbd snap create & rollback)',
    definition: 'Point-in-time, read-only copies of an RBD image. Created instantly using copy-on-write mechanisms, with zero initial storage consumption.',
    example: 'rbd snap create rbd_data/vm-disk1@snap1\nrbd snap ls rbd_data/vm-disk1\nrbd snap rollback rbd_data/vm-disk1@snap1',
    exampleExplanation: 'Creates snapshot snap1, lists snapshots, and rolls back disk contents to snap1 state.',
    question: 'Write the command to roll back the block device `mypool/disk1` to its snapshot named `pre-upgrade`.',
    answer: '`rbd snap rollback mypool/disk1@pre-upgrade`',
    category: 'Ceph RBD',
    examTip: 'Snapshots are identified by the `@` separator syntax: `<pool>/<image>@<snapshot>`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4881,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'RBD Snapshot Protection & Copy-on-Write Clones',
    definition: 'To create a writable clone of an image, the snapshot must first be "protected" against deletion (`rbd snap protect`). `rbd clone` then creates a thin-provisioned copy-on-write child image referencing the parent snapshot.',
    example: 'rbd snap protect rbd_data/gold-img@v1\nrbd clone rbd_data/gold-img@v1 rbd_data/vm-clone1',
    exampleExplanation: 'Protects the parent snapshot and provisions an instant writable clone vm-clone1.',
    question: 'Why must `rbd snap protect` be run on a snapshot before creating child clones with `rbd clone`?',
    answer: 'To prevent accidental deletion of the parent snapshot while child clones are actively referencing its shared data objects.',
    category: 'Ceph RBD',
    examTip: 'A protected snapshot cannot be deleted until all child clones are either removed or flattened.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4882,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Flattening RBD Clones (rbd flatten)',
    definition: 'Decouples a child clone from its parent snapshot by copying all parent objects directly into the child image, making the child completely independent so the parent snapshot can be deleted.',
    example: 'rbd flatten rbd_data/vm-clone1\nrbd snap unprotect rbd_data/gold-img@v1\nrbd snap rm rbd_data/gold-img@v1',
    exampleExplanation: 'Flattens the clone, unprotects the parent snapshot, and deletes the parent snapshot.',
    question: 'What operation breaks the dependency link between a cloned RBD image and its parent snapshot, copying all parent data into the clone?',
    answer: '`rbd flatten <pool>/<image>`',
    category: 'Ceph RBD',
    examTip: 'Flattening consumes additional storage capacity because shared parent objects are duplicated into the clone.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4883,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'CephFS Architecture: Metadata Pool & Data Pool',
    definition: 'A POSIX-compliant distributed filesystem requiring at least two dedicated RADOS pools: one pool strictly for metadata (directory trees, inode structures, managed by MDS daemons) and one or more pools for raw file data payloads.',
    example: 'ceph osd pool create cephfs_metadata 32 32\nceph osd pool create cephfs_data 128 128',
    exampleExplanation: 'Provisions the required twin pools for CephFS deployment.',
    question: 'Why does CephFS require at least two distinct RADOS pools to function?',
    answer: 'One pool is dedicated exclusively to MDS metadata (directories, inodes, locks), and the second pool stores the actual file payload data.',
    category: 'CephFS',
    examTip: 'The CephFS metadata pool should be placed on fast SSD/NVMe OSDs to maximize directory traversal and file lookup performance.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4884,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'ceph fs new Command',
    definition: 'Initializes a new CephFS filesystem in the cluster, binding the designated metadata pool and data pool to the filesystem namespace.',
    example: 'ceph fs new myfs cephfs_metadata cephfs_data\nceph fs ls\nceph fs status myfs',
    exampleExplanation: 'Creates CephFS instance myfs using cephfs_metadata and cephfs_data pools.',
    question: 'What command binds a metadata pool named `fs_meta` and a data pool named `fs_data` into a new CephFS filesystem called `sharedfs`?',
    answer: '`ceph fs new sharedfs fs_meta fs_data`',
    category: 'CephFS',
    examTip: 'The metadata pool must always be passed before the data pool in `ceph fs new`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4885,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'CephFS MDS States: Active, Standby, Standby-Replay',
    definition: 'MDS daemons can operate as `active` (handling live client requests), `standby` (idle, ready for failover), or `standby-replay` (warm standby continuously pre-reading the active MDS journal for sub-second failovers).',
    example: 'ceph mds stat\n# Returns: myfs:1 {0=node1=active} 1 up:standby-replay 1 up:standby',
    exampleExplanation: 'Shows 1 active MDS, 1 warm standby-replay MDS, and 1 cold standby MDS.',
    question: 'What is the operational advantage of configuring a Ceph MDS daemon in `standby-replay` mode compared to standard `standby` mode?',
    answer: '`standby-replay` continuously mirrors the active MDS journal into memory, allowing near-instantaneous failover without cache rebuild delays.',
    category: 'CephFS',
    examTip: 'Configure `mds_standby_for_name` or `mds_standby_replay = true` in `ceph.conf` to enable warm standby-replay.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4886,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Mounting CephFS via Linux Kernel Driver',
    definition: 'The high-performance native in-kernel client for CephFS, utilizing the kernel `ceph.ko` module to communicate directly with MONs, MDSs, and OSDs.',
    example: 'mount -t ceph 192.168.10.11:6789,192.168.10.12:6789:/ /mnt/cephfs -o name=admin,secret=AQBxxxxxxxxxxxx==',
    exampleExplanation: 'Mounts the root of CephFS via kernel driver using monitor IPs and client.admin authentication secret.',
    question: 'What filesystem type is specified in the `mount` command to mount CephFS using the native Linux kernel driver?',
    answer: '`-t ceph` (e.g., `mount -t ceph <mon-ips>:/ <mountpoint> -o name=...,secret=...`)',
    category: 'CephFS',
    examTip: 'The kernel client delivers superior throughput compared to FUSE because it bypasses user-space context switches.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4887,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Mounting CephFS via User-Space FUSE (ceph-fuse)',
    definition: 'An alternative client access method running completely in user-space via FUSE, useful on clients with older kernel versions that lack native kernel CephFS support or for unprivileged containers.',
    example: 'ceph-fuse -m 192.168.10.11:6789 /mnt/cephfs\nceph-fuse /mnt/cephfs --id admin',
    exampleExplanation: 'Mounts CephFS at /mnt/cephfs using the ceph-fuse user-space daemon.',
    question: 'Which user-space utility allows mounting a CephFS filesystem without requiring the in-kernel `ceph.ko` driver?',
    answer: '`ceph-fuse`',
    category: 'CephFS',
    examTip: 'Use `ceph-fuse` when mounting on older Linux distributions or when kernel upgrades are restricted.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4888,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph Overall Health Commands: ceph status and ceph health detail',
    definition: '`ceph status` (or `ceph -s`) provides an executive dashboard summary of cluster health, monitor quorum, OSD map, PG states, and data usage. `ceph health detail` enumerates specific fault conditions and affected PG numbers.',
    example: 'ceph status\nceph health detail',
    exampleExplanation: 'Inspects overall cluster health and outputs specific diagnostic alerts for degraded or down components.',
    question: 'Which command provides granular, specific explanations and IDs of affected placement groups when Ceph enters a `HEALTH_WARN` or `HEALTH_ERR` state?',
    answer: '`ceph health detail`',
    category: 'Ceph Monitoring',
    examTip: 'Always check `ceph health detail` first whenever `ceph status` reports anything other than `HEALTH_OK`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4889,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Understanding Ceph Health States: OK, WARN, ERR',
    definition: 'HEALTH_OK: all daemons running, all PGs active+clean. HEALTH_WARN: degraded redundancy, low disk space, or offline standby daemons, but client I/O continues. HEALTH_ERR: data unavailability, loss of monitor quorum, or disk full.',
    example: 'ceph health',
    exampleExplanation: 'Outputs a single-line summary: HEALTH_OK, HEALTH_WARN, or HEALTH_ERR.',
    question: 'Can client applications continue reading and writing data when a Ceph cluster is in a `HEALTH_WARN` state?',
    answer: 'Yes, `HEALTH_WARN` indicates degraded redundancy or warnings, but client I/O operations continue unhindered.',
    category: 'Ceph Monitoring',
    examTip: 'In `HEALTH_ERR`, some or all client I/O may be blocked due to lack of quorum, missing replicas, or full pools.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4890,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'PG State: active+clean (Healthy Baseline)',
    definition: 'The ideal, fully healthy state of a placement group. `active` means Ceph will process read/write requests to the PG; `clean` means all replicas have synchronized and match the target replication size.',
    example: 'ceph pg stat\n# Returns: 512 pgs: 512 active+clean',
    exampleExplanation: 'Confirms all 512 placement groups are in an optimal, fully redundant state.',
    question: 'What do the terms `active` and `clean` indicate when a Placement Group is in the `active+clean` state?',
    answer: '`active` means the PG is online and processing client read/write I/O; `clean` means all target replicas are fully synchronized across OSDs.',
    category: 'Ceph Troubleshooting',
    examTip: 'A 100% healthy Ceph cluster should have all PGs in `active+clean` state.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4891,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'PG States: peering, activating, and backfilling',
    definition: '`peering` is the process where OSDs hosting a PG agree on the current state of objects. `activating` transitions the PG to accept I/O. `backfilling` is copying complete object datasets to a newly added OSD.',
    example: 'ceph -s | grep -i pgs',
    exampleExplanation: 'Monitors ongoing peering and backfill progress across placement groups.',
    question: 'What does the PG state `backfilling` mean during cluster rebalancing or recovery?',
    answer: 'Data is actively being copied in the background from existing OSDs onto a new or recovering OSD to fulfill replication requirements.',
    category: 'Ceph Troubleshooting',
    examTip: 'Backfilling occurs when an OSD has been offline long enough that its pg-log entries expired, requiring a full scan.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4892,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'PG States: degraded and undersized',
    definition: '`degraded` means some objects in the PG have fewer replicas than the configured pool `size`. `undersized` means the PG has fewer active OSDs than the target replica count (e.g. 2 OSDs instead of 3).',
    example: 'ceph health detail\n# 12 pgs degraded, 12 pgs undersized',
    exampleExplanation: 'Common transient state after an OSD fails or goes offline.',
    question: 'What is the distinction between a `degraded` PG and an `undersized` PG in Ceph?',
    answer: '`undersized` means the PG acting set has fewer OSDs than the configured `size`; `degraded` means individual objects within the PG have fewer synchronized copies than required.',
    category: 'Ceph Troubleshooting',
    examTip: 'As long as the PG is `active+undersized+degraded`, client I/O still succeeds because the number of available replicas satisfies `min_size`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4893,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'PG States: stale, down, and incomplete',
    definition: 'Critical error states. `stale`: MONs have not received heartbeat stats from the primary OSD. `down`: the PG has no replicas available to serve data. `incomplete`: peering failed because no OSD holds the latest write log entries.',
    example: 'ceph pg dump | grep -E "stale|down|incomplete"',
    exampleExplanation: 'Searches for blocked placement groups requiring immediate administrative attention.',
    question: 'What does the `down` state indicate for a placement group, and how does it affect client I/O?',
    answer: '`down` means no OSD with authoritative data for that PG is online; client read and write operations targeting that PG will freeze/block completely.',
    category: 'Ceph Troubleshooting',
    examTip: 'To recover a `down` PG, locate and start the original physical OSDs that formed the acting set prior to the outage.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4894,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph Data Scrubbing (Light Scrub vs. Deep Scrub)',
    definition: 'Background integrity check mechanism. Light scrub (daily) compares object sizes and attributes across replicas. Deep scrub (weekly) reads all object data bit-for-bit and computes CRC32c checksums to detect silent bit rot.',
    example: 'ceph osd pool set mypool osd_deep_scrub_interval 604800',
    exampleExplanation: 'Configures weekly deep scrubbing interval (7 days in seconds) for pool mypool.',
    question: 'What is the primary difference between a Ceph light scrub and a deep scrub?',
    answer: 'Light scrub only compares metadata (object names, sizes, attributes); deep scrub reads the actual data payload and calculates checksums to detect silent data corruption (bit rot).',
    category: 'Ceph Maintenance',
    examTip: 'Deep scrubbing is I/O intensive; schedule it during off-peak hours using `osd_scrub_begin_hour` and `osd_scrub_end_hour`.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4895,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Triggering Manual Scrubbing (ceph osd scrub / deep-scrub)',
    definition: 'Commands allowing administrators to manually trigger immediate integrity verification on specific OSDs or individual placement groups.',
    example: 'ceph osd deep-scrub 2\nceph pg deep-scrub 1.4a',
    exampleExplanation: 'Initiates immediate deep scrubbing on all PGs on OSD 2, or specifically on PG 1.4a.',
    question: 'Which command instructs Ceph to immediately perform a deep scrub on placement group `2.3f`?',
    answer: '`ceph pg deep-scrub 2.3f`',
    category: 'Ceph Maintenance',
    examTip: 'Use manual deep-scrubbing to verify an OSD after encountering hardware read errors on physical disks.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4896,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Inconsistent PGs and Bit-rot Detection',
    definition: 'When deep scrubbing reveals that CRC32c checksums or object replicas do not match across OSDs, Ceph marks the PG as `inconsistent` and raises a `HEALTH_ERR` alert, preserving the corrupted object for inspection.',
    example: 'ceph health detail\n# 1 pgs inconsistent; 1 scrub errors\nrados list-inconsistent-obj 1.12 --format json-pretty',
    exampleExplanation: 'Identifies the exact inconsistent object and the mismatching checksum details across OSD replicas.',
    question: 'Which command lists the exact objects and mismatching checksums inside an inconsistent placement group `1.4a`?',
    answer: '`rados list-inconsistent-obj 1.4a --format json-pretty`',
    category: 'Ceph Troubleshooting',
    examTip: 'Never ignore an `inconsistent` PG alert; investigate immediately to determine which physical drive has bad sectors.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
  {
    id: 4897,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Repairing Inconsistent Placement Groups (ceph pg repair)',
    definition: 'Instructs Ceph to resolve an `inconsistent` PG by overwriting corrupted object replicas with the authoritative copy that has a valid checksum.',
    example: 'ceph pg repair 1.12\nceph health detail',
    exampleExplanation: 'Executes automatic repair on PG 1.12 by re-replicating the healthy copy to all replica OSDs.',
    question: 'What command attempts automatic repair of an inconsistent placement group named `3.1b`?',
    answer: '`ceph pg repair 3.1b`',
    category: 'Ceph Troubleshooting',
    examTip: 'If BlueStore checksums identify that OSD.1 has a bad checksum while OSD.2 and OSD.3 match, `ceph pg repair` copies the valid object over the corrupted one.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4898,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Monitoring Capacity and Utilization: ceph df and ceph osd df',
    definition: '`ceph df` displays overall cluster RAW storage and pool-level usage. `ceph osd df` displays per-OSD disk utilization, showing variance percentages to detect uneven data distribution across disks.',
    example: 'ceph df\nceph osd df\nceph osd df tree',
    exampleExplanation: 'Shows raw storage capacity, pool usage, and individual OSD disk space percentage consumption.',
    question: 'Which command shows the individual storage capacity and percentage of space utilized for every single OSD in the cluster?',
    answer: '`ceph osd df` (or `ceph osd df tree`)',
    category: 'Ceph Monitoring',
    examTip: 'Look at the `VAR` (variance) column in `ceph osd df`; if variance exceeds 1.25, consider running `ceph osd reweight-by-utilization`.',
    difficulty: 'Fundamental',
    status: 'unseen',
  },
  {
    id: 4899,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Ceph Nearfull and Full Thresholds',
    definition: 'Safety ratios that protect against out-of-space crashes. `mon_osd_nearfull_ratio` (default: 85%) emits a warning. `mon_osd_backfillfull_ratio` (default: 90%) halts rebalancing. `mon_osd_full_ratio` (default: 95%) halts all client writes cluster-wide.',
    example: 'ceph osd dump | grep -E "full_ratio|nearfull"',
    exampleExplanation: 'Inspects active cluster safety ratios (nearfull: 0.85, backfillfull: 0.90, full: 0.95).',
    question: 'What happens cluster-wide when any single OSD reaches the `mon_osd_full_ratio` (typically 95% capacity)?',
    answer: 'Ceph blocks all client write I/O operations cluster-wide to prevent filesystem corruption and OSD crashes.',
    category: 'Ceph Monitoring',
    examTip: 'If a cluster is full, writes are blocked until an administrator adds OSD capacity, deletes data, or temporarily raises the full ratio.',
    difficulty: 'Intermediate',
    status: 'unseen',
  },
  {
    id: 4900,
    deck: 'LPIC-3 Topic 363',
    topicNumber: 363,
    objectiveId: '363.2',
    command: 'Handling Emergency Full Cluster States',
    definition: 'When a cluster blocks writes due to full ratio, administrators can set flags to prevent data migration storms (`noout`), temporarily elevate ratios to allow deletions, delete unneeded snapshots/pools, and inject additional OSDs.',
    example: 'ceph osd set noout\nceph osd set-nearfull-ratio 0.92\nceph osd set-full-ratio 0.97\n# Delete unneeded images or snapshots\nceph osd unset noout',
    exampleExplanation: 'Temporarily increases full threshold to 97% to unfreeze writes so administrators can purge old images and snapshots.',
    question: 'Why should the `noout` flag be set before taking emergency action on a cluster that has reached its full ratio threshold?',
    answer: 'Setting `noout` prevents Ceph from marking failing/full OSDs as `out`, avoiding catastrophic cascading rebalances that would fill remaining drives.',
    category: 'Ceph Troubleshooting',
    examTip: 'Once emergency deletion recovers disk space below 85%, restore original `full-ratio` and unset `noout`.',
    difficulty: 'Advanced',
    status: 'unseen',
  },
];
